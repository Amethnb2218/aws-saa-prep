export const questionBank8 = [
  // Networking deep-dive (heavily tested on exam)
  {q:"A company has a VPC with a public subnet (10.0.1.0/24) and a private subnet (10.0.2.0/24). The public subnet has a route to the Internet Gateway. The private subnet has a route to a NAT Gateway in the public subnet. A bastion host in the public subnet needs SSH access to instances in the private subnet. The private subnet NACL allows inbound SSH (port 22) from 10.0.1.0/24. SSH connections time out. What is missing?", opts:["The Security Group on private instances must allow SSH from the bastion's Security Group","The private subnet NACL must also allow outbound traffic on ephemeral ports (1024-65535) back to 10.0.1.0/24","Both A and B must be configured","The bastion host needs an Elastic IP"], correct:2, exp:"NACLs are stateless — both inbound rules (allowing SSH from bastion subnet) AND outbound rules (allowing return traffic on ephemeral ports to bastion subnet) are required. Additionally, Security Groups on the private instances must explicitly allow SSH from the bastion. Both controls must be correct. The bastion already has a public IP (it's in the public subnet for internet access) but that's irrelevant for the internal SSH path.", domain:1},
  {q:"A company has VPC A (10.0.0.0/16) peered with VPC B (172.16.0.0/16), and VPC A is also peered with VPC C (192.168.0.0/16). Instances in VPC B need to communicate with instances in VPC C. What is required?", opts:["Traffic can transit through VPC A since it's peered with both B and C","A direct VPC peering connection must be created between VPC B and VPC C","Use a Transit Gateway to connect all three VPCs","Either B or C — both work but Transit Gateway is simpler at scale"], correct:3, exp:"VPC peering is NOT transitive — traffic from B cannot route through A to reach C. Either create a direct B-C peering connection (works for 3 VPCs) or use Transit Gateway (hub-and-spoke model, scales better for many VPCs). The exam tests this non-transitive property heavily. At 3 VPCs, either works; at 10+ VPCs, Transit Gateway is clearly better (avoids n*(n-1)/2 peering connections).", domain:2},
  {q:"A company connects their on-premises data center to AWS via Direct Connect with a private VIF to VPC A. They also have VPC B peered with VPC A. Can on-premises hosts reach resources in VPC B through the Direct Connect -> VPC A -> VPC B path?", opts:["Yes — Direct Connect traffic can transit VPC peering connections","No — VPC peering doesn't support transitive routing from Direct Connect","No — but using a Transit Gateway with a Direct Connect Gateway solves this","Both B and C are correct"], correct:3, exp:"VPC peering doesn't support transitive routing. Traffic from Direct Connect entering VPC A CANNOT transit the peering connection to reach VPC B. Solution: Transit Gateway connects to both VPCs and to the Direct Connect Gateway, enabling on-premises to reach any VPC through the TGW hub. This is a classic networking question on the exam.", domain:2},
  {q:"A company uses AWS PrivateLink (Interface VPC Endpoint) to access S3 from their VPC. They notice that S3 API calls from EC2 instances still use the public S3 endpoint (s3.amazonaws.com) and don't go through the Interface Endpoint. What must be changed?", opts:["The application must use the VPC endpoint-specific DNS name (vpce-xxx.s3.region.vpce.amazonaws.com)","Enable Private DNS for the Interface Endpoint — this overrides the public S3 DNS resolution to point to the endpoint ENI","Configure the Security Group on the VPC endpoint to allow traffic from the instance","Add a route table entry pointing S3 prefix list to the Interface Endpoint"], correct:1, exp:"Interface Endpoints (PrivateLink) use ENIs with private IPs. By default, applications still resolve the public DNS name to public IPs (bypassing the endpoint). Enabling Private DNS associates the public hostname (s3.amazonaws.com) with the endpoint's private IPs via a private hosted zone. After this, all S3 API calls from the VPC automatically route through the Interface Endpoint without code changes. Note: S3 also supports Gateway Endpoints (free) which use route tables instead of DNS.", domain:1},
  {q:"A company uses AWS Transit Gateway to connect 50 VPCs and an on-premises network. They want to ensure that VPCs in the 'production' segment cannot communicate with VPCs in the 'development' segment, but both can reach shared services (DNS, logging). How is this network segmentation implemented?", opts:["Use Security Groups to block cross-segment traffic","Create separate Transit Gateway route tables for production, development, and shared services. Associate VPCs with the appropriate route table and add routes only to permitted destinations","Use NACLs on each VPC to block traffic from other segment CIDRs","Create separate Transit Gateways for each segment"], correct:1, exp:"Transit Gateway route tables control traffic flow between attachments. By creating separate route tables (production, development, shared) and associating VPCs accordingly, you control what each segment can reach. Production route table has routes to shared services only (not dev). Dev route table has routes to shared services only (not prod). Shared services route table has routes to both. NACLs can work but don't scale for 50 VPCs. Security Groups control instance-level, not VPC-level routing.", domain:1},

  // Serverless patterns (heavily tested)
  {q:"A company builds a serverless API using API Gateway and Lambda. They need to handle file uploads up to 100MB. API Gateway has a 10MB payload limit. How should large file uploads be handled?", opts:["Increase API Gateway payload limit to 100MB in the stage settings","Generate a pre-signed S3 URL via Lambda and have the client upload directly to S3, then trigger processing via S3 event notification","Configure API Gateway with binary media types to handle larger payloads","Use a Network Load Balancer instead of API Gateway to remove payload limits"], correct:1, exp:"API Gateway has a hard 10MB payload limit that cannot be increased. The pattern for large uploads: (1) Client calls API to get a pre-signed S3 URL, (2) Lambda generates the URL with PutObject permission and returns it, (3) Client uploads directly to S3 using the pre-signed URL (no size limit), (4) S3 event triggers Lambda for processing. This bypasses API Gateway for the data payload entirely. NLB doesn't integrate natively with Lambda.", domain:3},
  {q:"A company uses Lambda with Python runtime. The deployment package is 250MB (maximum). They need to add more libraries that push it over the limit. What are their options?", opts:["Use Lambda Layers to separate shared libraries (each layer up to 250MB, total unzipped limit 250MB)","Package the Lambda function as a container image (supports up to 10GB)","Use Lambda EFS mount to load libraries at runtime from a shared file system","Both B and C work for exceeding the 250MB limit"], correct:3, exp:"Lambda container images support up to 10GB, far exceeding the 250MB zip limit. EFS mount allows dynamically loading libraries at runtime from a file system (no deployment size limit, but adds latency on cold start). Layers help organize code but the TOTAL unzipped size (function + all layers) still can't exceed 250MB. Both container image and EFS mount actually solve the >250MB problem.", domain:3},
  {q:"A company uses Step Functions to orchestrate a multi-step order processing workflow. One step calls a third-party payment API that sometimes takes 5 minutes to process. Step Functions charges per state transition. What is the MOST cost-effective way to handle this long-running API call?", opts:["Use a Wait state with a fixed duration of 5 minutes","Use a Task state with a .waitForTaskToken integration pattern — Lambda sends the task token to the third party, which calls back when done","Use an Activity state where a worker polls for tasks","Direct Lambda invocation with a 5-minute timeout"], correct:1, exp:"The .waitForTaskToken pattern pauses the execution (no charge while waiting) until a callback is received with the task token. Lambda sends the request + token to the payment API, which calls back to Step Functions SendTaskSuccess when done. This costs ONE state transition regardless of wait time. Wait states charge for the transition. Lambda sitting idle for 5 minutes costs for duration. Activity workers require managed compute.", domain:4},
  {q:"A company uses EventBridge to route events from multiple sources (S3, EC2, custom applications) to various targets. They need to ensure that a specific event is processed by a Lambda function EXACTLY ONCE, even if the event is delivered multiple times due to EventBridge's at-least-once delivery. How should this be implemented?", opts:["EventBridge guarantees exactly-once delivery so no action needed","Implement idempotency in the Lambda function using a DynamoDB table with conditional writes to track processed event IDs","Use SQS FIFO queue between EventBridge and Lambda for deduplication","Configure EventBridge retry policy with maximum of 1 retry"], correct:1, exp:"EventBridge provides at-least-once delivery (not exactly-once). The same event MAY be delivered more than once. Idempotency in the consumer (Lambda) is the correct pattern: store a unique event ID in DynamoDB with a conditional write (PutItem with ConditionExpression attribute_not_exists). If the event was already processed, the conditional write fails and Lambda skips processing. SQS FIFO dedup only works within a 5-minute window.", domain:2},
  {q:"A company uses API Gateway with Lambda authorizer (formerly Custom Authorizer) for authentication. The authorizer Lambda runs on every API request, adding 100ms latency and cost. Most requests come from the same users within short time windows. How should this be optimized?", opts:["Enable API Gateway authorization caching with an appropriate TTL (e.g., 300 seconds)","Move authentication logic into the main Lambda function to save an invocation","Use Cognito User Pool authorizer instead of Lambda authorizer","Implement JWT validation in the Lambda function without a separate authorizer"], correct:0, exp:"API Gateway caches authorizer responses for a configurable TTL. Subsequent requests with the same authorization token reuse the cached policy (no Lambda invocation). With a 300-second TTL, a user making 50 requests in 5 minutes triggers the authorizer ONCE instead of 50 times. This eliminates 98% of authorizer invocations (cost and latency). Cognito authorizer would work but changes the auth mechanism entirely.", domain:4},
  {q:"A serverless application uses DynamoDB. A new feature requires complex aggregation queries (GROUP BY, COUNT, AVG) across the entire table. DynamoDB doesn't natively support these operations. What is the BEST architectural approach?", opts:["Use DynamoDB Scan with FilterExpressions to compute aggregations in Lambda","Stream DynamoDB changes via DynamoDB Streams to Lambda, which maintains pre-computed aggregations in another DynamoDB table","Export DynamoDB table to S3 and query with Athena for ad-hoc aggregations","Use PartiQL SELECT statements with DynamoDB for SQL-like queries"], correct:1, exp:"DynamoDB Streams + Lambda maintains materialized aggregations in real-time. When items change, Lambda updates running counts/averages in an aggregation table. Reads of aggregations are then single GetItem operations (fast, cheap). Scanning the entire table is expensive and slow. Athena works for ad-hoc but has minutes of latency. PartiQL doesn't add GROUP BY/aggregate capabilities beyond what DynamoDB already supports.", domain:3},
  {q:"A company uses AWS SAM (Serverless Application Model) to deploy their serverless application. They want to deploy a new Lambda function version to production gradually, routing 10% of traffic to the new version initially. If error rate exceeds 1%, the deployment should automatically rollback. Which SAM deployment preference achieves this?", opts:["DeploymentPreference: Type: Linear10PercentEvery1Minute with Alarms referencing a CloudWatch alarm on error rate","DeploymentPreference: Type: Canary10Percent5Minutes with Alarms referencing a CloudWatch alarm on Lambda Errors metric","AutoPublishAlias with traffic routing weight manually set to 10%","CodeDeploy custom deployment configuration with halting condition"], correct:1, exp:"SAM DeploymentPreference with Canary10Percent5Minutes routes 10% to the new version for 5 minutes, then shifts to 100% if no alarms trigger. The Alarms property references CloudWatch alarms (e.g., Lambda Errors > 1%). If the alarm fires during the canary phase, CodeDeploy (which SAM uses under the hood) automatically rolls back. Linear would gradually increase over time rather than canary testing.", domain:2},

  // Database selection (critical exam topic)
  {q:"A company needs a database for their user profile service. Requirements: flexible schema (different users have different attributes), low-latency reads (<5ms), automatic scaling, and global replication for users worldwide. Which database is BEST?", opts:["Aurora PostgreSQL Global Database","DynamoDB Global Tables","Amazon DocumentDB with cross-region replication","Amazon Neptune"], correct:1, exp:"DynamoDB Global Tables provides: flexible schema (schemaless, each item can have different attributes), single-digit ms latency, automatic scaling (on-demand or auto-scaling), and active-active global replication with <1 second cross-region replication. Aurora requires schema. DocumentDB doesn't have native global tables. Neptune is for graph data (relationships), not profiles.", domain:3},
  {q:"A company needs a database for storing and querying social network relationships (friends, follows, mutual connections, friend-of-friend queries). Queries like 'find all users within 3 degrees of connection who also like jazz' must return in under 100ms. Which database is BEST?", opts:["DynamoDB with adjacency list pattern","Amazon Neptune (graph database)","Aurora PostgreSQL with recursive CTEs","Amazon OpenSearch with nested documents"], correct:1, exp:"Neptune is a purpose-built graph database optimized for traversing relationships. Queries like 'friends of friends of friends who like jazz' (3-hop graph traversal) execute in milliseconds using Gremlin or SPARQL. Relational databases require expensive recursive joins for multi-hop queries. DynamoDB adjacency lists work for 1-2 hops but become complex for deeper traversals. OpenSearch is for search, not graph traversals.", domain:3},
  {q:"A company needs a database for time-series IoT sensor data. Requirements: ingest 1 million data points per second, automatic data lifecycle management (hot/warm/cold), built-in time-series functions (interpolation, smoothing), and query performance that doesn't degrade as data ages. Which is BEST?", opts:["DynamoDB with TTL for lifecycle management","Amazon Timestream","InfluxDB on EC2","Amazon Redshift with time-series functions"], correct:1, exp:"Amazon Timestream is purpose-built for time-series: high-volume ingestion (millions of points/sec), automatic tiering (in-memory -> magnetic storage based on age), built-in time-series functions (interpolation, smoothing, approximation), and optimized time-based queries regardless of data volume. DynamoDB can store time-series but lacks specialized functions and automatic tiering. InfluxDB requires managing EC2. Redshift is for analytics, not real-time ingestion.", domain:3},
  {q:"A company needs to search 50 million product listings with faceted search (filter by price range, category, brand, rating), full-text search with typo tolerance, and autocomplete suggestions. Results must return in under 200ms. Which service provides this?", opts:["DynamoDB with scan and filter expressions","Amazon OpenSearch Service (Elasticsearch)","Amazon RDS with FULLTEXT indexes","Amazon Kendra"], correct:1, exp:"OpenSearch (Elasticsearch) is purpose-built for full-text search with faceted navigation, fuzzy matching (typo tolerance), autocomplete (suggestion API), and sub-200ms responses on 50M documents. DynamoDB can't do full-text search or facets. RDS FULLTEXT doesn't scale to 50M with facets efficiently. Kendra is for enterprise document search (AI-powered Q&A), not product catalog search.", domain:3},
  {q:"A company needs a database for a ledger application where every change must be cryptographically verifiable, immutable, and provide a complete audit trail. No record can ever be modified or deleted after creation. Which AWS service is purpose-built for this?", opts:["DynamoDB with delete protection enabled","Amazon QLDB (Quantum Ledger Database)","Aurora with delete prevention policies","S3 with Object Lock"], correct:1, exp:"Amazon QLDB is an immutable, cryptographically verifiable ledger database. Every change is recorded in an append-only journal. The hash chain makes any tampering detectable. It provides a complete, verifiable history of all changes. DynamoDB can be modified (not immutable). Aurora records can be updated. S3 Object Lock prevents deletion but isn't a database with query capabilities.", domain:3},

  // Multi-service architecture patterns
  {q:"A company needs to build a real-time notification system that pushes updates to millions of mobile devices, web browsers, and email addresses. Different users prefer different channels. Which AWS service handles multi-channel notification delivery?", opts:["Amazon SQS with multiple consumers per channel","Amazon SNS with platform application endpoints (push), email, and SMS subscriptions","Amazon Pinpoint for multi-channel messaging (push, email, SMS, voice) with user segmentation and campaign management","Amazon SES for email combined with SNS for push and SMS"], correct:2, exp:"Amazon Pinpoint is designed for multi-channel customer engagement: push notifications (iOS, Android, web), email, SMS, and voice. It includes user segmentation, campaign management, and analytics. SNS can do push/email/SMS but lacks user management, segmentation, and analytics. SES is email-only. For a notification system with channel preferences per user, Pinpoint provides the complete solution.", domain:3},
  {q:"A company needs to process PDF documents uploaded by customers: extract text, tables, and form data, then store the structured output. Documents are in various formats (scanned images, digital PDFs). Which service combination handles this?", opts:["Lambda with a PDF parsing library (pdf2text)","Amazon Textract for document processing (text, tables, forms) + S3 for storage + Lambda for orchestration","Amazon Comprehend for natural language processing of PDFs","Amazon Rekognition for image analysis of scanned documents"], correct:1, exp:"Amazon Textract uses ML to extract text, tables, and form key-value pairs from documents (both scanned images and digital PDFs). It handles handwriting, tables with merged cells, and form fields. Lambda orchestrates the workflow (trigger on S3 upload, call Textract, store results). Comprehend analyzes extracted TEXT (entity recognition, sentiment) but doesn't extract from documents. Rekognition is for images/video (faces, objects), not document parsing.", domain:3},
  {q:"A company uses AWS for a multi-tenant SaaS platform. Each tenant's data must be isolated. They have 5000 tenants and need the MOST operationally efficient isolation model. Which approach balances security with operational complexity?", opts:["Separate AWS account per tenant","Separate DynamoDB table per tenant","Shared DynamoDB table with tenant_id as partition key prefix and IAM policies with leading key condition for row-level isolation","Separate VPC per tenant"], correct:2, exp:"For 5000 tenants, shared infrastructure with logical isolation is the only operationally viable approach. Using tenant_id as partition key prefix with IAM policies (dynamodb:LeadingKeys condition) provides row-level isolation within a shared table. This scales to millions of tenants with minimal operational overhead. Separate accounts, tables, or VPCs for 5000 tenants is an operational nightmare (managing 5000 CloudFormation stacks, monitoring, etc.).", domain:1},
  {q:"A company needs to implement a fan-out pattern where a single event triggers 15 different processing pipelines. Each pipeline takes between 1 second and 10 minutes. Some pipelines are critical (must succeed), others are best-effort. How should this be architected?", opts:["SNS topic with 15 Lambda subscriptions","EventBridge with 15 rules targeting different services","SNS topic -> SQS queues for critical pipelines (guaranteed delivery with DLQ) and SNS -> Lambda direct for best-effort pipelines","Step Functions Parallel state with 15 branches"], correct:2, exp:"SNS -> SQS for critical pipelines ensures messages persist even if the consumer is down (SQS retries, DLQ for poison messages). SNS -> Lambda directly for best-effort pipelines provides simpler setup with fewer guarantees (if Lambda fails after retries, the message is lost). This tiered approach matches reliability requirements to infrastructure. EventBridge works but SQS provides better reliability for critical paths. Step Functions would be one execution (expensive at scale).", domain:2},
  {q:"A company uses Terraform to manage their AWS infrastructure. They want to detect when someone makes manual changes to AWS resources outside of Terraform (configuration drift). Which AWS service detects this?", opts:["AWS Config with desired configuration recording","AWS CloudTrail to detect manual API calls","AWS Config rules checking resources against expected state recorded in a custom rule","AWS Config drift detection comparing actual state against the CloudFormation/Terraform state file"], correct:0, exp:"AWS Config continuously records resource configurations. Config rules can evaluate whether resources match desired state (custom rules can compare against Terraform state). CloudFormation has built-in drift detection. For Terraform specifically, Config recording + custom rules or third-party tools (like Driftctl) compare actual AWS state against Terraform state file. CloudTrail shows who made changes but doesn't identify drift systematically.", domain:1},
  {q:"A company needs to run SQL queries that join data from their RDS PostgreSQL database, DynamoDB table, and S3 data lake in a single query. What service enables this cross-source querying?", opts:["Amazon Athena with federated query (using connectors for RDS and DynamoDB)","Amazon Redshift Spectrum for S3 + Redshift federated query for RDS + custom connector for DynamoDB","AWS Glue ETL to consolidate all data into one location first","Amazon EMR with Presto/Trino connectors to all three sources"], correct:0, exp:"Athena Federated Query uses data source connectors to query RDS, DynamoDB, and S3 in a single SQL statement. Connectors for DynamoDB, RDS, Redshift, and many other sources are available. This enables cross-source JOINs without ETL or data movement. Redshift federated query works for RDS and S3 but DynamoDB requires a custom connector. ETL consolidation adds latency and cost. EMR Presto works but requires managing a cluster.", domain:3},
]
