export const questionBank23 = [
  {q:"A company uses AWS Shield Standard and AWS Shield Advanced. What is the key difference?", opts:["Shield Standard costs extra (it's free)","Shield Standard: FREE, automatic DDoS protection for all AWS customers (L3/L4 attacks: SYN floods, UDP reflection). Shield Advanced: PAID ($3,000/month), adds: L7 attack protection, DDoS Response Team (DRT) 24/7 access, cost protection (credits for scaling costs during attacks), advanced metrics, and WAF included at no extra cost.","Shield Advanced only protects CloudFront","Shield Standard provides the same features as Advanced"], correct:1, exp:"Shield Standard vs Advanced: Standard (free, automatic): protects against common L3/L4 DDoS attacks for all AWS resources. Advanced ($3K/month + data fees): protects ALB, CloudFront, Route 53, Global Accelerator, EIP. Adds: (1) Real-time attack visibility, (2) DRT (experts help during attack), (3) Cost protection (scaling costs refunded), (4) WAF included (no separate WAF charges), (5) SLA guarantee. For businesses where DDoS impacts revenue significantly: Advanced pays for itself in one incident.", domain:1},
  {q:"A company uses ECS Fargate. They want to run a sidecar container (Datadog agent) alongside their application container in the same task. How many containers can a Fargate task have?", opts:["Only 1 container per task","A Fargate task can have up to 10 containers defined in the task definition. Multiple containers share the same network namespace (localhost communication), CPU/memory allocation, and lifecycle. Common sidecar patterns: logging agents, monitoring agents, service mesh proxies.","Maximum 2 containers per task","Sidecars require separate task definitions"], correct:1, exp:"ECS task definition supports up to 10 containers (both EC2 and Fargate). Containers in the same task: (1) Share network (communicate via localhost:port). (2) Share ephemeral storage volume. (3) Can have dependency ordering (startOrder). (4) Share the task's CPU/memory allocation. Sidecar patterns: log forwarder (Fluent Bit), monitoring (Datadog, CloudWatch agent), service mesh (Envoy), auth proxy. Define essential: true on the app container (task fails if it dies), essential: false on sidecars if appropriate.", domain:3},
  {q:"A company uses Amazon S3. Their bucket receives PutObject requests with the header 'x-amz-server-side-encryption: aws:kms'. What type of encryption is being applied?", opts:["No encryption (header is informational only)","SSE-KMS (Server-Side Encryption with KMS-managed keys) — S3 encrypts the object using a KMS key. Each object gets a unique data key (envelope encryption). Provides: audit trail (CloudTrail logs key usage), key rotation, and centralized key policy control.","SSE-S3 (Amazon S3-managed keys)","Client-side encryption"], correct:1, exp:"S3 encryption types by header: (1) x-amz-server-side-encryption: AES256 → SSE-S3 (S3 manages keys, no audit trail). (2) x-amz-server-side-encryption: aws:kms → SSE-KMS (KMS manages keys, CloudTrail audit, key policies). (3) x-amz-server-side-encryption-customer-key → SSE-C (customer provides key with each request). (4) No header + client encrypts before upload → Client-side encryption. SSE-KMS is the most common for compliance: provides key management, audit trail, and policy control. Slightly more expensive than SSE-S3 (KMS API call charges).", domain:1},
  {q:"A company uses AWS Global Accelerator with endpoints in us-east-1 and eu-west-1. An endpoint in us-east-1 becomes unhealthy. What happens to traffic?", opts:["Traffic continues to us-east-1 (no health checking)","Global Accelerator health checks detect the unhealthy endpoint and automatically routes traffic to eu-west-1 (healthy endpoint). Failover happens within seconds (30 seconds or less) because clients use static Anycast IPs — no DNS TTL delay.","Traffic is dropped until the endpoint recovers","Manual failover required"], correct:1, exp:"Global Accelerator vs Route 53 failover: Route 53: DNS-based, depends on TTL (clients may cache stale DNS for 60-300 seconds). Global Accelerator: network-based, uses static Anycast IPs that route at the network layer. When health check fails: Accelerator immediately routes packets to healthy endpoints in other regions. No DNS change needed, no TTL waiting. Failover is nearly instantaneous (within health check interval, typically 10-30 seconds). This makes Global Accelerator superior for fast failover requirements.", domain:2},
  {q:"A company uses Amazon RDS for MySQL. They need to audit ALL database queries (not just DDL but also SELECT statements) for compliance. How is this achieved?", opts:["RDS doesn't support query logging","Enable RDS MySQL advanced auditing (MariaDB Audit Plugin) or general_log/slow_query_log parameters. Publish audit logs to CloudWatch Logs for centralized analysis. For MySQL on RDS: enable audit log with EVENT='QUERY' to capture all SQL statements.","CloudTrail captures all database queries (it only captures API calls, not SQL queries)","Enable Performance Insights (shows top queries but not full audit log)"], correct:1, exp:"RDS MySQL audit logging options: (1) MariaDB Audit Plugin (available on RDS MySQL): configure SERVER_AUDIT_EVENTS = 'CONNECT,QUERY,TABLE' to log connections and all queries. (2) general_log: logs ALL queries (high volume, performance impact). (3) slow_query_log: only queries exceeding threshold. For compliance audit: MariaDB Audit Plugin is recommended — configurable, lower overhead than general_log. Publish to CloudWatch Logs: retention, search (Logs Insights), and alerting. CloudTrail logs AWS API calls (CreateDBInstance) — NOT SQL queries.", domain:1},
  {q:"A company uses EKS with pods that need different levels of AWS access (Pod A needs S3, Pod B needs DynamoDB). What is the recommended way to assign fine-grained IAM permissions to individual pods?", opts:["One IAM role for all pods on the node (too permissive)","EKS IAM Roles for Service Accounts (IRSA) — associate an IAM role with a Kubernetes service account. Pods using that service account receive temporary credentials for that specific role. Different pods on the same node get different permissions.","Environment variables with access keys per pod (insecure)","IAM policies on the EKS cluster itself (cluster-level, not pod-level)"], correct:1, exp:"IRSA (IAM Roles for Service Accounts): (1) Create IAM role with trust policy for the EKS OIDC provider. (2) Create Kubernetes ServiceAccount annotated with the IAM role ARN. (3) Pods using that ServiceAccount automatically receive temporary credentials (via OIDC token → STS AssumeRoleWithWebIdentity). Result: Pod A with SA-S3 gets S3 permissions. Pod B with SA-DynamoDB gets DynamoDB permissions. Same node, different permissions. EKS Pod Identity is the newer alternative (simpler setup, same concept).", domain:1},
  {q:"A company uses Amazon CloudWatch. They want to create a single alarm that triggers ONLY when multiple conditions are met simultaneously (CPU > 80% AND memory > 90% AND disk > 95%). What CloudWatch feature enables this?", opts:["Create three separate alarms (triggers on each independently)","CloudWatch Composite Alarms — combine multiple alarms with AND/OR/NOT logic. The composite alarm only enters ALARM state when ALL child alarms are in ALARM (AND logic). Reduces alert noise from individual metric spikes that aren't actually problematic.","CloudWatch doesn't support multi-condition alarms","Use a Lambda function to evaluate multiple metrics"], correct:1, exp:"Composite Alarms: create individual metric alarms (CPU, memory, disk) + one composite alarm with rule: ALARM(cpu_alarm) AND ALARM(memory_alarm) AND ALARM(disk_alarm). The composite alarm triggers only when ALL conditions are met simultaneously. Reduces false positives: CPU spike alone isn't concerning, but CPU + memory + disk together indicates real trouble. Also supports OR logic: alert if ANY system component is down. Composite alarms can nest up to 5 levels.", domain:2},
  {q:"A company uses AWS Transit Gateway. They want to inspect ALL traffic between VPCs (east-west traffic) using a third-party firewall in an inspection VPC. What architecture pattern achieves this?", opts:["Route all inter-VPC traffic through the firewall VPC","Transit Gateway with a centralized inspection VPC: (1) Create inspection VPC with firewall appliances behind a Gateway Load Balancer. (2) TGW route tables route all inter-VPC traffic through the inspection VPC. (3) Firewall inspects traffic and forwards clean traffic back to TGW for delivery to the destination VPC.","VPC Peering with packet inspection (not possible)","Security Groups between VPCs (SGs are per-instance, not per-VPC)"], correct:1, exp:"Centralized inspection architecture: (1) Spoke VPCs attach to TGW. (2) Inspection VPC hosts firewall fleet behind GWLB. (3) TGW route tables: spoke VPCs' default route (0.0.0.0/0 or specific VPC CIDRs) points to the inspection VPC attachment. (4) Traffic flow: Spoke A → TGW → Inspection VPC → GWLB → Firewall → GWLB → TGW → Spoke B. (5) Firewall inspects/logs/filters traffic. This is the AWS-recommended pattern for centralized traffic inspection. Gateway Load Balancer provides HA/scaling for the firewall fleet.", domain:1},
  {q:"A company uses Amazon EFS. They need to restrict specific EC2 instances from accessing certain directories on the EFS file system. Does EFS support directory-level access control?", opts:["EFS has no access control","EFS Access Points — define access points that provide a specific directory view and enforce a POSIX user/group ID. Different EC2 instances (or ECS tasks) use different access points, each seeing only their assigned directory with enforced permissions.","Standard POSIX file permissions only (managed by OS)","IAM policies per directory (not how EFS works)"], correct:1, exp:"EFS Access Points: (1) Define root directory path (e.g., /app1/data). (2) Enforce POSIX identity (UID/GID) for all operations through this access point. (3) Mount using the access point: mount -t efs -o accesspoint=fsap-xxx fs-xxx /mnt. The client sees ONLY the access point's root directory (can't navigate above it). Different access points = different directory views. Combined with IAM policies (condition on elasticfilesystem:AccessPointArn): control which roles can use which access points. Multi-tenant isolation on shared EFS.", domain:1},
  {q:"A company uses AWS Lambda@Edge to process CloudFront requests. They want to A/B test their website (50% of users see version A, 50% see version B). How can Lambda@Edge implement this?", opts:["CloudFront natively supports A/B testing (it doesn't)","Lambda@Edge on Viewer Request: check if the user has an A/B cookie. If not: randomly assign group A or B, set a cookie, and modify the request URI to serve the appropriate version. If cookie exists: route to their assigned version. This ensures each user consistently sees the same version.","Route 53 weighted routing between two distributions","Use API Gateway for A/B testing"], correct:1, exp:"Lambda@Edge A/B testing: (1) Viewer Request trigger checks for 'X-AB-Test' cookie. (2) No cookie: generate random 0-1, assign 'A' or 'B', modify origin request path (e.g., /v-a/ or /v-b/). (3) Viewer Response trigger: set Set-Cookie header with the assignment (persists for future requests). (4) Has cookie: read assignment, route accordingly. Cache key must include the cookie (separate cached versions per group). This is stateless, scalable A/B testing at the edge without backend changes.", domain:3},
  {q:"A company uses Amazon Aurora MySQL. They notice that writer instance CPU is at 90% but the queries are mostly reads. What is happening and how should they fix it?", opts:["Aurora writers can't handle reads","Reads are being sent to the writer endpoint instead of the reader endpoint. Application should use the READER endpoint (xxx-ro.cluster-xxx.region.rds.amazonaws.com) for read queries and the WRITER/cluster endpoint for writes only. This distributes read load across read replicas.","Aurora doesn't support read replicas","The writer instance is too small"], correct:1, exp:"Aurora endpoints: (1) Cluster (writer) endpoint: connects to the primary (read/write). (2) Reader endpoint: load-balances across read replicas (read-only). (3) Custom endpoints: subset of instances for specific workloads. If ALL queries hit the cluster endpoint: writer handles everything (reads + writes) → CPU maxed. Fix: application uses reader endpoint for SELECT queries and cluster endpoint for INSERT/UPDATE/DELETE. Connection poolers or ORMs often support separate read/write data sources. This immediately distributes load.", domain:3},
  {q:"A company uses AWS Cost Explorer's Reserved Instance recommendations. It recommends purchasing 10 × m5.xlarge 1-year All Upfront RIs. The recommendation shows 40% savings. What factors should they consider BEFORE purchasing?", opts:["Just buy them immediately","Consider: (1) Is the workload stable for 12 months? (will they migrate/resize?). (2) Compute Savings Plans might offer similar savings with more flexibility. (3) Are Spot Instances viable for any portion? (even more savings). (4) Partial upfront vs All upfront (cash flow). (5) Convertible vs Standard RI (can they exchange if needs change?). All affect the real ROI.","RIs always save money regardless of circumstances","Cost Explorer recommendations are always wrong"], correct:1, exp:"RI purchase considerations: (1) Workload stability: if migrating to containers/serverless in 6 months, RI is wasted. (2) Flexibility: Compute Savings Plans give ~5% less discount but apply to ANY instance type/region/service (EC2/Fargate/Lambda). (3) Commitment type: Standard RI (non-exchangeable, highest discount) vs Convertible (exchangeable, lower discount). (4) Payment: All Upfront (highest discount, capital outlay) vs Partial/No Upfront (less discount, better cash flow). (5) Right-sizing BEFORE buying: don't lock in oversized instances.", domain:4},
  {q:"A company uses AWS IoT Core for 100,000 IoT devices. Devices publish sensor data every 5 seconds. They need to store this data and run real-time analytics. What architecture handles this scale?", opts:["API Gateway + Lambda (API Gateway can't handle 100K concurrent WebSocket/MQTT connections at this message rate)","IoT Core (MQTT broker) → IoT Rules Engine → Kinesis Data Streams (buffer) → Lambda/Flink (real-time analytics) + Firehose → S3 (storage) + Athena (historical queries)","Direct DynamoDB writes from each device (connection management nightmare)","SQS queue per device (100K queues is unmanageable)"], correct:1, exp:"IoT architecture at scale: IoT Core handles MQTT connections for 100K+ devices natively (managed, auto-scaling MQTT broker). IoT Rules Engine routes messages to AWS services based on SQL-like rules. For 100K × 0.2 msg/sec = 20K messages/sec: Kinesis handles this easily (20 shards). From Kinesis: Flink/Lambda for real-time processing (anomaly detection, aggregation), Firehose for S3 delivery (Parquet format, partitioned by device/time). Athena for historical analysis. This is the reference IoT analytics architecture.", domain:3},
  {q:"A company uses Amazon RDS Oracle. They are concerned about licensing costs. Oracle licenses are expensive. What RDS options can reduce Oracle licensing costs?", opts:["No way to reduce Oracle costs on RDS","Use RDS License Included pricing (simpler but expensive) vs Bring Your Own License (BYOL — use existing Oracle licenses, reduce RDS costs). OR: migrate to Aurora PostgreSQL (eliminate Oracle licensing entirely — most cost-effective long-term solution). AWS SCT assesses migration effort.","Oracle is free on AWS","Use smaller instances (reduces license cost proportionally)"], correct:1, exp:"Oracle cost optimization on RDS: (1) BYOL (Bring Your Own License): use existing Oracle licenses → RDS charges only for infrastructure (significant savings if you already own licenses). (2) License Included: AWS includes the Oracle license in the hourly rate (simpler, no license management). (3) MIGRATE OFF ORACLE: Aurora PostgreSQL or MySQL → eliminate Oracle licensing completely. AWS SCT assesses conversion effort (many apps migrate with 80%+ automatic conversion). Long-term: migration saves the most. Short-term: BYOL reduces immediate costs.", domain:4},
  {q:"A company uses Amazon CloudWatch Synthetics (Canaries) to monitor their application's login flow. The Canary navigates to the login page, enters credentials, and verifies the dashboard loads. If the Canary fails at 3 AM, how is the team alerted?", opts:["Canaries don't have alerting capabilities","Canary failures generate CloudWatch Metrics (SuccessPercent, Duration). Create a CloudWatch Alarm on SuccessPercent < 100 → SNS notification → PagerDuty/email/SMS. Also: Canary failures can trigger CloudWatch Alarm → EventBridge rule → Lambda for automated response.","Must manually check Canary results daily","Canary sends direct emails on failure"], correct:1, exp:"Canary monitoring chain: (1) Canary runs on schedule (e.g., every 5 minutes). (2) Publishes metrics to CloudWatch: SuccessPercent (pass/fail), Duration (latency), step-level metrics. (3) CloudWatch Alarm: SuccessPercent < 100 for 2 consecutive evaluations → ALARM state. (4) Alarm action: SNS topic → email/SMS/PagerDuty. (5) Optional: EventBridge rule on alarm state → Lambda (attempt automated recovery like restarting a service). Full monitoring loop: synthetic test → metric → alarm → notification → response.", domain:2},
  {q:"A company uses S3 for backup. They enable Cross-Region Replication from us-east-1 to eu-west-1 for DR. After enabling, they notice replication metrics show failures for some objects. What are common causes of replication failures?", opts:["CRR has a 5TB object size limit (S3 objects are 5TB max anyway)","Common failure causes: (1) Objects encrypted with SSE-C (customer-provided keys — can't replicate without the key). (2) Source bucket owner doesn't own the object (objects uploaded by other accounts). (3) Missing replication role permissions. (4) Objects in Glacier/Deep Archive (must restore first). (5) Destination bucket doesn't allow the write.","CRR never fails once configured","Network issues between regions"], correct:1, exp:"CRR failure causes: (1) SSE-C encrypted objects: replication role doesn't have the encryption key. Fix: use SSE-S3 or SSE-KMS (specify dest KMS key in replication config). (2) Object ownership: if Account B put an object in Account A's bucket, Account A's replication role may not have access. Fix: use bucket owner enforced ownership. (3) Glacier/Deep Archive: objects must be in an active storage class. (4) IAM role lacks permissions: s3:GetObject on source + s3:ReplicateObject on destination. (5) Destination bucket policy blocks writes.", domain:2},
  {q:"A company uses Amazon API Gateway. They want to monetize their API — charge customers $0.01 per API call. How do they track per-customer usage and bill accordingly?", opts:["CloudWatch metrics per customer (not granular enough)","API Gateway Usage Plans + API Keys: (1) Create Usage Plans with rate limits and quotas per tier. (2) Assign API Keys to customers. (3) API Gateway tracks per-key invocation counts. (4) Use CloudWatch or API Gateway metrics to generate billing reports per API key. (5) Combine with AWS Marketplace for listing and billing.","Lambda tracks usage internally","API Gateway doesn't support usage tracking"], correct:1, exp:"API monetization on API Gateway: (1) Usage Plans define: rate limit (requests/second), burst, and quota (requests/day/month) per plan tier. (2) API Keys: each customer gets a unique key. (3) Per-key metrics: API Gateway logs per-key invocation count in CloudWatch. (4) Billing: query CloudWatch metrics per key to calculate charges. (5) For fully managed billing: list on AWS Marketplace (handles metering, billing, payments). Usage Plans provide both access control (quotas) and metering (tracking) in one feature.", domain:3},
  {q:"A company uses Elastic Load Balancing. Their ALB serves HTTPS (port 443) with a TLS certificate. They want to enforce that clients use TLS 1.2 or higher (disable TLS 1.0 and 1.1 for security). What ALB setting controls this?", opts:["TLS version is controlled by the client (can't be enforced server-side)","ALB Security Policy — select a security policy that only supports TLS 1.2+ (e.g., ELBSecurityPolicy-TLS-1-2-2017-01 or newer). Security policies define which TLS versions and cipher suites are accepted by the listener.","Modify the SSL certificate to disable old TLS versions","Use NLB instead (NLB also uses security policies for TLS listeners)"], correct:1, exp:"ALB/NLB HTTPS listener Security Policies: pre-defined combinations of TLS protocols and cipher suites. Examples: (1) ELBSecurityPolicy-2016-08 (default): supports TLS 1.0, 1.1, 1.2. (2) ELBSecurityPolicy-TLS-1-2-2017-01: TLS 1.2 ONLY (disables 1.0 and 1.1). (3) ELBSecurityPolicy-TLS13-1-2-2021-06: TLS 1.2 + 1.3. Choose based on client compatibility vs security requirements. Most modern browsers support TLS 1.2. AWS regularly updates policies. For strict compliance: select a TLS 1.2-only (or TLS 1.3) policy.", domain:1},
  {q:"A company uses Lambda functions with environment variables for configuration. They store a database URL in an environment variable. They're concerned about security — can other AWS services or users see the environment variable values?", opts:["Environment variables are visible to anyone with Lambda:GetFunction permission","Lambda environment variables are encrypted at rest using KMS (AWS managed key by default, or specify your own CMK). They are visible to anyone with lambda:GetFunctionConfiguration permission. For sensitive values: encrypt with a CMK and grant decrypt only to the function's execution role, or better: use Secrets Manager/Parameter Store SecureString instead of env vars.","Environment variables are always encrypted and never visible","Only the function code can access environment variables"], correct:1, exp:"Lambda env var security: (1) At rest: encrypted with KMS (AWS-managed or customer CMK). (2) Visibility: anyone with lambda:GetFunction or lambda:GetFunctionConfiguration IAM permission can see the DECRYPTED values in the console/API. (3) For truly sensitive values: use Secrets Manager (accessed at runtime, not stored with the function config, separate access control). (4) Additional protection: use Lambda's encryption helpers (client-side encryption with CMK — value appears encrypted in GetFunction, only decrypted at runtime by the function). Best practice: secrets go in Secrets Manager, not env vars.", domain:1},
  {q:"A company uses S3. They want to analyze access patterns across ALL their buckets (which buckets are most accessed, growth trends, storage class distribution, encryption status). What S3 feature provides this organization-wide view?", opts:["S3 Storage Lens — provides organization-wide visibility into S3 storage: usage metrics, activity metrics, storage class analysis, and recommendations across all accounts and buckets. Interactive dashboard with 28+ days of historical data.","S3 Analytics (per-bucket only)","CloudWatch S3 metrics (limited to request-level)","AWS Cost Explorer S3 breakdown (cost only, not operational metrics)"], correct:0, exp:"S3 Storage Lens: (1) Organization-wide dashboard covering ALL accounts and buckets. (2) Metrics: storage amount, object count, average object size, requests per bucket/account, storage class breakdown, encryption percentage, versioning status, replication coverage. (3) 28+ days of FREE tier history, 15 months with Advanced. (4) Recommendations: identify cost optimization opportunities (unencrypted buckets, incomplete multipart uploads, non-current versions). (5) Advanced: prefix-level metrics, CloudWatch publishing. The 'single pane of glass' for S3 operations.", domain:4},
  {q:"A company uses ECS with an ALB. They have two services (User Service and Order Service) on the same ECS cluster. They want ONE ALB with path-based routing: /users/* → User Service, /orders/* → Order Service. How is this configured?", opts:["Two separate ALBs required (one per service)","Single ALB with two Target Groups and listener rules: Rule 1: path=/users/* → Target Group A (User Service tasks). Rule 2: path=/orders/* → Target Group B (Order Service tasks). ECS automatically registers tasks in the appropriate target group based on service configuration.","ALB doesn't support path-based routing (it does)","ECS doesn't integrate with ALB path routing"], correct:1, exp:"ALB path-based routing with ECS: (1) Create ALB with HTTPS listener. (2) Create Target Group A (User Service), Target Group B (Order Service). (3) Listener rules: IF path IS /users/* THEN forward to TG-A, IF path IS /orders/* THEN forward to TG-B. (4) ECS Service A: configure load balancer = ALB, target group = TG-A. (5) ECS Service B: configure load balancer = ALB, target group = TG-B. ECS automatically registers/deregisters tasks as they scale. One ALB serves multiple services — cost-efficient and simple.", domain:3},
  {q:"A company uses Amazon EventBridge. They want to receive events from a PARTNER SaaS application (e.g., Shopify order created, Zendesk ticket opened). Does EventBridge support this?", opts:["EventBridge only receives AWS events","Yes — EventBridge Partner Event Sources: SaaS partners (Shopify, Zendesk, Auth0, Datadog, MongoDB, etc.) publish events directly to your EventBridge event bus. Configure the partner integration, create rules matching partner events, and route to any AWS target (Lambda, Step Functions, SQS, etc.).","Must use webhooks with API Gateway instead","Partners can only publish to SNS"], correct:1, exp:"EventBridge partner integrations: 30+ SaaS partners can publish events directly to an EventBridge partner event bus in your account. Setup: (1) Partner creates an event source mapping. (2) You accept the partner event source in your account. (3) Create rules matching the partner's event patterns. (4) Route to targets (Lambda, Step Functions, SQS, etc.). Examples: Shopify order events → Lambda for fulfillment, Zendesk ticket → Step Functions for SLA tracking. Also supports custom events via PutEvents API for your own applications.", domain:2},
  {q:"A company uses Amazon Redshift Serverless. How does it differ from provisioned Redshift?", opts:["Same thing with a different name","Redshift Serverless: no cluster management — automatically provisions and scales compute (measured in RPU — Redshift Processing Units). Pay per query based on compute consumed. Ideal for: variable/unpredictable workloads, ad-hoc analytics, and teams that don't want to manage cluster sizing. Provisioned: you choose node type and count (predictable cost, full control, better for steady workloads).","Serverless is always cheaper","Serverless doesn't support SQL"], correct:1, exp:"Redshift Serverless vs Provisioned: Serverless: (1) No cluster/node selection — specify base RPU capacity (auto-scales). (2) Pay per RPU-second of compute consumed. (3) Automatically suspends when idle (no charge). (4) Instantly available (no cluster creation wait). Provisioned: (1) Choose node type (ra3.xlplus, ra3.4xlarge, ra3.16xlarge) and count. (2) Pay per node-hour (running or not). (3) Full control of concurrency scaling, WLM, maintenance windows. For predictable heavy workloads: provisioned (lower cost). For variable/bursty: Serverless (simplicity + no idle cost).", domain:4},
  {q:"A company has a Lambda function that needs to process messages from 5 different SQS queues. Must they create 5 separate Lambda functions?", opts:["Yes — each SQS queue needs its own Lambda function","No — one Lambda function can have MULTIPLE event source mappings from different SQS queues. The same function code processes messages from all 5 queues. Use the event source ARN in the event payload to identify which queue the message came from if different handling is needed.","Lambda can only have one trigger","SQS doesn't support Lambda integration"], correct:1, exp:"Lambda event source mappings: one function can be triggered by multiple sources. Add 5 SQS event source mappings to the same function — each queue triggers the same function independently. In the Lambda event: each message includes eventSourceARN (identifies which queue). Function code can branch logic based on the source queue. Scaling: each queue's event source mapping scales Lambda independently (up to the function's concurrency limit total across all sources). Simplifies deployment when processing logic is shared.", domain:2},
  {q:"A company uses AWS Config. They want to automatically tag all new EC2 instances that are launched without a 'CostCenter' tag. What approach implements this?", opts:["Config can't modify resources","AWS Config rule detects untagged instances (non-compliant) → triggers automatic remediation → SSM Automation document 'AWS-SetRequiredTags' applies the missing tag with a default value. Or: EventBridge rule on RunInstances → Lambda adds the tag immediately after launch.","AWS Organizations tag policies add missing tags (they only enforce, not add)","IAM policy that prevents launching without tags (preventive, not additive)"], correct:1, exp:"Two approaches: (1) REACTIVE (Config): Config rule aws-required-tags evaluates compliance. Non-compliant → auto-remediation SSM document tags the instance. Delay: 5-15 minutes after launch. (2) IMMEDIATE (EventBridge): EventBridge rule on EC2 RunInstances API → Lambda checks for CostCenter tag → adds default if missing. Executes within seconds of launch. (3) PREVENTIVE (bonus): IAM Condition aws:RequestTag/CostCenter denying RunInstances without the tag — blocks launch entirely. Choose: preventive (strictest), immediate remediation (flexible), or Config (audit + gradual fix).", domain:2},
  {q:"A company uses Amazon Forecast. What does this service do?", opts:["Weather forecasting for AWS regions","Amazon Forecast is a MANAGED time-series forecasting service using ML — predict future values based on historical data (demand forecasting, inventory planning, resource capacity, financial forecasting). Input: historical time-series data + related data (weather, promotions). Output: point and probabilistic forecasts (P10, P50, P90 confidence intervals). No ML expertise needed.","Database query optimization","Cost forecasting only (that's Cost Explorer)"], correct:1, exp:"Amazon Forecast: (1) Input: historical data (sales by product/day, server metrics, energy consumption). (2) ML algorithms: DeepAR+, Prophet, ETS, NPTS, CNN-QR — automatically selects best model. (3) Output: future predictions with confidence intervals (P10: 10% chance of being below this, P50: median, P90: upper bound). (4) Use cases: retail demand forecasting, workforce scheduling, capacity planning, financial planning. Related time-series (weather, holidays, promotions) improve accuracy. No model selection or hyperparameter tuning required.", domain:3},
  {q:"A company has an RDS instance in a private subnet. A developer needs to connect to it from their laptop for debugging. The instance has no public IP and no VPN is set up. What is the quickest secure access method?", opts:["Make the RDS instance public (security risk)","Use Systems Manager Session Manager port forwarding: start a session to a bastion-like EC2 instance (or even directly via SSM), forward the RDS port to the developer's localhost. No SSH keys, no bastion instance needed if SSM is configured on any instance in the VPC.","Temporarily add a public IP to RDS (not possible after creation)","Open port 3306 to the developer's IP in the SG (RDS is in private subnet, no route from internet)"], correct:1, exp:"SSM Session Manager port forwarding: (1) An EC2 instance in the same VPC with SSM Agent (can be a tiny t3.nano). (2) Developer runs: aws ssm start-session --target i-xxx --document-name AWS-StartPortForwardingSessionToRemoteHost --parameters host=rds-endpoint,portNumber=3306,localPortNumber=3306. (3) Connect MySQL client to localhost:3306 → tunneled through SSM to RDS. No VPN, no SSH keys, no public IPs, full IAM authentication, and CloudTrail audit trail. Fastest zero-infrastructure secure access method.", domain:2},
  {q:"A company uses AWS Outposts. They want to use S3 on Outposts for local data residency. How does S3 on Outposts differ from regular S3?", opts:["S3 on Outposts is identical to regional S3","S3 on Outposts stores data LOCALLY on the Outpost hardware (never leaves your premises). Capacity is limited to what's provisioned on the Outpost. Uses S3 access points (mandatory) and has a subset of S3 features. No storage classes — all data stays on the Outpost. For data sovereignty/residency requirements.","S3 on Outposts replicates to the AWS region","Outposts don't support S3"], correct:1, exp:"S3 on Outposts specifics: (1) Data residency: data stored ONLY on the Outpost (guaranteed to not leave your premises). (2) Capacity: limited to the Outpost's storage capacity (must order sufficient capacity). (3) Access: via S3 access points only (no direct bucket URL). (4) Features: subset of regional S3 (no storage classes, no lifecycle rules to other classes, no CRR to regions). (5) Use case: data sovereignty, local processing, low-latency access to on-premises data using S3 API. Management via AWS console/API same as regional S3.", domain:3},
  {q:"A company uses VPC Flow Logs and wants to analyze network traffic patterns. They store Flow Logs in S3 in Parquet format. Which query best identifies rejected traffic attempts (potential security probes)?", opts:["CloudWatch Metrics (only aggregate counts)","Athena query on Flow Logs in S3: SELECT srcaddr, dstaddr, dstport, protocol, COUNT(*) as attempts FROM vpc_flow_logs WHERE action = 'REJECT' AND date = '2024/01/15' GROUP BY srcaddr, dstaddr, dstport, protocol ORDER BY attempts DESC LIMIT 100 — identifies top rejected connection attempts (potential attackers or misconfigured resources).","VPC Flow Logs don't include action (ACCEPT/REJECT)","Must use GuardDuty for all traffic analysis"], correct:1, exp:"VPC Flow Logs + Athena security analysis: Flow Logs include an 'action' field (ACCEPT or REJECT). REJECT = traffic blocked by Security Group or NACL. Aggregating rejected traffic reveals: (1) Port scans (many different ports from one source). (2) Unauthorized access attempts (repeated REJECT to known ports). (3) Misconfigured applications (internal services being rejected). Parquet format in S3 with Athena provides cost-effective, ad-hoc security analysis. Partitioned by date for efficient queries. This complements GuardDuty's automated detection.", domain:1},
  {q:"A company uses Amazon Rekognition. What computer vision capabilities does it provide?", opts:["Only text recognition (that's Textract for documents)","Rekognition provides: object/scene detection, facial analysis (age, emotion, gender), face comparison/search, celebrity recognition, text-in-image detection, content moderation (NSFW), PPE detection, and custom labels (train on your images). Image and video analysis via API — no ML expertise needed.","Only facial recognition","Only image classification"], correct:1, exp:"Amazon Rekognition capabilities: (1) Label Detection: objects and scenes (car, tree, beach). (2) Face Detection & Analysis: landmarks, emotions, age range, glasses, gender. (3) Face Comparison: compare faces across images (identity verification). (4) Face Search: search a collection of indexed faces. (5) Celebrity Recognition. (6) Text in Image: detect and read text (signs, license plates). (7) Content Moderation: detect inappropriate content. (8) Custom Labels: train on your images for custom detection. (9) Video analysis: tracking objects/people across frames. All via simple API calls.", domain:3},
  {q:"A company has a highly available application deployed in us-east-1 across 3 AZs. They want to add a SECOND region (eu-west-1) for disaster recovery with a target RTO of 15 minutes. What is the recommended DR architecture?", opts:["Backup and Restore (RTO would exceed 15 minutes for complex apps)","Warm Standby: scaled-down but RUNNING infrastructure in eu-west-1. Aurora Global Database (replication). ALB + minimal EC2/ECS capacity running. During disaster: scale up to full capacity (takes minutes) + Route 53 failover switches DNS. Pre-deployed infrastructure = fast recovery within 15 minutes.","Multi-site active/active (overkill and expensive for DR with 15-min RTO)","Only use one region with Multi-AZ (doesn't protect against region failure)"], correct:1, exp:"DR strategies by RTO: (1) Backup/Restore: RTO hours. (2) Pilot Light: RTO 30-60 min. (3) Warm Standby: RTO 5-15 min. (4) Multi-site: RTO near zero. For 15-min RTO: Warm Standby is optimal. Deploy in eu-west-1: Aurora Global Database reader (promote in <1 min), scaled-down ECS/EC2 (scale up in 5-10 min), pre-configured ALB, Route 53 failover health check. Total: promote DB (1 min) + scale compute (5-10 min) + DNS failover (30 sec) = well within 15 minutes. Cost: 10-30% of production (minimal running infrastructure).", domain:2},
  {q:"A company uses DynamoDB and needs to perform complex aggregations (SUM, AVG, GROUP BY) across millions of items. DynamoDB Query/Scan doesn't support aggregation functions. What approach works?", opts:["DynamoDB supports GROUP BY (it doesn't)","Export DynamoDB table to S3 (DynamoDB Export feature → Parquet format) → query with Athena using full SQL aggregation. For real-time: DynamoDB Streams → Lambda that maintains pre-computed aggregations in a separate DynamoDB table or ElastiCache.","DynamoDB Global Secondary Indexes support aggregations (they don't)","Scan the entire table and aggregate in application code (expensive and slow at scale)"], correct:1, exp:"DynamoDB analytics patterns: (1) Batch analytics: DynamoDB Export to S3 (no RCU consumed, exports to Parquet) → Athena for SQL aggregations. Ideal for periodic reports. (2) Real-time aggregations: DynamoDB Streams → Lambda → maintains running aggregates (e.g., sum_by_category table, updated incrementally on each change). (3) Complex analytics: export to Redshift/OpenSearch for advanced queries. DynamoDB is optimized for key-value access patterns — aggregations should be pushed to purpose-built analytics services.", domain:3},
  {q:"A company uses AWS Network Firewall in their VPC. What does Network Firewall provide that Security Groups and NACLs don't?", opts:["Same as NACLs with a different interface","AWS Network Firewall provides: stateful deep packet inspection (L3-L7), IDS/IPS (intrusion detection/prevention), domain name filtering (allow/block specific domains), TLS inspection (decrypt and inspect HTTPS traffic), and Suricata-compatible rules. SGs and NACLs operate at L3/L4 only (IP/port) with no payload inspection.","Network Firewall only works with NLB","It's only for blocking DDoS attacks (that's Shield)"], correct:1, exp:"Network Firewall vs SGs/NACLs: SGs: L4, stateful, instance-level, allow rules only. NACLs: L4, stateless, subnet-level, allow/deny. Network Firewall: L3-L7, stateful, VPC-level, advanced features. Unique capabilities: (1) Domain filtering: allow only *.amazonaws.com traffic. (2) Deep packet inspection: inspect HTTP headers/body. (3) IPS rules: detect and block exploit patterns (Suricata rules). (4) TLS inspection: decrypt HTTPS, inspect, re-encrypt. (5) Centralized logging of all traffic decisions. Use for: compliance requirements needing deep inspection, outbound domain control, IDS/IPS.", domain:1},
  {q:"A company uses Amazon QuickSight for business intelligence dashboards. What is SPICE in QuickSight?", opts:["A type of QuickSight chart","SPICE (Super-fast, Parallel, In-memory Calculation Engine) is QuickSight's in-memory cache/storage. Data imported into SPICE is stored in QuickSight's infrastructure for fast querying — dashboards load instantly without querying the source database. SPICE capacity: allocated per region. Refresh schedules keep SPICE data current.","A data source connector","A security feature"], correct:1, exp:"QuickSight SPICE: (1) In-memory engine storing imported data. (2) Dashboards query SPICE (not the source DB) → instant response. (3) Scheduled refresh: hourly/daily/weekly updates from source (Athena, RDS, Redshift, S3, etc.). (4) Capacity: starts at 10GB per author, purchasable per GB. (5) Benefits: fast dashboards without hitting production databases, no per-query cost on sources. (6) Alternative: Direct Query mode (queries source live — slower but always current). SPICE is recommended for most dashboards; Direct Query for real-time data needs.", domain:3},
]
