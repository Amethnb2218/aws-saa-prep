export const questionBank9 = [
  // S3 Deep Dive (most tested service on the exam)
  {q:"A company stores 200TB of data in S3. They need to ensure that accidentally deleted objects can be recovered for up to 30 days. The solution should not significantly increase storage costs. What should they enable?", opts:["S3 Versioning with lifecycle policies to delete old versions after 30 days","S3 Cross-Region Replication","S3 Object Lock in Governance mode","Daily S3 snapshots to another bucket"], correct:0, exp:"S3 Versioning keeps all versions of an object. When deleted, a delete marker is placed (recoverable by removing the marker). A lifecycle policy can automatically delete non-current versions after 30 days to control costs. CRR replicates but doesn't help with accidental deletes (the delete replicates too unless configured otherwise). Object Lock prevents deletion entirely.", domain:1},
  {q:"A company has an S3 bucket that receives 50,000 PUT requests and 200,000 GET requests per second. They experience HTTP 503 Slow Down errors. How should they resolve this?", opts:["Request an S3 service limit increase from AWS Support","Use randomized prefixes in object key names to distribute requests across partitions","Enable S3 Transfer Acceleration","Add CloudFront in front of S3"], correct:1, exp:"S3 automatically partitions based on key name prefix. Randomized prefixes (like adding a hash) distribute requests evenly across partitions, each supporting 3,500 PUT/5,500 GET per second. With random distribution across many prefixes, total throughput scales horizontally. CloudFront helps GETs but not PUTs. Transfer Acceleration is for upload speed, not throughput.", domain:3},
  {q:"A company needs to host a static website with a custom domain (www.example.com) over HTTPS. The website assets are stored in S3. What is the correct architecture?", opts:["S3 static website hosting with an ACM certificate attached to the bucket","CloudFront distribution with S3 origin, ACM certificate in us-east-1, and Route 53 alias record to CloudFront","S3 static website hosting with Route 53 alias record directly to the S3 website endpoint","ALB with S3 as a target and ACM certificate on the ALB"], correct:1, exp:"S3 static website hosting doesn't support HTTPS with custom domains directly. CloudFront provides HTTPS with an ACM certificate (must be in us-east-1 for CloudFront). Route 53 alias to the CloudFront distribution maps the custom domain. S3 can't attach ACM certificates. ALB can't target S3 directly.", domain:3},
  {q:"A company uses S3 for storing sensitive financial documents. They need to ensure that no object can ever be publicly accessible, regardless of individual bucket policies or ACLs. What provides the STRONGEST preventive control?", opts:["S3 Block Public Access at the account level (all 4 settings enabled)","Bucket policies denying public access on each bucket","Disable ACLs on each bucket (BucketOwnerEnforced)","AWS Config rule detecting public buckets"], correct:0, exp:"S3 Block Public Access at the ACCOUNT level overrides any bucket-level policy or ACL that would grant public access. Even if someone creates a bucket with a public policy, the account-level block prevents it from working. This is the strongest preventive control. Bucket policies can be modified per-bucket. Disabling ACLs helps but doesn't prevent public bucket policies. Config is detective.", domain:1},
  {q:"A company stores log files in S3 that are queried with Athena. Currently using CSV format, queries scan the entire dataset (100TB) and take 15 minutes. Monthly Athena cost is $50,000. Which optimization provides the GREATEST cost and performance improvement?", opts:["Enable S3 Select to pre-filter data before Athena processes it","Convert CSV to Apache Parquet format with Snappy compression and partition by date","Move data to Amazon Redshift for faster queries","Use S3 Intelligent-Tiering to reduce storage costs"], correct:1, exp:"Parquet is columnar — Athena reads only needed columns (CSV reads entire rows). A query reading 5 of 50 columns scans 90% less data. Snappy compression further reduces scanned bytes. Partitioning by date means queries with date filters skip irrelevant partitions entirely. Combined: 100TB scan -> 1-5TB scan = 95% cost reduction + 10x faster. S3 Select doesn't work with Athena. Intelligent-Tiering affects storage cost, not query cost.", domain:3},
  {q:"A company needs to replicate S3 objects from a bucket in us-east-1 to a bucket in eu-west-1 for disaster recovery. They also need to replicate to another bucket in the same region for a different team. Which replication configuration supports this?", opts:["Create two separate replication rules in the source bucket — one for CRR (cross-region) and one for SRR (same-region)","CRR only supports one destination — use Lambda to copy to the second bucket","Create a replication rule with multiple destinations (S3 supports multi-destination replication)","Enable bi-directional replication between all three buckets"], correct:0, exp:"S3 supports multiple replication rules on the same bucket. Each rule can have a different destination (same-region or cross-region), different filters (prefix/tag), and different storage classes. You create one CRR rule targeting eu-west-1 and one SRR rule targeting the same-region bucket. Multi-destination in a single rule isn't supported — it's multiple rules.", domain:2},
  {q:"A company uses pre-signed URLs to allow customers to download files from S3. They notice that some customers share the URLs publicly. How can they prevent unauthorized redistribution of pre-signed URLs?", opts:["Set a very short expiration time (5 minutes) on the pre-signed URL","Use CloudFront signed URLs with IP address restriction in the policy","Disable pre-signed URL generation on the bucket","Use S3 Object Lock to prevent downloading"], correct:1, exp:"CloudFront signed URLs support custom policies that can restrict by IP address, date range, and URL path. This prevents shared URLs from working on different IPs. S3 pre-signed URLs cannot restrict by IP. Short expiration helps but doesn't prevent sharing during the valid window. Disabling pre-signed URLs isn't possible (it's a feature of the SDK). Object Lock prevents deletion, not downloading.", domain:1},
  {q:"A company stores objects in S3 Standard. Analysis shows the following access pattern: 20% of objects are accessed daily, 30% are accessed weekly, 50% are never accessed after the first day. The company doesn't know in advance which objects fall into which category. What storage optimization should they use?", opts:["Lifecycle policy moving all objects to Standard-IA after 1 day","S3 Intelligent-Tiering","Move all objects to S3 One Zone-IA","Use S3 Analytics to identify access patterns, then create lifecycle rules"], correct:1, exp:"S3 Intelligent-Tiering automatically monitors access patterns per-object and moves objects between tiers (Frequent, Infrequent, Archive Instant, Archive, Deep Archive) without retrieval fees. Since access patterns are unpredictable per-object, IT is ideal. Moving everything to IA after 1 day would charge retrieval fees for the 20% accessed daily. S3 Analytics helps identify patterns but requires manual action.", domain:4},

  // ELB & Auto Scaling (extremely common on exam)
  {q:"A company runs a web application behind an Application Load Balancer. They need to route requests to different target groups based on the URL path: /api/* to backend servers and /static/* to a Lambda function. How should the ALB be configured?", opts:["Create two listeners on different ports, each pointing to a different target group","Create path-based routing rules on a single listener: /api/* routes to EC2 target group, /static/* routes to Lambda target group","Deploy two ALBs — one for each path","Use a Network Load Balancer with TCP routing based on content"], correct:1, exp:"ALB supports path-based routing rules on a single listener. Multiple rules can route to different target groups based on URL path patterns. ALB target groups can contain EC2 instances, IP addresses, OR Lambda functions. A single ALB with listener rules is the simplest architecture. NLB operates at Layer 4 and can't route based on URL paths.", domain:3},
  {q:"A company needs a load balancer that provides a static IP address for whitelisting by partner companies. The application is a TCP-based service that requires millions of requests per second with ultra-low latency. Which load balancer type is appropriate?", opts:["Application Load Balancer with Elastic IP","Network Load Balancer (NLB) with Elastic IP per AZ","Classic Load Balancer with Elastic IP","Global Accelerator with an ALB"], correct:1, exp:"NLB operates at Layer 4 (TCP/UDP), supports Elastic IPs for static IP addresses, handles millions of connections per second with ultra-low latency (<100 microseconds). ALB operates at Layer 7 and doesn't support Elastic IPs directly. Classic LB doesn't support EIP either. Global Accelerator provides static IPs but adds latency and cost vs NLB for pure TCP.", domain:3},
  {q:"An Auto Scaling Group uses a target tracking scaling policy targeting 50% CPU utilization. During a sudden traffic spike, it takes 5 minutes for new instances to become healthy and serve traffic. How can the response time be improved?", opts:["Use step scaling instead of target tracking","Enable predictive scaling based on historical patterns in addition to target tracking","Use warm pools to keep pre-initialized instances in a stopped state, ready to start quickly","Both B and C: predictive scaling anticipates spikes, warm pools reduce launch time"], correct:3, exp:"Predictive scaling uses ML to forecast traffic and pre-scales before the spike hits (based on recurring patterns). Warm pools maintain pre-initialized instances in stopped state — when scaling out, they start (not launch from scratch), skipping the 5-minute bootstrap. Together: instances are provisioned before the spike (predictive) and start quickly (warm pools). Step scaling reacts after the fact.", domain:2},
  {q:"A company's ALB health check is configured with: interval 30s, healthy threshold 3, unhealthy threshold 2, timeout 5s. What is the FASTEST an unhealthy instance can be detected and removed from service?", opts:["35 seconds (timeout + interval)","65 seconds (2 failed checks × interval + timeout)","35 seconds (1 interval for first check + timeout for second check)","Between 5 and 35 seconds depending on when the failure occurs relative to the check cycle"], correct:0, exp:"The fastest detection: the next health check runs (up to 30s wait), it times out after 5s = 35 seconds for the first unhealthy check. Then the next check in 30s also fails = total 65s for 2 consecutive failures. But the FASTEST possible is when the health check is about to run — the minimum is 5+5=10s (two immediate consecutive failures). With defaults, typical detection is 35-65 seconds. The answer considers unhealthy threshold of 2.", domain:2},
  {q:"A company uses an Auto Scaling Group with On-Demand instances. They want to reduce costs by using Spot Instances for 70% of capacity while maintaining reliability. What ASG configuration achieves this?", opts:["Create two separate ASGs — one for On-Demand (30%) and one for Spot (70%)","Use a mixed instances policy in a single ASG with On-Demand base capacity and Spot percentage above base","Replace all instances with Spot and add an On-Demand failover ASG","Use Spot Fleet instead of Auto Scaling Group"], correct:1, exp:"ASG mixed instances policy supports defining On-Demand base capacity (minimum reliable instances) and a percentage split above base (e.g., 70% Spot, 30% On-Demand). Multiple instance types diversify the Spot pool for availability. Single ASG manages the entire fleet (scaling, health checks, lifecycle). Two separate ASGs don't coordinate scaling. Spot Fleet doesn't integrate with ASG features.", domain:4},
  {q:"A company wants to terminate instances with the old launch template version first when the Auto Scaling Group scales in. Which termination policy should they use?", opts:["OldestLaunchTemplate — terminates instances from the oldest launch template first","OldestInstance — terminates the oldest running instance first","Default — already prefers instances from the oldest launch configuration/template","NewestInstance — keep old instances running longer"], correct:0, exp:"OldestLaunchTemplate termination policy removes instances using older launch template versions first. This helps ensure that after scaling in, the remaining instances use the newest launch template (latest AMI, configuration). The default policy first balances across AZs, then considers launch template age — it doesn't always pick the oldest template first. OldestInstance is by age, not template version.", domain:2},

  // RDS & Aurora (very heavily tested)
  {q:"A company needs to migrate their on-premises Oracle database to AWS. They want to move to an open-source database to reduce licensing costs. The application uses Oracle-specific PL/SQL stored procedures extensively. What is the recommended migration approach?", opts:["Use DMS to migrate directly from Oracle to Aurora PostgreSQL","Use AWS SCT (Schema Conversion Tool) to convert Oracle schema and PL/SQL to PostgreSQL, then DMS for data migration","Rewrite all stored procedures manually, then use DMS","Use RDS for Oracle to maintain compatibility"], correct:1, exp:"AWS SCT automates the conversion of Oracle PL/SQL stored procedures, triggers, and schema to PostgreSQL equivalents. It identifies what converts automatically vs. what needs manual review. DMS then handles the data migration (full load + CDC for minimal downtime). Direct DMS migration doesn't convert stored procedures. Manual rewrite is expensive. RDS Oracle doesn't save licensing costs.", domain:2},
  {q:"A company's Aurora MySQL cluster performs well for OLTP workloads but a new reporting requirement needs complex queries that would impact production. The reports can tolerate data up to 1 hour old. What is the MOST cost-effective solution?", opts:["Add Read Replicas dedicated to reporting","Use Aurora cloning to create a periodic copy for reporting (refresh hourly)","Create an Aurora custom endpoint for reporting queries","Replicate to Redshift with DMS for reporting"], correct:1, exp:"Aurora cloning creates a copy-on-write clone of the database in minutes (nearly instant, minimal storage initially). Clone hourly for reporting — it's isolated from production with no performance impact. Cost: only storage for changed data. Read Replicas share the storage volume (could still impact I/O). Custom endpoints still use the same cluster. Redshift + DMS is expensive for this simple use case.", domain:4},
  {q:"A company uses RDS PostgreSQL with Multi-AZ. They need to perform a major version upgrade (PostgreSQL 11 to 15). The application can tolerate a maximum of 5 minutes of downtime. What is the BEST upgrade approach?", opts:["Use RDS in-place major version upgrade (ModifyDBInstance) which requires a short outage","Create an Aurora PostgreSQL 15 cluster and use DMS for near-zero-downtime migration","Create a Blue/Green deployment (RDS Blue/Green), upgrade the green environment, then switch over","Snapshot the database, restore to a new PostgreSQL 15 instance, and switch the application endpoint"], correct:2, exp:"RDS Blue/Green Deployments create a staging environment (green) that is kept in sync with production (blue) via logical replication. Upgrade the green environment to PostgreSQL 15, test, then switch over with minimal downtime (<1 minute, typically seconds). In-place upgrades can take 10-30 minutes for large databases. DMS adds complexity. Snapshot/restore means data lag during restoration.", domain:2},
  {q:"A company has an RDS MySQL instance with 500GB of data. They need point-in-time recovery to a specific timestamp from 3 days ago. How is this performed and what should they know?", opts:["Restore directly to the existing instance from automated backups","Use the Point-in-Time Recovery feature which creates a NEW RDS instance restored to the specified timestamp","Export the binary logs and replay them to the desired point in time","Contact AWS support for point-in-time recovery assistance"], correct:1, exp:"RDS PITR creates a NEW instance (not in-place restore) using automated backups + transaction logs to the specified second. Important: the new instance has a different endpoint. The application must be updated to point to the new endpoint. The original instance remains unchanged. RDS automated backups support PITR within the retention window (1-35 days).", domain:2},
  {q:"A company's Aurora cluster processes 10,000 write transactions per second. They need disaster recovery in another region with RPO < 1 second. Read replicas in the secondary region must serve local reads. Which feature should they use?", opts:["Aurora cross-region Read Replica (standalone replica with async replication)","Aurora Global Database (dedicated replication infrastructure, <1s RPO)","DMS continuous replication to another Aurora cluster","RDS MySQL with binary log replication to another region"], correct:1, exp:"Aurora Global Database uses dedicated replication infrastructure separate from the database engine. It provides <1 second RPO (typical lag ~100ms) and up to 5 secondary regions. Each secondary region can have up to 16 Read Replicas for local reads. Cross-region Read Replicas use standard async replication with higher lag. DMS adds complexity and has higher lag. Aurora Global is purpose-built for this.", domain:2},
  {q:"A company uses RDS MySQL. They need to encrypt an existing unencrypted database. RDS does not support enabling encryption on an existing instance. What is the correct procedure?", opts:["Enable encryption in the RDS console (modify instance)","Create an encrypted snapshot of the unencrypted instance, then restore from the encrypted snapshot to a new encrypted instance","Create a Read Replica with encryption enabled, then promote it","Use AWS DMS to migrate data to a new encrypted instance"], correct:1, exp:"You cannot encrypt an existing unencrypted RDS instance directly. The procedure is: (1) Create a snapshot of the unencrypted instance, (2) Copy the snapshot with encryption enabled (specify KMS key), (3) Restore the encrypted snapshot to a new instance, (4) Update application endpoint. You cannot create an encrypted Read Replica from an unencrypted source. DMS works but is more complex.", domain:1},

  // VPC & Networking (heavily tested)
  {q:"A company has a VPC with public and private subnets in 2 AZs. They launch a new EC2 instance in a private subnet and try to install software updates from the internet using yum. The command hangs. The Security Group allows all outbound traffic. What is MOST likely missing?", opts:["The instance doesn't have a public IP address","There is no NAT Gateway or NAT instance in a public subnet with a route from the private subnet","The Internet Gateway is not attached to the VPC","The NACL is blocking outbound traffic"], correct:1, exp:"Private subnets need a NAT Gateway (in a public subnet) with a route table entry (0.0.0.0/0 -> NAT Gateway) to access the internet. Without this, instances in private subnets have no internet path. The instance correctly doesn't need a public IP (that's the point of NAT). The IGW must exist (for the NAT Gateway itself) but the private subnet routes to NAT, not IGW directly.", domain:1},
  {q:"A company sets up VPC peering between VPC A (10.0.0.0/16) and VPC B (10.0.0.0/16). The peering connection is created but instances cannot communicate. What is the problem?", opts:["VPC peering doesn't work across different accounts","The VPCs have overlapping CIDR blocks — VPC peering requires non-overlapping CIDRs","Route tables haven't been updated in both VPCs","Security Groups are blocking the traffic"], correct:1, exp:"VPC peering CANNOT be established between VPCs with overlapping CIDR blocks. Since both use 10.0.0.0/16, the routing would be ambiguous (a destination IP could belong to either VPC). This is a hard requirement — the peering connection might show as 'active' but routing can't work. One VPC would need to use a different CIDR range. This is a very commonly tested concept.", domain:2},
  {q:"A company has 100 VPCs that all need to communicate with each other and with an on-premises data center via Direct Connect. What is the MOST scalable architecture?", opts:["Full mesh VPC peering (4,950 peering connections)","AWS Transit Gateway connecting all VPCs and the Direct Connect Gateway","Hub-and-spoke VPC design with a shared services VPC","AWS Cloud WAN with a core network connecting all VPCs"], correct:1, exp:"Transit Gateway is a regional network hub that connects VPCs and on-premises networks. It supports up to 5,000 attachments (VPCs, VPN, Direct Connect Gateway). One TGW replaces the need for 4,950 peering connections. With Direct Connect Gateway attached, on-premises gets access to all VPCs through a single TGW. Cloud WAN works but is more expensive for single-region. Full mesh doesn't scale.", domain:2},
  {q:"A company uses a NAT Gateway in their VPC. They notice the NAT Gateway charges are $5,000/month — mostly data processing fees. Most of the traffic goes to S3 and DynamoDB. How should they reduce NAT Gateway costs?", opts:["Replace NAT Gateway with a smaller NAT instance (t3.nano)","Add VPC Gateway Endpoints for S3 and DynamoDB — traffic to these services will bypass the NAT Gateway entirely (free)","Use VPC Interface Endpoints for all services","Compress data before sending through NAT Gateway"], correct:1, exp:"VPC Gateway Endpoints for S3 and DynamoDB route traffic directly through the AWS network without going through the NAT Gateway. Gateway Endpoints are FREE (no hourly charge, no data processing fee). If most traffic is to S3/DynamoDB, this can eliminate 60-80% of NAT Gateway data processing charges. Interface Endpoints (PrivateLink) have an hourly charge ($0.01/hr/AZ) and data processing fee.", domain:4},
  {q:"A Security Group allows inbound SSH (port 22) from IP 203.0.113.50/32. A NACL on the same subnet allows inbound SSH from 0.0.0.0/0 but DENIES all outbound traffic. An administrator tries to SSH from 203.0.113.50. What happens?", opts:["SSH succeeds — Security Groups are stateful so return traffic is automatically allowed regardless of NACLs","SSH fails — while the inbound connection is established, the NACL blocks the return traffic (NACLs are stateless)","SSH succeeds — both Security Groups and NACLs allow the inbound traffic","SSH fails — the NACL deny rule takes precedence over Security Group rules"], correct:1, exp:"NACLs are STATELESS — inbound and outbound traffic are evaluated independently. Even though inbound SSH is allowed, the NACL denies ALL outbound traffic, which blocks the TCP return packets (ephemeral ports). SSH connection fails. Security Groups are stateful (return traffic auto-allowed), but NACLs are not. Both layers must allow traffic. This is the #1 networking trap question on the exam.", domain:1},
  {q:"A company needs to connect their VPC to over 100 SaaS services and partner VPCs privately without routing traffic over the internet. Each partner/SaaS exposes their service via their own VPC. What is the BEST approach?", opts:["Create 100 VPC peering connections","Use AWS PrivateLink — each partner creates a VPC endpoint service (NLB-backed), and the company creates Interface VPC Endpoints to each service","Set up a Transit Gateway with 100 attachments","Use a VPN mesh connecting to each partner"], correct:1, exp:"AWS PrivateLink (VPC Endpoint Services) is designed for this exact use case. Service providers expose their service via an NLB + endpoint service. Consumers create Interface Endpoints in their VPC. Traffic flows privately over AWS backbone. No VPC CIDR overlap issues (unlike peering). Scalable to thousands of services. Each endpoint is independent and manageable. Transit Gateway connects networks, not services. Peering requires non-overlapping CIDRs with all 100 partners.", domain:2},

  // Migration & Transfer Services (common exam topic)
  {q:"A company needs to migrate 500TB of data from on-premises to S3 within 4 weeks. Their internet connection is 1 Gbps. What is the FASTEST approach?", opts:["Use AWS DataSync over the internet with parallel transfer tasks","Order multiple AWS Snowball Edge devices and transfer in parallel","Set up AWS Direct Connect (takes 2-4 weeks to provision) then transfer via DataSync","Use S3 Transfer Acceleration with multipart upload"], correct:1, exp:"1 Gbps = ~10TB/week maximum theoretical. 500TB would take 50 weeks over the internet. Snowball Edge (80TB per device) — order 7 devices, load in parallel (~1 week), ship (~1 week round trip). Total: 2-3 weeks. Direct Connect takes too long to provision. Transfer Acceleration speeds up the path but can't overcome bandwidth limitations. For large offline transfers, Snow family is the answer.", domain:3},
  {q:"A company wants to migrate their on-premises file server (NFS-based, 10TB) to AWS with minimal disruption. Users must continue accessing files through the same NFS protocol during and after migration. What should they use?", opts:["AWS DataSync for one-time migration to EFS, then mount EFS directly","AWS Storage Gateway (File Gateway) in NFS mode connected to S3, with DataSync for initial bulk transfer","Direct Connect with NFS mount to EFS","Copy files to S3 using CLI and use S3 as the new file system"], correct:1, exp:"Storage Gateway File Gateway presents S3 storage as NFS/SMB shares to on-premises clients. DataSync handles the initial bulk migration to S3. After migration, the File Gateway provides continued NFS access with local caching for frequently accessed files. EFS could work for cloud-native clients but File Gateway bridges on-premises NFS seamlessly. Users don't need to change anything.", domain:2},
  {q:"A company runs a 10TB Oracle database on-premises. They want to migrate to Aurora PostgreSQL with less than 1 hour of downtime. The migration also involves schema conversion. What is the correct migration approach?", opts:["Use AWS DMS with full-load only mode, then switch over","Use AWS SCT for schema conversion, DMS with full-load and CDC (Change Data Capture) for continuous replication, then cut over when in sync","Export Oracle dump, import into Aurora, switch over","Use Aurora migration from Oracle (native feature)"], correct:1, exp:"For minimal downtime heterogeneous migration: (1) SCT converts Oracle schema/procedures to PostgreSQL, (2) DMS full-load migrates existing data while application runs on Oracle, (3) DMS CDC continues replicating changes in real-time, (4) When replication lag is zero, cut over to Aurora (seconds of downtime). Full-load only would require downtime during the entire copy. There's no native Oracle-to-Aurora migration feature.", domain:2},
  {q:"A company has a hybrid architecture. On-premises applications need to access S3 frequently. They currently use a VPN connection which is slow for large transfers. They want higher bandwidth and more consistent performance. What should they implement?", opts:["AWS Direct Connect (dedicated network connection from on-premises to AWS)","S3 Transfer Acceleration","Multiple VPN connections with load balancing","AWS Outposts in the data center"], correct:0, exp:"AWS Direct Connect provides a dedicated network connection (1 Gbps or 10 Gbps) from on-premises to AWS with consistent low latency (unlike VPN over internet). It's the standard solution for hybrid architectures needing reliable, high-bandwidth connectivity to AWS. Transfer Acceleration helps for S3 specifically over internet but doesn't solve general connectivity. Multiple VPNs still share internet limitations.", domain:3},
  {q:"A company wants to use AWS as a disaster recovery site for their on-premises VMware environment. They need RTO of 15 minutes. They don't want to maintain running instances in AWS during normal operations (cost concern). Which service provides this?", opts:["AWS Elastic Disaster Recovery (DRS) — continuous replication with rapid launch on demand","CloudEndure (now part of DRS) with pilot light instances","VMware Cloud on AWS with mirrored infrastructure","AWS Backup with cross-region restore"], correct:0, exp:"AWS Elastic Disaster Recovery continuously replicates on-premises servers to AWS (lightweight staging instances). During disaster, it launches full-sized recovery instances within minutes. No production-sized instances run during normal operations (only lightweight replication servers). RTO is typically 5-15 minutes. VMware Cloud on AWS is expensive (running instances). AWS Backup has longer RTO (restore from backup).", domain:2},

  // CloudFront & Edge (frequently tested)
  {q:"A company serves API responses through CloudFront. They want different cache behaviors: /api/public/* should cache for 1 hour, /api/private/* should never be cached (always forward to origin). How is this configured?", opts:["Two separate CloudFront distributions with different TTL settings","Single distribution with two cache behaviors: /api/public/* with TTL 3600, /api/private/* with TTL 0 and all headers forwarded","Configure TTL at the origin level using Cache-Control headers only","Use Lambda@Edge to modify caching behavior per path"], correct:1, exp:"CloudFront supports multiple cache behaviors per distribution, each matched by path pattern. Different behaviors can have different TTLs, forwarded headers/cookies, and origin configurations. Setting TTL=0 and forwarding all headers ensures every request for /api/private/* goes to the origin. This is the standard CloudFront configuration — no Lambda@Edge or multiple distributions needed.", domain:3},
  {q:"A company uses CloudFront to serve content from an S3 origin. After updating objects in S3, users still see old versions from CloudFront's cache. The objects have the same key names. What is the MOST effective long-term solution?", opts:["Create a CloudFront invalidation for the updated objects","Use versioned object names (e.g., style.v2.css) so the CloudFront cache key changes automatically","Reduce CloudFront TTL to 60 seconds globally","Disable caching in CloudFront"], correct:1, exp:"Versioned file names (cache busting) are the most effective long-term solution because: (1) instant propagation (new URL = new cache entry), (2) no invalidation cost ($0.005 per path after first 1000), (3) enables aggressive caching (long TTL for old and new versions). Invalidations work but cost money at scale and take 10-15 minutes to propagate. Short TTL increases origin load and latency.", domain:3},
  {q:"A company wants to restrict access to their CloudFront distribution so that only users in specific countries can access the content. How should this be implemented?", opts:["Use CloudFront geo-restriction (geographic restrictions) to whitelist or blacklist countries","Configure Route 53 geolocation routing to deny certain countries","Use AWS WAF with geo-match condition attached to the CloudFront distribution","Both A and C work — geo-restriction is simpler, WAF provides more flexibility"], correct:3, exp:"CloudFront geo-restriction provides simple country-level allow/block lists. AWS WAF geo-match conditions on CloudFront provide the same functionality plus additional flexibility (combine with rate limiting, IP reputation, etc.). For simple country blocking, geo-restriction is sufficient. For complex rules (block country X but allow specific IPs from it), WAF is needed. Both are valid approaches.", domain:1},
  {q:"A company serves video content from S3 through CloudFront. They want paying subscribers to access premium content while free users can access basic content. Both are served from the same S3 bucket. What is the BEST approach?", opts:["Two separate CloudFront distributions — one for free, one for premium content","Use CloudFront signed URLs or signed cookies for premium content with an application that generates them after authentication","S3 bucket policies based on user identity","CloudFront Origin Access Control with different policies per path"], correct:1, exp:"CloudFront signed URLs (per-file access) or signed cookies (access to multiple files in a path) restrict access to premium content. The application authenticates the user, checks their subscription, and generates time-limited signed URLs/cookies for premium paths. Free content is served without signing. OAC secures the S3 origin, signed URLs secure user access. Separate distributions add complexity without benefit.", domain:1},

  // DynamoDB (very heavily tested)
  {q:"A company designs a DynamoDB table for an e-commerce order system. They need to efficiently support these queries: (1) Get order by order_id, (2) Get all orders for a customer sorted by date, (3) Get all orders with status 'PENDING' for a customer. What is the optimal key design?", opts:["Partition key: order_id. GSI1: customer_id (PK) + order_date (SK). GSI2: customer_id (PK) + status (SK)","Partition key: customer_id, Sort key: order_date. GSI on order_id","Partition key: customer_id#status, Sort key: order_date. GSI on order_id","Partition key: customer_id, Sort key: order_id. GSI on status"], correct:0, exp:"PK=order_id enables efficient single-item lookup (query 1). GSI1 with PK=customer_id, SK=order_date enables query 2 (Query all orders for a customer sorted by date). GSI2 with PK=customer_id, SK=status enables query 3 (KeyConditionExpression on PK=customer_id and SK='PENDING'). This covers all three access patterns efficiently without scans.", domain:3},
  {q:"A DynamoDB table uses on-demand capacity mode. The application suddenly experiences 10x traffic increase. Some requests return ProvisionedThroughputExceededException. How is this possible with on-demand mode?", opts:["On-demand mode has a bug — it should never throttle","On-demand mode can throttle if traffic MORE THAN DOUBLES the previous peak within 30 minutes — it needs time to adapt to extreme spikes","On-demand mode has the same limits as provisioned mode","The account-level DynamoDB limits have been reached"], correct:1, exp:"On-demand mode adapts instantly up to 2x the previous peak. Beyond that, it can throttle until DynamoDB allocates more capacity (typically minutes). A sudden 10x spike exceeds the 2x instant-adapt threshold. Solutions: (1) gradually warm up the table by increasing traffic slowly, (2) switch to provisioned mode with high capacity before a planned spike (like a sale), or (3) use an SQS buffer to absorb the spike.", domain:3},
  {q:"A company uses DynamoDB with a table that has a partition key of user_id. They need to find all users in a specific city for a marketing campaign. The table has 100 million items. There is no GSI on city. What is the MOST efficient approach?", opts:["Scan the table with a FilterExpression on city (reads entire table)","Add a GSI with city as the partition key and query it","Use PartiQL to SELECT where city = 'Paris'","Export to S3 with DynamoDB Export and query with Athena"], correct:1, exp:"Adding a GSI with city as PK allows efficient Query operations returning only matching items. Without a GSI, a Scan reads ALL 100M items (expensive, slow, throttling risk). PartiQL SELECT with a filter still performs a Scan under the hood. Export + Athena works for one-time analysis but isn't efficient for repeated queries. GSI is the correct DynamoDB approach for new access patterns.", domain:3},
  {q:"A company's DynamoDB table stores 500GB of data. They need to create a copy of this table in another region for disaster recovery, keeping it in sync with less than 1 second lag. Which feature should they use?", opts:["DynamoDB Streams with Lambda to replicate items cross-region","DynamoDB Global Tables (fully managed active-active replication)","AWS DMS continuous replication from source to target region table","DynamoDB point-in-time recovery with cross-region restore"], correct:1, exp:"DynamoDB Global Tables provide fully managed, active-active replication across regions with sub-second replication lag. Any region can read and write. AWS handles conflict resolution (last-writer-wins). No need to manage streams, Lambda, or DMS. PITR doesn't provide continuous cross-region sync. DMS works but requires managing the replication instance. Global Tables is the native, purpose-built feature.", domain:2},
  {q:"A company uses DynamoDB for a shopping cart. Items have a TTL attribute set to expire 24 hours after creation (abandoned carts). They notice that some expired items are still visible for several minutes after the TTL timestamp. Is this a bug?", opts:["Yes — TTL should delete items exactly at the timestamp","No — DynamoDB TTL is eventually consistent; items are deleted within 48 hours of expiration, typically within minutes, but not guaranteed to be instant","No — TTL only marks items for deletion; they are removed in the next compaction cycle which runs daily","Yes — the TTL attribute format is incorrect"], correct:1, exp:"DynamoDB TTL deletion is NOT instantaneous. AWS states items are typically deleted within 48 hours of expiry (usually within minutes). Until deleted, expired items still appear in Scans and Queries. Applications should filter expired items client-side (compare TTL attribute with current time). This is by design — DynamoDB processes TTL deletes in background without consuming WCU.", domain:3},

  // Lambda & Serverless (heavily tested)
  {q:"A Lambda function processes S3 events. The function reads a 2GB file from S3, processes it, and writes results to DynamoDB. The function is configured with 128MB memory. What will happen?", opts:["The function will work but slowly due to low CPU","The function will fail with an 'out of memory' error because it can't load a 2GB file into 128MB RAM","The function will stream the file from S3 without loading it entirely into memory","The function will fail because Lambda has a /tmp limit of 512MB"], correct:1, exp:"With 128MB memory, the Lambda function cannot load a 2GB file into memory. It will immediately crash with an out-of-memory error. Solutions: (1) Increase memory (Lambda supports up to 10GB), (2) Stream-process the file in chunks without loading entirely into memory, (3) Split the file before processing. Lambda /tmp is 10GB (not 512MB since 2022), so disk isn't the limit here — RAM is.", domain:3},
  {q:"A Lambda function is configured with a VPC (private subnets). After deployment, the function cannot access the internet (required to call a third-party API) and also cannot access DynamoDB. What TWO configurations are needed?", opts:["NAT Gateway in public subnet (for internet access) + VPC Gateway Endpoint for DynamoDB (for AWS service access)","Internet Gateway in the VPC + DynamoDB VPC endpoint","Public subnet for Lambda + Security Group allowing outbound","Remove VPC configuration from Lambda (it doesn't need VPC for DynamoDB or internet)"], correct:0, exp:"Lambda in a VPC private subnet has no internet or AWS service access by default. For internet: NAT Gateway in a public subnet with a route from Lambda's subnet. For DynamoDB: VPC Gateway Endpoint (free). Alternatively, option D works if Lambda doesn't need VPC resources — without VPC, Lambda has internet access and can reach DynamoDB natively. But if VPC is required (for RDS access, etc.), then A is correct.", domain:1},
  {q:"A company has a Lambda function that processes messages from SQS. The function takes 5 minutes per message. During traffic spikes, the SQS queue grows and messages hit the visibility timeout (default 30 seconds), causing duplicate processing. What should be adjusted?", opts:["Increase SQS visibility timeout to at least 6x the Lambda timeout (30 minutes)","Increase Lambda concurrency to process messages faster","Reduce Lambda batch size to process fewer messages at once","Enable SQS content-based deduplication"], correct:0, exp:"Visibility timeout must be >= Lambda function timeout. If Lambda takes 5 minutes but visibility timeout is 30 seconds, the message becomes visible again after 30 seconds (while still being processed), causing another Lambda to pick it up = duplicate processing. Set visibility timeout to 5 minutes minimum (AWS recommends 6x the function timeout for safety margin). Concurrency helps throughput but doesn't fix duplicates.", domain:2},
  {q:"A company needs to run a Lambda function on a schedule (every 5 minutes) to check a third-party API for new data. What is the correct trigger mechanism?", opts:["CloudWatch Events (EventBridge) scheduled rule with a rate expression: rate(5 minutes)","SQS queue with a timer message sent every 5 minutes","S3 event notification on a timer","Lambda built-in scheduler configuration"], correct:0, exp:"Amazon EventBridge (CloudWatch Events) scheduled rules trigger Lambda on a cron or rate expression. rate(5 minutes) invokes the Lambda every 5 minutes automatically. No other infrastructure needed. Lambda has no built-in scheduler. SQS doesn't have timer messages. S3 events are triggered by object operations, not schedules. EventBridge is THE trigger mechanism for scheduled Lambda invocations.", domain:3},
  {q:"A Lambda function processes API Gateway requests. It establishes a database connection on each invocation. Under load (1000 concurrent requests), the RDS database reports 'too many connections' and crashes. What is the BEST solution?", opts:["Move the database connection initialization outside the handler function (in the init phase) to reuse connections across invocations in the same container","Use Amazon RDS Proxy to pool and share database connections across all Lambda instances","Reduce Lambda concurrency to limit simultaneous connections","Both A and B: initialize connections outside the handler AND use RDS Proxy for true connection pooling across instances"], correct:3, exp:"Initializing connections outside the handler reuses them within a single container (warm invocations), reducing connections per container to 1. BUT with 1000 concurrent Lambdas, you still get 1000 connections. RDS Proxy multiplexes these 1000 Lambda connections into a smaller pool (e.g., 50) of database connections. Together: minimum connections per container + centralized pooling = maximum efficiency.", domain:3},

  // Organizations, Control Tower, Config (tested frequently)
  {q:"A company with 200 AWS accounts wants to ensure that no one can create resources outside of approved regions (us-east-1, eu-west-1). This must apply to ALL accounts including future accounts. What is the MOST scalable approach?", opts:["Create an IAM policy in each account restricting regions","Attach an SCP to the Organization root that denies all actions outside us-east-1 and eu-west-1, with exceptions for global services (IAM, Route 53, CloudFront, etc.)","Use AWS Config organizational rules to detect and delete non-compliant resources","Enable AWS Control Tower with region deny guardrail"], correct:1, exp:"SCPs at the Organization root apply to ALL accounts (current and future). Denying actions where aws:RequestedRegion is not in the approved list blocks resource creation in other regions. Must exclude global services (IAM, Organizations, CloudFront, Route 53, STS, etc.) which only operate from us-east-1. Control Tower has a similar feature but SCP is the fundamental mechanism. Config is detective, not preventive.", domain:1},
  {q:"A company uses AWS Organizations with multiple OUs: Production, Development, Security, and Shared Services. They want to ensure that ONLY the Security OU accounts can create or manage IAM roles that start with 'SecurityAudit-'. How can this be enforced?", opts:["IAM policies in non-security accounts denying iam:CreateRole for 'SecurityAudit-*'","Attach an SCP to the Production, Development, and Shared Services OUs that denies iam:CreateRole, iam:DeleteRole, iam:AttachRolePolicy where the role name starts with 'SecurityAudit-'","Permission boundaries on all roles in non-security accounts","AWS Config rule detecting non-compliant role creation with auto-remediation"], correct:1, exp:"SCPs on the non-security OUs deny IAM actions (Create/Delete/Modify) for roles matching the 'SecurityAudit-*' pattern. Security OU is NOT restricted by this SCP so its accounts can manage these roles. SCP conditions use iam:ResourceTag or StringLike on the role ARN to match the naming pattern. This is the proper way to reserve role naming patterns for specific accounts/OUs.", domain:1},
  {q:"A company wants to automatically provision new AWS accounts with a standard baseline (VPC configuration, Security Groups, IAM roles, CloudTrail, Config). When a new team requests an account, it should be ready in minutes. Which service automates this?", opts:["CloudFormation StackSets deploying baseline templates to new accounts","AWS Control Tower Account Factory (provisions accounts with guardrails and baselines automatically)","AWS Organizations CreateAccount API with a Lambda post-creation hook","AWS Service Catalog with an account-vending product"], correct:1, exp:"AWS Control Tower Account Factory automates new account provisioning with pre-configured baselines (guardrails, VPC, CloudTrail, Config, IAM). Accounts are created under managed OUs with governance built in. It uses Service Catalog under the hood but provides a complete, governed account vending solution. StackSets require manual setup. Raw CreateAccount needs extensive automation. Service Catalog alone requires building the vending machine yourself.", domain:1},
]
