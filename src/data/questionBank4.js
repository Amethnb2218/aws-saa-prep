export const questionBank4 = [
  // DOMAIN 1: Security - Advanced scenarios
  {q:"A company wants to prevent any IAM user from attaching the AdministratorAccess managed policy to any role or user. Which is the MOST effective preventive control?", opts:["Create an SCP denying iam:AttachRolePolicy and iam:AttachUserPolicy when the policy ARN matches AdministratorAccess","Create a permission boundary that excludes AdministratorAccess and attach it to all users","Use AWS Config to detect and alert when AdministratorAccess is attached","Create an IAM policy denying iam:AttachRolePolicy for the specific policy ARN"], correct:0, exp:"SCPs are the strongest preventive control because they apply to the entire account including the root user (except for certain root-only tasks). Even if someone has IAM full access, the SCP prevents the attach action. Permission boundaries must be attached to each user/role individually. Config is detective, not preventive.", domain:1},
  {q:"A company needs to allow EC2 instances in multiple VPCs across different accounts to access a shared S3 bucket without traversing the public internet. Each VPC already has its own Gateway Endpoint for S3. The company wants to enforce that only requests from these VPCs can access the bucket. What bucket policy condition achieves this?", opts:["aws:SourceVpc condition with a list of all VPC IDs","aws:SourceVpce condition with a list of all VPC endpoint IDs","aws:SourceIp with the VPC CIDR ranges","aws:PrincipalOrgID to restrict to the organization"], correct:0, exp:"aws:SourceVpc condition in the S3 bucket policy restricts access to requests that originate from specific VPCs via their Gateway Endpoints. This works across accounts. aws:SourceVpce would also work but requires listing each endpoint ID (more brittle as endpoints change). SourceIp doesn't work with VPC endpoints (private IPs aren't visible). PrincipalOrgID restricts by org but not by network path.", domain:1},
  {q:"A security team discovers that developers are creating IAM roles with excessive permissions. They want to limit what permissions developers can grant to roles they create, while still allowing them to create roles. What AWS feature enforces this?", opts:["Permission boundaries — require developers to attach a specified permission boundary to any role they create","SCPs limiting the maximum permissions of any role in the account","IAM Access Analyzer to detect overly permissive roles","AWS Config rule to check role policies against a baseline"], correct:0, exp:"Permission boundaries set the maximum permissions a role can have, regardless of its identity-based policies. By requiring developers to attach a permission boundary (enforced via their own policy), you delegate role creation while capping effective permissions. SCPs limit what the entire account can do, not individual roles. Access Analyzer is detective, not preventive.", domain:1},
  {q:"A company processes credit card payments and must comply with PCI DSS. They need to isolate their cardholder data environment (CDE) in AWS. Which combination provides the STRONGEST network isolation for the CDE?", opts:["Separate VPC for CDE with no peering, access only via AWS PrivateLink endpoints, and Security Groups limiting source CIDR","Separate AWS account for CDE under a dedicated OU with SCPs, separate VPC, private subnets only, and NACLs + Security Groups + Network Firewall","CDE in private subnets within the main VPC with strict Security Groups and WAF","Dedicated Hosts in an isolated VPC with Direct Connect for all access"], correct:1, exp:"Separate account provides the strongest isolation boundary (blast radius containment). A dedicated OU with SCPs enforces compliance controls organization-wide. The separate VPC with private subnets, NACLs, Security Groups, and Network Firewall provides defense in depth at every layer. PCI DSS requires segmentation — a separate account is the gold standard for AWS.", domain:1},
  {q:"An application uses S3 for object storage. The security team requires that objects can only be uploaded if encrypted with a specific KMS key owned by the security team. Uploads with any other encryption method must be denied. What bucket policy achieves this?", opts:["Deny s3:PutObject where s3:x-amz-server-side-encryption is not aws:kms","Deny s3:PutObject where s3:x-amz-server-side-encryption-aws-kms-key-id is not equal to the specific key ARN","Allow s3:PutObject only when s3:x-amz-server-side-encryption-aws-kms-key-id equals the specific key ARN","Set default bucket encryption to the specific KMS key"], correct:1, exp:"The condition key s3:x-amz-server-side-encryption-aws-kms-key-id with a Deny + StringNotEquals ensures ALL uploads MUST use the specific KMS key. Denying when not aws:kms still allows other KMS keys. An Allow-only policy doesn't prevent uploads without the header (implicit deny doesn't override explicit allows elsewhere). Default encryption applies only when no encryption header is specified.", domain:1},
  {q:"A solutions architect needs to ensure that EC2 instances can only be launched with encrypted EBS volumes. Instances with unencrypted volumes must be blocked at launch time, not detected after. Which approach provides this enforcement?", opts:["Enable EBS encryption by default in the account settings","Create an SCP with a Deny on ec2:RunInstances with condition ec2:Encrypted = false","Use AWS Config rule to detect unencrypted EBS volumes and auto-terminate instances","Lambda function triggered by CloudTrail RunInstances events to terminate non-compliant instances"], correct:1, exp:"An SCP (or IAM policy) with Deny on RunInstances when ec2:Encrypted is false blocks the launch attempt entirely. This is preventive — the instance never launches. EBS encryption by default only applies when no encryption is specified in the launch (doesn't block explicit unencrypted requests). Config and Lambda are detective (after-the-fact).", domain:1},

  // DOMAIN 2: Resilient Architectures - Advanced
  {q:"A company uses an Aurora MySQL cluster with one writer and two reader instances. During a failover test, the application experiences 30 seconds of downtime because the connection string points to the writer instance IP. How should the architecture be improved?", opts:["Use Route 53 with a CNAME record pointing to the writer, update on failover","Use the Aurora cluster endpoint (writer endpoint) which automatically points to the current writer","Implement connection pooling with RDS Proxy which handles failover transparently","Both B and C: use the cluster endpoint AND add RDS Proxy for fastest failover and connection management"], correct:3, exp:"The Aurora cluster endpoint automatically resolves to the current writer (handles DNS failover). RDS Proxy additionally maintains a connection pool, pins connections to the current writer, and can reduce failover time to <1 second by pre-establishing connections. Together they provide the fastest failover with zero connection management. Direct IP connections break on failover.", domain:2},
  {q:"A company uses a multi-AZ deployment pattern with an ALB. During an AZ outage, they noticed that the ALB took 5 minutes to drain connections from the failed AZ before routing to healthy targets. This caused user-facing errors. How can failover time be minimized?", opts:["Reduce the deregistration delay (connection draining) timeout from 300 seconds to 30 seconds","Enable cross-zone load balancing and configure shorter health check intervals (5 seconds) with a lower healthy threshold (2)","Deploy a Network Load Balancer instead of an ALB for faster failover","Use Route 53 failover routing with health checks instead of an ALB"], correct:1, exp:"Shorter health check intervals (5s vs default 30s) with lower healthy threshold (2 vs default 5) means the ALB detects unhealthy targets in 10 seconds instead of 150 seconds. Cross-zone load balancing ensures remaining AZs share load evenly. Reducing deregistration delay helps but doesn't speed up detection. NLB has similar detection timing. Route 53 adds DNS TTL delay.", domain:2},
  {q:"A company operates a globally distributed application using DynamoDB Global Tables. They experience a conflict when two users in different regions update the same item simultaneously. How does DynamoDB resolve this, and how should the application be designed?", opts:["DynamoDB uses vector clocks and returns both versions for the application to resolve","DynamoDB uses last-writer-wins (LWW) based on the timestamp — design the application to handle eventual consistency and use conditional writes for critical operations","DynamoDB rejects the second write with a ConditionalCheckFailedException","DynamoDB queues conflicting writes and processes them in order"], correct:1, exp:"DynamoDB Global Tables uses last-writer-wins reconciliation based on the item timestamp. Writes don't fail — the latest timestamp wins after replication. Applications should: (1) accept eventual consistency for non-critical data, (2) use conditional writes (ConditionExpression) for critical operations to prevent stale overwrites, (3) design idempotent operations where possible.", domain:2},
  {q:"A company runs a batch processing system where each job takes 2-4 hours. If an instance is terminated mid-job, the entire job must restart from the beginning (no checkpointing possible). They want to use Spot Instances for cost savings. How can they minimize wasted work from Spot interruptions?", opts:["Use Spot Fleet with capacity-optimized allocation strategy to minimize interruption frequency","Request Spot Instances in a single capacity pool with the lowest interruption rate","Use a diversified Spot Fleet across multiple instance types and AZs with capacity-optimized strategy, and implement a Spot interruption handler that migrates running jobs to On-Demand instances","Use Spot Blocks (defined duration) for 4-hour blocks to guarantee the instance runs for the job duration"], correct:2, exp:"Capacity-optimized strategy across diverse pools minimizes interruptions. The interruption handler (using the 2-minute warning) can launch an On-Demand instance and gracefully fail-over the job (or at minimum, save state). This combines Spot savings with protection against wasted work. Spot Blocks were deprecated. Single pool is risky. Capacity-optimized alone doesn't handle actual interruptions.", domain:2},
  {q:"An e-commerce company needs exactly-once payment processing. Their current architecture uses SQS Standard queue with Lambda, and they observe occasional duplicate charges when Lambda retries. What is the BEST fix?", opts:["Switch to SQS FIFO queue with deduplication enabled","Implement idempotency in the Lambda function using a DynamoDB table to track processed payment IDs","Use Step Functions to manage the payment workflow with an idempotency key","Both A and B provide different protections and should both be implemented"], correct:1, exp:"Idempotency at the processing level (DynamoDB tracking of processed payment IDs) is the most robust solution. Before processing a payment, check if the ID exists in DynamoDB (conditional write). This handles ALL retry scenarios regardless of source (SQS, Lambda, network issues). FIFO dedup only prevents duplicate sends within 5 minutes. Step Functions add complexity. Idempotent processing is the fundamental fix.", domain:2},
  {q:"A video streaming platform uses a single EBS io2 volume for its transcoding server. If the volume fails, transcoding stops for hours while restoring from snapshot. The application requires 64,000 IOPS and 1,000 MB/s throughput. What provides both high performance AND resilience?", opts:["Use EBS io2 Block Express with Multi-Attach across two instances","Migrate to an instance store-backed instance for maximum performance with an S3 backup strategy","Use RAID 0 across 4 io2 volumes for performance and RAID 1 for redundancy (RAID 10)","Migrate to Amazon FSx for Lustre for shared high-performance storage with automatic backup"], correct:0, exp:"EBS io2 Block Express supports up to 256,000 IOPS and 4,000 MB/s on a single volume, meeting requirements. Multi-Attach allows the same volume to attach to multiple instances in the same AZ — if one instance fails, the other continues processing. Built-in io2 durability is 99.999%. Instance store data is lost on stop. RAID 10 adds complexity. FSx for Lustre is for parallel workloads, not block storage.", domain:2},
  {q:"A company runs a containerized application on ECS with a task definition that mounts an EFS file system. During an AZ failure, tasks in the failed AZ are rescheduled to healthy AZs, but they report filesystem errors for 30 seconds before recovering. What causes this and how can it be mitigated?", opts:["EFS mount targets in the failed AZ are unreachable — ensure mount targets exist in all AZs used by ECS tasks","EFS has eventual consistency — add retry logic with exponential backoff in the application","The EFS throughput mode is limiting — switch to provisioned throughput","ECS task networking is resolving the failed AZ's mount target DNS — configure the mount helper with cross-AZ failover"], correct:0, exp:"When an AZ fails, EFS mount targets in that AZ become unreachable. Tasks rescheduled to another AZ need mount targets in THAT AZ to function. If mount targets exist in all AZs, the EFS client (mount helper) resolves to the local AZ's mount target automatically. The 30-second errors occur during DNS failover to another AZ's mount target. Having mount targets in every AZ used eliminates this.", domain:2},
  {q:"A company processes real-time stock trades and needs to guarantee that no trade message is ever lost, even if the entire processing system goes down for hours. Messages must be replayable. What architecture provides the STRONGEST durability guarantee?", opts:["SQS Standard queue with long polling (messages retained up to 14 days)","Kinesis Data Streams with extended retention (up to 365 days)","Amazon MSK (Kafka) with tiered storage (unlimited retention)","EventBridge event bus with an archive enabled and replay capability"], correct:2, exp:"Amazon MSK with tiered storage provides unlimited retention with automatic offloading to cost-effective storage. Kafka's architecture guarantees no message loss with replication factor 3. Messages are replayable from any offset at any time. Kinesis maxes at 365 days. SQS deletes after consumption. EventBridge archive is for replaying events but has higher latency and less throughput for high-frequency trades.", domain:2},

  // DOMAIN 3: High-Performing - Advanced
  {q:"A company runs a read-heavy application using Aurora PostgreSQL. They have 5 read replicas but queries to the reader endpoint still show uneven load (some replicas are heavily loaded while others are idle). What is the MOST likely cause and fix?", opts:["Connection pooling is sticking connections to specific replicas — use RDS Proxy with reader endpoint for better distribution","The reader endpoint uses round-robin DNS but long-lived connections stay on the same replica — implement client-side connection refresh or use RDS Proxy","Some replicas are in different AZs with higher latency — move all replicas to the same AZ","The reader endpoint prioritizes replicas by tier — set all replicas to the same priority"], correct:1, exp:"Aurora reader endpoints use DNS round-robin. Long-lived connections (common with connection pools) establish once and stay connected to the same replica. This creates uneven distribution. RDS Proxy maintains its own connection pool and distributes queries across replicas actively. Alternatively, client-side connection refresh (periodic reconnection) helps. AZ placement doesn't affect reader endpoint distribution.", domain:3},
  {q:"A company uses API Gateway with Lambda for their REST API. They need to handle a flash sale event expecting 50,000 requests per second for 5 minutes. The current API Gateway throttle limit is 10,000 requests/second. What combination of actions prepares the system?", opts:["Request an API Gateway throttle limit increase to 50,000 RPS and configure Lambda reserved concurrency","Request API Gateway limit increase, enable API Gateway caching for GET requests, configure Lambda provisioned concurrency, and implement request throttling with usage plans for non-critical endpoints","Switch from REST API to HTTP API for higher throughput, and scale Lambda accordingly","Deploy the API directly on EC2 instances behind an ALB to avoid API Gateway limits"], correct:1, exp:"Multi-pronged approach: (1) Request limit increase (can take days, must plan ahead), (2) API Gateway caching for read endpoints reduces backend load by 80%+, (3) Provisioned concurrency eliminates Lambda cold starts under spike, (4) Usage plans with throttling protect the system by deprioritizing non-critical traffic. HTTP API has same limits. EC2/ALB loses API Gateway features.", domain:3},
  {q:"A data pipeline processes 500GB of JSON files daily, transforming and loading into Redshift. Currently, a single Glue ETL job takes 6 hours. The team needs it done in 1 hour. What combination of optimizations achieves this?", opts:["Increase the number of Glue DPUs (Data Processing Units) from default to maximum","Convert input to Parquet first, use Glue DPU auto-scaling, enable job bookmarking, and use pushdown predicates for source filtering","Switch from Glue to an EMR cluster with more nodes","Use COPY command to load JSON directly into Redshift and do transformations there"], correct:1, exp:"Converting to Parquet dramatically reduces read time (columnar, compressed). DPU auto-scaling adapts capacity to the workload. Job bookmarking processes only new data (not reprocessing). Pushdown predicates filter at the source, reducing data movement. Together these can achieve 5-10x speedup. Simply adding DPUs has diminishing returns if the data format is inefficient. COPY from JSON is slow for transformations.", domain:3},
  {q:"A social media application needs to serve the user's home feed which aggregates posts from 500+ followed accounts, ranked by relevance, in under 200ms. Each request is unique per user. Which architecture serves this requirement?", opts:["DynamoDB query per followed account, aggregate in Lambda, return via API Gateway","Pre-compute and store each user's feed in ElastiCache Redis (fan-out on write pattern) and serve directly","S3 pre-computed feed files with CloudFront distribution","Aurora with a materialized view of the feed, refreshed every minute"], correct:1, exp:"Fan-out on write pre-computes the feed when posts are created (async, can tolerate latency). The pre-computed feed stored in Redis serves reads in <5ms. This is how Twitter/Instagram work at scale. Querying 500+ accounts per request (fan-out on read) is too slow for 200ms. S3 files can't update fast enough. Materialized views can't refresh per-user in real-time.", domain:3},
  {q:"A company needs to run a Monte Carlo simulation with 10,000 iterations, each taking 5 minutes of CPU time. Results must be aggregated after all iterations complete. The job runs weekly. What is the FASTEST and most cost-effective architecture?", opts:["A single c5.24xlarge instance running iterations in parallel threads","AWS Batch with a Spot Instance compute environment, submitting 10,000 array job elements","Lambda functions (one per iteration) triggered in parallel, results stored in S3, aggregation Lambda triggered by S3 events","Step Functions Map state with 10,000 iterations running Lambda functions in parallel with max concurrency"], correct:1, exp:"AWS Batch with array jobs distributes 10,000 independent tasks across a fleet of Spot Instances. It auto-scales to hundreds of instances, uses Spot for 70-90% savings, handles failures with retries, and scales to zero when done. Total time: ~5 minutes (fully parallel) at Spot pricing. Lambda is limited to 15 minutes but that's fine for 5 min tasks — however, 10K concurrent Lambdas may hit account concurrency limits. Batch has higher parallelism potential.", domain:3},
  {q:"A company has a DynamoDB table with 1TB of data. They need to add a new Global Secondary Index. They're concerned about the impact on the production table during index creation. What should they know?", opts:["Index creation locks the table — schedule during maintenance window","Index creation uses provisioned write capacity from the base table — ensure sufficient capacity or use on-demand","Index backfill reads the entire table and uses separate backfill capacity — monitor ThrottleEvents and adjust base table capacity if needed","Index creation happens instantly with no impact on the base table"], correct:2, exp:"When creating a GSI on an existing table, DynamoDB backfills the index by reading all existing items. This backfill uses the table's read capacity (for provisioned mode). For 1TB, this takes time and can throttle production reads if capacity is insufficient. Monitor ThrottleEvents and increase read capacity temporarily. On-demand mode handles this better. The table remains fully available during backfill.", domain:3},
  {q:"A company streams 1 million events per second to Kinesis Data Streams. Each event is 1KB. They have 500 shards but still see ReadProvisionedThroughputExceeded errors. Each shard supports 2MB/s reads. What's happening?", opts:["Too many consumer applications reading the same stream — use Enhanced Fan-Out (dedicated read throughput per consumer)","The shard count is insufficient — add more shards","Events are not distributed evenly across shards (hot shards) — improve partition key distribution","The events are too large — compress them before sending"], correct:0, exp:"With 500 shards at 2MB/s read each, total read capacity is 1GB/s — plenty for 1M x 1KB = 1GB/s write (reads can handle it). ReadProvisionedThroughputExceeded occurs when MULTIPLE consumers read the same shards (shared 2MB/s). Enhanced Fan-Out gives each consumer dedicated 2MB/s per shard, eliminating the contention. Hot shards would cause WriteProvisionedThroughputExceeded.", domain:3},
  {q:"A GraphQL API backed by multiple data sources (DynamoDB, RDS, Elasticsearch) experiences N+1 query problems where resolving a list of 100 items results in 100 additional database calls. Which AWS service and technique resolves this?", opts:["AppSync with pipeline resolvers and BatchGetItem for DynamoDB","AppSync with resolver mapping templates that use BatchInvoke for Lambda data sources and DataLoader pattern","API Gateway with Lambda and DataLoader library for batching and caching within a single request","AppSync with caching enabled at the resolver level"], correct:1, exp:"AppSync with BatchInvoke Lambda resolvers can batch multiple resolver calls into a single Lambda invocation. The DataLoader pattern (batching + request-level caching) collects all IDs needed in a single execution frame and makes one batched call. This reduces 100 DB calls to 1 batched call. Pipeline resolvers execute sequentially. Caching helps repeated data but not N+1 within a single query.", domain:3},

  // DOMAIN 4: Cost-Optimized - Advanced
  {q:"A company runs 500 t3.medium On-Demand instances across 10 AWS accounts. The instances run 24/7 and are in the same region. What Savings Plan provides the BEST value?", opts:["EC2 Instance Savings Plan for t3.medium in the specific region (highest discount ~72%)","Compute Savings Plan (lower discount ~66% but applies to any instance family, size, region, OS, or tenancy)","Both plans can be combined: EC2 Instance SP for the known stable workload, Compute SP for the flexible portion","Standard Reserved Instances for all 500 t3.medium instances"], correct:0, exp:"Since all instances are the same type (t3.medium), same region, and run 24/7, EC2 Instance Savings Plan provides the maximum discount (~72% vs ~66% for Compute SP). The specificity of committing to instance family + region yields the highest savings. Compute SP is better when flexibility is needed. RIs provide similar savings but are less flexible than Savings Plans for same discount level.", domain:4},
  {q:"A company has 10TB of data in Redshift that is queried by analysts only during business hours (8am-6pm). They pay $20,000/month for a dc2.8xlarge cluster running 24/7. What reduces costs the MOST while maintaining query performance?", opts:["Pause the Redshift cluster outside business hours (pay only for storage when paused)","Migrate to Redshift Serverless (pay per query)","Use Redshift Concurrency Scaling (free for first credits, then per-second billing)","Resize to smaller nodes and enable RA3 with managed storage"], correct:1, exp:"Redshift Serverless charges only for compute when queries run. For business-hours-only usage (10 hours/day, 5 days/week = 30% utilization), serverless can save 60-70% vs provisioned. Pausing also works but has resume delay (30-60 seconds). Serverless provides instant availability. Concurrency Scaling is for burst capacity, not reducing base costs. Right-sizing helps but still pays for idle time.", domain:4},
  {q:"A company uploads 10TB of video files to S3 daily for transcoding. After transcoding, the original files are never accessed again but must be retained for 1 year. The transcoded files are accessed frequently for 7 days then rarely. What minimizes total storage costs?", opts:["Original files: S3 Standard -> Glacier Deep Archive after 1 day. Transcoded: S3 Standard -> Standard-IA after 7 days -> Glacier after 30 days","Original files: S3 One Zone-IA immediately (never accessed again). Transcoded: S3 Intelligent-Tiering","Original files: S3 Standard with lifecycle to Glacier Deep Archive after 1 day. Transcoded: S3 Standard -> Glacier Instant Retrieval after 7 days","Original files: Upload directly to Glacier Deep Archive via S3 PUT with storage class header. Transcoded: S3 Standard -> Standard-IA after 7 days"], correct:3, exp:"Original files can be uploaded directly to Glacier Deep Archive (using the x-amz-storage-class header on PUT) — no need to ever be in Standard since they're never accessed after transcoding. This is cheapest ($1/TB/month). Transcoded files in Standard for 7 days of frequent access, then Standard-IA for the rare access period. One Zone-IA risks data loss for required-retention data.", domain:4},
  {q:"A data team runs Athena queries against a 100TB data lake. Their monthly Athena bill is $25,000 (5TB scanned per query average, 1000 queries/month at $5/TB). What combination provides the MOST cost reduction?", opts:["Convert all data to Parquet and partition by the most common query filter columns","Use Redshift Spectrum instead of Athena (same per-scan pricing but can benefit from Redshift caching)","Implement Athena workgroups with per-query byte limits and convert to Apache Iceberg tables with partition evolution","Convert to Parquet (90% scan reduction), partition data, use Athena result caching (reuse results), and create Athena views for common query patterns"], correct:3, exp:"Parquet reduces scanned data by 90%+ vs CSV/JSON (columnar + compression). Partitioning eliminates scanning irrelevant partitions. Result caching ($0 for repeated queries within TTL) eliminates cost for common queries. Views standardize efficient access patterns. Combined: 5TB/query could drop to 50GB/query = $250/month vs $25,000. Iceberg helps but is more complex. Redshift Spectrum has same pricing model.", domain:4},
  {q:"A company runs 100 microservices on EKS. Each service requests 1 vCPU and 2GB RAM but actual utilization averages 20% CPU and 40% memory. The cluster has 50 m5.xlarge nodes (4 vCPU, 16GB each). Monthly compute cost is $35,000. How should they optimize?", opts:["Enable Cluster Autoscaler with aggressive scale-down and rightsize pod resource requests based on actual usage","Migrate from EKS to ECS Fargate to eliminate node management overhead","Use Karpenter for node provisioning with diverse instance types (including Spot) and implement Vertical Pod Autoscaler (VPA) to rightsize requests","Convert to Graviton-based instances (m6g.xlarge) for 20% cost reduction"], correct:2, exp:"Karpenter provisions right-sized nodes on-demand (no over-provisioning). Diverse instance types + Spot = 60-90% savings on compute. VPA automatically adjusts pod CPU/memory requests to actual usage (reducing from 1 vCPU to 0.2 vCPU per pod). This means fewer nodes needed. Combined savings: 60-75%. Cluster Autoscaler is less efficient than Karpenter. Fargate is more expensive per unit. Graviton saves only 20%.", domain:4},
  {q:"A startup scales rapidly from 10 to 1000 customers. Their per-customer infrastructure cost hasn't decreased with scale. Each customer gets isolated resources (VPC, RDS, EC2). What architectural change enables cost-per-customer reduction at scale?", opts:["Move to a multi-tenant architecture with shared infrastructure (shared RDS with row-level security, shared ECS cluster with namespace isolation)","Use smaller instance types for each customer's dedicated resources","Negotiate volume discounts through AWS Enterprise Discount Program","Use Reserved Instances for all customer resources"], correct:0, exp:"Multi-tenancy shares infrastructure costs across customers. A shared RDS with row-level security serves all customers on one database (amortized). Shared ECS/EKS cluster with namespace isolation runs all services efficiently. Cost per customer drops dramatically (shared DB: $500/mo for all vs $50/customer x 1000 = $50K). This is the fundamental architectural shift for SaaS cost optimization at scale. Smaller instances provide linear, not sublinear, savings.", domain:4},
  {q:"A company's CloudWatch bill is $8,000/month. Investigation shows: custom metrics ($3,000), log ingestion ($3,000), and dashboards/alarms ($2,000). What reduces this the MOST?", opts:["Reduce log retention period from 30 days to 7 days","Use CloudWatch Embedded Metric Format to combine custom metrics with logs (pay once for logs, get metrics free from log data)","Switch from custom high-resolution metrics (1-second) to standard resolution (1-minute)","Export logs to S3 and use Athena for analysis instead of CloudWatch Logs Insights"], correct:1, exp:"Embedded Metric Format (EMF) publishes metrics through log data. You pay for log ingestion once, and CloudWatch automatically extracts metrics from the log entries at no additional custom metric charge. This eliminates the $3,000 custom metrics cost. Combined with log optimization, total savings can be 40-50%. Reducing retention doesn't affect ingestion cost. Resolution change helps some but EMF eliminates the entire category.", domain:4},
]
