export const questionBank13 = [
  {q:"A company wants to allow employees to access AWS resources using their corporate Active Directory credentials without creating IAM users. They have 20 AWS accounts. What is the recommended solution?", opts:["AWS IAM Identity Center (SSO) with AD Connector to on-premises Active Directory","SAML 2.0 federation configured in each AWS account separately","Sync all AD users to IAM users using a custom Lambda function","AWS Managed Microsoft AD with trust relationships to each account"], correct:0, exp:"IAM Identity Center provides centralized SSO to all AWS accounts in an Organization. AD Connector connects to on-premises AD without replicating data. Users sign in once with corporate credentials and access all permitted accounts. Per-account SAML setup doesn't scale for 20 accounts. Syncing IAM users defeats federated access purpose.", domain:1},
  {q:"A company uses CloudFront for a web application. They want to add a custom HTTP response header (X-Frame-Options: DENY) to all responses for clickjacking protection without modifying the origin. Which CloudFront feature achieves this?", opts:["CloudFront response headers policy with custom headers configured","Lambda@Edge on origin-response event adding the header","Origin custom headers in the CloudFront distribution settings","CloudFront Functions on viewer-response event adding the header"], correct:0, exp:"CloudFront response headers policies are a managed feature to add security headers (X-Frame-Options, Content-Security-Policy, HSTS, etc.) without any code. It's simpler and cheaper than Lambda@Edge or CloudFront Functions. Origin custom headers add headers to the REQUEST going to origin, not to the RESPONSE going to viewers.", domain:1},
  {q:"An application on EC2 instances behind an ALB stores user sessions in instance memory. After scaling out, new instances don't have existing session data. Users experience intermittent logouts. The team wants the simplest fix without code refactoring. What should they do?", opts:["Enable ALB sticky sessions (session affinity) using application-based cookies","Migrate session storage to ElastiCache Redis","Store sessions in DynamoDB with TTL","Enable session replication between instances"], correct:0, exp:"ALB sticky sessions route a user to the same instance for the duration of their session (based on a cookie). This is the simplest fix requiring NO code changes. The downside: if the instance fails, sessions are lost. For a longer-term solution, ElastiCache Redis is better (sessions survive instance failures), but requires code changes. The question asks for simplest fix without code refactoring.", domain:2},
  {q:"A company has a VPC with IPv4 CIDR 10.0.0.0/24 (256 addresses). Each subnet uses some addresses for AWS reserved IPs. How many IP addresses does AWS reserve in each subnet?", opts:["3 (network, broadcast, DNS)","5 (network address, VPC router, DNS, future use, broadcast)","2 (network and broadcast)","4 (first 4 addresses in the range)"], correct:1, exp:"AWS reserves 5 IP addresses in each subnet: .0 (network address), .1 (VPC router), .2 (DNS server), .3 (reserved for future use), and .255 (broadcast — VPC doesn't support broadcast but reserves it). So a /24 subnet with 256 addresses has 251 usable addresses. This is a frequently tested factual question.", domain:1},
  {q:"A company runs a web application that must meet 99.99% availability SLA. They deploy in a single AWS Region across 3 AZs. A single AZ failure should not impact availability. What is the MINIMUM number of instances needed if the application requires 6 instances to handle production load?", opts:["6 instances (2 per AZ)","9 instances (3 per AZ — if one AZ fails, 6 remain)","12 instances (4 per AZ)","8 instances (staggered across 3 AZs)"], correct:1, exp:"If one AZ fails, 2/3 of instances remain. To have 6 instances after losing one AZ: 6 = (2/3) × total → total = 9 instances (3 per AZ). If any single AZ fails, 6 instances remain in the other 2 AZs to handle full production load. With only 6 total (2 per AZ), losing one AZ leaves only 4 instances — insufficient.", domain:2},
  {q:"A company runs EC2 instances that process jobs from an SQS queue. They want to scale based on the number of messages in the queue divided by the number of running instances (backlog per instance). Which Auto Scaling metric achieves this?", opts:["ApproximateNumberOfMessagesVisible metric with target tracking","Custom metric published to CloudWatch: ApproximateNumberOfMessagesVisible / NumberOfRunningInstances, with target tracking policy","Simple scaling based on queue depth","Step scaling based on ApproximateNumberOfMessages thresholds"], correct:1, exp:"The 'backlog per instance' metric requires dividing queue depth by fleet size — this must be calculated and published as a custom CloudWatch metric. Target tracking then maintains this custom metric at a desired value (e.g., target 10 messages per instance). AWS documents this as the recommended pattern for SQS-based scaling. The raw queue depth doesn't account for fleet size (would over-scale with many instances).", domain:3},
  {q:"A company's application uses presigned S3 URLs to allow users to download files. The URLs are valid for 1 hour. A user downloads a 10GB file that takes 2 hours to complete. Will the download succeed?", opts:["No — the URL expires after 1 hour and the download will fail mid-transfer","Yes — once the download starts, the HTTP connection remains open and the transfer completes regardless of URL expiry","It depends on whether the download was initiated before expiry","No — S3 will terminate the connection at the 1-hour mark"], correct:1, exp:"S3 presigned URL expiration is checked at the TIME the request is initiated, not during data transfer. Once S3 begins serving the response (HTTP 200), the download continues even if the presigned URL expires during transfer. The TCP connection stays open. The URL only matters for the initial authentication of the request. This is a commonly tested concept.", domain:1},
  {q:"A solutions architect needs to design a system that processes images uploaded to S3. Each image must be processed by 3 independent services: thumbnail generation, metadata extraction, and content moderation. Each service should process every image independently. What is the BEST event-driven architecture?", opts:["S3 event notification to Lambda that calls all 3 services sequentially","S3 event notification to SNS topic with 3 SQS queue subscriptions (one per service)","S3 event notification to EventBridge with 3 separate rules targeting each service","Both B and C work — SNS fanout with SQS provides buffering, EventBridge provides flexibility"], correct:3, exp:"Both patterns work: SNS→SQS fanout delivers each event to all 3 queues independently (with SQS buffering for reliability). EventBridge can route the same S3 event to 3 different targets. SNS+SQS is better when services need buffering/retry. EventBridge is better when you need content-based filtering or direct Lambda targets. The sequential Lambda approach creates coupling and single point of failure.", domain:2},
  {q:"A company has a DynamoDB table with provisioned capacity. They run a one-time backfill job that writes 10 million items. To avoid throttling their production workload, they want the backfill to use capacity that doesn't impact online users. What approach should they use?", opts:["Run the backfill during off-peak hours","Temporarily switch to on-demand capacity mode for the backfill","Use DynamoDB reserved capacity for the backfill","Write to a different table and use DynamoDB Import from S3 for bulk loading without consuming table WCU"], correct:3, exp:"DynamoDB Import from S3 loads data into a new or existing table without consuming any provisioned WCU. The data is loaded directly from S3 into the table. For a one-time 10M item backfill, export items to S3 format and use Import. This completely avoids impacting production capacity. On-demand switch has adaptation time. Off-peak still shares capacity. Import from S3 is the zero-impact option.", domain:3},
  {q:"A company uses API Gateway REST API. They need to transform the request body before sending to the Lambda backend (add fields, rename fields, change format). Where is this transformation configured?", opts:["Lambda function (handle transformation in code)","API Gateway Integration Request mapping template (VTL template)","API Gateway Method Request validation","CloudFront Function for request transformation"], correct:1, exp:"API Gateway mapping templates (using Apache Velocity Template Language - VTL) transform requests and responses in the Integration Request/Response. You can restructure JSON, add fields, rename fields, and transform format without modifying Lambda code. This keeps Lambda focused on business logic. Lambda proxy integration passes everything through unchanged. Method Request is for validation only.", domain:3},
  {q:"A company needs a managed service for running Apache Kafka workloads. They want exactly the same Kafka APIs, tools, and client libraries they use on-premises. Which AWS service provides this?", opts:["Amazon Kinesis Data Streams (similar to Kafka)","Amazon MSK (Managed Streaming for Apache Kafka)","Amazon MQ with ActiveMQ","Amazon EventBridge"], correct:1, exp:"Amazon MSK (Managed Streaming for Apache Kafka) provides fully managed Apache Kafka clusters. It uses the same Kafka APIs, client libraries, and tools — 100% compatible. You use standard Kafka producers/consumers/connect. Kinesis is similar in concept but has different APIs. MQ is for traditional message queuing (JMS, AMQP). EventBridge is for event routing.", domain:3},
  {q:"A company uses Route 53 with weighted routing (70% to us-east-1, 30% to eu-west-1). They discover that one region is returning errors. They want Route 53 to automatically stop sending traffic to the unhealthy region. What should be added?", opts:["Failover routing policy instead of weighted","Health checks associated with each weighted record — Route 53 will stop routing to records whose health checks fail","CloudWatch alarm to modify Route 53 weights via Lambda","Manual intervention to set the unhealthy region's weight to 0"], correct:1, exp:"Adding health checks to weighted records makes Route 53 health-aware. If a health check fails, Route 53 stops including that record in responses and redistributes traffic to remaining healthy records (proportional to their weights). The weighted policy still applies — but only among healthy records. No need to change to failover policy. Automatic, no Lambda needed.", domain:2},
  {q:"A company's Aurora MySQL cluster has a writer instance and 2 reader instances. They want to ensure that if the writer fails, the LARGEST reader instance is promoted (not the smallest). How is this configured?", opts:["Set promotion priority (tier) — the reader with the lowest priority number (tier-0) is promoted first","It's random — Aurora picks any reader","The newest reader is always promoted","Configure failover priority in Route 53"], correct:0, exp:"Aurora uses promotion tiers (priority 0-15) to determine which reader becomes the new writer during failover. Tier 0 is the highest priority (promoted first). You assign the largest or most capable reader to tier 0. If multiple readers share the same tier, Aurora promotes the largest instance. This gives you control over which reader takes over. Configure in the instance settings.", domain:2},
  {q:"A company needs to encrypt data in transit between their on-premises data center and AWS VPC. They use Direct Connect for the physical link. Direct Connect itself does NOT encrypt traffic. What adds encryption?", opts:["Enable encryption on the Direct Connect connection settings","Create a VPN connection over the Direct Connect link (Site-to-Site VPN over DX using a public VIF or transit VIF)","Use TLS/SSL in the application layer only","Direct Connect with MACsec encryption (Layer 2 encryption) on supported connections"], correct:3, exp:"MACsec (IEEE 802.1AE) provides Layer 2 encryption on Direct Connect connections at line rate with no performance impact. Available on dedicated 10Gbps and 100Gbps connections at supported locations. VPN over DX also works (encrypts at Layer 3) but adds overhead and reduces throughput. Application-level TLS only encrypts specific traffic. MACsec encrypts everything on the physical link.", domain:1},
  {q:"A company's application uses SQS to process orders. They want to ensure that even if the consumer application crashes, messages are not lost and will be retried. However, messages that continuously fail (poison messages) should be moved aside after 5 attempts. What configuration achieves this?", opts:["Set visibility timeout to 5 minutes","Configure a Dead Letter Queue (DLQ) with maxReceiveCount = 5 on the redrive policy","Enable long polling with a wait time of 20 seconds","Set message retention period to 14 days"], correct:1, exp:"The redrive policy with maxReceiveCount = 5 means: if a message is received (become visible) 5 times without being deleted (processed successfully), it's automatically moved to the Dead Letter Queue. This handles poison messages (always failing) by moving them aside while allowing normal messages to retry on transient failures. DLQ preserves the message for investigation. Message retention keeps unprocessed messages for up to 14 days, but doesn't handle poison messages.", domain:2},
  {q:"A company has a containerized application that requires GPUs for machine learning inference. The application processes bursts of requests and should scale to zero between bursts. Which compute option supports GPU + scale-to-zero?", opts:["ECS on EC2 with GPU instances in an Auto Scaling Group (min=0)","ECS on Fargate (Fargate doesn't support GPU)","SageMaker Serverless Inference (doesn't support GPU currently)","AWS Batch with EC2 GPU compute environment (scales to zero, provisions GPU instances on demand)"], correct:3, exp:"AWS Batch manages compute environments that scale to zero when no jobs are queued. It supports GPU instances (p3, p4, g4, g5 families). When requests arrive (submitted as Batch jobs), it provisions GPU instances, runs the container, and scales back to zero. ECS ASG min=0 doesn't auto-launch on demand without custom triggering. Fargate doesn't support GPUs. SageMaker Serverless doesn't support GPU.", domain:4},
  {q:"A company migrates an on-premises Redis cluster (4 shards, 100GB total) to AWS. The application uses Redis CLUSTER commands and hash slots. Which ElastiCache configuration provides the most compatible migration?", opts:["ElastiCache Redis (Cluster Mode Disabled) with a large node","ElastiCache Redis (Cluster Mode Enabled) with 4 shards matching the on-premises topology","ElastiCache Memcached with 4 nodes","Amazon MemoryDB for Redis with 4 shards"], correct:1, exp:"Cluster Mode Enabled supports Redis Cluster protocol (CLUSTER commands, hash slot distribution, multi-key operations across slots). Matching 4 shards preserves the existing key distribution. The application's CLUSTER-aware client works unchanged. Cluster Mode Disabled doesn't support sharding. Memcached isn't Redis-compatible. MemoryDB works but is more expensive and provides unnecessary durability for a cache migration.", domain:2},
  {q:"A company's Lambda function needs to access an RDS database in a private subnet. After adding VPC configuration to the Lambda function, it can connect to RDS but can NO longer access external APIs on the internet. Why, and what's the fix?", opts:["Lambda in VPC uses public IP — the issue is Security Group outbound rules","Lambda in VPC has no internet access by default — add a NAT Gateway in a public subnet and route Lambda's subnet through it","Lambda VPC configuration prevents ALL external access permanently","The issue is DNS resolution — enable DNS support in VPC"], correct:1, exp:"Lambda functions in a VPC private subnet have no internet access (same as any EC2 in a private subnet). To access both RDS (private) and external APIs (internet): place Lambda in a private subnet with a route to a NAT Gateway in a public subnet. NAT Gateway provides outbound internet access while Lambda remains in the private subnet for RDS connectivity. This is the most common Lambda+VPC question.", domain:1},
  {q:"A company needs to store Terraform state files for 50 team members. The state must be locked during modifications to prevent concurrent writes. What is the AWS-native solution for remote Terraform state with locking?", opts:["S3 bucket for state storage + DynamoDB table for state locking","EFS shared file system accessed by all team members","CodeCommit repository with branch protection","S3 bucket with versioning only (S3 provides locking)"], correct:0, exp:"S3 stores the Terraform state file (with versioning for history). DynamoDB provides state locking via a lock table (Terraform acquires a lock in DynamoDB before modifying state, preventing concurrent modifications). This is the standard Terraform remote backend for AWS. S3 alone doesn't provide locking. EFS doesn't provide atomic locking for state files. This is a very commonly tested Terraform+AWS integration.", domain:2},
  {q:"A company uses API Gateway with Lambda. They need to return responses larger than 6MB (API Gateway payload limit). How can they serve large responses?", opts:["Increase API Gateway payload limit in settings","Generate the large response in Lambda, store it in S3, and return a pre-signed URL to the client for direct download","Use multipart responses through API Gateway","Switch to ALB which has a 1GB response limit"], correct:1, exp:"API Gateway has a hard 10MB payload limit (6MB for Lambda proxy integration). For larger responses: Lambda generates the data, stores it in S3, and returns a pre-signed URL (small response). The client downloads the large payload directly from S3 (no size limit). This bypasses the API Gateway limit entirely. ALB does support larger payloads (up to 1GB) but changing the architecture may not be necessary.", domain:3},
  {q:"A company runs an application that writes logs to CloudWatch Logs. The log group retains logs for 30 days, but the compliance team needs logs searchable for 1 year and archived for 7 years. What is the MOST cost-effective approach?", opts:["Set CloudWatch Logs retention to 7 years (expensive at scale)","CloudWatch Logs subscription filter → Kinesis Firehose → S3 (Standard for 1 year, Glacier Deep Archive after 1 year, delete after 7 years) + query with Athena","Export logs to S3 periodically using CreateExportTask","Set retention to 1 year and use CloudWatch Logs Insights for queries"], correct:1, exp:"Real-time streaming via subscription filter → Firehose → S3 provides continuous log delivery with Parquet conversion and partitioning. S3 lifecycle transitions to Glacier Deep Archive after 1 year (cheapest long-term). Athena queries S3 data for the searchable requirement. CloudWatch Logs at 7 years would be extremely expensive ($0.03/GB/month vs S3's $0.023/GB or Glacier's $0.004/GB). Export tasks have delays and aren't continuous.", domain:4},
  {q:"A gaming company stores player inventories in DynamoDB. Each player has hundreds of items. They frequently need to read the ENTIRE inventory (all items) for a player. Each item is 400 bytes. What DynamoDB design optimizes this access pattern?", opts:["One item per inventory item (partition key: player_id, sort key: item_id) — Query returns all items","Single item per player containing all inventory items as a List/Map attribute (single GetItem)","One item per inventory item stored as individual DynamoDB items with no sort key","GSI with inventory_count as sort key"], correct:0, exp:"Using player_id as partition key and item_id as sort key allows a single Query operation to retrieve ALL items for a player efficiently (consecutive items on the same partition). At 400 bytes × 100s of items, the total is well under DynamoDB's 400KB item size limit per response page. Single-item design (option B) risks hitting the 400KB max item size with many items. Query with PK+SK is the standard collection pattern.", domain:3},
  {q:"A company needs to deploy a web application that serves traffic from both IPv4 and IPv6 addresses. The application runs on EC2 behind an ALB in a VPC. What must be configured for IPv6 support?", opts:["ALB supports IPv6 by default — no changes needed","Add an IPv6 CIDR block to the VPC and subnets, configure the ALB as dual-stack (IPv4+IPv6), and ensure Security Groups allow IPv6 traffic","Create a separate NLB for IPv6 traffic","Use CloudFront to handle IPv6 (ALB doesn't support IPv6)"], correct:1, exp:"For end-to-end IPv6: VPC needs an IPv6 CIDR (Amazon-provided /56), subnets need IPv6 ranges, ALB is set to dual-stack (ipv4+ipv6), Security Groups must include IPv6 rules (e.g., ::/0), and instances need IPv6 addresses. ALB does support IPv6 (dual-stack) but requires proper VPC configuration. CloudFront supports IPv6 natively but that only covers the edge-to-CloudFront path.", domain:2},
  {q:"A solutions architect reviews a DynamoDB table design where the partition key is date (2024-01-15). During peak hours, ALL traffic goes to today's partition (hot partition). Read/write capacity is wasted on other partitions. How should this be fixed?", opts:["Add a random suffix to the date (2024-01-15_03) to spread across multiple partitions, and scatter-gather for reads","Change to on-demand mode which handles hot partitions better","Add a GSI to distribute the load","Use DAX to cache hot partition reads"], correct:0, exp:"Write sharding (appending a random suffix like _00 to _09) spreads today's data across 10 partitions instead of one. For writes: randomly pick a suffix. For reads: query all 10 suffixes and merge results (scatter-gather). On-demand mode still has per-partition limits. GSI would have the same hot partition issue. DAX helps reads but not writes. This is the canonical DynamoDB hot partition solution.", domain:3},
  {q:"A company has an EC2-based application that calls the S3 API to upload objects. They want to monitor the latency of S3 API calls from their application's perspective (not just S3-side metrics). Which tool provides this?", opts:["S3 request metrics in CloudWatch (server-side latency only)","AWS X-Ray SDK instrumented in the application (traces API calls including S3 latency from client perspective)","CloudTrail data events (logs API calls but not latency)","VPC Flow Logs (shows network flow but not API latency)"], correct:1, exp:"X-Ray SDK instruments the AWS SDK calls in your application. When the app calls S3 PutObject, X-Ray traces the entire call including: DNS resolution time, TCP connection time, SSL handshake, time to first byte, and total duration — all from the CLIENT perspective. S3 request metrics show server-side processing time only. CloudTrail logs the event but not timing. This gives true end-to-end latency visibility.", domain:2},
  {q:"A company needs to configure their VPC so that EC2 instances can resolve DNS names for AWS services (like s3.amazonaws.com) to private IP addresses when using VPC endpoints. What must be enabled?", opts:["DNS resolution and DNS hostnames must both be enabled on the VPC","Private DNS must be enabled on the VPC Interface Endpoint","Both A and B — VPC DNS settings must be enabled AND the Interface Endpoint must have private DNS enabled","Only route table entries pointing to the VPC endpoint are needed"], correct:2, exp:"Two requirements: (1) The VPC must have 'enableDnsSupport' and 'enableDnsHostnames' set to true (allows private hosted zone resolution). (2) The Interface Endpoint must have 'Private DNS' enabled (creates a private hosted zone that overrides the public DNS name to resolve to the endpoint's private IPs). Without both, the AWS service DNS name still resolves to public IPs. Gateway Endpoints use route tables (no DNS change needed).", domain:1},
  {q:"A company wants to use Spot Instances for a web application (stateless, behind ALB). They need to handle Spot interruptions gracefully — removing the instance from the ALB target group before termination so users don't get errors. What is the BEST approach?", opts:["Use Spot Instance interruption notice (2-minute warning via instance metadata) with a shutdown script that deregisters from the target group","Enable connection draining on the ALB (deregistration delay)","Use Spot Fleet with maintain mode for automatic replacement","All: Spot Fleet for replacement + deregistration delay for graceful draining + interruption handler for proactive removal"], correct:3, exp:"Complete solution: Spot Fleet with capacity maintenance replaces interrupted instances automatically. The interruption handler (checking metadata every 5 seconds) proactively sets the instance to draining when the 2-minute warning arrives. ALB deregistration delay ensures in-flight requests complete before the instance is terminated. Together: proactive removal + graceful draining + automatic replacement = seamless experience.", domain:2},
  {q:"A company uses ElastiCache Redis for caching database queries. They implement a lazy-loading (cache-aside) pattern. After updating a record in the database, the cache still returns stale data until TTL expires (30 minutes). How should they ensure cache consistency?", opts:["Reduce TTL to 1 minute (increases cache misses and database load)","Implement write-through: when updating the database, also update the cache immediately","Implement cache invalidation: when updating the database, delete the cache key (next read will populate fresh data)","Both B and C work — write-through for frequently read keys, invalidation for rarely read keys"], correct:3, exp:"Write-through updates the cache synchronously with the database (cache is always fresh). Cache invalidation deletes the stale key (lazy-loading repopulates on next read). Best practice: use write-through for hot keys (avoids cache miss penalty) and invalidation for cold keys (avoids caching data nobody reads). Reducing TTL is a brute-force approach that trades hit ratio for freshness — suboptimal.", domain:3},
  {q:"A company needs to run Windows containers on AWS. The containers require .NET Framework (not .NET Core) which only runs on Windows. What is the MOST operationally simple platform?", opts:["ECS on Fargate (Fargate supports Windows containers)","ECS on EC2 with Windows AMI and Windows container support","EKS on Windows node groups","EC2 instances running Docker for Windows"], correct:0, exp:"ECS on Fargate supports Windows containers (launched in 2023), providing serverless Windows container execution. No EC2 instances to manage, patch, or scale. For .NET Framework workloads that require Windows OS, this is the simplest option. ECS on EC2 Windows works but requires managing Windows instances. EKS Windows nodes also require management. Fargate for Windows eliminates operational overhead.", domain:2},
  {q:"A company has 100 microservices communicating over HTTP. They want to implement mutual TLS (mTLS) between services, traffic routing (canary, weighted), and observability (metrics, tracing) without modifying application code. Which AWS service provides this?", opts:["ALB with SSL certificates","AWS App Mesh (service mesh with Envoy sidecar proxies)","API Gateway between each service pair","AWS Cloud Map for service discovery"], correct:1, exp:"AWS App Mesh provides a service mesh using Envoy sidecar proxies. It handles mTLS (certificate-based authentication between services), traffic routing (canary/weighted/header-based), and observability (metrics to CloudWatch, traces to X-Ray) — all without modifying application code. The Envoy proxy handles everything at the network level. ALB doesn't provide mTLS between services. API Gateway between every pair is impractical.", domain:1},
  {q:"A company stores sensitive configuration data (database passwords, API keys, certificates) that EC2 instances and Lambda functions need at runtime. The data must be encrypted at rest, access must be auditable, and values must be retrievable by name. Which service is MOST appropriate?", opts:["AWS Systems Manager Parameter Store (SecureString type) for configuration values and Secrets Manager for credentials that need rotation","Environment variables in EC2/Lambda with KMS encryption","S3 encrypted objects with bucket policies","DynamoDB encrypted table with IAM access control"], correct:0, exp:"Parameter Store SecureString encrypts values with KMS (free for standard parameters, audited via CloudTrail). Secrets Manager adds automatic rotation for credentials (RDS, Redshift, etc.) but costs $0.40/secret/month. Best practice: use Parameter Store for config values (API URLs, feature flags) and Secrets Manager for credentials requiring rotation (database passwords). Both integrate natively with EC2/Lambda.", domain:1},
  {q:"A company deploys a new version of their Lambda function. They want to gradually shift traffic from the old version to the new version over 30 minutes while monitoring for errors. If the error rate exceeds 1%, traffic should automatically shift back to the old version. Which feature enables this?", opts:["Lambda function aliases with weighted routing + CodeDeploy Linear deployment configuration + CloudWatch alarm for automatic rollback","Lambda versioning with manual weight updates","API Gateway canary deployment","Step Functions with gradual invocation of new version"], correct:0, exp:"Lambda aliases support weighted routing between two function versions. CodeDeploy manages the weight shift over time (Linear10PercentEvery3Minutes for 30-minute rollout). A CloudWatch alarm monitors the error rate, and if triggered, CodeDeploy automatically rolls back (shifts 100% back to old version). This is the native AWS solution for safe Lambda deployments. API Gateway canary deploys the API, not the function itself.", domain:2},
  {q:"A company processes financial transactions. Regulations require that transaction records cannot be modified or deleted for 5 years. The records must be queryable by transaction_id and customer_id. Which database provides immutability with query capability?", opts:["DynamoDB with delete protection and no IAM delete permissions","Amazon QLDB (Quantum Ledger Database) — immutable, cryptographically verifiable journal with PartiQL queries","Aurora with triggers preventing UPDATE/DELETE operations","S3 with Object Lock in Compliance mode"], correct:1, exp:"QLDB provides a purpose-built immutable ledger. Every change is appended to a journal (never modified). The journal is cryptographically verifiable (hash chain). PartiQL enables SQL-like queries by transaction_id, customer_id, or any field. DynamoDB delete protection can be bypassed by admins. Aurora triggers can be disabled. S3 Object Lock doesn't provide database query capabilities.", domain:1},
  {q:"A company runs a data lake on S3 with Athena for querying. Different teams need access to different columns of the same dataset. The HR team should see all columns, but the analytics team should NOT see salary and SSN columns. How can this be enforced without creating separate copies of the data?", opts:["Create different Athena views per team that exclude restricted columns","Use AWS Lake Formation column-level permissions (grant access to specific columns per team)","Create separate Glue tables with different column definitions","IAM policies restricting specific S3 prefixes per team"], correct:1, exp:"AWS Lake Formation provides column-level security on data lake tables. You grant specific columns to each team (HR gets all columns, analytics gets everything except salary/SSN). Lake Formation enforces this at query time — restricted columns return null or are hidden. No data duplication. Athena views work but can be bypassed by querying the underlying table directly. Lake Formation is the proper authorization layer.", domain:1},
  {q:"A company uses an NLB for a TCP-based application (port 5000). The application needs to know the client's original source IP address for logging. With NLB, does the target see the original client IP?", opts:["No — NLB always replaces the source IP with its own IP","Yes — NLB preserves the client source IP by default for instance targets (it's a pass-through load balancer at Layer 4)","Only with proxy protocol enabled","Only for HTTP/HTTPS traffic, not TCP"], correct:1, exp:"NLB is a Layer 4 pass-through load balancer — it does NOT modify packets. Instance targets see the original client source IP directly in the TCP connection. This is different from ALB (Layer 7) which always shows the ALB's IP as source (client IP is in X-Forwarded-For header). For NLB with IP targets (e.g., containers), proxy protocol v2 is needed. But for instance targets, source IP is preserved natively.", domain:2},
  {q:"A company has a SaaS application where each tenant's data is in a separate S3 prefix (s3://bucket/tenant-123/). They use Cognito for authentication. How can they ensure each authenticated user can ONLY access their own tenant's prefix without creating separate IAM policies per user?", opts:["S3 bucket policy with Cognito identity conditions","IAM role with a policy using ${cognito-identity.amazonaws.com:sub} as a variable in the S3 resource ARN, assumed via Cognito Identity Pool","Lambda authorizer that checks tenant_id before allowing S3 access","Separate S3 buckets per tenant with individual bucket policies"], correct:1, exp:"Cognito Identity Pool provides temporary AWS credentials. The IAM role's policy uses the Cognito identity variable as a dynamic S3 prefix: Resource: 'arn:aws:s3:::bucket/${cognito-identity.amazonaws.com:sub}/*'. Each user gets credentials scoped to only their prefix automatically — no per-user policy needed. This scales to millions of tenants with a single IAM role. The variable is resolved at credential-generation time.", domain:1},
  {q:"A company uses Aurora Serverless v2 for a development database. It scales between 0.5 and 16 ACUs. During business hours, it uses 8 ACUs. At night and weekends, it drops to 0.5 ACU. They pay for average usage. What would save MORE money?", opts:["Switch to Aurora Provisioned with a db.r5.large instance (always on)","Use Aurora Serverless v2 with lower max ACUs (8 instead of 16)","Pause the cluster outside business hours (Aurora Serverless v2 doesn't support auto-pause, but you can use scheduled pause/resume)","The current setup is already cost-optimal for variable workloads"], correct:2, exp:"Aurora Serverless v2 cannot auto-pause (v1 could). Business hours = 50 hours/week at 8 ACUs. Off-hours = 118 hours/week at 0.5 ACU. Monthly cost: ~(50×8 + 118×0.5) × 4 × $0.12/ACU-hour = ~$220. If paused outside business hours: ~50×8×4×$0.12 = ~$192 (saving the 0.5 ACU off-hours cost). However, pause/resume takes 30-60 seconds. For dev environments, scheduled pause/resume is the cheapest option.", domain:4},
  {q:"A company needs to audit which IAM users and roles have ACTUALLY used permissions versus what they HAVE. They want to identify overly permissive policies. Which tool provides this analysis?", opts:["IAM credential report (shows user creation dates and access key usage)","IAM Access Analyzer with unused access findings (compares granted permissions against CloudTrail activity)","AWS Config IAM rules","Trusted Advisor IAM checks"], correct:1, exp:"IAM Access Analyzer unused access analysis reviews CloudTrail logs to identify permissions that were granted but never used. It highlights: unused roles, unused permissions within active roles, and unused access keys. This is the data-driven way to implement least privilege — remove what's never been used. Credential report shows basic usage info but not per-permission analysis. Config checks for compliance rules, not usage patterns.", domain:1},
  {q:"A company uses AWS Glue for ETL. Their Glue job reads from an RDS MySQL database, transforms data, and writes to S3 in Parquet format. The job needs to run daily and only process NEW or CHANGED records since the last run (not reprocess the entire table). Which Glue feature enables this?", opts:["Glue Job Bookmarks (tracks processed records and picks up only new/modified data on subsequent runs)","Glue Crawlers to detect schema changes","A custom timestamp-based query in the Glue script","Glue Data Catalog versioning"], correct:0, exp:"Glue Job Bookmarks track the 'high water mark' of processed data (e.g., the maximum primary key or timestamp seen in the last run). On subsequent runs, only records beyond the bookmark are processed. This enables incremental processing without custom logic. It works with JDBC sources (RDS), S3 sources, and Kinesis. Custom timestamp queries work but require manual bookmark management. Job Bookmarks are the native feature for this.", domain:3},
  {q:"A company uses Amazon Connect for their contact center. They want to analyze customer calls for sentiment, detect common issues, and extract key topics from conversations. Which service combination provides this?", opts:["Amazon Transcribe (speech-to-text) + Amazon Comprehend (NLP: sentiment, topics, entities)","Amazon Polly (text-to-speech) + Amazon Lex (chatbot)","Amazon Rekognition (image analysis)","Contact Lens for Amazon Connect (built-in conversation analytics: sentiment, issues, call categories)"], correct:3, exp:"Contact Lens for Amazon Connect is a built-in ML-powered analytics feature that provides: real-time and post-call sentiment analysis, automatic issue detection, topic extraction, call categorization, and agent performance metrics. It's integrated directly into Connect (no separate services to configure). Transcribe + Comprehend would work as separate services but Contact Lens is the native, integrated solution for Connect.", domain:3},
  {q:"A company runs a high-traffic website. During peak events, their RDS MySQL database becomes a bottleneck (90% CPU). The application issues the same SELECT queries repeatedly (product catalog, user profiles). What provides the FASTEST relief?", opts:["Scale up the RDS instance to the next larger size","Add ElastiCache Redis in front of the database for read caching (cache frequently queried results)","Add RDS Read Replicas and distribute reads","Switch to Aurora for better read performance"], correct:1, exp:"ElastiCache Redis provides sub-millisecond response times for cached data, immediately offloading 80-90% of repetitive reads from the database. For product catalog and user profiles (read-heavy, change infrequently), caching is the fastest relief. Read Replicas help but still have ms-level latency and take time to provision. Scaling up provides only linear improvement. ElastiCache provides orders of magnitude improvement for cache-friendly workloads.", domain:3},
  {q:"A company uses AWS Step Functions for a long-running order fulfillment workflow (can take days for physical shipping). They're concerned about Step Functions pricing since Standard Workflows charge per state transition. The workflow has 20 steps per order and processes 100,000 orders per month. What should they evaluate?", opts:["The cost is $0.025 per 1000 transitions: 20 × 100,000 = 2M transitions = $50/month (reasonable)","Switch to Express Workflows for lower cost (charges per execution and duration, not transitions)","Express Workflows won't work here — they have a 5-minute maximum duration. Standard Workflows at $50/month for this volume is cost-effective","Use SQS + Lambda instead to avoid Step Functions costs entirely"], correct:2, exp:"Express Workflows have a 5-minute MAX duration — they cannot run for days (physical shipping fulfillment). Standard Workflows can run up to 1 year. At $0.025/1000 transitions: 2M transitions = $50/month, which is very reasonable for orchestrating 100,000 orders. The question tests whether you know Express Workflows' 5-minute limit. For long-running processes, Standard is the only option. $50/month is negligible vs building custom orchestration.", domain:4},
  {q:"A company needs to monitor the health of their entire AWS infrastructure and get a unified view of operational issues across EC2, ECS, Lambda, and RDS. They want automatic detection of anomalies (CPU spikes, error rate increases) without configuring individual alarms for each resource. Which service provides this?", opts:["CloudWatch Dashboards with manual alarm configuration","Amazon DevOps Guru (ML-powered anomaly detection across AWS services)","AWS Health Dashboard (shows AWS service health, not customer infrastructure)","CloudWatch Application Insights (groups related resources and detects anomalies)"], correct:1, exp:"Amazon DevOps Guru uses ML to automatically detect operational anomalies across AWS resources without manual alarm configuration. It monitors metrics, logs, and events, identifies anomalies, correlates related issues, and provides recommendations. No per-resource setup needed — it learns normal patterns automatically. CloudWatch Application Insights is similar but scoped to applications. DevOps Guru covers the entire account/organization.", domain:2},
  {q:"A company has an application that makes exactly 1 request per second to a downstream API. If the downstream API is unavailable, the request must be retried up to 3 times with exponential backoff (1s, 2s, 4s delays). If all retries fail, the request must be saved for later processing. Which service implements this pattern with ZERO custom code?", opts:["SQS with retry configuration","EventBridge with API Destinations (built-in retry with exponential backoff and DLQ)","Step Functions with retry/catch in a Task state","Lambda with destinations for failure routing"], correct:1, exp:"EventBridge API Destinations support: HTTP endpoints as targets, configurable retry policies (with exponential backoff and maximum retries), rate limiting (requests per second), and dead-letter queues for failed invocations. Zero custom code — configuration only. Step Functions also implement retry with backoff but require defining a state machine (more setup). EventBridge API Destinations are purpose-built for calling external APIs with retry semantics.", domain:2},
  {q:"A company runs a legacy application that writes to a local file system (/data/output/). They want to migrate to AWS and have the output files automatically appear in S3 without changing the application code. What provides a file system interface backed by S3?", opts:["EFS with S3 sync (doesn't exist natively)","S3 mounted with s3fs-fuse (not recommended for production)","AWS Storage Gateway File Gateway (presents S3 as NFS/SMB mount point)","Amazon FSx for Lustre linked to an S3 bucket (provides POSIX file system backed by S3)"], correct:3, exp:"FSx for Lustre linked to S3 provides a high-performance POSIX-compatible file system. The application writes to the mounted file system, and data automatically syncs to S3. For a production workload that writes to a local path, FSx for Lustre provides transparent S3 integration. Storage Gateway also works (NFS mount → S3) but is typically for on-premises. FSx for Lustre is the cloud-native option for EC2 workloads needing file system + S3.", domain:2},
  {q:"A company wants to ensure that RDS database snapshots cannot be shared with external AWS accounts. Which preventive control blocks this?", opts:["IAM policy denying rds:ModifyDBSnapshotAttribute in all accounts","SCP denying rds:ModifyDBSnapshotAttribute (which is the API used to share snapshots with other accounts)","RDS snapshot encryption (encrypted snapshots can't be shared cross-account)","AWS Config rule detecting shared snapshots"], correct:1, exp:"ModifyDBSnapshotAttribute is the API call that adds AWS account IDs to a snapshot's sharing permissions. An SCP denying this action across the organization prevents anyone from sharing snapshots externally. IAM policies can be modified by account admins, but SCPs cannot be overridden. Encrypted snapshots CAN be shared cross-account (by sharing the KMS key). Config is detective, not preventive.", domain:1},
  {q:"A company's application uses DynamoDB On-Demand mode. They're evaluating cost. The table handles 50 million reads and 10 million writes per day, consistently. Each read is 4KB (1 RCU) and each write is 1KB (1 WCU). On-Demand charges $1.25 per million WCU and $0.25 per million RCU. Should they switch to provisioned?", opts:["Stay On-Demand — it's simpler and cost is acceptable","Switch to Provisioned with auto-scaling — for consistent predictable workloads, provisioned is 5-7x cheaper than on-demand","Switch to Provisioned with Reserved Capacity for maximum savings on predictable workloads","Need more information about traffic patterns"], correct:2, exp:"On-Demand cost: 10M WCU/day × $1.25/M = $12.50/day writes + 50M RCU/day × $0.25/M = $12.50/day reads = $25/day = $750/month. Provisioned (sustained): ~116 WCU + 579 RCU constant = ~$47/month (much cheaper). With Reserved Capacity (1 year): even cheaper. For CONSISTENT workloads, provisioned is dramatically cheaper (5-7x). On-Demand is best for unpredictable or spiky traffic. This is an important cost optimization question.", domain:4},
  {q:"A company uses AWS WAF to protect their ALB. They want to allow requests only from their mobile app (blocking browser/bot traffic). The app includes a custom header (X-App-Token: secret123) in all requests. How should WAF be configured?", opts:["Block all requests that don't contain the X-App-Token header with the correct value using a WAF string match rule","Rate-based rule to block suspicious traffic patterns","Geo-restriction to block certain countries","Bot Control managed rule group"], correct:0, exp:"A WAF string match rule checking for the X-App-Token header with the expected value allows app traffic and blocks everything else. However, note: this is security through obscurity (the token could be extracted from the app). For stronger protection, combine with: rate limiting, device fingerprinting, and WAF Bot Control. For the exam, the string match rule on a custom header is the direct answer to the stated requirement.", domain:1},
  {q:"A company runs a distributed system where exactly one instance of a scheduled job must run at any given time across multiple EC2 instances (leader election). If the current leader fails, another instance should take over. Which AWS service provides distributed locking for leader election?", opts:["DynamoDB with conditional writes (PutItem with ConditionExpression) and TTL for lock expiry","ElastiCache Redis with SET NX (set-if-not-exists) and TTL","Both A and B work — DynamoDB for serverless environments, Redis for low-latency requirements","S3 with object locking"], correct:2, exp:"Both DynamoDB and Redis provide distributed locking: DynamoDB uses conditional writes (only succeeds if lock doesn't exist) with TTL for automatic expiry if the leader crashes. Redis uses SET NX (atomic set-if-not-exists) with EXPIRE. DynamoDB is better for serverless (no infrastructure). Redis is better for sub-millisecond lock acquisition. Both are valid leader election implementations.", domain:2},
  {q:"A company uses CloudFormation to manage infrastructure. They have a production stack that includes an RDS instance. A developer accidentally changes the RDS EngineVersion in the template, which would cause CloudFormation to REPLACE the database (delete old, create new = data loss). How do they prevent this?", opts:["Enable Stack Termination Protection","Add a Stack Policy that denies Update:Replace on the RDS resource","Set DeletionPolicy: Retain on the RDS resource","All three provide different layers of protection and should all be implemented"], correct:3, exp:"Stack Policy prevents CloudFormation from performing replacement updates on protected resources (catches the template change attempt). DeletionPolicy: Retain ensures that IF the resource is somehow removed from the stack, the actual database survives. Termination Protection prevents accidental stack deletion. Together: Stack Policy blocks dangerous updates, DeletionPolicy protects if it somehow gets through, Termination Protection blocks full deletion.", domain:2},
  {q:"A company has an application that generates reports. Users request a report, and the system sends an email when it's ready (typically 5-10 minutes). They want to track the status of each report request. What is a simple serverless architecture for this?", opts:["SQS → Lambda → S3 with DynamoDB tracking status","Step Functions with status tracking: API Gateway receives request → Step Functions executes (generate report → store in S3 → send email via SES) — status queryable via DescribeExecution","API Gateway → Lambda → direct processing → email","EC2 background worker with a status database"], correct:1, exp:"Step Functions is ideal for multi-step workflows with status tracking. The execution state is queryable at any time via DescribeExecution API (RUNNING, SUCCEEDED, FAILED). API Gateway starts the execution and returns the execution ARN. A separate API endpoint calls DescribeExecution for status checks. Steps: generate report (Lambda) → store (S3) → email (SES via Lambda). Built-in retry, error handling, and status visibility.", domain:2},
]
