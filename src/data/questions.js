export const questions = [
  // DOMAIN 1: Design Secure Architectures (30%)
  {q:"A company is migrating to AWS and wants to ensure that only the production account can launch EC2 instances in the us-east-1 region. Which approach should a solutions architect use?", opts:["Create an IAM policy attached to all users preventing other regions","Use Service Control Policies (SCPs) in AWS Organizations to restrict regions","Configure Security Groups to block traffic from other regions","Use AWS Config to terminate instances in non-approved regions"], correct:1, exp:"SCPs in AWS Organizations restrict what services and regions member accounts can use. They apply to the entire account, including the root user. IAM policies won't prevent all actions organization-wide.", domain:1},
  {q:"A solutions architect needs to provide temporary access to a third-party auditor to review CloudTrail logs in an S3 bucket. The access should expire after 12 hours. What is the MOST secure approach?", opts:["Create an IAM user with time-limited credentials","Create a cross-account IAM role with a 12-hour maximum session duration","Share the root account credentials temporarily","Generate a pre-signed URL for the S3 bucket with 12-hour expiry"], correct:1, exp:"Cross-account IAM roles with temporary credentials are the most secure way to grant third-party access. They automatically expire, require no long-term credentials, and can be easily revoked.", domain:1},
  {q:"An application running on EC2 instances needs to read objects from an S3 bucket. What is the MOST secure way to grant this access?", opts:["Store AWS access keys in the application configuration file","Create an IAM role with S3 read permissions and attach it to the EC2 instances","Make the S3 bucket public with a bucket policy","Store credentials in AWS Systems Manager Parameter Store and retrieve at startup"], correct:1, exp:"IAM roles for EC2 provide temporary credentials that are automatically rotated. No long-term credentials are stored anywhere. This follows the principle of least privilege.", domain:1},
  {q:"A company wants to encrypt all data at rest in their RDS database and control the key rotation schedule. Which solution meets these requirements?", opts:["Use default RDS encryption with AWS-managed keys","Use AWS KMS with a customer-managed CMK and configure automatic rotation","Use SSL/TLS certificates from ACM","Encrypt data at the application level before storing"], correct:1, exp:"Customer-managed CMKs in KMS allow you to control the key rotation schedule, define key policies, and audit key usage. AWS-managed keys rotate automatically every year but you can't control the schedule.", domain:1},
  {q:"A company requires that all S3 buckets have server-side encryption enabled and that no bucket can ever be made public. How should a solutions architect enforce this across all AWS accounts in the organization?", opts:["Create IAM policies for each user denying unencrypted uploads","Use SCPs to deny s3:PutBucketPolicy with public access and require encryption headers","Enable S3 Block Public Access at the organization level and use AWS Config rules","Send email reminders to developers about encryption requirements"], correct:2, exp:"S3 Block Public Access at the organization level prevents any bucket from being made public. Combined with AWS Config rules that check for encryption, this provides automated enforcement and compliance monitoring.", domain:1},
  {q:"A web application needs to allow users to upload files directly to S3 without routing through the application server. The upload must be secure and time-limited. What should the architect recommend?", opts:["Make the S3 bucket public and restrict with CORS","Use S3 pre-signed URLs generated by the application backend","Use CloudFront signed URLs with OAC","Create temporary IAM users for each upload session"], correct:1, exp:"Pre-signed URLs allow secure, time-limited uploads directly to S3 without making the bucket public. The application generates the URL with specific expiry and permissions.", domain:1},
  {q:"A solutions architect is designing a multi-tier web application. The web tier is in a public subnet and the database tier must not be accessible from the internet. How should the VPC be configured?", opts:["Place the database in a public subnet with restrictive Security Groups","Place the database in a private subnet with no route to the Internet Gateway","Place both tiers in the same public subnet and use NACLs","Use a single subnet with Security Groups to separate traffic"], correct:1, exp:"Private subnets have no route to an Internet Gateway, making resources in them unreachable from the internet. Security Groups alone in a public subnet still expose the instance to potential attacks.", domain:1},
  {q:"An application uses an ALB in a public subnet to distribute traffic to EC2 instances in private subnets. The instances need to download patches from the internet. What is needed?", opts:["Add an Internet Gateway route to the private subnet","Deploy a NAT Gateway in the public subnet and add a route from the private subnet","Make the EC2 instances public with Elastic IPs","Use VPC peering to connect to the internet"], correct:1, exp:"A NAT Gateway in a public subnet allows instances in private subnets to initiate outbound connections to the internet while remaining unreachable from the internet for inbound traffic.", domain:1},

  // DOMAIN 2: Design Resilient Architectures (26%)
  {q:"A company runs a critical application that requires 99.99% availability. The application uses an RDS MySQL database. What architecture provides the required availability?", opts:["Single RDS instance with automated backups","RDS Multi-AZ deployment with Read Replicas in the same region","RDS Multi-AZ deployment with cross-region Read Replicas and Route 53 failover","RDS in a single AZ with frequent snapshots to another region"], correct:2, exp:"For 99.99% availability, you need Multi-AZ for automatic failover within a region AND cross-region Read Replicas with Route 53 failover for regional disaster recovery. Single-region solutions can't achieve 99.99%.", domain:2},
  {q:"A stateless web application behind an ALB experiences unpredictable traffic spikes that can increase load 10x within minutes. How should the architect design for this?", opts:["Use a single large EC2 instance that can handle peak traffic","Use an Auto Scaling Group with target tracking policy based on CPU utilization","Use Reserved Instances sized for peak capacity","Deploy on a fixed fleet of Spot Instances"], correct:1, exp:"Auto Scaling Groups with target tracking policies automatically adjust capacity based on metrics like CPU utilization. This handles unpredictable spikes efficiently while maintaining cost optimization during low-traffic periods.", domain:2},
  {q:"An application stores session data in local EC2 instance memory. Users report losing their session when the load balancer routes them to a different instance. What is the BEST solution?", opts:["Enable sticky sessions on the ALB","Store session data in Amazon ElastiCache Redis","Store session data in an EBS volume shared between instances","Increase the number of instances to reduce session loss probability"], correct:1, exp:"ElastiCache Redis provides a centralized, high-performance session store accessible by all instances. Sticky sessions reduce the benefit of load balancing and don't survive instance failures. EBS cannot be shared across instances (use EFS for that).", domain:2},
  {q:"A company needs a disaster recovery solution with an RTO of 1 hour and RPO of 15 minutes for their web application. Which DR strategy is MOST cost-effective?", opts:["Multi-site active-active across two regions","Warm standby with a scaled-down version running in a second region","Pilot light with core infrastructure pre-provisioned in a second region","Backup and restore with daily snapshots stored in another region"], correct:2, exp:"Warm standby keeps a scaled-down version running continuously and can be scaled up within minutes (meeting 1-hour RTO). Pilot light has longer RTO. Multi-site is expensive. Backup/restore can't meet 1-hour RTO or 15-min RPO.", domain:2},
  {q:"An application processes uploaded images asynchronously. The processing can take up to 5 minutes per image. During peak hours, uploads exceed processing capacity. What architecture ensures no images are lost?", opts:["Increase the size of the processing EC2 instances","Use SQS queue between the upload and processing tiers with Auto Scaling on queue depth","Store images in EBS and process them in order","Use SNS to notify processing instances of new uploads"], correct:1, exp:"SQS decouples the upload tier from processing. Messages persist in the queue (up to 14 days) so no uploads are lost during peaks. Auto Scaling based on queue depth adds processing capacity when needed.", domain:2},
  {q:"A company runs a critical database that cannot tolerate any data loss. The solution must survive the failure of an entire AWS Region. What should the architect recommend?", opts:["RDS Multi-AZ in a single region","Aurora Global Database with RPO of 1 second","DynamoDB Global Tables with multi-region replication","S3 Cross-Region Replication with versioning"], correct:2, exp:"DynamoDB Global Tables provides active-active multi-region replication with eventual consistency. For zero data loss across regions, this with its built-in conflict resolution is the most robust. Aurora Global has ~1 second RPO which means potential data loss.", domain:2},
  {q:"A media company serves video content globally. Users in Asia experience high latency accessing content stored in us-east-1. What solution reduces latency with minimal architectural changes?", opts:["Deploy the entire application in an Asian region","Use Amazon CloudFront with the S3 bucket as origin","Use S3 Cross-Region Replication to an Asian region","Deploy a VPN connection to Asia"], correct:1, exp:"CloudFront caches content at 400+ edge locations worldwide, dramatically reducing latency for static content delivery. It requires minimal changes — just create a distribution with S3 as the origin.", domain:2},

  // DOMAIN 3: Design High-Performing Architectures (24%)
  {q:"A company's DynamoDB table has a partition key of user_id. They notice hot partitions because a small number of users generate most of the traffic. What is the BEST solution?", opts:["Increase the provisioned read/write capacity units","Add a random suffix to the partition key to distribute traffic","Switch to RDS for better distribution","Enable DynamoDB Accelerator (DAX) for read caching"], correct:3, exp:"DAX provides a write-through cache for DynamoDB with microsecond read latency. It absorbs the hot-partition read traffic. Adding random suffixes complicates queries. For hot-partition READ issues, caching is the best approach.", domain:3},
  {q:"A real-time analytics application needs to ingest millions of events per second from IoT devices and process them within seconds. Which architecture is BEST?", opts:["SQS Standard queue with Lambda consumers","Amazon Kinesis Data Streams with Kinesis Data Analytics","API Gateway with DynamoDB direct integration","SNS with SQS fanout pattern"], correct:1, exp:"Kinesis Data Streams handles millions of records per second with real-time processing. Kinesis Data Analytics enables SQL queries on streaming data. SQS has higher latency and isn't designed for real-time analytics.", domain:3},
  {q:"A web application needs to serve 100,000 concurrent users with sub-millisecond read latency for frequently accessed data. The underlying data changes infrequently. What caching strategy should be used?", opts:["Store data in S3 with CloudFront distribution","Use ElastiCache Redis with a lazy-loading (cache-aside) pattern","Use DynamoDB with provisioned capacity","Store data in EC2 instance memory with sticky sessions"], correct:1, exp:"ElastiCache Redis with lazy-loading provides sub-millisecond reads for hot data. Lazy-loading fills the cache on demand, which is ideal when data changes infrequently. CloudFront is for edge caching of web content, not application-level data.", domain:3},
  {q:"A data lake stores 50TB of raw data in S3. Data scientists need to run ad-hoc SQL queries on this data without provisioning infrastructure. Which service should they use?", opts:["Amazon Redshift Serverless","Amazon Athena","Amazon RDS","Amazon EMR with Hive"], correct:1, exp:"Athena is a serverless query service that runs SQL directly against S3 data. No infrastructure to provision, pay-per-query. Perfect for ad-hoc analytics on data lakes. Redshift is better for repeated complex queries on structured data.", domain:3},
  {q:"An application requires a database that supports millions of requests per second, has single-digit millisecond latency, and can automatically scale storage. The data model is key-value. Which service fits BEST?", opts:["Amazon RDS with Multi-AZ","Amazon Aurora Serverless","Amazon DynamoDB with on-demand capacity","Amazon ElastiCache Redis"], correct:2, exp:"DynamoDB with on-demand capacity provides single-digit millisecond latency at any scale, automatic storage scaling, and is purpose-built for key-value workloads. On-demand mode handles millions of requests without capacity planning.", domain:3},
  {q:"A company wants to run complex SQL queries on petabytes of structured data for business intelligence reporting. Queries run nightly and take several hours. Which service is BEST?", opts:["Amazon Athena","Amazon Redshift","Amazon DynamoDB","Amazon RDS Aurora"], correct:1, exp:"Redshift is a data warehouse optimized for complex analytical queries on petabyte-scale structured data. It uses columnar storage and massively parallel processing (MPP) for fast BI queries. Athena is better for ad-hoc queries on semi-structured data.", domain:3},
  {q:"An application serves static assets (images, CSS, JS) from an S3 bucket. Users worldwide report slow load times. What is the MOST effective improvement?", opts:["Enable S3 Transfer Acceleration","Place a CloudFront distribution in front of S3","Move S3 to a central region","Increase S3 request rate limits"], correct:1, exp:"CloudFront caches static content at edge locations close to users worldwide, dramatically reducing latency. Transfer Acceleration speeds uploads TO S3, not downloads FROM S3. S3 itself doesn't have configurable request rate limits.", domain:3},

  // DOMAIN 4: Design Cost-Optimized Architectures (20%)
  {q:"A company runs batch processing jobs every night from 2 AM to 6 AM. The jobs are fault-tolerant and can be restarted if interrupted. What is the MOST cost-effective EC2 purchasing option?", opts:["On-Demand Instances","Reserved Instances with scheduled reservation","Spot Instances with checkpointing","Dedicated Hosts"], correct:2, exp:"Spot Instances offer up to 90% discount and are perfect for fault-tolerant batch jobs that can handle interruptions. Checkpointing allows jobs to resume from where they stopped. Scheduled RI would work but costs more than Spot.", domain:4},
  {q:"A company has multiple AWS accounts and wants to reduce costs by sharing Reserved Instance discounts across accounts. What should they do?", opts:["Purchase RIs in each account separately","Use AWS Organizations with consolidated billing to share RI benefits","Transfer RIs between accounts manually","Use Savings Plans instead which automatically apply cross-account"], correct:1, exp:"With consolidated billing in AWS Organizations, Reserved Instance discounts are automatically shared across all accounts in the organization. Savings Plans also share cross-account with consolidated billing.", domain:4},
  {q:"An application stores log files in S3 that are accessed frequently for the first 30 days, occasionally for the next 60 days, and rarely after that but must be retained for 7 years. What is the MOST cost-effective storage strategy?", opts:["Keep all data in S3 Standard","Use S3 Intelligent-Tiering for all data","Create a lifecycle policy: Standard (30 days) → Standard-IA (60 days) → Glacier Deep Archive","Store directly in Glacier from day 1"], correct:2, exp:"Lifecycle policies automate transitions between storage classes based on access patterns. Standard for hot data, Standard-IA for infrequent (cheaper but retrieval fee), and Glacier Deep Archive for long-term retention (cheapest).", domain:4},
  {q:"A development team runs EC2 instances for testing only during business hours (8 AM - 6 PM, Monday-Friday). Instances sit idle on evenings and weekends. What reduces costs the MOST?", opts:["Switch to smaller instance types","Use AWS Instance Scheduler to stop instances outside business hours","Purchase Reserved Instances for the testing instances","Use Auto Scaling with a minimum of 0 instances"], correct:1, exp:"Instance Scheduler stops and starts instances on a defined schedule, eliminating charges for 128 hours/week of idle time (evenings + weekends). You only pay for EBS storage when stopped. This saves ~76% vs running 24/7.", domain:4},
  {q:"A company processes large datasets using EMR clusters. Processing time varies from 1 to 4 hours. The cluster needs a minimum of 10 nodes but can use up to 50 during peak processing. How should nodes be provisioned for cost optimization?", opts:["Use 50 On-Demand instances","Use 10 On-Demand instances for core nodes and up to 40 Spot instances for task nodes","Use all Spot instances","Use 50 Reserved Instances"], correct:1, exp:"Core nodes (reliable capacity) should use On-Demand to ensure job completion. Task nodes (extra capacity) can use Spot for up to 90% savings since EMR can handle Spot interruptions for task nodes gracefully.", domain:4},
  {q:"A company wants to choose between deploying an application on Lambda versus EC2. The application handles 1 million requests per day with an average execution time of 200ms. Which is MORE cost-effective?", opts:["Always Lambda because serverless is cheaper","Always EC2 because it has no per-request pricing","It depends — Lambda is cheaper for sporadic traffic, EC2 is cheaper for steady high-volume traffic","They cost the same"], correct:2, exp:"For steady high-volume traffic (1M requests/day continuously), EC2 with Reserved Instances is often cheaper. Lambda's per-request pricing makes it more economical for sporadic or unpredictable workloads. Always calculate both.", domain:4},
]

export function shuffleArray(arr) {
  const a = [...arr]
  for (let i = a.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [a[i], a[j]] = [a[j], a[i]]
  }
  return a
}
