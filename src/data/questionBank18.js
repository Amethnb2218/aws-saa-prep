export const questionBank18 = [
  {q:"A company processes financial transactions that require ACID compliance. Each transaction involves multiple DynamoDB table writes that must ALL succeed or ALL fail. How can they implement atomic multi-item transactions?", opts:["Write items individually and roll back manually on failure","DynamoDB Transactions (TransactWriteItems) — atomic writes to up to 100 items across multiple tables. Either ALL succeed or NONE are applied.","Use SQS to ensure each write eventually completes","Implement two-phase commit in application code"], correct:1, exp:"DynamoDB TransactWriteItems provides ACID transactions across up to 100 items (25 per table) in a single account and region. All operations succeed atomically or none are applied. Supports: Put, Update, Delete, and ConditionCheck within the transaction. Double the WCU cost of individual writes. Use for: financial transfers, inventory management, or any multi-item operation requiring consistency. This is native DynamoDB — no external coordination needed.", domain:3},
  {q:"A company uses Amazon CloudFront. They want to modify HTTP response headers (add security headers like Content-Security-Policy, Strict-Transport-Security) without modifying their origin application. What CloudFront feature does this?", opts:["Origin Custom Headers (adds headers from CloudFront to origin, not to viewer)","CloudFront Response Headers Policy — add/modify/remove response headers at the edge (security headers, CORS headers, custom headers) without Lambda@Edge or origin changes","Lambda@Edge on origin-response event (works but overkill for static headers)","Modify headers at the origin server"], correct:1, exp:"CloudFront Response Headers Policies (released 2021) add security headers, CORS headers, or custom headers to ALL responses without needing Lambda@Edge or origin modifications. Pre-built managed policies exist for common security headers. Configure once, applies to all responses from that distribution behavior. Much simpler and cheaper than Lambda@Edge for static header additions. Lambda@Edge is only needed for dynamic/conditional header logic.", domain:1},
  {q:"A company uses a microservices architecture. Service A calls Service B, which calls Service C. When a request fails, they need to identify WHICH service caused the failure and WHERE the latency bottleneck is. They use X-Ray for tracing. What X-Ray concept connects the traces across all three services?", opts:["Trace ID — a unique identifier propagated through all services in the request chain. Each service adds a segment to the same trace.","Separate traces per service that must be manually correlated","CloudWatch log correlation IDs","Service mesh headers"], correct:0, exp:"X-Ray Trace ID: a unique ID generated by the first service (or ALB/API Gateway). As the request flows through services A→B→C, each service adds a 'segment' to the SAME trace (identified by the trace ID passed in the X-Amzn-Trace-Id header). The X-Ray console shows the complete request path, timing at each service, and where failures/latency occur. This is distributed tracing — a single trace spans the entire request across all services.", domain:2},
  {q:"A company uses AWS Glue for ETL. They have a Glue job that runs daily and processes 100GB of data. The job takes 45 minutes with 10 DPUs (Data Processing Units). They want to reduce execution time to under 15 minutes. What should they try?", opts:["Optimize the ETL script code (may help but limited impact)","Increase DPUs to 30-40 (Glue scales linearly — 3x DPUs ≈ 3x faster for parallelizable workloads). Also consider Glue job bookmarks to process only new data.","Switch from Spark to Python Shell job type (slower, not faster)","Reduce data to process by filtering at source"], correct:1, exp:"Glue Spark jobs parallelize across DPUs (workers). For data processing that's parallelizable (most ETL): increasing DPUs proportionally reduces execution time. 10 DPU × 45 min → 30 DPU × ~15 min. Also: enable job bookmarks to process only NEW/changed data (skip already-processed files). Glue Auto Scaling can also dynamically adjust workers. Cost: more DPUs × less time = similar total cost (DPU-hours), but faster completion.", domain:3},
  {q:"A company uses S3 for their data lake. An analyst runs an Athena query but gets zero results even though they know the data exists. The table was created by a Glue Crawler. What is a common cause?", opts:["Athena doesn't support the file format","The Glue Crawler created PARTITIONS but the analyst's query doesn't include a partition filter — AND the table uses partition projection or has partitions that haven't been loaded with MSCK REPAIR TABLE","Athena has a timeout","S3 bucket is in a different region than Athena"], correct:1, exp:"Common Athena/Glue issue: partitioned tables require partition metadata in the Glue catalog. If the Crawler didn't detect all partitions, or new data was added after crawling, those partitions are invisible to Athena. Fix: (1) Run MSCK REPAIR TABLE to discover new partitions, (2) Re-run the Crawler, or (3) Use partition projection (defines partition pattern, no catalog updates needed). Also check: SerDe configuration, file format, and column types match actual data.", domain:3},
  {q:"A company uses AWS CloudTrail. A security team member asks: does CloudTrail log READ operations (like DescribeInstances, ListBuckets) or only WRITE operations (like RunInstances, CreateBucket)?", opts:["Only write/mutating operations","CloudTrail logs BOTH read and write management events by default (DescribeInstances, ListBuckets, RunInstances, CreateBucket). However, high-volume read events like GetObject and GetRecords (data events) require explicit configuration.","Only write operations by default, read operations require additional configuration","CloudTrail doesn't distinguish between read and write operations"], correct:1, exp:"CloudTrail management events include BOTH read (Describe*, List*, Get*) and write (Create*, Delete*, Put*, Run*) API calls by default. These are free to log. DATA events (S3 GetObject/PutObject, Lambda Invoke, DynamoDB GetItem) are high-volume and cost extra — must be explicitly enabled. You can configure trails to log: management events (read+write, read-only, or write-only) and/or data events (per S3 bucket or Lambda function).", domain:1},
  {q:"A company migrates their on-premises Redis cluster to AWS. They need high availability (Multi-AZ) and automatic failover. The Redis cluster has 6 nodes with data sharded across 3 shards. Which ElastiCache configuration matches?", opts:["ElastiCache Redis (Cluster Mode Disabled) with 3 shards (Cluster Mode Disabled only supports 1 shard)","ElastiCache Redis (Cluster Mode Enabled) with 3 shards, 1 replica per shard (total 6 nodes: 3 primary + 3 replica). Multi-AZ enabled for automatic failover.","ElastiCache Memcached with 6 nodes (Memcached doesn't support replication/failover)","Run Redis on EC2 manually"], correct:1, exp:"ElastiCache Redis Cluster Mode Enabled: multiple shards with replicas per shard. 3 shards with 1 replica each = 6 nodes (3 primary for writes, 3 replicas for reads + failover). Multi-AZ enabled = automatic failover if a primary fails (replica promoted). This matches their on-premises 6-node sharded cluster. Cluster Mode Disabled supports only 1 shard (max 6 nodes = 1 primary + 5 replicas, no sharding). Memcached doesn't support replication.", domain:2},
  {q:"A company uses S3 and wants to be notified whenever someone accesses a specific sensitive file (audit requirement). What is the MOST direct approach?", opts:["S3 Event Notifications for GetObject (not supported — S3 events only trigger on Put/Delete/Restore/Replication)","Enable CloudTrail S3 Data Events → EventBridge rule matching GetObject on the specific key → SNS notification","S3 access logging with periodic log analysis","CloudWatch custom metric per object access (not native)"], correct:1, exp:"S3 Event Notifications support: s3:ObjectCreated:*, s3:ObjectRemoved:*, s3:ObjectRestore:*, etc. — but NOT s3:ObjectAccessed (GetObject). For access auditing: enable CloudTrail S3 Data Events which log GetObject. Create an EventBridge rule matching the CloudTrail event with specific bucket/key → SNS topic → email/Slack. This provides near-real-time notification on sensitive file access. Alternative: S3 access logs + Athena for after-the-fact analysis.", domain:1},
  {q:"A company uses API Gateway with a Lambda authorizer (formerly Custom Authorizer). The authorizer validates JWT tokens. They notice the authorizer is called for every request, adding latency. How can they reduce authorizer invocations?", opts:["Cache the token validation result in DynamoDB","Enable API Gateway authorizer caching — results are cached for a configurable TTL (up to 3600 seconds). Subsequent requests with the same token skip the authorizer Lambda.","Use Cognito User Pools authorizer instead (no caching either)","Increase Lambda memory for faster execution"], correct:1, exp:"API Gateway Lambda Authorizer caching: set the TTL (0-3600 seconds). The cache key is the authorization token (or identity sources). Same token within TTL = cached response (Allow/Deny) without invoking Lambda. This dramatically reduces authorizer Lambda invocations and latency (cached response is <1ms vs 50-200ms for Lambda invocation). Set TTL based on token validity duration. TTL=0 disables caching (every request invokes authorizer).", domain:3},
  {q:"A company uses a Network Load Balancer (NLB). They want to see the client's original source IP address in their application logs. Is this possible with NLB?", opts:["NLB uses proxy protocol — must enable Proxy Protocol v2 to see source IP","NLB preserves the client source IP by default (Layer 4 pass-through — no proxying, client IP is visible without any configuration). Unlike ALB which requires X-Forwarded-For.","NLB doesn't preserve source IP — use X-Forwarded-For header","Source IP is never visible behind a load balancer"], correct:1, exp:"NLB operates at Layer 4 (TCP/UDP pass-through). It preserves the client's source IP address in the IP packet. Applications see the real client IP without any configuration (no Proxy Protocol needed for instance targets in the same VPC). Exceptions: (1) targets registered by IP (Lambda, IPs in other VPCs) need Proxy Protocol v2, (2) TLS termination at NLB may require Proxy Protocol. For standard instance targets: source IP is preserved automatically.", domain:3},
  {q:"A company uses Amazon Macie to discover sensitive data in their S3 buckets. Macie finds PII (credit card numbers, SSN) in a bucket that should only contain non-sensitive analytics data. What is Macie's role here?", opts:["Macie encrypts the sensitive data automatically","Macie is a data discovery and classification service — it identifies and alerts on sensitive data in S3 using ML and pattern matching. It DETECTS PII/sensitive data but does not automatically remediate. The company must take action to move/delete/encrypt the sensitive data.","Macie blocks access to the sensitive files","Macie moves sensitive files to a secure bucket"], correct:1, exp:"Amazon Macie uses ML and pattern matching to automatically discover and classify sensitive data in S3: PII (SSN, credit cards, names, addresses), financial data, credentials, etc. It generates findings showing: which buckets have sensitive data, what type, and how much. Macie does NOT remediate — it alerts. Organizations use Macie findings to: implement data loss prevention policies, move misplaced sensitive data, or apply additional encryption/access controls.", domain:1},
  {q:"A company needs a managed graph database for a social network application (users, connections, recommendations). They need to traverse millions of relationships efficiently. Which AWS database service?", opts:["DynamoDB (key-value/document, not optimized for graph traversals)","Amazon Neptune — fully managed graph database supporting Gremlin and SPARQL query languages, optimized for storing and traversing billions of relationships","Amazon RDS with complex JOINs (extremely slow for deep graph traversals)","Amazon DocumentDB (document database, not graph)"], correct:1, exp:"Amazon Neptune: purpose-built graph database for highly connected datasets. Use cases: social networks (friend-of-friend queries), recommendation engines, knowledge graphs, fraud detection (relationship patterns), and identity graphs. Supports: Apache TinkerPop Gremlin (property graph) and W3C SPARQL (RDF). Optimized for traversals like 'find all friends of friends who like X within 3 hops' — queries that would require expensive recursive JOINs in relational databases.", domain:3},
  {q:"A company wants to migrate their on-premises application to AWS. The application consists of: a frontend (React SPA), a REST API (Java Spring Boot), and a PostgreSQL database. They want to use managed services and minimize operational overhead. What is the recommended architecture?", opts:["EC2 for everything (lift and shift — same operational burden)","Frontend: S3 + CloudFront | API: ECS Fargate (or Elastic Beanstalk) | Database: Aurora PostgreSQL — fully managed, scalable, minimal operations","Frontend: EC2 | API: Lambda | Database: RDS PostgreSQL","Frontend: Amplify | API: API Gateway + Lambda | Database: DynamoDB (requires app rewrite)"], correct:1, exp:"Recommended managed services architecture: S3+CloudFront for static SPA (zero server management, global CDN), ECS Fargate for the Spring Boot API (containerized, auto-scaling, no instance management), Aurora PostgreSQL for the database (compatible with existing PostgreSQL code, managed, scalable, Multi-AZ). This minimizes operational overhead while requiring minimal code changes. The application works as-is with just packaging and deployment changes.", domain:2},
  {q:"A company uses CloudWatch Contributor Insights on their DynamoDB table. What information does this provide?", opts:["Table storage usage by item","Identifies the TOP CONTRIBUTORS to traffic — shows which partition keys (items) are receiving the most reads/writes (hot key identification). Helps identify hot partitions causing throttling.","Query performance metrics","Table cost breakdown"], correct:1, exp:"CloudWatch Contributor Insights for DynamoDB shows: the most accessed partition keys (top N), most throttled keys, and traffic patterns over time. This directly identifies HOT KEYS — items receiving disproportionate traffic. Hot keys cause partition-level throttling even when table-level capacity is sufficient. Insights help you: redesign hot keys, implement caching for frequently accessed items (DAX), or add write sharding. Essential for DynamoDB performance troubleshooting.", domain:3},
  {q:"A company uses AWS Budgets to monitor spending. They want to automatically STOP EC2 instances when the monthly budget reaches 80% to prevent overspending. Can AWS Budgets do this?", opts:["No — Budgets can only send notifications","Yes — AWS Budgets supports Budget Actions: automatically execute predefined actions (stop EC2 instances, apply IAM policies, apply SCPs) when budget thresholds are breached","Budgets can only alert SNS topics","Use Lambda triggered by billing alarm instead"], correct:1, exp:"AWS Budgets Actions (released 2020): when a budget threshold is reached, automatically execute actions like: (1) Apply IAM policy denying ec2:RunInstances, (2) Apply SCP restricting regions, (3) Stop specific EC2 instances. Configure approval workflow (automatic or requires manual approval). This provides automated cost controls beyond just notifications. Example: at 80% budget → stop non-production instances, at 100% → apply restrictive SCP.", domain:4},
  {q:"A company uses S3 Batch Operations to perform bulk operations on millions of objects. They want to copy 50 million objects from one bucket to another (same region, changing storage class to Glacier). What does S3 Batch Operations provide?", opts:["Just a wrapper around individual PutObject calls","S3 Batch Operations: takes a manifest (list of objects from S3 Inventory or CSV), performs specified operation on all listed objects at scale (copy, invoke Lambda, restore, tag, ACL change). Tracks progress, retries failures, and generates completion report.","Only supports tagging operations","Maximum 1 million objects per job"], correct:1, exp:"S3 Batch Operations processes BILLIONS of objects in a single job. Provide a manifest (S3 Inventory report or custom CSV listing objects). Operations: Copy, Invoke Lambda, Restore (from Glacier), Replace ACL, Replace Tags, Delete. Features: progress tracking in console, automatic retry of failed objects, completion report with success/failure per object. For 50M objects: creates a job, S3 processes them in parallel at massive scale. No custom code or infrastructure needed.", domain:3},
  {q:"A company runs a stateful application on EC2 that stores session data on the local instance. They want to add an ALB for high availability with multiple instances. The problem: if a user's request goes to a different instance, their session is lost. What are TWO solutions?", opts:["Only run one instance (defeats HA purpose)","Enable ALB Sticky Sessions (session affinity — routes same user to same instance) OR externalize session state to ElastiCache Redis (shared across all instances)","Use NLB instead of ALB","Store sessions in EBS (not shared across instances)"], correct:1, exp:"Two approaches: (1) ALB Sticky Sessions (application-based or duration-based cookies): routes the same client to the same target for cookie duration. Simple but creates imbalanced load and doesn't handle instance failure. (2) Externalize sessions to ElastiCache Redis: all instances read/write sessions from shared Redis. Any instance can serve any user. Handles instance failure gracefully. Redis approach is the recommended long-term solution; sticky sessions are a quick fix.", domain:2},
  {q:"A company uses CloudFormation. A stack creation fails on one resource (e.g., security group rule conflict). The stack rolls back and all resources are deleted. They want to keep the successfully created resources for debugging. What option should they use?", opts:["--disable-rollback (or DisableRollback: true) — preserves ALL resources in their current state (successful + failed) for investigation. Stack enters CREATE_FAILED state instead of rolling back.","Stack Policy preventing deletion","DeletionPolicy: Retain","Change Set instead of direct creation"], correct:0, exp:"--disable-rollback (console: 'Preserve successfully provisioned resources'): if creation fails, CloudFormation does NOT roll back. Successfully created resources remain for investigation. The stack is in CREATE_FAILED state. After debugging: either fix the issue and continue (ContinueUpdateRollback), or delete the stack. This is invaluable for debugging creation failures in complex stacks. Don't use in production (leaves partial stacks), but essential for development/debugging.", domain:2},
  {q:"A company uses AWS Step Functions for a data processing workflow. One step might take between 1 second and 2 hours depending on data size. They want to avoid paying for idle waiting time. Which Step Functions type should they use?", opts:["Standard Workflows (billed per state transition, not by time — there IS no idle time charge for waiting states)","Express Workflows (limited to 5-minute duration, won't work for 2-hour tasks)","Standard Workflows with activities (poll-based)","Use Lambda instead of Step Functions"], correct:0, exp:"Step Functions Standard Workflows charge PER STATE TRANSITION ($0.025 per 1,000 transitions), NOT by execution time. A Wait state or a long-running Task state doesn't accumulate charges while waiting — only the transition into/out of the state is billed. A 2-hour execution with 10 state transitions costs the same as a 1-second execution with 10 transitions. Express Workflows charge by duration (per 100ms) and are limited to 5 minutes. Standard is correct for long-running workflows.", domain:4},
  {q:"A company needs to deploy a third-party firewall appliance (Palo Alto, Fortinet) in their VPC to inspect ALL traffic between subnets and to/from the internet. Which VPC feature routes traffic through the appliance?", opts:["Security Groups (can't route traffic through appliances)","VPC Gateway Load Balancer (GWLB) — transparently inserts network appliances into the traffic flow. Uses Geneve encapsulation to route traffic through the appliance and back without modifying packets.","Network ACLs (filter only, don't route through appliances)","VPC route table pointing to the appliance ENI (works but no HA/scaling)"], correct:1, exp:"Gateway Load Balancer operates at Layer 3 (network layer). It creates a transparent bump-in-the-wire for network appliances. Traffic flow: Route table directs traffic to GWLB endpoint → GWLB distributes to appliance fleet (Geneve encapsulation) → appliance inspects/filters → returns to GWLB → forwards to destination. Provides: HA across AZs, auto-scaling appliances, single point of routing configuration. Purpose-built for third-party firewall/IDS/IPS deployment at scale.", domain:1},
  {q:"A company uses Amazon WorkSpaces (virtual desktops) for their remote workforce. 500 users work 9am-5pm weekdays only. Which billing model is MOST cost-effective?", opts:["Monthly billing for all 500 WorkSpaces (pay 24/7 even though used only 8 hours × 5 days = 40 hours/week)","Hourly billing (AutoStop mode) — WorkSpaces automatically stop after idle period, billed only for active hours. For 40 hours/week usage, hourly is ~60% cheaper than monthly.","On-Demand EC2 instances as virtual desktops (more management overhead)","Monthly billing with scheduled shutdown scripts"], correct:1, exp:"WorkSpaces AutoStop (hourly billing): base monthly fee (storage/OS) + hourly rate only when running. WorkSpaces auto-stop after idle timeout. For predictable 8×5 usage (160 hours/month): hourly billing is significantly cheaper than monthly (which charges for 744 hours/month of compute). AWS provides a calculator. Rule of thumb: if used <80 hours/month → hourly is cheaper; if used constantly → monthly is cheaper.", domain:4},
  {q:"A company uses Amazon RDS with a Multi-AZ deployment. They want to take a database snapshot for backup. Will the snapshot cause any performance impact on the primary instance?", opts:["Yes — snapshots always impact primary instance performance","No — in Multi-AZ deployments, snapshots are taken from the STANDBY instance, causing zero performance impact on the primary. For Single-AZ, snapshots cause brief I/O suspension on the primary.","Snapshots are taken from both instances simultaneously","RDS doesn't support manual snapshots with Multi-AZ"], correct:1, exp:"Multi-AZ snapshot benefit: RDS takes the snapshot from the standby instance, completely isolating the primary from any I/O impact. With Single-AZ deployments, the snapshot causes a brief I/O suspension (can cause latency spike). This is one of the often-overlooked benefits of Multi-AZ beyond just HA — operational activities (snapshots, maintenance) happen on the standby first, minimizing primary impact. AWS recommends Multi-AZ for production workloads.", domain:2},
  {q:"A company has an S3 bucket policy that DENIES all access except from their VPC endpoint. An administrator trying to access objects from the AWS Console (outside the VPC) is denied. They can't modify the bucket policy because they can't access the bucket. How do they fix this?", opts:["Contact AWS Support to delete the bucket","Use the ROOT account user to modify the bucket policy (root always has access to S3 bucket policies regardless of bucket policy Deny statements)","Delete and recreate the bucket","Use AWS CLI from within the VPC to fix the policy"], correct:1, exp:"The root account user has ultimate access to all resources in the account. Even if a bucket policy has an explicit Deny, the root user can still modify the bucket policy itself (access the policy document). Log in as root → modify or delete the restrictive bucket policy → restore normal access. This is why: (1) always include an escape clause in Deny policies, and (2) secure but don't disable root account access. Common gotcha: locking yourself out of S3 with overly restrictive policies.", domain:1},
  {q:"A company deploys a Lambda function using CloudFormation. They update the function code (new .zip) and run a stack update. The update succeeds but the function still runs OLD code. Why?", opts:["Lambda caches old code indefinitely","The S3 key for the deployment package didn't change (same filename). CloudFormation only triggers a Lambda function update if the resource PROPERTIES change. Same S3 key = no detected change = no update. Include a version hash in the S3 key name.","CloudFormation can't update Lambda code","Lambda needs to be manually re-deployed after CloudFormation updates"], correct:1, exp:"CloudFormation detects changes by comparing resource properties. If your Lambda function references S3Bucket + S3Key and you upload new code to the SAME key, CloudFormation sees no property change → doesn't update the function. Solutions: (1) Include version/hash in S3 key name (code-v2.zip, code-abc123.zip), (2) Use the CloudFormation package command (auto-generates unique S3 keys), (3) Use AWS SAM (handles this automatically). This is a common CloudFormation + Lambda gotcha.", domain:2},
  {q:"A company uses Amazon Inspector to scan their EC2 instances for vulnerabilities. Inspector reports a critical CVE in the operating system. What does Inspector do vs. what must the company do?", opts:["Inspector patches the vulnerability automatically","Inspector IDENTIFIES and REPORTS vulnerabilities (CVEs, network exposure, CIS benchmarks). It does NOT patch or remediate. The company must apply OS patches (via Systems Manager Patch Manager or manual updates) to fix the vulnerability.","Inspector blocks exploit attempts (that's WAF/IDS)","Inspector quarantines the affected instance"], correct:1, exp:"Amazon Inspector is an ASSESSMENT service — it discovers vulnerabilities by scanning OS packages and network configurations against CVE databases and CIS benchmarks. Output: findings with severity, CVE ID, affected package, and recommended fix (usually: update package to version X). Inspector does NOT: apply patches, block exploits, or quarantine instances. For automated patching: use Systems Manager Patch Manager. For exploit prevention: use security groups, NACLs, or IDS.", domain:1},
  {q:"A company wants to use AWS for HPC (High Performance Computing). Their simulations require low-latency, high-throughput communication between compute nodes (MPI workloads). Which EC2 feature optimizes inter-node communication?", opts:["Enhanced Networking (improves single-instance network, not inter-node latency)","EFA (Elastic Fabric Adapter) — a network interface that provides OS-bypass communication for HPC workloads, offering significantly lower latency than TCP/IP for inter-node MPI communication","Placement Groups (co-location helps but EFA provides actual bypass)","Instance Store networking"], correct:1, exp:"EFA (Elastic Fabric Adapter) provides OS-bypass hardware interface for MPI communication between instances. It bypasses the kernel TCP/IP stack, providing latency similar to on-premises HPC interconnects (InfiniBand-like). Use with Cluster Placement Groups (physical co-location) for minimum latency. Supported on specific instance types (c5n, p4d, hpc6a, etc.). Essential for tightly-coupled HPC workloads (computational fluid dynamics, weather modeling, molecular dynamics).", domain:3},
  {q:"A company has a Lambda function that needs to process an S3 object that is 6GB (lambda can only hold 10GB in /tmp). The function needs to process the file in chunks without downloading it entirely. What S3 feature helps?", opts:["S3 Select — query the file using SQL-like expressions to extract only needed rows/columns without downloading the entire object","S3 Byte-Range Fetches (GET with Range header) — download specific byte ranges of the object, process chunks sequentially","Both work for different use cases — Select for structured data (CSV/JSON/Parquet), Byte-Range for arbitrary binary data","Multipart download (same as full download, just in parts)"], correct:2, exp:"Two approaches: (1) S3 Select: for structured files (CSV, JSON, Parquet), send a SQL expression to S3, receive ONLY the matching rows/columns. S3 does the filtering — transfers dramatically less data. (2) Byte-Range Fetches: for any file, request specific byte ranges (e.g., bytes 0-1000000). Process chunks sequentially without loading the full file into memory. Choose based on data format and processing needs. Both avoid downloading 6GB to Lambda.", domain:3},
  {q:"A company uses AWS Organizations with multiple OUs (Development, Staging, Production). They want to allow developers to use ANY AWS service in Development OU but restrict Production OU to ONLY approved services. How are SCPs used here?", opts:["Attach a permissive SCP to Development OU and a restrictive SCP (allowlist) to Production OU","SCPs can only deny, not allow — you need separate IAM policies per account","Attach the same SCP to all OUs","Remove SCPs from Development OU (not possible — FullAWSAccess SCP is default)"], correct:0, exp:"SCP strategy: (1) Development OU: attach only the default FullAWSAccess SCP (allows everything — developers have full freedom, constrained only by their IAM policies). (2) Production OU: attach an allowlist SCP listing ONLY approved services. Since SCPs are a ceiling (intersection of SCP + IAM = effective permissions), any service not in the Production SCP is implicitly denied. This is the standard OU-based governance pattern: loose in dev, strict in prod.", domain:1},
  {q:"A company uses RDS MySQL. They need to create a reporting replica for their analytics team, but the analytics queries are so heavy that replication lag becomes a problem. What can they do?", opts:["Increase Read Replica instance size (helps but doesn't eliminate lag during heavy queries)","Create the Read Replica as an Aurora MySQL replica — Aurora's storage architecture handles read scaling better, or use RDS export to S3 + Athena for analytics","Set up a separate RDS instance with periodic data refresh from backups (no replication lag but stale data)","Both B and C are viable — choose based on freshness requirements"], correct:3, exp:"Two strategies based on data freshness needs: (1) Aurora Read Replica: Aurora's storage decouples compute from storage, reducing replication lag. Multiple Aurora replicas share the same storage volume (lag typically <100ms). (2) Periodic refresh: restore from automated backups to a separate analytics instance (data is as fresh as the last backup — max several hours old). If near-real-time isn't required for analytics, option 2 eliminates lag entirely. Choose based on freshness vs cost tradeoff.", domain:3},
  {q:"A company uses AWS Amplify for their web application frontend. They want to add serverless backend APIs quickly without managing infrastructure. What does Amplify provide for backend?", opts:["Only frontend hosting (no backend support)","Amplify provides full-stack capabilities: backend APIs (GraphQL via AppSync or REST via API Gateway + Lambda), authentication (Cognito), storage (S3), database (DynamoDB), and functions (Lambda) — all configurable via CLI/Studio","Amplify only works with Elastic Beanstalk backends","Amplify requires manual API Gateway configuration"], correct:1, exp:"AWS Amplify is a full-stack development platform: Frontend (hosting, CI/CD, previews) + Backend (APIs, auth, storage, functions). The Amplify CLI/Studio generates CloudFormation templates for: AppSync GraphQL API or API Gateway REST API, Cognito User Pools for auth, S3 for file storage, DynamoDB for data, and Lambda functions. Designed for frontend developers who want to add cloud backends without deep AWS expertise.", domain:2},
  {q:"A company processes messages from SQS. Some messages consistently fail (bad data, unprocessable). After the maximum receive count, they go to the Dead Letter Queue (DLQ). The operations team wants to be alerted when messages arrive in the DLQ. What is the simplest approach?", opts:["Poll the DLQ every minute with a Lambda function","CloudWatch Alarm on the DLQ's ApproximateNumberOfMessagesVisible metric > 0 → SNS notification","CloudTrail event for SendMessage to DLQ","S3 event notification (DLQ is not S3)"], correct:1, exp:"SQS publishes metrics to CloudWatch, including ApproximateNumberOfMessagesVisible per queue. Create a CloudWatch Alarm: when DLQ message count > 0 for 1+ period → trigger SNS notification (email, PagerDuty, etc.). This is the simplest, fully managed approach — no Lambda, no polling, just a metric alarm. The alarm triggers as soon as a message lands in the DLQ. Extend: use EventBridge for SQS (newer) or Lambda to process DLQ messages for automatic retry/investigation.", domain:2},
  {q:"A company uses Auto Scaling with an ALB. They want new instances to receive traffic ONLY after their application has fully started (health check passes). New instances take 3 minutes to start. What configurations are needed?", opts:["ALB health check with 180-second interval (too slow for detection)","ALB health check path pointing to application readiness endpoint + ALB health check grace period of 180 seconds on the Auto Scaling group (ASG won't terminate unhealthy instances during this grace period)","No configuration needed — ALB automatically waits","Use a lifecycle hook to delay registration with the target group"], correct:1, exp:"Two configurations: (1) ALB health check: check a /health endpoint that returns 200 only when the app is ready (not just the OS is up). ALB won't send traffic to targets that haven't passed health checks. (2) ASG health check grace period: gives new instances time to start before ASG evaluates their health. Without this, ASG might terminate a still-starting instance for being 'unhealthy'. Together: ALB holds traffic until ready, ASG doesn't kill instances during startup.", domain:2},
  {q:"A company uses Amazon EKS and wants to store Kubernetes secrets (API keys, passwords) securely. By default, EKS stores secrets as base64-encoded text in etcd (not encrypted). What is the recommended secure approach?", opts:["Use Kubernetes native secrets with RBAC (still base64, not encrypted)","Enable EKS envelope encryption using AWS KMS — secrets are encrypted at rest in etcd using a KMS CMK. Optionally, use AWS Secrets Manager CSI Driver to inject secrets from Secrets Manager directly into pods.","Store secrets in ConfigMaps (even less secure)","Encrypt secrets in the application code"], correct:1, exp:"EKS envelope encryption: secrets are encrypted in etcd using a KMS key (DEK encrypted by CMK). Enable during or after cluster creation. For additional security: AWS Secrets Manager CSI Driver mounts Secrets Manager secrets as files in pods — secrets never stored in etcd at all. This provides: encryption at rest (KMS), centralized management (Secrets Manager), automatic rotation, and audit trail (CloudTrail). The CSI Driver is the most secure approach for EKS secrets.", domain:1},
  {q:"A company runs a real-time leaderboard for a mobile game. The leaderboard shows the top 100 players and a user's rank among millions of players. They need sub-millisecond response time. Which data store is BEST?", opts:["DynamoDB (can't efficiently calculate rank among millions of items)","ElastiCache Redis with Sorted Sets — ZRANGEBYRANK for top 100 (O(log(N)+M)) and ZRANK for user's position (O(log(N))). Sub-millisecond with millions of members.","RDS with ORDER BY score LIMIT 100 (too slow for millions of rows)","DynamoDB with GSI sorted by score (scanning for rank is expensive)"], correct:1, exp:"Redis Sorted Sets are purpose-built for leaderboards: ZADD adds/updates scores, ZRANGE returns top N (top 100), ZRANK returns a member's rank, ZSCORE returns a member's score. All operations are O(log(N)) — sub-millisecond even with millions of members. No need to scan or sort — Redis maintains the sorted order internally using a skip list. DynamoDB and RDS can't efficiently calculate rank (position) among millions of items. Redis leaderboard is a textbook use case.", domain:3},
  {q:"A company uses AWS DataSync to migrate file data from on-premises NFS to EFS. They have 10TB of data across 50 million small files. DataSync transfer is slower than expected. What can they do to improve performance?", opts:["Transfer fewer files at a time","Increase the DataSync task's bandwidth limit and configure multiple DataSync agents. For millions of small files, the bottleneck is often metadata operations — DataSync parallelizes these with multiple agents.","Use larger files instead","Switch to S3 instead of EFS"], correct:1, exp:"DataSync with small files: the bottleneck is metadata (open/stat/close per file), not bandwidth. Solutions: (1) Deploy multiple DataSync agents to parallelize metadata operations, (2) Remove bandwidth throttling limits, (3) Ensure the DataSync agent is on a high-network instance or appliance. DataSync handles 50M files but multiple agents significantly improve small-file transfer speed. Also: task settings can configure concurrent file transfers. DataSync is still faster than manual rsync/copy approaches.", domain:2},
  {q:"A company uses AWS CloudFormation and needs to reference a value from another stack (e.g., Stack A creates a VPC and Stack B needs the VPC ID). How do they share values between stacks?", opts:["Hardcode the VPC ID in Stack B","CloudFormation Cross-Stack References: Stack A exports the VPC ID (Outputs section with Export), Stack B imports it using Fn::ImportValue. Values are shared via the export name.","Use SSM Parameter Store to pass values between stacks","Both B and C work — Exports are native CloudFormation, SSM is more flexible (can be used across regions/accounts)"], correct:3, exp:"Both approaches work: (1) CloudFormation Exports/Imports: Stack A declares Output with Export:Name, Stack B uses Fn::ImportValue('name'). Native, simple, but limited to same account/region and creates a dependency (can't delete Stack A while Stack B references it). (2) SSM Parameter Store: Stack A writes VPC ID to /vpc/prod/id, Stack B reads it via dynamic reference. More flexible: works cross-region, cross-account, and doesn't create hard stack dependencies.", domain:2},
  {q:"A company uses S3 for backup storage. They want the CHEAPEST possible storage for backups that are accessed once a year during compliance audits (access needed within 12 hours). What storage class?", opts:["S3 Standard (most expensive)","S3 Glacier Flexible Retrieval (standard retrieval: 3-5 hours, $0.0036/GB — low cost)","S3 Glacier Deep Archive (standard retrieval: 12 hours, $0.00099/GB — cheapest possible storage class)","S3 One Zone-IA ($0.01/GB — cheaper than Standard but expensive vs Glacier)"], correct:2, exp:"S3 Glacier Deep Archive: $0.00099/GB/month — the cheapest AWS storage class. Standard retrieval: within 12 hours (meets the requirement). For annual compliance access where 12-hour retrieval is acceptable, Deep Archive is optimal. Glacier Flexible Retrieval ($0.0036/GB) is 3.6x more expensive. Deep Archive is designed for: long-term compliance archives, rarely-accessed backups, and data that doesn't need immediate access. Minimum storage duration: 180 days.", domain:4},
  {q:"A company uses AWS Glue for data catalog. They want their data lake to be accessible from both Athena (serverless SQL) and Redshift Spectrum (Redshift's S3 query engine). Do they need separate catalogs?", opts:["Yes — each service has its own metadata store","No — both Athena and Redshift Spectrum use the AWS Glue Data Catalog as their shared metastore. Define tables once, query from either service.","Only Athena uses Glue Data Catalog","Redshift has its own incompatible schema definition"], correct:1, exp:"AWS Glue Data Catalog is the shared metadata repository for the AWS analytics ecosystem. Create a table definition once (via Crawler or manual) → query from: Athena, Redshift Spectrum, EMR (Spark/Hive/Presto), and Glue ETL jobs. All reference the same catalog — no duplication. This is a key architectural benefit: single catalog, multiple query engines. Choose the engine based on the query pattern (ad-hoc→Athena, join with Redshift data→Spectrum, ML→EMR).", domain:3},
  {q:"A company uses Amazon EventBridge to route events. They have an event rule that matches all EC2 state changes. They want to route 'running' state changes to Lambda A and 'terminated' state changes to Lambda B using the SAME rule. Can one rule have conditional routing?", opts:["Yes — a single rule can have input transformers that route to different targets","No — each EventBridge rule can have multiple targets but ALL targets receive ALL matched events. For different routing, create TWO separate rules with different event patterns (one matching 'running', one matching 'terminated').","Use content-based filtering on targets","EventBridge doesn't support EC2 state changes"], correct:1, exp:"EventBridge rule: ONE event pattern → multiple targets (all receive the same matched events). There's no per-target filtering within a rule. For conditional routing: create separate rules. Rule 1: pattern matches state='running' → target Lambda A. Rule 2: pattern matches state='terminated' → target Lambda B. Each rule independently matches its specific pattern and routes to its target. This is a common architecture question about EventBridge routing capabilities.", domain:2},
  {q:"A company uses S3 Intelligent-Tiering for their data. They notice monitoring and automation charges on their bill. Objects smaller than 128KB are being charged monitoring fees but never transition between tiers. How can they optimize?", opts:["Disable Intelligent-Tiering entirely","S3 Intelligent-Tiering has NO minimum size requirement — BUT objects smaller than 128KB are never transitioned to IA tiers (always stay in Frequent Access tier). The $0.0025/1000 objects monitoring fee still applies. For buckets with millions of tiny objects, use a lifecycle rule to put small objects in Standard or One Zone-IA instead.","Objects under 128KB aren't charged monitoring fees","Increase object sizes by combining small files"], correct:1, exp:"Intelligent-Tiering charges a monthly monitoring fee per object ($0.0025 per 1,000 objects) regardless of size. Objects <128KB are NEVER moved to IA tiers — they always incur the Frequent Access rate PLUS the monitoring fee. For millions of small objects, this monitoring cost adds up without any tiering benefit. Optimization: use lifecycle rules to place small objects directly in the appropriate fixed storage class (Standard-IA, One Zone-IA), avoiding unnecessary monitoring charges.", domain:4},
  {q:"A company uses ECS with EC2 launch type. They want to ensure no two tasks of the same service run on the same container instance (spread tasks across instances for HA). What configuration achieves this?", opts:["Task placement constraint: distinctInstance — ensures each task is placed on a different container instance","Task placement strategy: spread by attribute:ecs.availability-zone (spreads across AZs but multiple tasks can land on same instance)","Increase the number of instances to exceed the task count","Service auto scaling target tracking"], correct:0, exp:"Task placement constraints control WHERE tasks can be placed. 'distinctInstance' constraint ensures that no two tasks from the same service are on the same EC2 instance — maximum fault isolation. If an instance fails, only one task is affected. Different from 'spread' strategy which distributes evenly (best-effort) but doesn't guarantee distinct instances. Combined ideal: constraint=distinctInstance + strategy=spread by AZ → guaranteed one task per instance, spread across AZs.", domain:2},
  {q:"A company is evaluating whether to use AWS Outposts, AWS Local Zones, or AWS Wavelength for low-latency workloads. What is the PRIMARY differentiator between them?", opts:["They're all the same service in different regions","Outposts: AWS hardware IN YOUR data center (on-premises). Local Zones: AWS infrastructure in metro areas (near large cities, not in standard AZs). Wavelength: AWS compute embedded IN telecom provider 5G networks (ultra-low latency for mobile/IoT). Each serves a different proximity need.","Local Zones are cheaper than Outposts","Wavelength only works with Lambda"], correct:1, exp:"Proximity gradient: (1) Outposts — AWS in YOUR building. For: data residency, local data processing, migration stepping stone. You own the rack. (2) Local Zones — AWS in cities without full regions (e.g., Los Angeles, Denver). For: <10ms latency to end users in that metro. (3) Wavelength — AWS inside telecom 5G base stations. For: single-digit ms latency for mobile/IoT at the network edge. All provide AWS APIs and services, differentiated by WHERE the compute physically lives.", domain:3},
  {q:"A company runs a stateless API on ECS Fargate behind an ALB. They want to reduce costs. The traffic pattern is predictable: high during business hours (8AM-6PM), near zero at night. What scheduling-based optimization can they implement?", opts:["Manually adjust desired count twice daily","ECS Scheduled Scaling — define scheduled actions that set min/max/desired capacity based on time. Scale up at 8AM, scale down at 6PM. Combined with target tracking for within-day variations.","Turn off the entire service at night (might miss occasional requests)","Use Lambda instead of ECS (may not be suitable for all APIs)"], correct:1, exp:"ECS Application Auto Scaling supports Scheduled Scaling: define cron-like schedules that change capacity (min, max, desired). Example: Scale to min=5 at 07:50 (pre-warm before 8AM traffic), Scale to min=1 at 18:10 (after business hours). Combine with Target Tracking scaling (e.g., CPU 60%) for reactive scaling during the day. At night, min=1 handles occasional requests at minimal cost. Scheduled + target tracking is the optimal hybrid approach.", domain:4},
  {q:"A company migrates their application to AWS. The application uses multicast networking (one-to-many communication). Standard AWS VPCs do not support multicast. What AWS feature enables multicast?", opts:["VPCs fully support multicast natively","Transit Gateway Multicast — Transit Gateway supports multicast domain creation. Register sources and group members. Multicast traffic is distributed through the TGW to all members in the multicast group.","Use broadcast instead (also not supported in VPCs)","Implement application-level multicast using SNS (not network-level)"], correct:1, exp:"AWS Transit Gateway Multicast: create a multicast domain on a TGW, register multicast group members (ENIs in VPCs attached to the TGW), and designate sources. Multicast packets from sources are replicated by TGW and delivered to all group members. Supports: IGMP for group management, static group membership, and multiple multicast domains. This is the ONLY way to do network-level multicast in AWS (VPCs don't support it natively). Required for some legacy/specialized applications.", domain:1},
]
