export const questionBank6 = [
  // Mixed domains - Exam simulation style (scenarios crossing multiple domains)
  {q:"A company is designing a new microservices application. Service A publishes events that need to be consumed by Services B, C, and D. Each consumer must process every event independently. If one consumer is down, it should not affect others, and messages must not be lost. Which architecture pattern is correct?", opts:["Service A -> SQS -> Services B, C, D (all polling the same queue)","Service A -> SNS topic -> 3 SQS queues (one per consumer service)","Service A -> EventBridge event bus -> 3 Lambda targets","Service A -> Kinesis Data Stream -> 3 consumer applications"], correct:1, exp:"SNS + SQS fanout pattern delivers every message to all subscribers independently. Each service has its own SQS queue (messages persist even if the consumer is down). One consumer failure doesn't affect others. If all services polled one SQS queue, each message would be processed by only ONE consumer (competing consumers pattern). EventBridge works but SQS provides better buffering for downed consumers.", domain:2},
  {q:"A healthcare application needs to process patient records. Due to HIPAA, all data must be encrypted in transit and at rest, access must be logged, and PHI must never leave the AWS account boundary. The application uses S3, RDS, and Lambda. Which combination ensures compliance?", opts:["Enable SSE-S3, RDS encryption, enforce HTTPS bucket policy, enable CloudTrail, and use VPC endpoints for all services","Use SSE-KMS (customer-managed CMK) for S3, RDS encryption with KMS, require ssl in RDS parameter group, VPC Lambda, VPC endpoints for S3, CloudTrail with data events, and deny all cross-account access via bucket policy","Enable default encryption on S3 and RDS, use AWS Shield for protection","Use client-side encryption for all data and store keys in Secrets Manager"], correct:1, exp:"HIPAA compliance requires comprehensive controls: CMK gives key management control (audit key usage), RDS ssl parameter forces encrypted connections, VPC Lambda prevents data from traversing public internet, VPC endpoints keep S3 traffic private, CloudTrail data events log all object access, and explicit bucket policy denying cross-account access prevents data leakage. SSE-S3 keys are AWS-managed (less audit control).", domain:1},
  {q:"A startup runs their application on a single t3.large instance with a local PostgreSQL database. They expect 100x growth in 6 months. The application is a Node.js REST API with a React frontend. What migration path provides scalability while minimizing re-architecture effort?", opts:["Move to ECS Fargate (containerize) + Aurora PostgreSQL Serverless v2 + CloudFront for frontend","Re-architect to Lambda + DynamoDB + S3/CloudFront for frontend","Move to a larger EC2 instance (c5.4xlarge) with RDS PostgreSQL","Deploy on Elastic Beanstalk with Multi-AZ RDS"], correct:0, exp:"Containerizing a Node.js app is minimal effort (add Dockerfile). Fargate auto-scales without managing servers. Aurora Serverless v2 scales PostgreSQL automatically (no application changes needed for the database). CloudFront serves the React SPA from S3 globally. This path: minimal code changes, maximum scalability. Lambda + DynamoDB requires significant re-architecture. Larger EC2 doesn't scale. Beanstalk is a middle ground but less scalable than Fargate.", domain:3},
  {q:"A company has a monolithic application that uses a single RDS PostgreSQL database. The database is hitting connection limits (max_connections reached) with 5000 concurrent users. The application creates a new database connection for each request. What is the FASTEST fix?", opts:["Increase the RDS instance size to get more max_connections","Deploy RDS Proxy to manage connection pooling between the application and database","Implement connection pooling in the application code (e.g., pg-pool)","Switch to Aurora Serverless which handles more connections"], correct:1, exp:"RDS Proxy manages a connection pool, multiplexing thousands of application connections into a smaller number of database connections. It's deployed without code changes (just change the connection string endpoint). It reduces connections from 5000 to typically <100 actual database connections. Application-level pooling requires code changes. Larger instances help but don't solve the fundamental problem of 5000 direct connections. RDS Proxy is the fastest operational fix.", domain:3},
  {q:"A company migrates their application to AWS. The application uses a message queue for asynchronous processing. Messages must be processed in order within each customer's context, but different customers' messages can be processed in parallel. The system handles 10,000 messages per second. Which service and configuration achieves this?", opts:["SQS FIFO queue with MessageGroupId set to customer_id","SQS Standard queue with application-level ordering","Kinesis Data Streams with customer_id as partition key","Amazon MQ (RabbitMQ) with per-customer queues"], correct:2, exp:"Kinesis Data Streams with customer_id as partition key ensures all messages for the same customer go to the same shard (ordered within shard). Different customers map to different shards (parallel processing). Kinesis handles 10,000+ msg/sec easily by adding shards. SQS FIFO is limited to 3,000 msg/sec per queue (300 per message group per second without high throughput). Amazon MQ creating 10,000 customer queues is impractical.", domain:3},
  {q:"A company needs to deploy the same application in 3 AWS regions simultaneously. Infrastructure changes must be applied to all regions atomically — if deployment fails in any region, all regions should remain on the previous version. Which tool and strategy achieves this?", opts:["CloudFormation StackSets with SEQUENTIAL deployment and failure tolerance of 0","CodePipeline with parallel deploy stages to each region and a manual approval gate","CloudFormation StackSets with a service-managed deployment targeting all regions, with max concurrent deployments set to 100% and failure tolerance of 0%","Step Functions orchestrating CloudFormation deployments with rollback logic"], correct:2, exp:"CloudFormation StackSets with 100% max concurrent (parallel deployment to all regions) and 0% failure tolerance means: if ANY region fails, the entire operation fails and all regions get rolled back to the previous state. This provides atomic multi-region deployment. Sequential deployment would leave some regions updated even if a later region fails. CodePipeline manual approval isn't atomic. Step Functions require custom rollback logic.", domain:2},
  {q:"An e-commerce company notices their RDS MySQL database has 80% read traffic and 20% write traffic. During sales events, read latency increases from 5ms to 500ms. What combination provides the MOST improvement?", opts:["Scale up the RDS instance to a larger type","Add 3 Aurora Read Replicas and direct read traffic to the reader endpoint","Add ElastiCache Redis in front of the database for frequently accessed product and session data, plus Aurora Read Replicas for complex queries","Enable RDS Multi-AZ (the standby can serve reads)"], correct:2, exp:"ElastiCache absorbs the hot-path reads (product pages, sessions) with sub-millisecond latency. Read Replicas handle complex queries that can't be cached (search, filtering). Together they reduce primary database load by 90%+. Scaling up has diminishing returns. RDS Multi-AZ standby does NOT serve reads (it's for failover only). Read Replicas alone can't handle the hot cache patterns efficiently.", domain:3},
  {q:"A company needs to collect, transform, and load (ETL) streaming data from 10,000 IoT sensors into S3 in Parquet format, partitioned by date and sensor_type. Each sensor sends data every second. What is the simplest architecture?", opts:["Kinesis Data Streams -> Lambda -> S3 (write Parquet files)","IoT Core -> Kinesis Data Firehose with data transformation (Lambda) and Parquet conversion, configured with dynamic partitioning by sensor_type and date prefix","IoT Core -> Lambda -> Glue Streaming ETL -> S3","API Gateway -> Kinesis Data Firehose -> S3 with Glue Crawler for Parquet conversion"], correct:1, exp:"Kinesis Data Firehose natively supports: (1) data transformation via Lambda, (2) Parquet/ORC conversion using a Glue table schema, (3) dynamic partitioning (custom prefixes based on data content like sensor_type and date). All in one managed service with automatic batching, compression, and retry. This is the simplest architecture — single service handles transform + format + partition. Glue Streaming ETL is more complex. Lambda writing Parquet requires managing file sizes.", domain:3},
  {q:"A global company has applications in us-east-1 and eu-west-1. Each region has its own VPC. Applications in both regions need to communicate with each other over private IP addresses. They also have an on-premises data center connected via Direct Connect to us-east-1. All three networks must communicate. What is the MOST scalable architecture?", opts:["VPC Peering between the two VPCs + a VPN from eu-west-1 to on-premises","AWS Transit Gateway in us-east-1 peered with Transit Gateway in eu-west-1 (inter-region peering) + Direct Connect Gateway attached to both Transit Gateways","Direct Connect to both regions separately with VPC peering between regions","AWS Cloud WAN with a global network connecting both regions and on-premises via Direct Connect"], correct:1, exp:"Transit Gateway in each region with inter-region peering provides scalable hub-and-spoke networking. Direct Connect Gateway connects on-premises to both Transit Gateways through a single DX connection. This creates a full mesh: us-east-1 VPC <-> eu-west-1 VPC <-> on-premises. Scales to hundreds of VPCs without mesh peering. VPC peering doesn't transit (on-premises can't reach eu-west-1 through us-east-1's peering). Cloud WAN works but is more expensive.", domain:2},
  {q:"A company uses S3 for storing application logs. They have 500TB of logs across multiple buckets. Compliance requires searching specific log entries within seconds. They query logs 2-3 times per day. Currently using Athena but queries take 10 minutes on 500TB. How should they optimize?", opts:["Move all logs to Elasticsearch/OpenSearch for instant search","Partition logs by date, convert to Parquet, and use S3 Select for targeted queries within specific partitions","Partition logs by date/hour/service, convert to Parquet with columnar compression, create Athena workgroups with result caching, and use partition projection to eliminate Glue Crawler overhead","Move logs to Redshift for faster query execution"], correct:2, exp:"Partitioning by date/hour/service dramatically reduces data scanned (from 500TB to maybe 1-5GB for a specific query). Parquet conversion reduces scan by 90%+ (columnar). Partition projection eliminates Glue Crawler latency (instant partition discovery). Result caching eliminates cost/time for repeated queries. Together: 10-minute queries drop to seconds at fraction of cost. OpenSearch for 500TB is extremely expensive. S3 Select works on single objects, not across datasets.", domain:3},
  {q:"A company has a VPC with a CIDR block of 10.0.0.0/16 (65,536 IPs). They've used all available IP addresses and need more. They cannot change the existing VPC CIDR or migrate workloads. What should they do?", opts:["Create a new VPC and use VPC peering","Add a secondary CIDR block to the existing VPC (e.g., 10.1.0.0/16) and create new subnets in the additional range","Use IPv6 addresses for new workloads","Launch instances with multiple ENIs to share IP addresses"], correct:1, exp:"AWS allows adding secondary CIDR blocks to an existing VPC (up to 5 IPv4 CIDRs). New subnets can be created in the additional range. Existing workloads are unaffected. This is the simplest solution that doesn't require migration. VPC peering creates a separate network (not the same VPC). IPv6 requires application changes. Multiple ENIs don't create new IPs.", domain:2},
  {q:"A company has an Auto Scaling Group with mixed instance types (m5.large, m5.xlarge, c5.large). They want to ensure that when scaling in, the ASG terminates the instances that will save the most money. What termination policy achieves this?", opts:["Default termination policy","Closest to next billing hour termination policy","AllocationStrategy termination policy that removes instances to balance across instance types","Custom Lambda function as a termination policy hook"], correct:0, exp:"The default ASG termination policy first selects the AZ with the most instances (for balance), then within that AZ, selects the instance with the oldest launch template/configuration, then the one closest to the next billing hour. For cost optimization, you can override this with 'closest to billing hour' to avoid paying for unused time, but with per-second billing (current), the default policy with allocation strategy rebalancing is typically sufficient.", domain:4},
  {q:"A company runs a data analytics cluster that processes jobs submitted by users. Average CPU utilization is 30% across 20 instances. However, individual jobs are bursty and occasionally need significant CPU. Which instance type strategy optimizes cost for this pattern?", opts:["Use burstable instances (t3) that can burst above baseline when needed","Use Compute-optimized (c5) instances sized for peak per-job CPU","Use a mix of smaller On-Demand instances for baseline with Spot Instances that auto-scale for bursts","Right-size to fewer, larger instances to improve utilization percentage"], correct:0, exp:"Burstable T3 instances are designed for variable CPU workloads (low average, occasional bursts). At 30% average utilization on t3.2xlarge (40% baseline), most time is below baseline (earning credits). During job bursts, credits are consumed for above-baseline CPU. T3 Unlimited mode prevents throttling if credits are exhausted. This matches the usage pattern perfectly. Spot works for fault-tolerant burst but adds complexity.", domain:4},
  {q:"A company has an S3 bucket receiving 10,000 PUT requests per second (each object ~100KB). Their monthly S3 bill is $15,000 split between storage ($5K) and requests ($10K). How can they reduce the request costs?", opts:["Batch small objects into larger objects before uploading (aggregate 100 objects into 1)","Use S3 Express One Zone for high request rates (lower per-request pricing)","Reduce the number of PUTs by buffering in Kinesis Firehose before delivery to S3 (writes in larger batches)","Use S3 Intelligent-Tiering to optimize costs"], correct:2, exp:"Kinesis Firehose buffers incoming records and delivers to S3 in configurable batch sizes (by time or size). Instead of 10,000 individual PUT requests, Firehose might make 100 larger PUT requests (aggregating 100 objects per file). PUT request costs drop from $10K to ~$100/month. The tradeoff is data arrives in batches (configurable 60-900 seconds). Intelligent-Tiering optimizes storage cost, not request cost. Application-level batching requires code changes.", domain:4},
  {q:"A company wants to monitor their AWS spending in real-time and get alerted BEFORE exceeding their monthly budget. They also want to automatically stop non-production instances if spending reaches 80% of budget. What combination of services achieves this?", opts:["AWS Cost Explorer with daily email reports and manual instance management","AWS Budgets with email alerts at 80% threshold + AWS Budgets Actions to stop EC2 instances in non-production accounts","CloudWatch billing alarms + Lambda function triggered by the alarm to stop instances","AWS Trusted Advisor cost optimization checks with SNS notifications"], correct:1, exp:"AWS Budgets supports custom budget thresholds with automated actions. Budget Actions can automatically stop EC2 instances, apply SCPs, or trigger custom actions when spending reaches defined thresholds. This provides both the alert (email/SNS at 80%) and the automated remediation (stop non-prod instances) in one service. CloudWatch billing alarms work for alerts but require custom Lambda for actions. Cost Explorer is for analysis, not real-time alerting.", domain:4},
  {q:"A company has 200 AWS Lambda functions. Their total Lambda bill is $5,000/month. Using AWS Lambda Power Tuning, they discover that 80% of functions are over-provisioned on memory (using 512MB but needing only 256MB). The remaining 20% are under-provisioned (using 512MB but performing better at 1024MB due to proportional CPU allocation). What is the expected impact of right-sizing?", opts:["~30% cost reduction (over-provisioned savings minus under-provisioned increase)","~20% cost reduction (halving memory on 80% saves 40%, doubling on 20% adds 20% to that portion)","~35% cost reduction (over-provisioned save 50% on memory cost, under-provisioned run faster so duration decreases despite higher memory, net positive)","Cannot determine without knowing duration impact"], correct:2, exp:"Over-provisioned (80%): halving memory = 50% cost reduction on those functions (memory cost is proportional). Under-provisioned (20%): doubling memory gives 2x CPU, which often reduces duration by 40-50% for CPU-bound functions. So cost might only increase 10-20% despite 2x memory (2x rate * 0.5x duration = same cost or less). Net effect: significant reduction from the 80% that are over-provisioned, neutral/slight improvement from the 20%. Overall ~35% savings.", domain:4},
  {q:"A company operates in AWS GovCloud for compliance reasons. They have workloads that could run in standard commercial regions but are deployed in GovCloud due to organizational policy (not regulatory requirement). GovCloud pricing is 20-30% higher. What should the architect recommend?", opts:["Move all non-regulated workloads to commercial regions and use VPC peering to connect with GovCloud resources","Keep everything in GovCloud for simplicity","Use GovCloud for regulated data only and commercial regions for non-regulated workloads, connected via Transit Gateway inter-region peering","Negotiate a discount with AWS for GovCloud pricing"], correct:2, exp:"Only workloads with actual regulatory requirements (FedRAMP, ITAR, etc.) should remain in GovCloud. Non-regulated workloads in commercial regions save 20-30%. Transit Gateway inter-region peering connects the environments securely over AWS backbone. This optimizes cost without violating compliance. VPC peering between GovCloud and commercial regions is not supported (different partitions). Moving everything out may violate actual regulated workloads.", domain:4},
  {q:"A company runs Aurora MySQL with r5.2xlarge instances (8 vCPU, 64GB RAM). Performance Insights shows the database uses only 2 vCPUs and 16GB RAM on average, with occasional spikes to 6 vCPU during monthly reporting. What is the MOST cost-effective right-sizing approach?", opts:["Switch to r5.large (2 vCPU, 16GB) — reports will be slower but acceptable","Switch to Aurora Serverless v2 with minimum 2 ACUs and maximum 32 ACUs (auto-scales during reporting)","Keep current instance but purchase a Reserved Instance for the discount","Switch to r5.xlarge (4 vCPU, 32GB) as a compromise"], correct:1, exp:"Aurora Serverless v2 scales compute between min and max ACUs within seconds. During normal operations, it uses 2 ACUs (matching 2 vCPU need). During monthly reporting, it scales to 12+ ACUs automatically, then scales back. You pay only for what you use. This is cheaper than maintaining an r5.2xlarge 24/7 for occasional reporting spikes. Fixed r5.large would suffer during reports. RI still pays for unused capacity.", domain:4},
]
