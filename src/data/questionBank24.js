export const questionBank24 = [
  {q:"A company uses Amazon ECS with an ALB. They want to deploy a new version of their service with the ability to instantly rollback if something goes wrong. Blue/Green deployment with CodeDeploy provides this. During the deployment, both old (blue) and new (green) task sets exist. How does traffic switching work?", opts:["DNS change (too slow for instant)","ALB listener rules switch traffic from blue target group to green target group. CodeDeploy manages the switch: test traffic via a test listener first, then production traffic shift (all-at-once, canary, or linear). Rollback: instantly switch ALB back to the blue target group (sub-second). No DNS changes needed.","NLB port mapping","New ALB for each deployment (expensive)"], correct:1, exp:"ECS Blue/Green (CodeDeploy): (1) Original tasks in Target Group Blue (active). (2) New tasks in Target Group Green (launched). (3) Test Listener (port 8443): CodeDeploy routes test traffic to Green for validation. (4) After tests pass: Production Listener shifts traffic to Green (configurable: AllAtOnce, Canary, Linear). (5) Rollback: CodeDeploy switches Production Listener back to Blue — instant (ALB rule change). (6) After stabilization: Blue tasks terminated. The ALB handles traffic switching without DNS — sub-second cutover and rollback.", domain:2},
  {q:"A company uses CloudFront. They want to ensure their S3 origin objects are NEVER served stale (always the latest version). But they also want caching for performance. What cache invalidation strategy works?", opts:["Disable caching entirely (defeats CloudFront's purpose)","Use versioned URLs (file.v2.js, image-abc123.png) — each new version gets a new URL that isn't in cache, forcing CloudFront to fetch from origin. No invalidation needed. Old URLs remain cached until TTL expires (harmless — nothing references them). Best practice for static assets.","Invalidate on every update (expensive — $0.005 per path per invalidation)","Set TTL to 0 (every request goes to origin — no caching benefit)"], correct:1, exp:"Cache busting with versioned URLs: instead of /style.css (same URL forever), use /style.v3.css or /style.abc123.css (content hash in filename). New content = new URL = guaranteed cache miss = fresh content served. Old URL stays cached (no one uses it). Benefits: zero invalidation cost, instant deployment (no propagation delay), and perfect caching (long TTL on each version). Implementation: build tools (webpack, vite) auto-generate content hashes. For dynamic content: shorter TTLs or invalidation API.", domain:3},
  {q:"A company uses Amazon Managed Blockchain. What does this service provide?", opts:["Cryptocurrency mining","Managed Blockchain creates and manages scalable blockchain networks using Hyperledger Fabric or Ethereum. Use cases: supply chain tracking, financial settlements, decentralized applications. AWS manages: network infrastructure, certificate authority, peer nodes, ordering service. You manage: chaincode (smart contracts) and application logic.","Database management","Container orchestration"], correct:1, exp:"Amazon Managed Blockchain: (1) Hyperledger Fabric: private, permissioned blockchain for enterprises (supply chain, financial reconciliation, healthcare records). AWS manages the infrastructure — you write chaincode (business logic). (2) Ethereum: public blockchain access for DApps and smart contracts. Use cases: immutable audit trail, multi-party workflows where trust is needed, digital asset management. Not a common SAA-C03 topic but tests understanding of AWS service breadth.", domain:3},
  {q:"A company uses Amazon S3 and wants to allow a specific third-party AWS account to upload objects to their bucket. The third-party must NOT be able to read existing objects or delete anything. What bucket policy implements this?", opts:["Give the third-party full S3 access","Bucket policy with: Principal: {AWS: 'arn:aws:iam::THIRD_PARTY_ACCOUNT:root'}, Action: ['s3:PutObject'], Resource: 'arn:aws:s3:::bucket/*'. This grants ONLY PutObject (upload) — no GetObject (read) or DeleteObject (delete). The third-party can only add, not view or remove existing data.","Share IAM credentials with the third-party (security violation)","Make the bucket public (too permissive)"], correct:1, exp:"Least privilege cross-account bucket policy: grant EXACTLY the needed action (s3:PutObject) to the specific account. The third-party can upload objects but: can't list bucket contents (no s3:ListBucket), can't read objects (no s3:GetObject), can't delete (no s3:DeleteObject). Consider also: (1) Require specific encryption (Condition: s3:x-amz-server-side-encryption), (2) Require specific prefix (Resource: bucket/uploads/third-party/*), (3) Enable bucket owner full control via object ownership settings.", domain:1},
  {q:"A company uses CloudWatch Container Insights for EKS. They notice a pod is being OOMKilled (Out of Memory). Container Insights shows the pod memory usage over time. The pod uses 1.5GB but the container memory limit is 1GB. What Kubernetes setting should be adjusted?", opts:["Increase the EC2 node size (node might have capacity, pod limit is the issue)","Increase the container's memory LIMIT in the pod spec (resources.limits.memory: 2Gi) to accommodate actual usage. Also adjust the REQUEST (resources.requests.memory) for scheduling. The OOMKill happens because the container exceeds its limit, regardless of node capacity.","Add more replicas (doesn't fix per-pod memory usage)","Use a different container runtime"], correct:1, exp:"Kubernetes resource management: (1) requests.memory: how much memory the scheduler guarantees (used for pod placement). (2) limits.memory: hard ceiling — container is OOMKilled if it exceeds this. If actual usage (1.5GB) > limit (1GB) → OOMKill. Fix: increase limits.memory to accommodate peak usage + buffer (e.g., 2Gi). Also adjust requests: set to typical usage (1.5Gi) for accurate scheduling. If the underlying cause is a memory leak: fix the application (limits.memory increase just delays the inevitable).", domain:3},
  {q:"A company uses Amazon Aurora PostgreSQL. They want to analyze query performance and identify slow queries without impacting production. What tools are available?", opts:["Only EXPLAIN ANALYZE (impacts production — runs the query)","Aurora Performance Insights + pg_stat_statements: Performance Insights shows top SQL by wait time (no production impact — reads from performance data). pg_stat_statements extension tracks cumulative query statistics (execution count, total time, rows returned). Both are read-only observability — they monitor without adding load.","Must run queries in a replica only","Query logging to CloudWatch Logs (adds I/O overhead)"], correct:1, exp:"Aurora PostgreSQL performance tools: (1) Performance Insights: visual dashboard showing database load, wait events, and top SQL statements in real-time. Zero production impact — reads from Aurora's performance metadata. (2) pg_stat_statements: extension tracking per-query statistics (calls, total_time, rows, shared_blks_hit). Query it: SELECT query, calls, mean_time FROM pg_stat_statements ORDER BY mean_time DESC. (3) Enhanced Monitoring: OS-level metrics. (4) Auto explain: logs execution plans for slow queries. All are read-only or low-overhead monitoring.", domain:3},
  {q:"A company uses S3 for a data lake. They want to enforce that all objects MUST be encrypted with SSE-KMS (not SSE-S3, not unencrypted). How does the bucket policy enforce this?", opts:["Default encryption setting is sufficient (it's a fallback, not enforcement)","Bucket policy with DENY on s3:PutObject with Condition: StringNotEquals s3:x-amz-server-side-encryption 'aws:kms'. This REJECTS any upload that doesn't specify SSE-KMS encryption. Combined with default encryption (applies KMS if nothing specified), ensures all objects are SSE-KMS encrypted.","KMS key policy handles this","IAM policy on all users"], correct:1, exp:"SSE-KMS enforcement: Deny statement with condition: if the request's x-amz-server-side-encryption header is NOT 'aws:kms', deny the PutObject. This catches: unencrypted uploads (no header), SSE-S3 uploads (header = AES256), and SSE-C uploads. ONLY SSE-KMS passes. Also add: default encryption = KMS key (catches uploads that forget the header entirely — applies KMS by default). Belt and suspenders: deny policy (strict enforcement) + default encryption (user-friendly fallback). Both together = guaranteed SSE-KMS on all objects.", domain:1},
  {q:"A company uses Amazon Aurora. A developer accidentally drops a critical table. They realize the mistake 5 minutes later. How can they recover the table?", opts:["Tables can't be recovered after DROP","Aurora Backtrack — rewinds the ENTIRE cluster to a point in time (within the configured backtrack window, up to 72 hours). Takes seconds — no restore from snapshot needed. Backtrack returns the entire database to the state at the specified timestamp (including the dropped table). Does NOT create a new instance.","PITR (Point-in-Time Restore) — creates a new cluster from backup (takes 10-20 minutes)","Check the recycling bin (Aurora doesn't have one)"], correct:1, exp:"Aurora Backtrack (MySQL-compatible only, not PostgreSQL): rewind the DB cluster to any point within the backtrack window. For a dropped table 5 minutes ago: backtrack to 6 minutes ago (before the DROP). The table reappears. Key differences from PITR: (1) Backtrack: same cluster, seconds to complete, all changes after the target time are lost. (2) PITR: creates a NEW cluster from backups, takes 10-30 minutes, original cluster unaffected. Backtrack is faster for recent mistakes. Configure: BacktrackWindow during cluster creation (can't add later).", domain:2},
  {q:"A company uses AWS Organizations. They want all CloudTrail logs from all member accounts to be delivered to a centralized S3 bucket in the security audit account. How is this set up?", opts:["Create a trail in each account individually (not centralized management)","Create an Organization Trail in the management account — automatically captures API events from ALL member accounts and delivers to a centralized S3 bucket. One trail covers the entire organization. Members can't disable or modify the organization trail.","Members must opt-in to share logs","CloudTrail doesn't support organization-level trails"], correct:1, exp:"CloudTrail Organization Trail: (1) Created in management account (or delegated admin). (2) Automatically applies to ALL current and future member accounts. (3) All logs delivered to one S3 bucket (in the audit account). (4) Member accounts can't modify or delete the trail (SCP enforced). (5) Bucket policy allows CloudTrail from all accounts to write. This ensures: comprehensive audit coverage, centralized analysis (Athena queries across all accounts), and tamper-proof logging (members can't interfere). The foundation of organizational security posture.", domain:1},
  {q:"A company uses AWS Global Accelerator with two endpoint groups (us-east-1 and eu-west-1). They want to control what percentage of traffic goes to each region (e.g., 80% us-east-1, 20% eu-west-1 regardless of client location). What Accelerator feature controls this?", opts:["Global Accelerator always routes to nearest region (can't override)","Traffic Dials on endpoint groups — set a percentage (0-100) per endpoint group that determines what proportion of traffic it receives. Set us-east-1 to 80 and eu-west-1 to 20 to control distribution. Useful for: gradual region migration, capacity-proportional routing, or testing.","Weighted routing in Route 53 (that's a different service)","Endpoint weights within a group (that's per-endpoint, not per-region)"], correct:1, exp:"Global Accelerator Traffic Dials: per-endpoint-group setting (0-100%) controlling the portion of traffic directed to that group. Default: 100% (each group gets traffic based on proximity). Setting us-east-1 dial to 80, eu-west-1 to 20: regardless of user location, roughly 80% of all traffic goes to us-east-1. Use cases: (1) Gradual region migration (slowly increase traffic to new region). (2) Capacity-proportional routing. (3) Canary testing a new regional deployment. Different from endpoint weights (which distribute within a single group).", domain:2},
  {q:"A company processes streaming data with Kinesis Data Streams. They want multiple independent applications to read the same stream simultaneously without interfering with each other. Is this possible?", opts:["Only one consumer per stream","Yes — Kinesis supports multiple independent consumers per stream. Each consumer (application) maintains its own checkpoint position (using KCL + DynamoDB table). Enhanced Fan-Out provides dedicated 2MB/sec per shard per consumer (instead of sharing 2MB/sec among all consumers on shared throughput).","Consumers must coordinate read positions","Only 2 consumers maximum per stream"], correct:1, exp:"Kinesis multi-consumer: (1) Standard (shared throughput): all consumers share 2MB/sec read per shard (5 consumers = 0.4MB/sec each). Each tracks its own position via KCL checkpointing. (2) Enhanced Fan-Out: each registered consumer gets a dedicated 2MB/sec per shard (push-based via SubscribeToShard). Up to 20 enhanced fan-out consumers per stream. Use cases: real-time analytics consumer, S3 archival consumer, anomaly detection consumer — all reading independently from the same stream. Each consumer maintains independent state.", domain:3},
  {q:"A company uses S3 Object Lock. They want to protect specific objects from deletion for 30 days (compliance requirement), but an administrator should be able to override the protection if absolutely necessary. Which Object Lock mode?", opts:["Compliance mode (NO ONE can override, including root)","Governance mode — objects are protected from deletion by users WITHOUT special permissions. Users WITH s3:BypassGovernanceRetention permission CAN override the protection if needed. Provides protection with an administrative escape hatch.","Legal Hold (no specific retention period)","Retention period without Object Lock (not enforceable)"], correct:1, exp:"Object Lock modes: (1) Governance: protected, but users with s3:BypassGovernanceRetention + versioning permissions can delete/override. Provides administrative override for exceptional cases. (2) Compliance: IMMUTABLE — nobody (including root, AWS support) can delete before retention period expires. No override possible. Choose Governance when: you want protection from accidental deletion but need admin escape hatch. Choose Compliance when: regulatory requirements demand absolute immutability (SEC 17a-4, WORM storage). Also: Legal Hold (infinite retention until explicitly removed, no expiration date).", domain:1},
  {q:"A company uses Amazon EKS. They want to expose a Kubernetes service to the internet. They apply a Service of type LoadBalancer. What AWS resource is created?", opts:["An Application Load Balancer (ALB)","By default, a Classic Load Balancer (CLB) is created. To create an NLB: add annotation service.beta.kubernetes.io/aws-load-balancer-type: nlb. For ALB: use the AWS Load Balancer Controller with an Ingress resource (not Service type LoadBalancer). The Load Balancer Controller provides the most features.","A Network Load Balancer by default","A Route 53 record only"], correct:1, exp:"EKS load balancer behavior: (1) Service type: LoadBalancer (no annotations) → Classic LB (legacy, being deprecated). (2) Service type: LoadBalancer + annotation nlb → Network Load Balancer. (3) Ingress resource + AWS Load Balancer Controller → Application Load Balancer (most features: path routing, host routing, WAF integration). Best practice: install the AWS Load Balancer Controller add-on and use Ingress resources for ALB, or annotated Services for NLB. Avoid CLB (limited features, fewer AZ support).", domain:3},
  {q:"A company uses Amazon RDS with automated backups. They delete the RDS instance (accidentally). Can they restore from automated backups?", opts:["Yes — automated backups persist after instance deletion (only for a limited time set by retention period)","By default, automated backups are DELETED when the RDS instance is deleted. To preserve: either take a final snapshot (checked by default in console), or enable 'Retain automated backups' option during deletion. Without either: all automated backups are gone.","Automated backups are always retained forever","AWS Support can restore deleted instance backups"], correct:1, exp:"RDS backup behavior on deletion: (1) Automated backups: deleted by default when instance is deleted. UNLESS 'Retain automated backups' is enabled (new feature) — then they persist for the retention period. (2) Manual snapshots: ALWAYS retained (never auto-deleted). (3) Final snapshot: console prompts to take one during deletion (recommended). Best practice: always take a final snapshot when deleting. Enable DeletionPolicy: Snapshot in CloudFormation. This prevents accidental data loss during instance cleanup.", domain:2},
  {q:"A company uses AWS Step Functions. Their workflow has a step that calls a third-party API. The API is rate-limited to 10 requests per second. The workflow processes 100 items in a Map state simultaneously. How do they stay under the rate limit?", opts:["Step Functions automatically respects external rate limits (it doesn't)","Configure MaxConcurrency on the Map state: set MaxConcurrency=10. This limits the Map state to 10 concurrent iterations. Combined with a 1-second Wait state in each iteration after the API call: effectively 10 requests/second maximum.","Use a queue between Step Functions and the API","Implement retry with backoff only (doesn't limit concurrency)"], correct:1, exp:"Map state MaxConcurrency: limits how many iterations execute in parallel. Set to 10: only 10 items are processed concurrently (others wait). For strict rate limiting: MaxConcurrency=10 + consider adding a Wait state or using a token bucket pattern. Without MaxConcurrency (default 0 = unlimited): all 100 items start simultaneously → API rate limit errors. MaxConcurrency is the key parameter for controlling parallelism in Map states. Also works with Distributed Map (up to 10,000 max concurrency).", domain:2},
  {q:"A company uses Amazon S3 with requester pays enabled on a bucket. What does this mean?", opts:["AWS pays for the storage (not a thing)","The REQUESTER (data consumer) pays for data transfer and request costs instead of the bucket owner. The bucket owner only pays for storage. Used when: sharing large public datasets (genomics, satellite imagery, open data) — providers share data without incurring massive transfer costs. Requesters must authenticate (anonymous access not allowed).","The bucket is free for everyone","Only applies to cross-account access"], correct:1, exp:"S3 Requester Pays: shifts data transfer and request costs from bucket owner to data consumer. Normal: bucket owner pays everything (storage + transfer + requests). Requester Pays: owner pays storage only, requester pays transfer + requests. Use cases: scientific data sharing, public datasets, data marketplace. Requirement: requesters must authenticate (include x-amz-request-payer header) — anonymous users can't access requester-pays buckets. This enables sharing petabytes of data without the provider absorbing transfer costs.", domain:4},
  {q:"A company uses Amazon ElastiCache for Redis. They need to encrypt data in transit (between application and Redis) AND at rest. What configuration enables both?", opts:["Redis doesn't support encryption","ElastiCache Redis: (1) Encryption at-rest: enable during cluster creation (AES-256, using KMS or service-managed key). (2) Encryption in-transit: enable TLS (client connects via rediss:// instead of redis://). Both must be enabled at cluster CREATION time — cannot be added to existing unencrypted clusters.","Only at-rest encryption is available","Only in-transit encryption is available"], correct:1, exp:"ElastiCache Redis encryption: (1) At-rest: AES-256, applied to disk/backup data, configurable KMS key. (2) In-transit (TLS): encrypts all communication between client and Redis (and between primary and replicas). IMPORTANT: both settings are IMMUTABLE after creation — if you need encryption, enable it when creating the cluster. Adding encryption to existing cluster: create new encrypted cluster, migrate data (backup + restore or replication), switch application endpoints. For compliance: enable both from the start. Performance impact of TLS: ~10-25% overhead.", domain:1},
  {q:"A company uses AWS Amplify Hosting for their React SPA. Developers push to a Git branch and Amplify automatically builds and deploys. They want preview deployments for pull requests (temporary environments per PR). Does Amplify support this?", opts:["Must deploy manually for each PR","Yes — Amplify Previews: automatically creates a temporary deployment for each pull request. Team members can access the preview URL to review changes before merging. Preview is deleted when the PR is closed/merged. Built-in feature — just enable in Amplify app settings.","Need separate AWS accounts per PR","Only supports production deployments"], correct:1, exp:"Amplify Previews: (1) Enable web previews in app settings. (2) When a PR is opened: Amplify builds and deploys to a unique preview URL (pr-123.d111111abcdef.amplifyapp.com). (3) Team reviews the PR with a live environment. (4) PR merged/closed: preview auto-deleted. Supports: backend previews (separate backend per PR), environment variables per branch, custom domain for previews. This is standard in modern CI/CD — Amplify provides it natively for frontend deployments. No infrastructure management needed.", domain:2},
  {q:"A company uses Amazon GuardDuty. It generates a HIGH severity finding: 'CryptoCurrency:EC2/BitcoinTool.B!DNS'. What should they do IMMEDIATELY?", opts:["Ignore it — might be a false positive","IMMEDIATELY: (1) Isolate the instance (restrict Security Group to block all traffic except investigation access). (2) Capture memory/disk for forensics (EBS snapshot). (3) Investigate: is this authorized mining or a compromised instance? (4) If compromised: terminate instance, rotate all credentials that were on it, check for lateral movement. This finding means the instance is communicating with cryptocurrency mining pools.","Wait for GuardDuty to resolve it","Reboot the instance"], correct:1, exp:"HIGH severity GuardDuty finding response: CryptoCurrency:EC2/BitcoinTool.B!DNS = instance querying DNS for known crypto mining domains. This indicates: either unauthorized crypto mining (compromise) or a misconfigured/rogue application. Immediate response: (1) ISOLATE: modify SG to allow only forensic access (don't terminate yet — preserve evidence). (2) SNAPSHOT: capture EBS volumes for investigation. (3) INVESTIGATE: check instance metadata, running processes, recent SSH logins. (4) REMEDIATE: terminate if compromised, rotate credentials, check other instances. Speed matters — crypto miners rack up compute costs quickly.", domain:1},
  {q:"A company uses S3 with versioning and MFA Delete enabled. What does MFA Delete protect against?", opts:["Object encryption","MFA Delete requires multi-factor authentication to: (1) permanently delete an object version, and (2) change the versioning state of the bucket (disable/suspend versioning). This prevents: accidental permanent deletion, malicious deletion by compromised credentials (attacker would also need the MFA device), and unauthorized versioning changes.","Bucket creation","Unauthorized PutObject"], correct:1, exp:"MFA Delete adds physical authentication (MFA device/token) for two destructive operations: (1) DeleteObjectVersion (permanent deletion of a specific version). (2) PutBucketVersioning (changing versioning status). Without MFA: even if credentials are stolen, attacker can delete markers (recoverable) but can't permanently delete versions or disable versioning. Limitation: can only be enabled/disabled by the ROOT account (not IAM users). Combined with Object Lock: defense-in-depth against data destruction.", domain:1},
  {q:"A company uses AWS CloudFormation. They need to deploy resources that CloudFormation doesn't natively support (e.g., a third-party SaaS configuration). What extension mechanism supports this?", opts:["CloudFormation only supports AWS resources","CloudFormation Registry and Custom Resource Types — third-party publishers (and you) can register resource types (e.g., Datadog::Monitors::Monitor, MongoDB::Atlas::Cluster). Use them in templates like native AWS resources. Also: Custom Resources (Lambda-backed) for arbitrary logic.","Must use Terraform for non-AWS resources","Custom resources only support deletion"], correct:1, exp:"CloudFormation extensibility: (1) CloudFormation Registry: third-party resource types (activated from the registry) used like native resources in templates. Available: Datadog, MongoDB Atlas, GitHub, PagerDuty, Cloudflare, etc. (2) Custom Resources: Lambda function called during create/update/delete for arbitrary logic. (3) Modules: reusable template building blocks. Registry types are first-class citizens (drift detection, rollback). Custom Resources are more flexible but require Lambda code. Together: CloudFormation manages anything.", domain:2},
  {q:"A company has a large number of S3 buckets (500+) across multiple accounts. They need to identify which buckets have public access, which are unencrypted, and which have versioning disabled. What is the most efficient way to get this visibility?", opts:["Check each bucket manually in the console","AWS Config rules (required-tags, s3-bucket-public-read-prohibited, s3-bucket-server-side-encryption-enabled, s3-bucket-versioning-enabled) with Config Aggregator across all accounts — provides a compliance dashboard showing which buckets violate each rule. S3 Storage Lens also shows encryption and versioning status.","S3 API ListBuckets then describe each (tedious at scale)","CloudTrail will show bucket configurations (it shows API calls, not current state)"], correct:1, exp:"Multi-account S3 governance: AWS Config rules evaluate each bucket against compliance policies. Config Aggregator centralizes findings across all accounts. Dashboard shows: 87/500 buckets compliant, 413 non-compliant (with details). Specific rules: s3-bucket-public-read-prohibited, s3-bucket-server-side-encryption-enabled, s3-bucket-versioning-enabled, s3-bucket-logging-enabled. Auto-remediation can fix non-compliant buckets. S3 Storage Lens complements with operational metrics (total size, object count per bucket). Together: comprehensive S3 governance at scale.", domain:1},
  {q:"A company uses Amazon ECS. They want to deploy a new service but only allow traffic from their existing 'frontend' service (not from the internet or other services). What provides service-to-service authorization on ECS?", opts:["Security Groups only (L4, not service-identity aware)","ECS Service Connect with service-to-service authorization OR Security Groups with the frontend service's SG as the source (allows traffic only from tasks in the frontend SG). For service-identity authorization: use App Mesh with mTLS or ECS Service Connect with IAM auth.","No way to restrict service-to-service traffic on ECS","Network ACLs between services"], correct:1, exp:"ECS service-to-service security options: (1) Security Groups (simplest): backend service SG allows inbound ONLY from frontend service's SG ID. Since all frontend tasks share the same SG, only they can reach the backend. (2) App Mesh with mTLS: service-identity-based authorization (cryptographic identity). (3) VPC Lattice (newest): service-to-service networking with IAM auth policies. SG-based is the most common and simplest for ECS. It provides L4 isolation — any task with the frontend SG can reach the backend, others cannot.", domain:1},
  {q:"A company needs to process clickstream data (100,000 events per second) for real-time user behavior analytics. They need sub-second aggregation (events per page per second). Which real-time processing service?", opts:["Lambda reading from SQS (can't achieve sub-second aggregation at this scale)","Amazon Managed Service for Apache Flink (Kinesis Data Analytics) — processes 100K+ events/sec with sub-second windowed aggregations using SQL or Java/Python. Reads from Kinesis Data Streams and outputs to dashboards, DynamoDB, or OpenSearch.","Amazon Athena (batch query, not real-time)","Amazon EMR with Spark Streaming (higher latency, more operational overhead)"], correct:1, exp:"Flink for real-time analytics: (1) Source: Kinesis Data Streams receiving 100K events/sec. (2) Flink application: windowed aggregation (1-second tumbling window) computing page views per second, session counts, etc. (3) Output: OpenSearch for dashboards, DynamoDB for serving, S3 for archival. Flink handles: stateful processing, exactly-once semantics, event-time processing (handles late events), and sub-second latency at massive scale. Purpose-built for real-time stream analytics.", domain:3},
  {q:"A company uses AWS Landing Zone (or Control Tower) and has accounts organized into OUs: Security, Shared Services, Production, Development. A new team is created and needs their own AWS account. What is the recommended process?", opts:["Create the account manually in the Organizations console","Use Control Tower Account Factory (or Service Catalog Account Factory): the team requests an account through the self-service portal. Account is automatically created with: governance guardrails (SCPs), networking (VPC), IAM roles, Config/CloudTrail enabled, and placed in the correct OU. Standardized, repeatable, governed.","Give the team access to an existing shared account","Create a separate Organization for the team"], correct:1, exp:"Account Factory process: (1) Request: team submits account request (name, email, OU placement). (2) Creation: Account Factory creates the account in the Organization. (3) Baseline: automatically applies: SCPs (from OU), VPC (standard networking), IAM roles (admin, read-only, pipeline), AWS Config (compliance rules), CloudTrail (audit logging), SSO permission sets. (4) Ready: team accesses their account via IAM Identity Center with appropriate permissions. This ensures every account is governance-compliant from day one.", domain:2},
  {q:"A company uses Amazon S3 for a data lake. Data scientists need to find relevant datasets among thousands of tables. They need to search by: business description, data owner, data quality score, and tags. What service provides a searchable data catalog with business context?", opts:["Glue Data Catalog alone (technical metadata only — schema, partitions, no business context)","Amazon DataZone — provides a business data catalog with: domain organization, data product descriptions, data quality metrics, ownership, glossary terms, and a search portal. Or: AWS Glue Data Catalog + custom tags + Lake Formation descriptions for a lighter-weight approach.","S3 tags are sufficient for discovery (limited metadata, not searchable by description)","Build a custom catalog application"], correct:1, exp:"Data discovery solutions: (1) Amazon DataZone (most capable): business data catalog with rich metadata, search portal, subscription workflows, data quality integration, business glossary. Purpose-built for data mesh/marketplace. (2) Glue Data Catalog + enhancements: add descriptions to tables/columns, use LakeFormation tags, create a search interface. (3) Third-party: Alation, Collibra, Atlan. For the exam: DataZone is the AWS answer for business data discovery and governance beyond technical schema. Glue provides the technical foundation.", domain:3},
  {q:"A company uses Route 53 with Multivalue Answer routing. How does it differ from a simple record with multiple IPs?", opts:["No difference","Multivalue Answer routing returns up to 8 healthy IP addresses (out of the total configured records). Each record has its own health check. Unhealthy records are excluded from responses. Simple records with multiple IPs return ALL IPs regardless of health. Multivalue provides health-checked client-side load balancing.","Multivalue provides weighted distribution","Multivalue provides geographic routing"], correct:1, exp:"Route 53 Multivalue Answer: (1) Up to 8 records, each with an IP and associated health check. (2) DNS response includes ONLY healthy IPs (unhealthy excluded). (3) Client picks one IP randomly (client-side load balancing). vs Simple record: returns ALL IPs (no health checking), includes unhealthy endpoints. Multivalue is NOT a replacement for a load balancer (no intelligent routing), but provides basic availability improvement over simple records. Used when: you want health-checked DNS but don't need a full LB.", domain:2},
  {q:"A company uses AWS Trusted Advisor. They are on a Basic Support plan. Which Trusted Advisor checks are available?", opts:["All checks are available on Basic plan","Basic/Developer support: only 7 core checks available (S3 bucket permissions, Security Groups, IAM use, MFA on root, EBS public snapshots, RDS public snapshots, service limits). Full Trusted Advisor checks (115+ including cost optimization, performance, fault tolerance) require Business or Enterprise Support plan.","No checks are available on Basic plan","Only security checks on Basic plan"], correct:1, exp:"Trusted Advisor tiers: (1) Basic/Developer Support: 7 checks — mostly security (open SGs, public S3/EBS/RDS, IAM MFA, service limits). (2) Business/Enterprise Support: ALL checks — Cost Optimization (idle instances, unused EIPs, underutilized EBS), Performance (high utilization, CloudFront optimization), Security (all 7 + more), Fault Tolerance (AZ balance, backup coverage), Service Limits (approaching quotas). Full Trusted Advisor is one of the key benefits of Business Support ($100/month minimum).", domain:4},
  {q:"A company uses AWS Config. They want to know the exact configuration of a resource AT A SPECIFIC POINT IN TIME (e.g., what were the security groups of instance i-123 on January 15 at 3 PM?). Can Config provide this?", opts:["Config only shows current configuration","Yes — AWS Config maintains a complete timeline of configuration changes (Configuration Items). Query any resource's configuration at any historical point within the retention period. The Configuration Timeline shows: every state change with timestamp, what changed, and who changed it (via CloudTrail correlation).","Must check CloudTrail for historical config (CloudTrail shows API calls, not resulting state)","Only available for the last 24 hours"], correct:1, exp:"AWS Config historical queries: Config records Configuration Items (CIs) — snapshots of a resource's complete configuration at each change point. Features: (1) Configuration Timeline: visual history of all configuration states. (2) Advanced Query (SQL): SELECT configuration WHERE resourceId = 'i-123' AND configurationItemCaptureTime < '2024-01-15T15:00:00'. (3) Retention: configurable 1-7 years. This answers compliance questions: 'prove this resource was configured correctly on date X'. Config provides WHAT the configuration WAS; CloudTrail provides WHO changed it and WHEN.", domain:1},
  {q:"A company is designing a multi-account strategy. They need to decide between one AWS account for everything vs. multiple accounts. What are the PRIMARY reasons for using multiple accounts?", opts:["Multiple accounts are always more expensive (not true — consolidated billing provides discounts)","Multiple accounts provide: (1) Blast radius isolation (security breach in dev can't reach prod). (2) Independent service quotas per account. (3) Separate billing boundaries (easy cost attribution). (4) Different security postures per environment. (5) Team autonomy with guardrails (SCPs). Single account = shared blast radius, quota contention, complex IAM policies.","One account is always sufficient","Multiple accounts require multiple Organizations (false — all in one Org)"], correct:1, exp:"Multi-account benefits: (1) SECURITY: compromise of dev account can't access prod (separate IAM boundary). (2) QUOTAS: each account has independent service limits (one team can't exhaust another's Lambda concurrency). (3) BILLING: clear cost attribution per account (dev, staging, prod, team accounts). (4) GOVERNANCE: different SCP policies per OU (strict in prod, relaxed in dev). (5) COMPLIANCE: isolate regulated workloads in dedicated accounts. (6) AUTONOMY: teams manage their account within guardrails. AWS recommends: minimum 4 accounts (management, security/audit, shared services, workload).", domain:2},
  {q:"A company has completed their AWS architecture. They want an AWS expert to review it for: reliability, security, performance, cost optimization, operational excellence, and sustainability. What AWS program provides this?", opts:["AWS Support ticket (general support, not structured review)","AWS Well-Architected Framework Review — use the Well-Architected Tool in the console to answer questions across all 6 pillars. Generates a report with high-risk issues (HRIs) and improvement plans. Also available: Well-Architected Partner program (certified partners conduct the review) or AWS Solutions Architect consultation.","Only available for Enterprise Support customers","AWS Certification exam (tests knowledge, not architecture)"], correct:1, exp:"Well-Architected Review: (1) Self-service: use AWS Well-Architected Tool — answer questions about your workload across 6 pillars (Operational Excellence, Security, Reliability, Performance Efficiency, Cost Optimization, Sustainability). (2) Tool identifies High-Risk Issues and provides remediation guidance. (3) Partner review: AWS Well-Architected Partners conduct deeper analysis. (4) AWS SA review: available for some support tiers. The framework provides: best practices, design principles, and specific recommendations. Free to use. Repeated periodically as architecture evolves.", domain:2},
]
