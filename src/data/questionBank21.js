export const questionBank21 = [
  {q:"A company uses AWS CloudHSM for key management. How does CloudHSM differ from KMS?", opts:["They are identical services","CloudHSM provides DEDICATED hardware security modules (single-tenant) in your VPC. You have full control of keys — AWS has NO access to key material. FIPS 140-2 Level 3 certified. KMS is multi-tenant, AWS-managed, FIPS 140-2 Level 2 (some Level 3). CloudHSM for: regulatory requirements needing single-tenant HSM, custom key stores, or non-AWS integrations.","CloudHSM is cheaper than KMS","KMS provides higher security than CloudHSM"], correct:1, exp:"CloudHSM vs KMS: CloudHSM: dedicated hardware, single-tenant, FIPS 140-2 Level 3, YOU manage keys (AWS can't access/recover), supports symmetric + asymmetric + custom algorithms, runs in YOUR VPC, YOU manage HA (cluster of HSMs). KMS: multi-tenant, AWS-managed, FIPS 140-2 Level 2/3, AWS manages availability, integrated with 100+ AWS services. Use CloudHSM for: strict compliance (single-tenant HSM requirement), custom crypto operations, Oracle TDE, or when regulations prohibit multi-tenant key management.", domain:1},
  {q:"A company migrates a monolithic application to microservices on ECS. They need a way to manage traffic routing between services (canary deployments, traffic splitting, retries). Which AWS service provides a service mesh?", opts:["ALB with path-based routing (no per-service traffic management)","AWS App Mesh — service mesh using Envoy sidecar proxies. Provides: traffic routing (weighted targets for canary), retries/timeouts per route, circuit breakers, mTLS between services, and observability (metrics to CloudWatch, traces to X-Ray).","Amazon API Gateway between every service (expensive and adds latency)","Kubernetes Ingress controller (only handles north-south traffic, not east-west)"], correct:1, exp:"AWS App Mesh: control plane for Envoy proxy sidecars. Deploys an Envoy proxy alongside each ECS task (sidecar container). Provides: (1) Traffic routing: weighted targets (90/10 canary between v1/v2). (2) Resilience: retries, timeouts, circuit breakers. (3) Security: mTLS between services (mutual authentication). (4) Observability: automatic metrics/traces without code changes. Works with ECS, EKS, and EC2. The Envoy proxy intercepts all inbound/outbound traffic — transparent to the application.", domain:2},
  {q:"A company uses AWS Backup. They want to define a backup policy: daily backups retained for 30 days, weekly backups retained for 1 year, monthly backups retained for 7 years. Can a single backup plan handle this?", opts:["Each retention period requires a separate backup plan","Yes — a single AWS Backup plan supports multiple backup rules with different schedules and retention periods. Rule 1: daily (retain 30 days). Rule 2: weekly (retain 365 days). Rule 3: monthly (retain 2555 days). Each rule runs independently on its schedule.","AWS Backup only supports a single retention period","Must use S3 lifecycle rules instead"], correct:1, exp:"AWS Backup plan structure: one plan can contain multiple rules. Each rule defines: schedule (cron), retention period, lifecycle (move to cold storage after N days), and optional cross-region copy. Multiple rules in one plan: daily (short retention) + weekly (medium retention) + monthly (long retention). All rules apply to the same assigned resources. Cold storage transition available for long-term backups. This implements the common GFS (Grandfather-Father-Son) backup rotation strategy.", domain:2},
  {q:"A company uses Amazon EFS. They notice costs are high because the file system stores 10TB but only 500GB is accessed regularly. What EFS feature automatically reduces costs for infrequently accessed data?", opts:["EFS doesn't have tiered storage","EFS Lifecycle Management with Infrequent Access (IA) storage class — files not accessed for a configurable period (7/14/30/60/90 days) automatically move to IA tier (~92% cheaper). Frequently accessed files stay in Standard tier. Transparent to applications.","Must manually move files between tiers","EFS only has one pricing tier"], correct:1, exp:"EFS storage classes: Standard ($0.30/GB-month) and Infrequent Access ($0.016/GB-month = 92% cheaper). Lifecycle Management automatically moves files based on last access time. For 10TB with 9.5TB rarely accessed: 9.5TB × $0.016 + 0.5TB × $0.30 = $152 + $150 = $302/month vs $3,000/month (all Standard). Savings: ~90%. IA has a per-access fee when files are read — suitable for rarely-accessed data. Enable lifecycle policy with one configuration change.", domain:4},
  {q:"A company uses Amazon Athena with federated query to join data from DynamoDB and S3. The DynamoDB connector Lambda is timing out on large tables. What can they do?", opts:["Increase DynamoDB read capacity (may not be the bottleneck)","Increase the Lambda connector's memory (and thus CPU/network) and timeout. Also: add DynamoDB indexes to support the query pattern, or export DynamoDB to S3 periodically and query the export instead (avoid real-time connector for large tables).","Athena federated queries have no timeout settings","Switch to Redshift for all queries"], correct:1, exp:"Athena federated query performance: the Lambda connector scans DynamoDB and returns results to Athena. For large tables: (1) Increase Lambda memory (proportionally increases CPU and network — faster DynamoDB reads). (2) Increase Lambda timeout (default 900s max). (3) Push down predicates (the connector should filter at DynamoDB level, not scan everything). (4) For recurring queries on large DynamoDB tables: export to S3 (native DynamoDB Export or Glue) and query S3 directly — much faster and cheaper than real-time connector.", domain:3},
  {q:"A company uses CloudFormation with nested stacks. The parent stack has 5 child stacks. An update to one child stack fails. What happens to the other child stacks?", opts:["Only the failed child stack rolls back","The ENTIRE parent stack (including all child stacks) rolls back. CloudFormation treats the nested stack hierarchy as a single unit — if any part fails, the entire operation rolls back to the previous state.","Other child stacks continue updating independently","The parent stack enters UPDATE_FAILED state and waits for manual intervention"], correct:1, exp:"Nested stacks atomic behavior: the parent stack's update is ONE atomic operation. If child stack 3/5 fails: CloudFormation rolls back ALL changes across ALL child stacks + parent. The entire hierarchy returns to its previous state. This ensures consistency — you never have a partially-updated application. For very large deployments where partial updates are acceptable: consider independent stacks with cross-stack references (Exports) instead of nested stacks. Trade-off: atomicity vs independent deployability.", domain:2},
  {q:"A company uses ElastiCache Redis Cluster Mode. They need to store 200GB of data with high availability. What cluster configuration is appropriate?", opts:["Single node with 200GB memory (no HA, single point of failure)","Cluster Mode Enabled with multiple shards: e.g., 4 shards × 50GB each = 200GB total capacity. Each shard has 1 primary + 1 replica = 8 nodes total. Multi-AZ enabled for automatic failover.","One shard with 200GB (Cluster Mode Disabled node max is ~340GB but limited throughput)","Store 200GB in a single r6g.4xlarge node"], correct:1, exp:"Redis Cluster Mode Enabled: data is partitioned across shards (each shard holds a subset of the keyspace). Multiple shards provide: (1) More total memory (distributed across nodes), (2) Higher throughput (writes distributed across shard primaries), (3) Better fault isolation. Configuration: 4 shards × r6g.2xlarge (52GB) ≈ 200GB capacity with 1 replica per shard (8 nodes). Multi-AZ ensures replicas are in different AZs from their primary. If a primary fails: replica promoted (< 10 seconds).", domain:3},
  {q:"A company uses S3 for hosting a static website with millions of objects. They want to find and fix broken internal links (links to objects that don't exist in the bucket). What approach can identify missing objects?", opts:["S3 automatically validates links (it doesn't)","Use S3 Inventory to get a complete object listing, then compare against the links referenced in the HTML/JSON files (run an Athena query or script to find references to non-existent keys)","CloudFront access logs showing 404s (reactive, not proactive)","Both B and C — Inventory for proactive discovery, access logs for monitoring ongoing 404s"], correct:3, exp:"Two complementary approaches: (1) PROACTIVE: S3 Inventory provides complete object list. Parse HTML files for internal links, cross-reference against inventory. Find broken links before users hit them. (2) REACTIVE: CloudFront/S3 access logs show 404 responses — identify actually-hit broken links in production. Fix the most impactful first (highest 404 count). Together: fix existing breaks and monitor for new ones. Athena queries on both Inventory and access logs make this analysis simple.", domain:3},
  {q:"A company uses AWS Control Tower. A developer in a member account tries to disable AWS Config (required by Control Tower guardrails). What happens?", opts:["Config is disabled successfully","Control Tower preventive guardrails (SCPs) BLOCK the action — the developer receives AccessDenied. Control Tower's mandatory guardrails include SCPs that prevent: disabling Config, deleting CloudTrail, modifying the audit account's log bucket, and leaving the Organization. These cannot be bypassed by member account administrators.","Control Tower sends a warning email but allows the action","Config can be disabled but is automatically re-enabled"], correct:0, exp:"Control Tower mandatory guardrails are preventive SCPs that CANNOT be disabled or circumvented from member accounts. They prevent: disabling AWS Config, disabling CloudTrail, deleting or modifying the logging S3 bucket, modifying IAM roles created by Control Tower, and leaving the Organization. These ensure the governance baseline is maintained. Additional elective guardrails can be enabled/disabled. Mandatory guardrails are non-negotiable — the foundation of the governed environment.", domain:1},
  {q:"A company uses Amazon Kinesis Data Firehose to deliver data to S3. They want the data partitioned in S3 by a field in the data (e.g., customer_id) for efficient Athena queries. Can Firehose dynamically route to different S3 prefixes based on data content?", opts:["No — Firehose only supports static S3 prefixes","Yes — Firehose Dynamic Partitioning: extract keys from the data (using JQ expressions or Lambda transformation) and use them in the S3 prefix pattern. Example: s3://bucket/customer_id=!{partitionKeyFromQuery:customer_id}/year=!{timestamp:yyyy}/","Must use Lambda to write to different S3 locations","Firehose can only partition by timestamp"], correct:1, exp:"Firehose Dynamic Partitioning: extract fields from incoming records and use them as S3 prefix components. Configuration: define JQ expressions to extract partition keys from JSON data, then use them in the prefix: bucket/customer_id=!{partitionKeyFromQuery:customer_id}/. Firehose writes each record to the appropriate S3 prefix based on its content. This creates Hive-style partitioning automatically — Athena queries with partition filters scan only relevant data. Massive cost and performance improvement for Athena queries.", domain:3},
  {q:"A company uses EC2 instances behind a Network Load Balancer. They need to allow traffic from specific IP addresses only (IP whitelist). NLB doesn't have security groups. How do they restrict source IPs?", opts:["NLB Security Groups (NLB now supports security groups as of 2023)","Since NLB preserves source IP, configure Security Groups on the TARGET EC2 instances to allow only the whitelisted IPs. The instances' SGs see the real client IP (not the NLB IP).","Use NACL on the NLB subnet","Configure IP restriction in the NLB listener (not a feature)"], correct:0, exp:"As of 2023, NLBs now support Security Groups directly. You can attach a SG to the NLB itself to restrict inbound traffic by source IP. Additionally, since NLB preserves client source IP (for instance targets), you could also restrict at the target instance SG level. The NLB SG is the cleaner approach. For older configurations without NLB SGs: use target instance SGs (which see real client IPs) or NACLs on the subnet.", domain:1},
  {q:"A company uses Step Functions for order processing. They want to process 10,000 orders in parallel (each order goes through: validate → charge → ship). What Step Functions feature handles massive parallelism?", opts:["Parallel state (limited to static parallel branches defined in the state machine)","Map state (Distributed mode) — iterates over a collection (10,000 orders) and runs the child workflow for EACH item concurrently. Distributed Map supports up to 10,000 concurrent executions and millions of items total.","Step Functions doesn't support parallel processing","Use multiple state machine executions manually"], correct:1, exp:"Step Functions Map state: (1) Inline mode: up to 40 concurrent iterations within one execution. (2) Distributed mode: up to 10,000 concurrent child executions, processes millions of items. Input from S3 (JSON array or CSV). For 10,000 orders: Distributed Map reads the order list from S3, launches up to 10,000 concurrent child workflows, each running validate→charge→ship independently. Results aggregated. This enables massive-scale parallel orchestration with full Step Functions features (retry, error handling) per item.", domain:2},
  {q:"A company has a REST API that returns paginated results. They use DynamoDB as the backend. The API supports 'next page' tokens. What DynamoDB feature enables efficient pagination?", opts:["Scan with Offset/Skip (DynamoDB doesn't support offset-based pagination)","Query with ExclusiveStartKey — DynamoDB returns LastEvaluatedKey in the response. Pass it as ExclusiveStartKey in the next request to get the next page. This is token-based pagination (efficient, O(1) seek to the next page).","Query with LIMIT and page number (DynamoDB doesn't support page numbers)","Global Secondary Index for each page (absurd)"], correct:1, exp:"DynamoDB pagination: Query/Scan returns up to 1MB of data + a LastEvaluatedKey. To get next page: pass LastEvaluatedKey as ExclusiveStartKey in the next call. DynamoDB resumes from exactly where it left off (efficient — no re-scanning previous pages). API design: encode LastEvaluatedKey as a base64 'nextToken' in the API response. Client passes it in the next request. This is cursor-based pagination — O(1) per page regardless of how deep you are. Offset pagination (page 500) would be O(N) in DynamoDB.", domain:3},
  {q:"A company uses Amazon CloudWatch Logs and wants to extract metrics from log data (e.g., count of 'ERROR' messages per minute, average response time from access logs). What feature converts log data into CloudWatch Metrics?", opts:["CloudWatch Logs Insights (ad-hoc queries, not continuous metrics)","CloudWatch Logs Metric Filters — define a filter pattern (e.g., 'ERROR' or JSON field extraction) and create a metric from matching log entries. The metric updates in real-time and can trigger CloudWatch Alarms.","Lambda function parsing logs (works but unnecessary overhead)","Must export logs to S3 and process with Athena"], correct:1, exp:"Metric Filters: continuous, real-time extraction of metrics from log streams. Pattern types: simple text match ('ERROR'), JSON field extraction ($.responseTime), space-delimited parsing. For each match: increment a counter metric, or extract a numeric value for statistical metrics (average, p99). Example: filter '{ $.status = 500 }' on access logs → creates a '5xx_errors' metric → alarm triggers if >10 per minute. Zero Lambda code, built into CloudWatch Logs. The bridge between logs and metrics.", domain:2},
  {q:"A company uses Amazon Aurora PostgreSQL. They want to invoke a Lambda function directly from the database (e.g., when a row is inserted, trigger external processing). Is this possible?", opts:["Databases can't call Lambda directly","Yes — Aurora PostgreSQL supports invoking Lambda functions using the aws_lambda extension. Call aws_lambda.invoke() from a trigger, stored procedure, or SQL statement. The Aurora cluster needs an IAM role with Lambda invoke permissions.","Only Aurora MySQL supports Lambda invocation","Must use DynamoDB Streams instead"], correct:1, exp:"Aurora PostgreSQL aws_lambda extension: call Lambda directly from SQL/triggers. Setup: (1) Create IAM role with lambda:InvokeFunction, (2) Associate role with the Aurora cluster, (3) Install the extension: CREATE EXTENSION aws_lambda, (4) Call: SELECT aws_lambda.invoke('function-arn', json_payload). Use in triggers for event-driven processing. Aurora MySQL also supports Lambda (via mysql.lambda_async()). This enables database-level event processing without polling or CDC. Powerful but use judiciously (don't overload Lambda from hot database paths).", domain:3},
  {q:"A company operates in the EU and must comply with GDPR. They store personal data in DynamoDB. A user requests data deletion ('right to be forgotten'). What is the technical approach?", opts:["DynamoDB doesn't support deletion","Delete all items containing the user's personal data using DeleteItem (by primary key). If data is replicated (Global Tables): deletion replicates automatically. If backups exist: document that backups contain now-deleted data (backup retention policy should be shorter than data subject request response time for practical compliance).","Flag the data as deleted but keep it (doesn't comply with GDPR erasure)","AWS automatically handles GDPR compliance"], correct:1, exp:"GDPR Right to Erasure technical implementation: (1) Identify ALL items containing the user's PII (across tables, GSIs). (2) DeleteItem for each item OR use UpdateItem to remove PII attributes (partial deletion if item contains shared data). (3) Global Tables: deletion replicates to all regions automatically. (4) Backups: document retention policy — automated backups expire naturally (up to 35 days for RDS, configurable for DynamoDB). (5) Consider tokenization: store PII in a separate 'identity' table and delete the token mapping (crypto-shredding pattern).", domain:1},
  {q:"A company uses EKS with Karpenter for node provisioning. What does Karpenter provide that the default Cluster Autoscaler doesn't?", opts:["They provide the same functionality","Karpenter provisions nodes FASTER (seconds vs minutes), selects optimal instance types from a wide pool automatically (right-sizing per pod requirements), consolidates underutilized nodes proactively, and handles Spot interruptions natively. No pre-defined node groups required — Karpenter matches pod requirements to instance capabilities directly.","Karpenter only works with ECS, not EKS","Cluster Autoscaler is always faster"], correct:1, exp:"Karpenter vs Cluster Autoscaler: Cluster Autoscaler: adjusts node group size (must pre-define instance types), reacts to unschedulable pods (slow — 30s-5min), limited instance type flexibility. Karpenter: provisions nodes in seconds (directly calls EC2 fleet API), automatically selects from ALL instance types meeting pod requirements (instance flexibility), consolidates (removes underutilized nodes and repacks), supports Spot with diversification, and deprovisioning strategies. Karpenter is the recommended autoscaler for EKS.", domain:2},
  {q:"A company uses S3 Select to query CSV files stored in S3. What does S3 Select provide?", opts:["Full SQL database functionality over S3","S3 Select allows server-side filtering of S3 objects using simple SQL expressions (SELECT, WHERE, LIMIT on CSV/JSON/Parquet files). Only matching rows are returned to the client — reduces data transfer by up to 400% and speeds up retrieval. Processing happens within S3 itself.","S3 Select replaces Athena","S3 Select only works with Parquet files"], correct:1, exp:"S3 Select: push filtering down to S3 storage layer. Instead of downloading 1GB CSV and filtering client-side, send a SQL expression to S3 → S3 returns only matching rows (maybe 10MB). Benefits: less data transfer (cost savings), faster (S3 filters in parallel), less client memory/CPU. Limitations: single file at a time, simple SQL (no JOINs, no aggregations beyond basic), supports CSV/JSON/Parquet. For complex queries across multiple files: use Athena. S3 Select is for targeted retrieval from known objects.", domain:3},
  {q:"A company uses AWS Transfer Family for SFTP file transfers to S3. They need to process files immediately after upload (validate format, move to correct folder). What integration enables post-upload processing?", opts:["Transfer Family doesn't support event-driven processing","Transfer Family managed workflows — define post-upload steps (Lambda function for custom processing, tagging, copying to another location, deleting). Steps execute automatically after each file upload completes. No external event plumbing needed.","Must configure S3 Event Notifications separately","Use CloudWatch Events to detect SFTP uploads"], correct:1, exp:"AWS Transfer Family Managed Workflows: attach a workflow to the Transfer Family server. When a file is uploaded via SFTP/FTPS/FTP: the workflow executes automatically. Steps: (1) Copy (to processing location), (2) Tag (add metadata), (3) Custom Lambda (validate, transform, route), (4) Delete (original after processing). Error handling: exception handler step for failed processing. This is native integration — no need for separate S3 event notifications or external orchestration.", domain:2},
  {q:"A company uses Amazon MSK (Managed Kafka). They want consumers in a DIFFERENT VPC (or different account) to connect to the MSK cluster. How is cross-VPC access configured?", opts:["MSK is only accessible within its own VPC","MSK supports Multi-VPC Connectivity (MSK Serverless and Provisioned) — uses AWS PrivateLink to expose MSK brokers to other VPCs/accounts via VPC endpoints. Also: VPC Peering or Transit Gateway for network connectivity to MSK's VPC.","Expose MSK to the public internet (not recommended for production)","Must replicate data to another MSK cluster in the consumer's VPC"], correct:1, exp:"MSK multi-VPC/cross-account access options: (1) MSK Multi-VPC Connectivity (PrivateLink-based): creates VPC endpoints in consumer VPCs that connect to MSK brokers. Most secure, no broad network exposure. (2) VPC Peering: peer consumer VPC with MSK VPC (simple but exposes entire VPC network). (3) Transit Gateway: hub-spoke model for multiple consumer VPCs. (4) MSK public access (for development only — exposes to internet). PrivateLink-based is recommended for cross-account: granular, secure, no CIDR conflicts.", domain:1},
  {q:"A company uses Amazon Aurora MySQL. Their database is 500GB but queries only frequently access 50GB of 'hot' data. They want to reduce costs. Aurora pricing is based on storage consumed. What can they do?", opts:["Aurora storage pricing is fixed ($0.10/GB-month) — can't tier within Aurora","Aurora I/O-Optimized pricing (flat rate, no per-I/O charge) vs Aurora Standard (cheaper storage, per-I/O charge). For cost reduction: archive cold data to S3 (export old records, delete from Aurora). Or consider Aurora Serverless v2 to scale compute down during low-usage periods.","Aurora automatically tiers hot and cold data","Use Aurora Read Replicas for cold data"], correct:1, exp:"Aurora cost optimization: (1) Archive cold data: export infrequently-accessed records to S3 (query with Athena when needed), delete from Aurora — reduces storage from 500GB to 50GB + reduces I/O. (2) Aurora I/O-Optimized vs Standard pricing: if I/O costs exceed 25% of total, I/O-Optimized is cheaper. (3) Aurora Serverless v2: auto-scales compute (saves during idle periods but doesn't reduce storage cost). The most impactful strategy: archive 450GB of cold data to S3 (storage drops from $50/month to $5/month + S3 costs pennies).", domain:4},
  {q:"A company runs Lambda functions that make HTTPS calls to external APIs. These calls occasionally fail transiently (timeout, 5xx). They want automatic retries with exponential backoff. Where should retry logic be implemented?", opts:["Lambda automatically retries all errors (only for async invocations — retries the entire function, not just the HTTP call)","Implement retry logic in the function code using exponential backoff with jitter. Use AWS SDK's built-in retry (for AWS calls) or a library like axios-retry/fetch-retry for external API calls.","API Gateway handles retries","CloudWatch triggers retries on failure"], correct:1, exp:"Retry strategy depends on invocation type: (1) Async (S3, SNS, EventBridge): Lambda retries the ENTIRE function 2 times — but this retries everything, not just the failed HTTP call. (2) Sync (API Gateway, SDK): caller retries. (3) Stream (Kinesis, DynamoDB): retries until record expires. For targeted retry of a SPECIFIC external API call within the function: implement in-code with exponential backoff + jitter (wait 100ms, 200ms, 400ms, 800ms with ±50% random jitter). Prevents thundering herd on the failing API.", domain:2},
  {q:"A company uses AWS Organizations and has 200 accounts. They want to ensure every new account automatically gets: a VPC with specific CIDR, a set of IAM roles, and AWS Config enabled. What tools achieve this?", opts:["Manually configure each new account","Control Tower Account Factory (standardized account provisioning with customizable blueprints) OR CloudFormation StackSets with automatic deployment to new accounts (target the Organization root OU — new accounts inherit). Or Service Catalog with Account Factory for Terraform (AFT).","Lambda triggered on account creation event","All of B and C — Account Factory for the full workflow, StackSets for specific resources, and EventBridge+Lambda for custom automation on CreateAccount events"], correct:3, exp:"Account bootstrapping toolchain: (1) Control Tower Account Factory: creates accounts with governance baseline (Config, CloudTrail, SCPs). Customizations for Control Tower (CfCT) adds custom resources. (2) StackSets: deploy CloudFormation to entire Organization — new accounts automatically get the stacks (if targeting OU). (3) EventBridge rule on CreateAccount event → Lambda for custom steps. (4) Account Factory for Terraform (AFT): Terraform-based account provisioning with customizations. Mature organizations use all of these together.", domain:2},
  {q:"A company uses ElastiCache Redis. They want to cache database query results. When the underlying database data changes, the cache should be invalidated. What caching strategies handle this?", opts:["Cache never expires (returns stale data permanently)","Cache-aside with TTL + event-driven invalidation: (1) TTL on cached items (eventual consistency — stale for up to TTL duration). (2) When database writes occur, explicitly delete/update the cache entry. (3) Or use DynamoDB Streams / CDC events to trigger cache invalidation Lambda.","Write-through caching (write to cache and DB simultaneously)","Both B and C — Cache-aside for reads, Write-through for write-heavy workloads with immediate consistency requirements. Choose based on consistency vs complexity trade-off."], correct:3, exp:"Caching strategies: (1) Cache-aside (lazy loading): app checks cache first, on miss reads from DB and populates cache. Simple, but can serve stale data until TTL. (2) Write-through: every DB write also writes to cache — cache always fresh. Higher write latency but strong read consistency. (3) Write-behind: write to cache, async flush to DB — fast writes but risk of data loss. For most read-heavy apps: cache-aside + short TTL + event-driven invalidation on writes. For write-heavy with consistency needs: write-through.", domain:3},
  {q:"A company uses AWS Savings Plans. They have two types available: Compute Savings Plans and EC2 Instance Savings Plans. What is the key difference?", opts:["They offer the same discount","Compute Savings Plans: discount applies to ANY compute (EC2 any family/region/OS, Fargate, Lambda). Maximum flexibility. EC2 Instance Savings Plans: higher discount percentage but locked to specific instance FAMILY and region (e.g., m5 in us-east-1). Less flexible but cheaper.","EC2 Instance Savings Plans cover all regions","Compute Savings Plans only cover Lambda"], correct:1, exp:"Savings Plans comparison: (1) Compute SP: up to 66% off. Applies to: EC2 (any family, region, OS, tenancy), Fargate (ECS/EKS), Lambda (duration + requests). MAXIMUM flexibility — change instance types, regions, or even services without losing discount. (2) EC2 Instance SP: up to 72% off. Locked to specific instance FAMILY (e.g., m5) in specific REGION. Can change: size (m5.large → m5.4xlarge), OS, tenancy. Recommendation: Compute SP for flexibility, EC2 Instance SP for stable workloads you're confident about.", domain:4},
  {q:"A company uses AWS Service Catalog. What problem does it solve?", opts:["It's a marketplace for buying third-party software","Service Catalog enables IT administrators to create a catalog of APPROVED, pre-configured AWS products (CloudFormation templates) that end users can launch self-service — with governance. Users deploy only approved architectures, with pre-set configurations, access controls, and tagging. Standardization without bottlenecking on the cloud team.","It lists all AWS services available in a region","It tracks service quotas"], correct:1, exp:"AWS Service Catalog: (1) Administrators create 'products' (CloudFormation templates) with approved configurations (instance types, VPC settings, encryption enabled). (2) Organize products into 'portfolios' shared with specific accounts/OUs. (3) End users browse the catalog and launch products self-service — they CAN'T modify the underlying template. (4) Constraints enforce: launch role (users don't need broad IAM permissions), approved instance types, required tags. Result: self-service + governance + standardization. Eliminates shadow IT.", domain:2},
  {q:"A company uses Amazon Textract. What does Textract do?", opts:["Text translation between languages (that's Amazon Translate)","Textract extracts text, tables, and form data from scanned documents and images using ML — OCR plus understanding of document STRUCTURE (forms with key-value pairs, tables with rows/columns). Goes beyond basic OCR to understand document layout.","Text-to-speech conversion (that's Amazon Polly)","Text summarization (that's Comprehend/Bedrock)"], correct:1, exp:"Amazon Textract capabilities: (1) Text Detection: OCR — extract raw text from images/PDFs. (2) Form Extraction: identify key-value pairs (e.g., 'Name: John Smith', 'Date: 2024-01-15'). (3) Table Extraction: detect tables and extract cell data with row/column structure. (4) Queries: ask specific questions about a document ('What is the total?'). Use cases: invoice processing, medical records digitization, ID verification, financial document analysis. More intelligent than basic OCR — understands document semantics.", domain:3},
  {q:"A company runs a multi-tenant SaaS application on a single RDS database. They want to implement row-level security so Tenant A can only see their own rows, never Tenant B's data. What database feature enables this?", opts:["Application-level filtering only (risky if bugs exist)","PostgreSQL Row Level Security (RLS) policies — define policies that automatically filter rows based on the current user/role. Even if the application has a bug and doesn't filter by tenant_id, the database enforces isolation. RDS PostgreSQL fully supports RLS.","DynamoDB fine-grained access control","Create separate tables per tenant (management nightmare at scale)"], correct:1, exp:"PostgreSQL Row Level Security: CREATE POLICY tenant_isolation ON orders USING (tenant_id = current_setting('app.current_tenant')). Every query against the 'orders' table automatically filters rows — the tenant can ONLY see their own data regardless of the query. Defense in depth: even if application code has a bug (missing WHERE tenant_id=X), the database enforces isolation. Set the session variable per-request: SET app.current_tenant = 'tenant_123'. Works with any SQL query (SELECT, UPDATE, DELETE). RDS PostgreSQL fully supports this.", domain:1},
  {q:"A company uses S3 for analytics data. They receive data in many small files (1KB-100KB each, millions per day). Athena queries are slow because of the high number of small files. What should they do?", opts:["Athena handles small files efficiently (it doesn't — each file has overhead)","Compact small files into larger files (128MB-1GB optimal for Athena). Use: Glue ETL compaction job (periodic), or S3 Object Lambda, or Firehose buffering (buffer to larger files before S3 delivery). Target file size: 128MB+ in Parquet format.","Delete the small files and re-upload as one big file","Use DynamoDB instead of S3 for small records"], correct:1, exp:"Small file problem: each file requires: S3 GET request (latency), open/parse overhead, task scheduling in the query engine. Millions of 1KB files = millions of overhead operations vs the same data in 1000 files of 128MB each. Solutions: (1) Input side: buffer data before writing to S3 (Firehose with 128MB buffer size). (2) Post-hoc: Glue compaction job — read small files, write large Parquet files, delete originals. (3) Partition wisely (avoid over-partitioning that creates tiny files). Optimal Athena file size: 128MB-512MB in columnar format.", domain:3},
  {q:"A company uses Amazon Redshift. They want to load data from Kinesis Data Streams directly into Redshift tables in near real-time. What Redshift feature enables this?", opts:["Kinesis to Firehose to Redshift (indirect, via S3 intermediate)","Redshift Streaming Ingestion — create a materialized view that maps to a Kinesis stream. Redshift continuously ingests from the stream with no intermediate S3 staging. Query the materialized view for near-real-time data alongside historical warehouse data.","COPY command from Kinesis (doesn't exist)","Must use Lambda to insert rows into Redshift"], correct:1, exp:"Redshift Streaming Ingestion: CREATE MATERIALIZED VIEW mv_stream AS SELECT * FROM kinesis_stream. Redshift continuously reads from Kinesis (or MSK) and materializes records into the view. No Firehose, no S3, no Lambda. Query the materialized view with SQL — data available within seconds of stream ingestion. REFRESH MATERIALIZED VIEW updates the view. Combine with historical data: JOIN mv_stream with regular Redshift tables. This enables real-time analytics on streaming data within Redshift.", domain:3},
  {q:"A company uses IAM Identity Center (SSO) for managing access to multiple AWS accounts. They want developers to have PowerUserAccess in dev accounts and ReadOnlyAccess in prod accounts — all managed centrally. What Identity Center concept enables this?", opts:["Create separate IAM users in each account","Permission Sets — define named sets of permissions (PowerUser, ReadOnly) in Identity Center. Assign each permission set to users/groups + target accounts. Users get different access levels in different accounts based on their permission set assignments.","IAM policies attached directly to federated users","SAML assertions with different roles per account"], correct:1, exp:"IAM Identity Center Permission Sets: (1) Create 'DeveloperPowerUser' permission set (policies: PowerUserAccess). (2) Create 'ProductionReadOnly' permission set (policies: ReadOnlyAccess). (3) Assign 'Developers' group → 'DeveloperPowerUser' → Dev accounts. (4) Assign 'Developers' group → 'ProductionReadOnly' → Prod accounts. Result: developers SSO into dev accounts with power user access, into prod with read-only. One central configuration, no per-account IAM management.", domain:1},
  {q:"A company uses Amazon ECS. They need containers to access sensitive configuration that changes frequently (updated multiple times per day). The configuration is stored in Systems Manager Parameter Store. How do containers get the LATEST value?", opts:["Set it at task launch time only (stale within hours)","ECS supports Parameter Store references in task definition — BUT values are resolved at task launch. For frequently-changing config: use the SSM SDK in the container to fetch the latest value at runtime, with a short cache TTL. Or use AppConfig with the ECS agent for dynamic configuration polling.","Environment variables are the only option","Mount a file from S3 with the configuration"], correct:1, exp:"ECS + dynamic config challenge: task definition 'secrets' and 'environment' are resolved at LAUNCH time. If config changes hourly and tasks run for days, they'll have stale values. Solutions: (1) Application polls SSM Parameter Store/AppConfig periodically (e.g., every 5 minutes) and refreshes in-memory config. (2) AWS AppConfig agent sidecar: polls for changes and exposes current config locally. (3) Short-lived tasks: if tasks are short, launch-time resolution is fine. For long-running services with frequent config changes: in-application polling is necessary.", domain:2},
  {q:"A company uses Route 53 Health Checks to monitor an endpoint in eu-west-1. They notice the health check is failing because their endpoint's security group only allows traffic from their corporate IP range, and Route 53 health checkers come from AWS IP ranges. How do they fix this?", opts:["Route 53 health checks can't be fixed (wrong — just need the right IPs allowed)","Allow Route 53 health checker IP ranges in the security group (AWS publishes these IP ranges) OR use a Route 53 health check type that doesn't connect to the endpoint: calculated health check or CloudWatch alarm-based health check (monitors a CloudWatch metric instead of making HTTP requests).","Disable health checks entirely","Move the endpoint to a public subnet without security groups"], correct:1, exp:"Two approaches: (1) Allow health checker IPs: AWS publishes Route 53 health checker IP ranges (in ip-ranges.json, service: ROUTE53_HEALTHCHECKS). Add them to the SG. (2) CloudWatch-alarm based health check: instead of Route 53 connecting to the endpoint, link the health check to a CloudWatch alarm. The alarm monitors a metric (from inside the VPC) — no external connectivity needed. Option 2 is better for private endpoints that shouldn't be accessible from public Route 53 IPs.", domain:2},
  {q:"A company uses Amazon Aurora Global Database. They need to perform a planned failover to the secondary region (e.g., for region migration or DR testing). What is the process?", opts:["Promote the secondary and delete the primary (destructive)","Aurora Global Database planned failover (switchover): demotes the current primary, promotes the selected secondary to new primary, and re-configures the old primary as a secondary — all automatically with RPO=0. The process ensures no data loss by waiting for all writes to replicate before switching.","Change DNS records manually","Stop writes, promote secondary, recreate replication"], correct:1, exp:"Aurora Global Database planned failover: (1) Initiate switchover from console/CLI. (2) Aurora stops writes to current primary (brief pause). (3) Ensures all data is replicated to target secondary (RPO=0). (4) Promotes secondary to new primary (starts accepting writes). (5) Old primary becomes secondary (replication reversed). (6) Applications using the global cluster endpoint automatically connect to new primary. Total planned switchover time: typically 1-2 minutes. For unplanned (disaster): 'detach and promote' (RPO = replication lag at time of failure).", domain:2},
  {q:"A company uses CloudWatch dashboards for monitoring. They have 50 microservices and creating individual dashboards is tedious. They want dashboards to auto-populate as new services are deployed. What approach enables this?", opts:["Manually create dashboards for each service (doesn't scale)","CloudWatch Automatic Dashboards (auto-generated per-service dashboards) OR use CloudWatch Dashboard API in your CI/CD pipeline to programmatically create/update dashboards during deployment. Also: Container Insights auto-dashboards for ECS/EKS services.","Third-party tools only (Grafana)","CloudWatch doesn't support dynamic dashboards"], correct:1, exp:"Automated dashboard approaches: (1) CloudWatch Automatic Dashboards: auto-generated per-service dashboards showing key metrics. (2) Infrastructure as Code: define dashboard JSON in CloudFormation/CDK — deployed with each service (dashboard updates automatically). (3) CloudWatch Dashboard API: CI/CD pipeline calls PutDashboard after each deployment. (4) Container Insights: automatic per-service dashboards for ECS/EKS. (5) Amazon Managed Grafana with CloudWatch data source: dynamic dashboards using Grafana variables. CDK/CloudFormation dashboards alongside service definitions is the most GitOps-aligned approach.", domain:2},
  {q:"A company uses S3 and wants to implement a 'soft delete' pattern — when users delete objects, they should be recoverable for 30 days before permanent deletion. What S3 features implement this?", opts:["Custom application logic tracking deletions","Enable S3 Versioning (delete operations add a delete marker — object is hidden but all versions are preserved). Add a lifecycle rule: expire non-current versions after 30 days. After 30 days: non-current versions are permanently deleted. Before that: remove the delete marker to restore.","S3 recycle bin (doesn't exist as a service)","S3 Object Lock in governance mode"], correct:1, exp:"Soft delete pattern with S3 Versioning: (1) Enable versioning on the bucket. (2) When user 'deletes': S3 adds a delete marker (object disappears from normal listings but previous versions remain). (3) To recover (within 30 days): delete the delete marker — object reappears. (4) Lifecycle rule: expire non-current versions after 30 days (permanent deletion). (5) Lifecycle rule: expire delete markers that have no non-current versions (cleanup). This provides: user-facing deletion + admin-recoverable + automatic permanent cleanup. Cost: pay for stored versions during the 30-day window.", domain:1},
  {q:"A company uses AWS Batch for running scientific simulations. Each simulation requires 64 vCPU and 256GB RAM for 4-6 hours. They run 50 simulations per day. They want to minimize cost. What compute environment should they configure?", opts:["On-Demand instances only (expensive for predictable workloads)","Managed compute environment with Spot Instances (50-90% cheaper) for the bulk of simulations. Use multi-instance-type configuration (r5.16xlarge, r6i.16xlarge, m5.16xlarge) for Spot availability. Set a lower-priority On-Demand job queue as fallback for urgent simulations.","Lambda functions (can't provide 256GB RAM)","Reserved Instances for Batch (Batch instances are ephemeral — RIs don't match well)"], correct:1, exp:"AWS Batch cost optimization: (1) Spot Instances for compute environment (60-90% savings). (2) Multiple instance types: ensures Spot capacity (if r5.16xlarge unavailable, Batch tries r6i.16xlarge). (3) Two job queues: high-priority (On-Demand, for urgent) and low-priority (Spot, for routine). (4) Jobs that checkpoint can tolerate Spot interruption (restart from checkpoint). For 50 sims/day × 5 hours × 64 vCPU: On-Demand ≈ $500/day. Spot at 70% off ≈ $150/day. Monthly savings: $10,500.", domain:4},
  {q:"A company uses Amazon API Gateway REST API with a Lambda backend. They want to set up a custom domain name (api.example.com) with HTTPS. What is required?", opts:["Point DNS to API Gateway's default URL (ugly invoke URL remains visible)","Create a custom domain in API Gateway → upload/reference an ACM certificate (must be in us-east-1 for edge-optimized, or same region for regional) → create an API mapping (base path to API/stage) → create a Route 53 alias record pointing api.example.com to the API Gateway domain name.","Custom domains require CloudFront separately","API Gateway doesn't support custom domains"], correct:1, exp:"Custom domain setup: (1) Request/import SSL certificate in ACM (us-east-1 for edge-optimized endpoints, regional endpoint = same region). (2) Create Custom Domain Name in API Gateway, select the ACM certificate. (3) Create API Mapping: map base path (e.g., / or /v1) to your API + stage. (4) Create DNS record: Route 53 alias (or CNAME) pointing your domain to the API Gateway domain name. Result: https://api.example.com routes to your API. Edge-optimized: uses CloudFront under the hood. Regional: direct to API Gateway in-region.", domain:3},
  {q:"A company uses Amazon Detective. What problem does Detective solve that GuardDuty and CloudTrail don't?", opts:["Detective blocks threats (it doesn't — it's investigative)","Detective automatically builds a GRAPH of related resources, accounts, and activities for security INVESTIGATION. After GuardDuty detects a threat, Detective helps analysts understand: what happened, what was affected, the blast radius, and root cause — using ML on CloudTrail, VPC Flow Logs, and GuardDuty findings. It connects the dots that would take hours of manual log correlation.","Detective is a newer version of GuardDuty","Detective replaces CloudTrail"], correct:1, exp:"AWS security service chain: GuardDuty DETECTS threats → Detective INVESTIGATES them. Detective: (1) Maintains a behavior graph (entities, relationships, activities over time). (2) When investigating a GuardDuty finding: shows all related entities (instances, users, IPs), unusual behaviors, API call patterns, and network flows. (3) Visualizes: who accessed what, when, from where, and what they did. (4) Identifies root cause and blast radius. Without Detective: analysts manually correlate CloudTrail + VPC Flow Logs + GuardDuty findings (hours). With Detective: automated correlation and visualization (minutes).", domain:1},
  {q:"A company uses Amazon S3 and CloudFront. They want to serve different versions of their website to users in different countries (e.g., different pricing pages for US vs EU). How can CloudFront handle this without separate deployments?", opts:["Create separate CloudFront distributions per country","Use CloudFront Functions or Lambda@Edge on Viewer Request — check the CloudFront-Viewer-Country header (populated by CloudFront), modify the request URI based on country (e.g., /us/pricing.html vs /eu/pricing.html), and serve country-specific content from the same S3 bucket.","CloudFront doesn't provide country information","Use Route 53 geolocation routing to different distributions"], correct:1, exp:"CloudFront geo-personalization: (1) Enable CloudFront-Viewer-Country header forwarding. (2) CloudFront Function (lightweight, fast) or Lambda@Edge: reads the country header, rewrites the request URI to the appropriate content path. (3) Cache policy includes the country header (separate cached versions per country). (4) All content lives in one S3 bucket organized by country. This is simpler and cheaper than separate distributions. CloudFront Functions execute at edge with <1ms latency — ideal for simple URL rewrites based on viewer attributes.", domain:3},
]
