export const questionBank12 = [
  // Final bank - Mixed high-difficulty scenarios
  {q:"A company runs an application on ECS that processes credit card transactions. PCI DSS requires network segmentation between cardholder data environment (CDE) and non-CDE systems. How should the ECS architecture be segmented?", opts:["Separate ECS clusters in different VPCs with strict Security Groups and NACLs between them","Separate task definitions with different Security Groups in the same cluster","Run CDE tasks on dedicated EC2 instances (not shared with non-CDE) in isolated subnets, with Network Firewall inspecting all traffic between segments","Use ECS Service Connect with mutual TLS between CDE and non-CDE services"], correct:2, exp:"PCI DSS requires strong network segmentation. Dedicated EC2 instances for CDE ensure no co-mingling at the host level. Isolated subnets with Network Firewall provide stateful inspection of all cross-segment traffic (logging who accesses what). Security Groups alone aren't sufficient for PCI — you need defense in depth with inspection. Separate VPCs provide isolation but Network Firewall provides the required traffic inspection and logging.", domain:1},
  {q:"A company uses S3 for storing data lake files. They accidentally made a bucket public for 2 hours before detecting and fixing it. They need to determine if any sensitive data was accessed during that window. Which combination of tools helps them investigate?", opts:["S3 Server Access Logs + Amazon Macie","CloudTrail S3 data events (showing GetObject requests during the window) + Amazon Macie (identifying which objects contain sensitive data)","AWS Config timeline showing the policy change + VPC Flow Logs","GuardDuty S3 findings + AWS Trusted Advisor"], correct:1, exp:"CloudTrail S3 data events log every GetObject request including source IP, time, and object key — this shows exactly what was accessed during the 2-hour window. Amazon Macie identifies which objects in the bucket contain sensitive data (PII, financial data). Cross-referencing: 'which sensitive objects were downloaded by external IPs during that window.' GuardDuty may alert on anomalous access but doesn't show individual object access detail.", domain:1},
  {q:"A company runs an Aurora PostgreSQL database. A compliance requirement mandates that all database queries (not just DDL/DML, but actual SELECT queries) must be logged with the full query text for audit. Which feature provides this?", opts:["CloudTrail logging for RDS API calls","Aurora Database Activity Streams sent to Kinesis for real-time audit logging","Enable PostgreSQL pgaudit extension with log_statement = 'all' and export logs to CloudWatch Logs","Enhanced Monitoring for database performance"], correct:1, exp:"Aurora Database Activity Streams captures all database activity (including full SQL query text) in near-real-time and streams to Kinesis Data Stream. From Kinesis, data can go to S3, OpenSearch, or a SIEM for compliance analysis. It captures everything including SELECTs with full query text. pgaudit also works but Activity Streams provides real-time streaming and tamper-proof capture (even the DBA can't disable without detection).", domain:1},
  {q:"A company needs their application to access resources in both AWS and Azure. The application runs on EC2 and needs to call Azure APIs using Azure AD credentials. They don't want to store long-lived Azure credentials on EC2. What is the BEST approach?", opts:["Store Azure client secrets in AWS Secrets Manager with rotation","Use AWS IAM Roles Anywhere with Azure AD as the trust anchor","Configure workload identity federation between AWS and Azure (EC2 instance role → Azure AD federated credential → Azure access token)","Use a VPN between AWS and Azure for credential pass-through"], correct:2, exp:"Workload identity federation allows EC2 instances to exchange their AWS IAM role credentials for Azure AD access tokens without storing any Azure secrets. The flow: EC2 assumes IAM role → gets AWS STS token → presents it to Azure AD (configured to trust AWS as identity provider) → receives Azure access token. Zero stored secrets. Secrets Manager still stores credentials. VPN doesn't handle identity.", domain:1},
  {q:"A company has a data pipeline: producers write events to Kinesis Data Streams. A Lambda consumer processes events and writes to DynamoDB. During peak hours, the Lambda function falls behind (iterator age increases to 5 minutes). The Kinesis stream has 10 shards. What should they do to reduce processing lag?", opts:["Increase the number of shards (resharding) to increase parallelism","Increase Lambda batch size and memory to process more records per invocation","Enable Lambda parallelization factor (up to 10 concurrent consumers per shard)","All of the above should be evaluated — parallelization factor first (no resharding cost), then batch optimization, then resharding as last resort"], correct:3, exp:"Parallelization factor (1-10) allows multiple Lambda invocations per shard concurrently (up to 10x throughput without resharding). This is the cheapest first step. Larger batch size + more memory processes more records per invocation (fewer invocations needed). Resharding doubles costs (more shards = more Lambda invocations + higher Kinesis cost). Start with parallelization factor, then optimize batch, then reshard only if still behind.", domain:3},
  {q:"A company uses CloudFormation and wants to avoid accidental deletion of production stacks (which would delete all resources). What protections should they implement?", opts:["Enable Stack Termination Protection and DeletionPolicy: Retain on critical resources (RDS, S3)","Use IAM policies denying cloudformation:DeleteStack for production stacks","Enable Stack Policy that prevents Update:Delete on critical resources","All three layers: Termination Protection (prevents accidental delete), DeletionPolicy: Retain (keeps resources if stack IS deleted), Stack Policy (prevents replacement during updates)"], correct:3, exp:"Defense in depth: Termination Protection requires explicitly disabling it before deletion (catches accidental clicks). DeletionPolicy: Retain ensures resources survive even if the stack is deleted (data safety net). Stack Policy prevents updates that would replace/delete specific resources (catches template changes that would destroy databases). Together they provide comprehensive protection against different failure modes.", domain:2},
  {q:"A company has an application that writes 10,000 records per second to DynamoDB. Each record is 2KB. They use provisioned capacity with 10,000 WCU. They start getting throttling errors. 10,000 records × 2KB = 20,000 WCU needed (each 1KB write = 1 WCU, 2KB write = 2 WCU). What is the cheapest fix?", opts:["Increase WCU to 20,000","Switch to on-demand capacity mode","Reduce item size below 1KB by compressing before storing","Buffer writes with SQS and write at a controlled rate below the provisioned capacity"], correct:0, exp:"The math: 2KB items require 2 WCU each (WCU = 1KB per write). At 10,000 writes/sec × 2 WCU = 20,000 WCU needed. They only have 10,000 WCU provisioned. Simple fix: increase to 20,000 WCU. On-demand mode works but is more expensive for predictable high throughput. Compression adds CPU overhead on every write/read. SQS buffering adds latency. The straightforward answer is matching provisioned capacity to actual need.", domain:3},
  {q:"A company's application uses an ALB with target groups in us-east-1. They want to add a disaster recovery region (eu-west-1) with automatic failover. Users should be routed to us-east-1 normally, and to eu-west-1 only if us-east-1 is unhealthy. What architecture achieves this?", opts:["Route 53 failover routing policy: primary record pointing to us-east-1 ALB with health check, secondary record pointing to eu-west-1 ALB","CloudFront with primary and failover origins (Origin Group)","Global Accelerator with endpoint groups in both regions","Route 53 latency-based routing with health checks"], correct:0, exp:"Route 53 failover routing with health checks is the standard DR pattern. Primary record (us-east-1 ALB) has a health check. If the health check fails, Route 53 automatically serves the secondary record (eu-west-1 ALB). Users always go to us-east-1 unless it's unhealthy. Global Accelerator also works but is more expensive. CloudFront origin failover works for static content but not ideal for dynamic API routing. Latency-based would split traffic.", domain:2},
  {q:"A company uses AWS Config to check compliance across their organization. They have 500 accounts and want to aggregate compliance data into a single dashboard. Which Config feature provides this?", opts:["Config Aggregator with organization-wide aggregation","Config rules deployed via CloudFormation StackSets to all accounts","Individual Config dashboards in each account viewed separately","AWS Security Hub integrating Config findings across accounts"], correct:0, exp:"AWS Config Aggregator collects compliance data from multiple accounts and regions into a single aggregation account. With Organizations integration, it automatically includes all accounts (current and future). One central dashboard shows cross-account compliance. StackSets deploy rules but don't aggregate results. Security Hub aggregates security findings but Config Aggregator is the native cross-account compliance view.", domain:1},
  {q:"A company wants to ensure that their EBS volumes are always encrypted. New volumes created from snapshots, launched by Auto Scaling Groups, or created manually must all be encrypted. What is the SIMPLEST enforcement mechanism?", opts:["IAM policy denying ec2:CreateVolume without encryption parameter","Enable 'EBS encryption by default' at the account level in each region — all new EBS volumes are automatically encrypted","AWS Config rule detecting unencrypted volumes with auto-remediation","SCP denying RunInstances without encrypted volumes"], correct:1, exp:"'EBS encryption by default' is an account-level setting (per region) that automatically encrypts ALL new EBS volumes, snapshots, and AMI copies — regardless of how they're created (console, API, Auto Scaling, from snapshot). Zero ongoing effort after enabling. IAM policies can be bypassed by admins. Config is detective. SCP works but is more complex. The account-level default is the simplest single action.", domain:1},
  {q:"A company runs a monolithic application on a single large EC2 instance (r5.8xlarge). They want to break it into microservices. The first service to extract is a PDF generation module that takes 30-60 seconds per request. What should this become?", opts:["Lambda function with 10GB memory (handles 30-60 second execution well within 15-minute timeout)","ECS Fargate task triggered by SQS","Separate EC2 instance dedicated to PDF generation","Step Functions workflow with Lambda"], correct:0, exp:"Lambda with high memory (up to 10GB, which also allocates proportional CPU) handles 30-60 second synchronous requests well within the 15-minute timeout. It scales automatically per request, scales to zero when idle, and requires no infrastructure management. This is the simplest serverless extraction. Fargate + SQS is async (adds complexity). EC2 requires management. Step Functions add unnecessary orchestration for a single-step operation.", domain:3},
  {q:"A company uses Amazon MQ (ActiveMQ) for their messaging between microservices. They want to migrate to a serverless, fully managed alternative that requires no broker management. The application uses request-reply pattern (send a message and wait for response). Which service is the BEST replacement?", opts:["Amazon SQS with temporary response queues","Amazon SNS with SQS subscriber","Amazon EventBridge with custom event bus","Step Functions with callback pattern"], correct:0, exp:"SQS supports request-reply pattern using temporary queues (or dedicated response queues with correlation IDs). It's fully serverless — no broker to manage. The requesting service sends to a request queue with a ReplyTo header pointing to its response queue. The consumer processes and sends the reply. SQS auto-scales with no capacity planning. EventBridge doesn't support request-reply natively. SNS is pub-sub only.", domain:3},
  {q:"A company's data science team needs to query a 50TB dataset stored in S3 (Parquet format, partitioned by date). They run complex analytical queries that take 20-30 minutes. They currently use Athena but the cost is $5/TB scanned and queries are slow. What provides BETTER performance for repeated complex queries?", opts:["Amazon Redshift Spectrum (external tables over S3 data, leveraging Redshift's MPP engine)","Increase Athena DPU allocation","Move all data into Redshift (COPY from S3)","Use EMR with Spark SQL"], correct:0, exp:"Redshift Spectrum queries S3 data using Redshift's MPP engine which is optimized for complex analytical queries (better query planner, result caching, materialized views). It costs $5/TB scanned same as Athena, but executes faster for complex joins. The data stays in S3 (no COPY needed). For repeated queries, Redshift result caching eliminates re-scanning. EMR Spark requires cluster management. Moving 50TB to Redshift storage is expensive.", domain:3},
  {q:"A company has an application that makes 1,000 requests per second to a third-party REST API. The third-party has rate limited them to 100 requests per second. How should the architecture handle this mismatch?", opts:["Cache responses in ElastiCache to reduce outbound requests","SQS queue with a Lambda consumer that is rate-limited (reserved concurrency controlling throughput to 100/sec)","API Gateway with throttling set to 100 requests per second","All approaches should be combined: cache for repeated requests + queue for smoothing bursts + rate limiting for protection"], correct:3, exp:"Multi-layer approach: Caching eliminates redundant outbound calls (if 80% of requests are the same, cache reduces to 200 unique/sec). SQS buffers spikes (absorbs burst while draining at 100/sec steady state). Rate limiting via reserved concurrency ensures you never exceed 100/sec to the third party. Together: cache reduces volume, queue buffers spikes, rate limit enforces ceiling. Any single approach may not handle all scenarios.", domain:3},
  {q:"A company needs to transfer data continuously from their on-premises data center to S3 with encryption in transit. The transfer should be automated, scheduled (every hour), and only transfer changed files. Bandwidth must be throttled to avoid impacting production traffic. Which service is designed for this?", opts:["AWS CLI s3 sync command in a cron job","AWS DataSync agent on-premises with a scheduled task (handles encryption, incremental transfer, and bandwidth throttling natively)","Storage Gateway File Gateway with S3","rsync over VPN with custom scripting"], correct:1, exp:"AWS DataSync is purpose-built for scheduled data transfers. It provides: incremental transfer (only changed files), TLS encryption in transit, bandwidth throttling, scheduling, verification checksums, CloudWatch metrics, and automatic retry. The on-premises agent handles all of this natively. S3 sync requires custom scripting for throttling and scheduling. Storage Gateway is for ongoing access, not scheduled transfers. rsync requires custom management.", domain:2},
  {q:"A company uses Step Functions to orchestrate an order processing workflow. One step calls a legacy payment system that takes 3-20 minutes to respond. The Step Functions execution must wait for the payment confirmation before proceeding. What integration pattern should be used?", opts:["Synchronous Lambda invocation with 15-minute timeout","Activity task that the legacy system polls for and reports completion","Task state with .waitForTaskToken — Lambda sends the token to the legacy system, which calls SendTaskSuccess when payment completes","Wait state with a fixed 20-minute delay followed by a check"], correct:2, exp:".waitForTaskToken is the callback pattern. Step Functions pauses (no cost while waiting), Lambda sends the task token to the legacy payment system. When payment completes, the legacy system (or a middleware) calls SendTaskSuccess with the token, resuming the workflow. Cost: one state transition. Activity tasks also work but require the legacy system to poll. Lambda timeout is 15 min max. Wait state wastes time and doesn't confirm completion.", domain:3},
  {q:"A company operates a multi-account AWS environment with consolidated billing. They want to track costs per team, per project, and per environment (dev/staging/prod) across all accounts. What is the MOST effective tagging and cost allocation strategy?", opts:["Create separate accounts for each team/project/environment combination","Implement mandatory cost allocation tags (Team, Project, Environment) enforced via SCP + IAM policies, activated in the billing console as cost allocation tags","Use AWS Organizations OUs to represent cost centers","Rely on account-level billing separation only"], correct:1, exp:"Cost allocation tags (when activated in the Billing console) appear in Cost Explorer and Cost & Usage Reports, enabling multi-dimensional cost analysis. Enforcing tags via SCPs (deny resource creation without required tags) ensures consistent tagging. This allows slicing costs by any combination: Team X's prod costs, Project Y's total costs, all dev costs across teams. Separate accounts per combination creates account sprawl. OUs alone don't tag individual resources.", domain:4},
  {q:"A company uses Elastic Beanstalk for a web application. During deployments, all instances are updated simultaneously, causing a brief outage. They want zero-downtime deployments. Which Elastic Beanstalk deployment policy should they use?", opts:["All at once (current setting — causes outage)","Rolling — updates instances in batches (some capacity available during deployment)","Rolling with additional batch — launches new instances first, then rolls the update (maintains full capacity)","Immutable — creates a new Auto Scaling Group with new instances, health checks pass, then swaps traffic"], correct:3, exp:"Immutable deployment provides zero-downtime with instant rollback capability. It launches a full set of new instances in a new ASG with the new version. After all new instances pass health checks, traffic switches. If anything fails, the new ASG is terminated (old one untouched). Rolling with additional batch maintains capacity but can leave mixed versions during deployment. Immutable is the safest zero-downtime option.", domain:2},
  {q:"A company stores customer photos in S3. They need to generate thumbnails automatically when photos are uploaded. The thumbnail generation takes 3-5 seconds per photo. Peak upload rate is 1,000 photos per minute. What is the MOST operationally simple architecture?", opts:["EC2 instance polling S3 for new objects and generating thumbnails","Lambda function triggered by S3 PUT event notification, generating thumbnail and storing back in S3 (different prefix or bucket)","ECS Fargate task triggered by EventBridge S3 events","Step Functions triggered by S3 event with Lambda generating thumbnail"], correct:1, exp:"S3 event notification → Lambda is the simplest serverless pattern for object-level processing. Lambda scales automatically to handle 1,000 events/minute (17/sec — well within default concurrency). 3-5 seconds per thumbnail fits easily in Lambda's timeout. Output goes to a different S3 prefix (/thumbnails/) or bucket. No infrastructure to manage. EC2 requires polling and management. ECS/Step Functions add unnecessary complexity for this simple use case.", domain:3},
  {q:"A company uses AWS WAF on their CloudFront distribution. They want to block requests that: (1) exceed 2,000 requests per 5 minutes from a single IP, (2) contain SQL injection patterns, (3) come from known bot networks. Which WAF rule types cover all three?", opts:["Custom rule for rate limiting + AWS Managed Rules SQLi rule set + AWS Managed Rules Bot Control","Rate-based rule only — it handles all three scenarios","IP reputation lists from AWS Shield Advanced","Geographic restriction in CloudFront"], correct:0, exp:"Rate-based rule (type=rate-based, limit=2000, period=5 min) handles rate limiting per IP. AWS Managed Rules Core Rule Set includes SQLi detection. AWS Managed Rules Bot Control identifies and blocks known bots and bot networks. Three rule types in a single WAF WebACL provide all three protections. Rate-based alone can't detect SQL injection or bot patterns. Shield is DDoS, not application-layer. Geo-restriction is country-based, not IP-based.", domain:1},
  {q:"A company needs to run a Windows-based application that requires Microsoft SQL Server with Always On Availability Groups for high availability. They want AWS to manage the infrastructure but must maintain SQL Server Enterprise features. Which option provides this?", opts:["RDS for SQL Server Multi-AZ (uses SQL Server Always On Availability Groups under the hood for Enterprise edition)","SQL Server on EC2 with self-managed Always On AG","Amazon Aurora (not compatible with SQL Server)","RDS for SQL Server with read replicas"], correct:0, exp:"RDS for SQL Server with Multi-AZ uses Always On Availability Groups (for SQL Server Enterprise edition) to provide synchronous replication and automatic failover. AWS manages the infrastructure (patching, backups, failover). You get Enterprise features in a managed service. EC2 works but you manage everything. Aurora is MySQL/PostgreSQL only. RDS SQL Server read replicas are separate from Multi-AZ.", domain:2},
  {q:"A company's application needs to send 100,000 emails per day (order confirmations, password resets, newsletters). They need high deliverability, bounce/complaint handling, and analytics. Which AWS service handles this?", opts:["Amazon SNS with email subscriptions","Amazon SES (Simple Email Service) with dedicated IPs, bounce handling, and sending statistics","Amazon Pinpoint email channel","Lambda sending emails via SMTP"], correct:1, exp:"Amazon SES is the purpose-built email service for high-volume transactional and marketing emails. Features: dedicated IPs for reputation management, automatic bounce/complaint handling via SNS notifications, sending statistics and deliverability dashboards, suppression list management, and DKIM/SPF authentication. SNS email is for notifications only (no analytics, poor deliverability). Pinpoint is for marketing campaigns. Lambda SMTP is unmanaged.", domain:3},
  {q:"A company has a REST API that returns large JSON responses (5MB average). Clients report slow downloads, especially on mobile networks. The data is compressible (JSON compresses 80-90%). What is the FASTEST improvement?", opts:["Enable GZIP compression in the API (response header Content-Encoding: gzip)","Put CloudFront in front of the API with automatic compression enabled","Paginate the API to return smaller responses","Use Protocol Buffers instead of JSON"], correct:1, exp:"CloudFront automatic compression compresses responses (gzip/brotli) when the viewer supports it (Accept-Encoding header). A 5MB JSON response compresses to ~500KB-1MB — 5-10x smaller transfer. CloudFront also caches at edge (if cacheable). This is the fastest improvement: add CloudFront, enable compression. API-level gzip also works but CloudFront additionally provides edge caching + global acceleration. Pagination changes the API contract.", domain:3},
  {q:"A company uses AWS Organizations with 100 accounts. They want to ensure that if a new AWS service is launched, NO account can use it until the security team explicitly approves it. Which SCP strategy achieves this?", opts:["Deny all by default with an allow-list of approved services (Deny * then Allow specific services)","Allow all by default with explicit denies for unapproved services","Use AWS Config to detect use of new services and notify the security team","Disable new service access at the Organization level"], correct:0, exp:"Allow-list SCP strategy: attach an SCP that denies all actions (Effect: Deny, Action: *) then attach another SCP that allows only approved services. Any NEW service is automatically denied because it's not in the allow-list. When security approves a new service, they add it to the allow SCP. Deny-list (option B) requires updating the deny list for EVERY new service launched — new services are accessible by default until explicitly denied. Allow-list is more secure.", domain:1},
  {q:"A company's application on EC2 needs to call AWS APIs (S3, DynamoDB). The security team mandates that no long-term credentials (access keys) should be used. What is the correct approach?", opts:["Create an IAM role with necessary permissions and attach it to the EC2 instance (instance profile)","Create an IAM user and store credentials in Secrets Manager, retrieved at startup","Use the EC2 metadata service to get temporary credentials directly","Use AWS STS AssumeRole from the application code"], correct:0, exp:"IAM roles for EC2 (via instance profiles) provide automatic temporary credentials that are rotated automatically. The application uses the SDK which automatically retrieves credentials from the instance metadata service. No long-term credentials stored anywhere. Secrets Manager with access keys still uses long-term credentials (just stored more securely). The instance metadata service IS how the role's temporary credentials are delivered (option C is the mechanism, A is the correct configuration).", domain:1},
  {q:"A company runs a Python application on Lambda that imports heavy ML libraries (TensorFlow, NumPy). Cold starts take 15 seconds due to importing these libraries. Users experience timeout on first requests. How should cold starts be eliminated?", opts:["Increase Lambda memory (more CPU for faster import)","Use Lambda Provisioned Concurrency (keeps environments initialized and ready)","Move initialization outside the handler function","Both B and C: provisioned concurrency eliminates cold starts, and external initialization ensures the init phase is optimized"], correct:3, exp:"Provisioned Concurrency keeps Lambda execution environments warm (pre-initialized) — NO cold starts for up to the provisioned amount. Combined with placing heavy imports in the init phase (outside handler), the initialization cost is paid once per environment and environments stay warm. Memory increase speeds up cold starts but doesn't eliminate them. Provisioned Concurrency is the only way to guarantee zero cold starts.", domain:3},
  {q:"A company uses Amazon OpenSearch for application logs. The cluster has 3 data nodes with 1TB EBS each (3TB total). They retain 30 days of logs (100GB/day = 3TB total). They need to reduce costs without losing older data. What should they implement?", opts:["Delete logs older than 7 days","Use UltraWarm nodes for data older than 7 days (warm storage: 80% cheaper, still queryable) and cold storage for data older than 14 days","Reduce the number of replicas from 1 to 0","Compress logs before indexing"], correct:1, exp:"OpenSearch UltraWarm provides warm storage at ~80% less cost than hot (standard data nodes). Data is still queryable but with slightly higher latency. Cold storage (S3-backed) is even cheaper for rarely accessed data. Lifecycle policy: 0-7 days hot (fast queries), 7-14 days warm (lower cost, slightly slower), 14-30 days cold (cheapest, slow but available). This tiered approach maintains 30-day retention at much lower cost. Deleting logs loses audit capability.", domain:4},
  {q:"A company has a legacy application that communicates with an on-premises mainframe via MQ Series (IBM MQ). They're migrating the application to AWS but the mainframe stays on-premises. How should the messaging bridge be maintained?", opts:["Amazon MQ (supports ActiveMQ and RabbitMQ, but NOT IBM MQ directly)","Amazon SQS with a custom bridge application on EC2 that connects to IBM MQ and forwards messages to SQS","Use AWS Transfer Family for mainframe integration","Deploy IBM MQ on EC2 in AWS and connect via Direct Connect to the on-premises mainframe"], correct:3, exp:"IBM MQ on EC2 maintains full compatibility with the mainframe's MQ Series protocol. Direct Connect provides the reliable, low-latency connection between AWS and on-premises. The application connects to the EC2-hosted MQ instance. No application changes needed — same MQ protocol. Amazon MQ doesn't support IBM MQ protocol. Custom SQS bridge requires writing and maintaining translation logic. Transfer Family is for file transfers.", domain:2},
  {q:"A company runs a web application that stores user-uploaded images. Images must be served from a custom domain (images.example.com) over HTTPS with low latency globally. Images are stored in S3. The bucket name is 'my-app-images-prod'. Which is the correct setup?", opts:["S3 static website hosting with Route 53 CNAME to the S3 endpoint","CloudFront distribution with S3 origin (using OAC), ACM certificate for images.example.com (in us-east-1), and Route 53 alias record for images.example.com pointing to CloudFront","Direct S3 access with HTTPS (S3 supports HTTPS natively with AWS certificate)","ALB routing to a Lambda function that retrieves from S3"], correct:1, exp:"CloudFront + S3 + ACM is the standard pattern for serving S3 content over HTTPS on a custom domain globally. ACM certificate MUST be in us-east-1 for CloudFront. OAC (Origin Access Control) restricts S3 access to only CloudFront. Route 53 alias maps the custom domain to CloudFront. S3 static website hosting doesn't support HTTPS with custom domains. Direct S3 custom domain HTTPS isn't supported. ALB+Lambda is overcomplicated.", domain:3},
  {q:"A company has an API that writes to DynamoDB. During a flash sale, write traffic increases 100x for 10 minutes. The DynamoDB table uses provisioned capacity. Auto-scaling cannot react fast enough (takes 5-10 minutes). How should they handle the spike without data loss?", opts:["Pre-scale the DynamoDB table before the sale (if timing is known)","Switch to on-demand capacity mode before the sale","Put an SQS queue in front of DynamoDB — writes go to SQS, a Lambda consumer writes to DynamoDB at a controlled rate","Option A or B if the timing is known; Option C if timing is unpredictable — SQS buffers ensure zero data loss regardless"], correct:3, exp:"If the sale time is known: pre-scale provisioned capacity or switch to on-demand (which handles known-time spikes). If timing is unpredictable or the spike is too sudden: SQS queue buffers all writes (no data loss, SQS scales to any throughput). Lambda consumer drains the queue at DynamoDB's actual capacity. Messages persist in SQS until successfully written. This ensures zero data loss regardless of DynamoDB's scaling speed.", domain:2},
  {q:"A company's security team wants to continuously monitor for: public S3 buckets, unrestricted Security Groups (0.0.0.0/0 on SSH), and IAM users with console access but no MFA. They want a single dashboard with remediation recommendations. Which service provides this?", opts:["AWS Config with managed rules for each check","AWS Security Hub (aggregates findings from Config, GuardDuty, Inspector, Macie, and provides a unified security posture dashboard with automated remediation)","AWS Trusted Advisor security checks","Amazon GuardDuty"], correct:1, exp:"Security Hub provides a unified security dashboard aggregating findings from multiple sources (Config rules, GuardDuty, Inspector, Macie, Firewall Manager). It includes security standards (CIS Benchmarks, AWS Foundational) that check for public S3, open SGs, and MFA compliance. Provides remediation recommendations and supports automated remediation via EventBridge + Lambda/SSM. Config provides the rules but Security Hub provides the aggregated dashboard and standards.", domain:1},
  {q:"A company operates a video conferencing application. They need ultra-low latency (< 10ms) communication between servers in the same availability zone. The servers exchange large amounts of data (100 Gbps between them). Which networking feature provides the lowest latency and highest bandwidth?", opts:["Enhanced Networking (ENA) with jumbo frames","Cluster Placement Group with Enhanced Networking (instances on the same physical rack)","Elastic Fabric Adapter (EFA) for HPC-grade networking","Dedicated Hosts in the same rack"], correct:1, exp:"Cluster Placement Group places instances on the same physical rack, providing the lowest inter-instance latency (single-digit microseconds) and highest bandwidth (up to 100 Gbps with enhanced networking). This is ideal for tightly-coupled applications needing ultra-low latency communication. EFA provides similar performance but is designed for HPC (MPI) workloads. Enhanced Networking alone doesn't guarantee physical proximity.", domain:3},
  {q:"A company needs to store 100 million small objects (1KB each) in S3 and access them by a unique key with consistent low-latency reads (< 50ms p99). They read 100,000 objects per second. Will S3 meet these requirements?", opts:["No — S3 is object storage and can't handle 100K reads/sec with consistent latency","Yes — S3 supports 5,500 GET requests per second per prefix. With hash-distributed prefixes, 100K/sec is achievable with consistent single-digit millisecond latency","Partially — S3 handles the throughput but latency will be inconsistent (50-200ms)","They should use DynamoDB instead for this access pattern"], correct:1, exp:"S3 supports 5,500 GET/sec per prefix. With properly distributed key prefixes (e.g., hash prefix), 100K/sec is easily achievable (20 prefixes × 5,500 = 110K/sec capacity). S3 provides consistent single-digit to low-double-digit millisecond latency for GETs. For 1KB objects, this is well within S3's sweet spot. S3 automatically partitions popular prefixes for higher performance. DynamoDB would also work but S3 is cheaper for this access pattern.", domain:3},
  {q:"A company uses AWS Backup for automated daily backups of their Aurora database, EBS volumes, and EFS file systems. They need to ensure backups are copied to another region for disaster recovery within 24 hours. How should this be configured?", opts:["AWS Backup cross-region backup copy rules in the backup plan (automatically copies backups to a destination region)","Manual Lambda function triggered daily to copy snapshots cross-region","DMS continuous replication to another region","Create separate backup plans in each region"], correct:0, exp:"AWS Backup supports cross-region copy rules directly in the backup plan configuration. When a backup is created, it's automatically copied to the specified destination region according to the schedule. This works for all supported resources (Aurora, EBS, EFS, DynamoDB). Fully automated, no Lambda needed. DMS is for live replication, not backup. Separate plans in each region would create independent backups, not copies.", domain:2},
  {q:"A company uses Transit Gateway to connect 50 VPCs. They notice that Transit Gateway data processing charges are $8,000/month because ALL inter-VPC traffic routes through it, even between VPCs that communicate heavily. How can they reduce this specific cost?", opts:["Add VPC peering connections between the highest-traffic VPC pairs (peering has no data processing charge) while keeping TGW for the rest","Use smaller instance types to reduce traffic volume","Compress all data before sending between VPCs","Remove Transit Gateway and use full-mesh VPC peering"], correct:0, exp:"VPC peering has NO data processing charges (only standard data transfer rates). For the highest-traffic VPC pairs, direct peering eliminates the Transit Gateway's $0.02/GB data processing charge. Keep Transit Gateway for general connectivity (50 VPCs would need 1,225 peering connections for full mesh — impractical). Hybrid approach: TGW for general, peering for heavy traffic pairs. This is a common cost optimization pattern.", domain:4},
  {q:"A company runs a globally distributed application. They need a database that supports multi-region active-active writes with automatic conflict resolution, single-digit millisecond latency for reads and writes in each region, and scales to millions of requests per second. Which service provides this?", opts:["Aurora Global Database (single writer, multiple reader regions)","DynamoDB Global Tables (active-active multi-region with last-writer-wins conflict resolution)","CockroachDB on EC2 (distributed SQL with multi-region writes)","Amazon Keyspaces (Cassandra-compatible, multi-region)"], correct:1, exp:"DynamoDB Global Tables provides: active-active writes in multiple regions (any region can write), automatic conflict resolution (last-writer-wins by timestamp), single-digit ms latency in each region (local table), and scales to millions of requests/sec per region. Aurora Global Database has a SINGLE writer region. Keyspaces supports multi-region but isn't as mature for this specific pattern. DynamoDB Global Tables is the AWS-native solution.", domain:3},
  {q:"A company needs to implement a data retention policy: data older than 90 days must be automatically deleted from DynamoDB tables. Items have a 'createdAt' timestamp attribute. What is the MOST operationally simple approach?", opts:["Scheduled Lambda function that scans the table and deletes old items","Enable DynamoDB TTL on a 'expiresAt' attribute that is set to createdAt + 90 days (in epoch seconds)","DynamoDB Streams with Lambda filtering for old items","AWS Glue job that periodically cleans up old records"], correct:1, exp:"DynamoDB TTL automatically deletes items when the specified attribute's epoch timestamp passes. Set 'expiresAt' = createdAt + (90 * 86400) at write time. DynamoDB background process deletes expired items (typically within 48 hours of expiry) at no WCU cost. Zero operational overhead after setup. Scan + delete consumes WCU and requires managing a Lambda. Streams add complexity. Glue is overkill for simple deletion.", domain:4},
  {q:"A company needs to deploy identical infrastructure in 10 AWS regions for a global application. Changes to infrastructure must be applied to ALL regions simultaneously from a single template. Which tool and approach achieves this?", opts:["Terraform with modules for each region","CloudFormation StackSets targeting all 10 regions with auto-deployment enabled","Manually deploying CloudFormation stacks in each region via a script","AWS CDK with multi-region deployment pipeline"], correct:1, exp:"CloudFormation StackSets deploy a single template to multiple regions and accounts simultaneously. With auto-deployment (in Organizations), new accounts/regions are automatically included. Changes to the template are propagated to all regions. This is the AWS-native tool for multi-region infrastructure deployment from a single source. Terraform modules work but require TF-specific tooling. CDK generates CloudFormation but still needs StackSets or a pipeline for multi-region.", domain:2},
]
