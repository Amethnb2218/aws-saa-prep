export const questionBank20 = [
  {q:"A company uses AWS Global Accelerator. How does it differ from CloudFront?", opts:["They are the same service","Global Accelerator provides static Anycast IPs that route TCP/UDP traffic to optimal AWS endpoints (ALB, NLB, EC2, EIP) via the AWS global network. CloudFront caches HTTP content at edge locations. Accelerator is for non-cacheable traffic (APIs, gaming, VoIP); CloudFront is for cacheable content (web, video, static files).","Global Accelerator replaces CloudFront","CloudFront supports TCP/UDP like Global Accelerator"], correct:1, exp:"Key differences: (1) CloudFront: HTTP/HTTPS content caching at 400+ edge locations (CDN). (2) Global Accelerator: network-layer optimization for ANY TCP/UDP traffic — routes via AWS backbone (less hops, lower latency), provides static Anycast IPs, and instant failover between regions. Use CloudFront for: websites, APIs with cacheable responses, streaming. Use Global Accelerator for: gaming, IoT, VoIP, non-HTTP protocols, or when you need static IPs.", domain:3},
  {q:"A company needs to run containers but doesn't want to manage the underlying infrastructure at all. They have variable workloads (0-100 containers). Which compute option eliminates ALL infrastructure management?", opts:["ECS on EC2 (must manage EC2 instances)","ECS on Fargate (serverless containers — no instances to manage, scales from 0 to thousands, pay per vCPU/memory/second)","EKS on EC2 (must manage node groups)","Docker on EC2 (full management responsibility)"], correct:1, exp:"Fargate is serverless containers: specify CPU/memory per task, deploy container image, Fargate handles everything else (instance provisioning, patching, scaling, placement). No EC2 instances to manage, no capacity planning. Scales from zero (no cost when idle) to thousands of tasks. Pay per vCPU-second and GB-second. For 0-100 variable containers: Fargate charges only for running tasks. ECS/EKS on EC2 require managing and paying for instances even when idle.", domain:2},
  {q:"A company uses S3 and wants to ensure that objects are encrypted with a SPECIFIC KMS key (not the default S3 key or any other key). How can they enforce this at the bucket level?", opts:["Enable default encryption with the specific KMS key","Bucket policy with a Condition denying PutObject requests where the encryption key ARN (s3:x-amz-server-side-encryption-aws-kms-key-id) does NOT match the required key ARN","Both A and B — default encryption uses the key if none specified, but the policy ENFORCES that no other key can be used","KMS key policy alone handles this"], correct:2, exp:"Two-layer approach: (1) Default encryption: if a PutObject doesn't specify a key, S3 uses the configured default KMS key. Handles the 'forgot to specify' case. (2) Bucket policy with Condition: DENIES any PutObject that specifies a DIFFERENT key. Handles the 'specified wrong key' case. Together: objects are always encrypted with the required key — either by default (unspecified) or by enforcement (specified but wrong → denied). Default encryption alone doesn't prevent using a different key explicitly.", domain:1},
  {q:"A company uses Amazon Timestream for IoT time-series data. What makes Timestream different from DynamoDB for time-series workloads?", opts:["Timestream is just DynamoDB with a different name","Timestream is purpose-built for time-series: automatic data lifecycle (recent data in memory, older in magnetic storage), built-in time-series functions (interpolation, smoothing, aggregation), automatic scaling, and faster queries on time-range data than DynamoDB.","DynamoDB is better for all time-series workloads","Timestream doesn't support real-time ingestion"], correct:1, exp:"Amazon Timestream advantages for time-series: (1) Automatic tiering: hot data in memory (fast queries), cold data in magnetic (cheap storage) — no manual lifecycle rules. (2) Built-in analytics functions: time_series_interpolate, derivative, moving_average, bin() for time bucketing. (3) SQL with time-series extensions. (4) Adaptive query processing optimized for time-range queries. DynamoDB CAN store time-series but requires manual TTL, lacks analytical functions, and queries are less efficient for time-range patterns.", domain:3},
  {q:"A company has EC2 instances in a private subnet that need to access the internet for software updates. They use a NAT Gateway. The NAT Gateway costs $0.045/hour + $0.045/GB processed. They want to reduce NAT costs. What options exist?", opts:["NAT Gateway pricing is fixed — no way to reduce costs","Use NAT Instances instead (cheaper but less managed/available). OR use VPC endpoints for AWS services (S3, DynamoDB, ECR — free Gateway endpoints bypass NAT). OR use S3-hosted package repositories instead of internet repos.","Remove the NAT Gateway and use a proxy server","Switch to IPv6 (egress-only internet gateway is free for IPv6 traffic to the internet)"], correct:1, exp:"NAT cost reduction strategies: (1) VPC Gateway Endpoints for S3/DynamoDB (free, traffic bypasses NAT). (2) VPC Interface Endpoints for other AWS services (ECR, CloudWatch, SSM — per-hour cost but less than NAT data processing). (3) NAT Instance (t3.micro) for low-traffic environments (much cheaper per-hour than NAT GW). (4) Centralized NAT in a shared VPC with Transit Gateway (amortize across accounts). (5) S3-hosted package mirrors. Analysis: often 50-80% of NAT traffic is to AWS services — endpoints eliminate this.", domain:4},
  {q:"A company runs a machine learning training job that takes 12 hours and uses 8 GPU instances (p3.16xlarge). The job is fault-tolerant (checkpoints every 30 minutes and can resume from checkpoint). How can they reduce costs by 60-70%?", opts:["Reserved Instances for GPU instances (requires 1-year commitment for infrequent jobs)","Spot Instances — up to 90% cheaper than On-Demand. If Spot is interrupted: job resumes from last checkpoint (max 30 min lost). Use Spot Fleet with multiple instance types for availability.","Smaller instance types (would take longer, same or more total cost)","Negotiate custom pricing with AWS"], correct:1, exp:"ML training jobs are ideal Spot candidates: fault-tolerant (checkpointing), can resume from interruption, flexible on instance type. p3.16xlarge On-Demand: ~$24/hour × 8 × 12 hours = ~$2,304. Spot (~70% off): ~$691. Savings: ~$1,613. Best practices: (1) Checkpoint every 30 min to S3, (2) Use multiple instance types/AZs for availability, (3) Handle Spot interruption (2-min warning) gracefully, (4) Use Spot Fleet or EC2 Fleet for diversification.", domain:4},
  {q:"A company uses Amazon DocumentDB (MongoDB compatible). What is the key difference between DocumentDB and running MongoDB on EC2?", opts:["No difference — DocumentDB IS MongoDB","DocumentDB is a fully managed service: automatic backups, Multi-AZ replication, automatic failover, patching, and scaling — while being MongoDB-compatible (wire protocol level). You don't manage servers, replication, or backups. Trade-off: not 100% MongoDB feature-complete.","DocumentDB is cheaper than self-managed MongoDB","DocumentDB supports all MongoDB features including the latest version"], correct:1, exp:"Amazon DocumentDB: MongoDB-compatible (drivers, query syntax, tools work) but NOT MongoDB itself. It uses Aurora's storage architecture: shared cluster volume, up to 15 read replicas, automatic 6-way replication across 3 AZs. Managed: backups, patching, failover, encryption. Limitation: compatible with MongoDB 3.6/4.0/5.0 APIs but may not support every MongoDB feature (especially newest versions). For most MongoDB workloads, DocumentDB provides operational simplicity without code changes.", domain:2},
  {q:"A company uses CloudFormation to deploy a Lambda function and an API Gateway. They want to test the API before it goes live. They use CloudFormation Change Sets. What do Change Sets show?", opts:["Change Sets execute the deployment","Change Sets show a PREVIEW of what CloudFormation WILL change (create, modify, delete) without actually executing. Review which resources will be added, modified (and whether it's a replacement or update), or deleted — then decide to execute or cancel.","Change Sets show the template differences only","Change Sets validate IAM permissions"], correct:1, exp:"CloudFormation Change Sets: create a change set → CloudFormation analyzes what would change → shows you: (1) NEW resources to create, (2) MODIFIED resources (update-in-place vs replacement), (3) DELETED resources. Critically: 'Replacement' means the resource will be DELETED and recreated (potential downtime, new physical ID). Review this before executing. If acceptable: execute the change set. If risky: discard it and modify the template. Change Sets are essential for safe production updates.", domain:2},
  {q:"A company uses Amazon Managed Grafana for dashboards. They need to visualize data from multiple sources: CloudWatch, Prometheus, X-Ray, and a custom PostgreSQL database. Does Managed Grafana support this?", opts:["Managed Grafana only supports CloudWatch","Yes — Amazon Managed Grafana supports multiple data sources: CloudWatch, Prometheus (AMP), X-Ray, Athena, Redshift, Timestream, OpenSearch, PostgreSQL, MySQL, and more. It provides pre-built dashboards for AWS services and supports cross-account/cross-region data visualization.","Grafana can only display one data source per dashboard","Must use self-managed Grafana for multiple data sources"], correct:1, exp:"Amazon Managed Grafana: fully managed Grafana with AWS-native integrations. Supports 30+ data sources including all major AWS services and external databases. Key features: (1) SSO via IAM Identity Center, (2) Fine-grained dashboard permissions, (3) Cross-account CloudWatch access, (4) Pre-built dashboards for AWS services, (5) Alerting to SNS/PagerDuty/Slack. Eliminates Grafana server management while providing the full Grafana visualization experience for multi-source observability.", domain:2},
  {q:"A company uses Auto Scaling. They want to ensure their ASG always has instances in at least 2 Availability Zones for high availability, even if one AZ becomes unavailable. What ASG setting controls AZ distribution?", opts:["Configure AZ rebalancing (default behavior)","Specify multiple AZs in the ASG configuration AND set minimum capacity ≥ 2. ASG automatically distributes instances across specified AZs. If one AZ fails, ASG launches replacement instances in healthy AZs.","Use Placement Groups for AZ distribution","Manually launch instances in different AZs"], correct:1, exp:"ASG Multi-AZ: specify 2+ AZs in the ASG configuration. ASG automatically balances instances across AZs (AZ rebalancing). If an AZ becomes unhealthy: ASG detects unhealthy instances, terminates them, and launches replacements in HEALTHY AZs. With min=2 across 2 AZs: normally 1 per AZ. If AZ fails: both instances in the remaining AZ. This is the fundamental ASG HA pattern. Always use ≥2 AZs. For critical workloads: use 3 AZs.", domain:2},
  {q:"A company uses Amazon Kinesis Data Analytics (now Managed Service for Apache Flink). They want to detect anomalies in streaming data in real-time (e.g., sudden spike in error rates). What capability does Flink provide?", opts:["Only batch processing (not real-time)","Real-time stream processing with windowing functions — tumbling, sliding, and session windows enable aggregation over time periods. Use Flink SQL or Java/Python to calculate metrics per window and trigger alerts when thresholds are breached. Sub-second latency.","Only data delivery to destinations (like Firehose)","Requires ML model training in SageMaker"], correct:1, exp:"Apache Flink on AWS (Managed Service for Apache Flink): stateful stream processing with sub-second latency. For anomaly detection: (1) Define windows (5-minute tumbling) to aggregate error counts, (2) Compare current window to historical baseline, (3) Trigger alert (to SNS/Lambda) when deviation exceeds threshold. Flink SQL makes this accessible: SELECT COUNT(*) as errors, TUMBLE_START(event_time, INTERVAL '5' MINUTE) FROM stream WHERE status='error' GROUP BY TUMBLE(event_time, INTERVAL '5' MINUTE) HAVING errors > 100.", domain:3},
  {q:"A company uses an S3 bucket for storing application data. They notice S3 PUT request costs are high because the application makes millions of tiny PUT requests (1KB each). Each PUT costs $0.005 per 1,000 requests. How can they reduce PUT costs?", opts:["S3 PUT pricing is fixed — can't reduce","Batch small objects into larger objects before uploading (e.g., aggregate 1000 1KB objects into one 1MB object). Reduces PUTs by 1000x. Alternatively, use S3 Express One Zone for high-request workloads (different pricing model).","Use multipart upload for small objects (multipart adds overhead for small files)","Change storage class (class doesn't affect request costs)"], correct:1, exp:"Request cost optimization: million 1KB PUTs = $5,000 per million × many millions = expensive. Solutions: (1) Client-side batching: aggregate records into larger objects (common pattern: buffer 1000 records → PUT as one object). Use Kinesis Firehose to automatically batch and deliver to S3. (2) S3 Express One Zone: 50% cheaper request pricing for high-request workloads. (3) Application redesign to reduce PUT frequency. Batching is the most impactful — reduces both request count and per-request overhead.", domain:4},
  {q:"A company uses AWS Config to track resource configuration changes over time. They want to see the COMPLETE history of a specific EC2 instance's configuration (security groups, IAM role, instance type changes over 6 months). Where do they find this?", opts:["CloudTrail event history (shows API calls but not configuration state)","AWS Config Resource Timeline — shows the complete configuration history of a resource: every configuration change, when it changed, what changed, and who changed it (via CloudTrail correlation). Visual timeline of the resource's configuration over time.","EC2 instance metadata","CloudWatch Logs"], correct:1, exp:"AWS Config records resource configuration as 'Configuration Items' (CIs) — snapshots of a resource's complete configuration at a point in time. When a configuration changes: new CI is recorded. The Resource Timeline shows: (1) All configuration states over time, (2) What specifically changed between states (diff), (3) Compliance status at each point, (4) Related CloudTrail event (who made the change). This provides a complete audit history of resource configuration — essential for compliance and troubleshooting.", domain:1},
  {q:"A company has a Lambda function that occasionally times out after 15 minutes (the maximum). The function processes variable-size files (some very large). They can't reduce processing time. What alternatives exist for processing that exceeds 15 minutes?", opts:["Increase Lambda timeout beyond 15 minutes (not possible — 15 minutes is the hard max)","Use AWS Step Functions to orchestrate the processing: split the work into chunks, each processed by a separate Lambda invocation (map state). Or move to ECS Fargate/Batch for longer-running tasks.","Lambda supports up to 60 minutes (incorrect)","Use provisioned concurrency to extend timeout (doesn't affect timeout limit)"], correct:1, exp:"Lambda hard limit: 15 minutes. For longer processing: (1) Step Functions Map state: split large file into chunks → process each chunk in parallel Lambda invocations (each <15 min). (2) ECS Fargate: no time limit, containerized processing. (3) AWS Batch: for compute-intensive jobs with no time limit and Spot pricing. (4) Redesign: stream processing instead of batch. Step Functions is ideal when the work is parallelizable. Fargate/Batch for sequential long-running processes.", domain:2},
  {q:"A company uses AWS WAF. They want to automatically block IP addresses that make more than 2000 requests in 5 minutes (rate limiting for DDoS protection). What WAF rule type handles this?", opts:["Regular match rule with IP Set","Rate-based rule — counts requests per IP address within a 5-minute window. When a source IP exceeds the threshold (minimum 100 requests per 5 minutes), WAF automatically blocks subsequent requests from that IP until the rate drops below the threshold.","AWS Shield handles all rate limiting","Geographic restriction rule"], correct:1, exp:"WAF Rate-based rules: define a threshold (100-20,000,000 requests per 5-minute window). WAF counts requests per source IP. When exceeded: automatically blocks that IP (returns 403). When rate drops below threshold: automatically unblocks. Can be combined with conditions (only count requests matching certain patterns). This provides: DDoS mitigation, brute-force protection, and API abuse prevention. Works at the application layer (L7). Shield provides network-layer (L3/L4) DDoS protection.", domain:1},
  {q:"A company stores time-series IoT data in DynamoDB. Each item has: device_id (PK), timestamp (SK), and sensor_data. They want to automatically delete data older than 90 days. What DynamoDB feature handles this?", opts:["Lifecycle policies (that's S3, not DynamoDB)","DynamoDB Time to Live (TTL) — set a TTL attribute with the expiration timestamp (epoch seconds). DynamoDB automatically deletes items after the TTL timestamp passes. Free of charge (no WCU consumed).","Scheduled Lambda to delete old items (works but costly)","DynamoDB Streams cleanup (not for deletion)"], correct:1, exp:"DynamoDB TTL: designate an attribute as the TTL attribute (must contain Unix epoch timestamp). DynamoDB automatically deletes items once current_time > TTL_value. Benefits: (1) FREE — no WCU consumed for TTL deletions, (2) No custom code needed, (3) Deletions appear in DynamoDB Streams (can archive to S3 before deletion). Items are typically deleted within 48 hours of expiration (not instant, but eventually). Perfect for: session data, logs, temporary records, sensor data with retention periods.", domain:3},
  {q:"A company uses S3 Transfer Acceleration. When is Transfer Acceleration beneficial and when is it NOT worth the extra cost?", opts:["Always beneficial for all uploads","Transfer Acceleration is beneficial for LONG-DISTANCE uploads (intercontinental) and LARGE files. It routes traffic through CloudFront edge locations to AWS backbone. NOT beneficial for: same-region uploads, very small files (<1MB where overhead matters), or when the edge location is farther from the bucket than the client.","Only beneficial for downloads, not uploads","Only works with multipart upload"], correct:1, exp:"S3 Transfer Acceleration: uploads enter the nearest CloudFront edge location → travel over AWS backbone (optimized) → reach the S3 bucket region. Beneficial: uploads from far away (Asia to US bucket), large files where the acceleration percentage matters. Not beneficial: same-region uploads (already on AWS network), very small files (connection setup overhead dominates), or if the direct path is already optimal. AWS provides a speed comparison tool to test if acceleration helps for your use case. Extra cost: $0.04-0.08/GB.", domain:3},
  {q:"A company uses Amazon WorkSpaces for 200 users. They want to restrict users from copying data from WorkSpaces to their local machines (DLP - Data Loss Prevention). What control is available?", opts:["WorkSpaces doesn't support clipboard/DLP controls","WorkSpaces supports: disable clipboard redirection (copy/paste between WorkSpace and local disabled), disable drive redirection (no access to local drives from WorkSpace), disable printing redirection (can't print to local printer). Configure via WorkSpaces Directory → Group Policy or Amazon WorkSpaces web access policy.","Use third-party DLP software only","Block all network traffic with Security Groups"], correct:1, exp:"WorkSpaces DLP controls (per-directory configuration): (1) Clipboard redirection: disable copy/paste between WorkSpace and local machine (both directions). (2) Drive redirection: prevent WorkSpace from accessing local drives (can't drag files to local). (3) Printing: disable local printer access. (4) Audio: disable audio output. (5) USB: control USB device redirection. These are configured in the WorkSpaces directory settings or via Group Policy for domain-joined WorkSpaces. Effective DLP without additional software.", domain:1},
  {q:"A company uses Amazon Aurora MySQL. They need to export the entire database to S3 for data lake analytics (queried by Athena). What is the MOST efficient way to export Aurora data to S3?", opts:["mysqldump to EC2 then upload to S3 (slow, resource-intensive)","Aurora native export to S3 (SELECT INTO OUTFILE S3) — runs directly on the Aurora cluster, exports query results in CSV/text format to S3. Or use Aurora Snapshots Export to S3 (Parquet format, parallel export).","DMS continuous replication to S3 (for ongoing sync, overkill for one-time export)","Read Replica with manual export"], correct:1, exp:"Two Aurora-to-S3 export methods: (1) SELECT INTO OUTFILE S3: export specific queries to S3 in CSV/TSV format. Good for specific tables/queries. (2) Aurora Snapshot Export to S3: exports a full snapshot in Apache Parquet format — optimized for analytics (Athena, Redshift Spectrum). Runs in parallel, doesn't impact the running cluster. The Snapshot Export produces Parquet files that Athena can query directly with excellent performance. This is the recommended approach for data lake integration.", domain:3},
  {q:"A company uses EC2 Reserved Instances. They bought 10 × m5.xlarge Reserved Instances in us-east-1a. They want to move workloads to us-east-1b. Can they use the same RIs?", opts:["No — RIs are locked to the specific AZ where purchased","Depends on the RI scope: Zonal RIs are AZ-specific (capacity reservation + discount). Regional RIs apply the discount to any AZ in the region (but no capacity reservation). They should check their RI scope and consider converting to Regional scope.","RIs automatically apply to any AZ in the region","RIs can be transferred between AZs with no changes"], correct:1, exp:"RI scopes: (1) Zonal (AZ-specific): discount applies ONLY to instances in that specific AZ + provides capacity reservation (guaranteed instance availability). (2) Regional: discount applies to instances in ANY AZ within the region — more flexible but NO capacity reservation. If they purchased Zonal RIs for us-east-1a: discount won't apply to us-east-1b instances. Solution: modify RI scope from Zonal to Regional (or exchange for Regional). Regional is recommended unless capacity reservation is specifically needed.", domain:4},
  {q:"A company has a VPC with both IPv4 and IPv6 (dual-stack). Their EC2 instances have IPv6 addresses. They want IPv6 instances in private subnets to reach the internet (for apt-get/yum updates) without being reachable FROM the internet. What is the IPv6 equivalent of a NAT Gateway?", opts:["NAT Gateway supports IPv6 (it doesn't — NAT Gateway is IPv4 only)","Egress-Only Internet Gateway — allows IPv6 outbound traffic to the internet but blocks unsolicited inbound IPv6 traffic (stateful, like NAT for IPv6). Free of charge.","IPv6 instances always need to be in public subnets","There's no way to have private IPv6 instances accessing the internet"], correct:1, exp:"Egress-Only Internet Gateway (EIGW): the IPv6 equivalent of NAT Gateway's functionality. IPv6 addresses are globally unique (no NAT needed) but EIGW provides stateful filtering: allows outbound connections and their responses, blocks unsolicited inbound. Route table: ::/0 → EIGW for IPv6 private subnets. Free (no hourly or data processing charge — unlike NAT Gateway). For IPv4 in the same subnet: still need NAT Gateway. EIGW is one of the cost-saving reasons to adopt IPv6.", domain:1},
  {q:"A company uses AWS CodeBuild for CI. Build times are slow because dependencies are re-downloaded every build (node_modules, Maven .m2, pip packages). How can they speed up builds?", opts:["Use a larger build instance (faster but still downloads deps)","Enable CodeBuild caching — supports S3 cache (dependencies cached in S3 between builds) and local cache (reuses cache from previous build on same build host). Subsequent builds skip dependency download.","Pre-install dependencies in a custom Docker build image","Both B and C — caching for runtime deps, custom image for build tools"], correct:3, exp:"Two complementary approaches: (1) CodeBuild caching (S3 or local): cache directories like node_modules/, .m2/repository/, or pip cache between builds. Next build downloads only new/changed packages. S3 cache: reliable across build hosts. Local cache: faster but only if same host. (2) Custom Docker build image: pre-install heavy build tools, SDKs, and common libraries. Reduces both download and install time. Together: custom image for tools + cache for project-specific dependencies = maximum build speed.", domain:2},
  {q:"A company uses Amazon Connect for their contact center. They want to analyze customer sentiment during calls in real-time (detect angry/frustrated customers and alert a supervisor). Which service enables this?", opts:["Amazon Transcribe only (speech-to-text, no sentiment)","Amazon Connect Contact Lens — real-time transcription + sentiment analysis during calls. Alerts supervisors when negative sentiment is detected. Also provides: keyword detection, talk-time metrics, and post-call analytics.","Amazon Comprehend (batch text analysis, not real-time on calls)","Custom ML model on SageMaker"], correct:1, exp:"Contact Lens for Amazon Connect: (1) Real-time transcription of calls, (2) Sentiment analysis per turn (positive/negative/neutral), (3) Real-time supervisor alerts on negative sentiment or specific keywords (escalation), (4) Post-call analytics (summary, action items, compliance check). Fully integrated with Connect — no additional infrastructure. Uses ML models trained on contact center conversations. Supervisors can listen in or assist when sentiment drops. Purpose-built for contact center analytics.", domain:3},
  {q:"A company uses CloudFront and needs to restrict access to premium video content. Only users who have paid should be able to view videos. The videos are stored in S3. What is the complete architecture?", opts:["S3 bucket policy allowing access only from specific IPs","Application server validates payment → generates CloudFront Signed Cookies (or Signed URLs) with expiration → user's browser presents cookie → CloudFront validates signature and serves content if valid. S3 is restricted to CloudFront only via OAC.","CloudFront Field-Level Encryption","WAF rules checking payment status"], correct:1, exp:"Premium content architecture: (1) S3 bucket: private, accessible ONLY via CloudFront (OAC/OAI blocks direct access). (2) Application: verifies user's subscription/payment status. (3) Signed Cookies: app generates cookies using CloudFront private key, includes expiration time. Set cookies in user's browser. (4) CloudFront: validates cookie signature on each request. Valid → serve video. Invalid/expired → return 403. Signed Cookies work for all URLs matching a pattern (ideal for streaming multiple segments). Signed URLs for individual file access.", domain:1},
  {q:"A company uses DynamoDB Streams and Lambda to maintain a search index in OpenSearch. When a DynamoDB item is created/updated/deleted, Lambda updates OpenSearch. Occasionally, Lambda fails and the change is lost (OpenSearch becomes stale). How do they ensure no changes are lost?", opts:["Lambda already retries automatically for stream events","Lambda DOES retry on failure for stream events (up to the record's retention period of 24h). However, if the batch fails repeatedly, the stream iterator advances (records expire). Solution: configure a Dead Letter Queue (DLQ) or On-Failure Destination for the Lambda event source mapping to capture failed records. Also: enable Bisect on Error to isolate the failing record.","Changes can't be lost with DynamoDB Streams","Use SQS instead of Lambda for processing"], correct:1, exp:"DynamoDB Streams + Lambda retries: Lambda retries the failed batch until records expire from the stream (24 hours). Problems: one bad record blocks the entire shard (all subsequent records delayed). Solutions: (1) BisectBatchOnFunctionError: splits failed batch in half to isolate the bad record. (2) MaximumRetryAttempts: limit retries before moving on. (3) On-Failure Destination (SQS/SNS): captures details of failed records for later processing. (4) MaximumRecordAge: skip records older than a threshold. Together: no blocking, no data loss, failed records captured for investigation.", domain:2},
  {q:"A company runs a web application. During peak load, their RDS MySQL write operations become the bottleneck (too many writes per second). Read Replicas won't help because the problem is WRITES. What are their options?", opts:["Add more Read Replicas (only helps reads, not writes)","Options: (1) Vertical scaling — larger RDS instance (more IOPS). (2) Aurora MySQL — better write performance (optimized storage). (3) DynamoDB for high-write tables (if applicable). (4) Write-behind caching — buffer writes in ElastiCache/SQS, flush to DB asynchronously. (5) Sharding across multiple RDS instances.","Use Multi-AZ (standby doesn't accept writes)","Enable caching (caches help reads, not writes)"], correct:1, exp:"Write scaling options: (1) Vertical: bigger instance = more write IOPS. (2) Aurora: 5x throughput vs standard MySQL (optimized storage layer). (3) DynamoDB: near-infinite write scaling for suitable workloads (key-value, document). (4) Write-behind pattern: application writes to fast cache (Redis) or queue (SQS), background worker flushes to DB — smooths spikes. (5) Horizontal sharding: split writes across multiple DB instances by tenant/key. Most impactful: Aurora migration or DynamoDB for high-write tables.", domain:3},
  {q:"A company uses Amazon S3 Glacier Flexible Retrieval. They initiate a retrieval request for an archived object using Standard retrieval. How long until the object is available?", opts:["Immediately (that's S3 Standard)","3-5 hours for Standard retrieval. Options: Expedited (1-5 minutes, $$$), Standard (3-5 hours, $$), Bulk (5-12 hours, $).","12 hours minimum","24 hours minimum"], correct:1, exp:"S3 Glacier Flexible Retrieval tiers: (1) Expedited: 1-5 minutes ($0.03/GB + $10/request). Use for urgent access. (2) Standard: 3-5 hours ($0.01/GB + $0.05/request). Default option. (3) Bulk: 5-12 hours ($0.0025/GB + $0.025/request). Cheapest, for large non-urgent retrievals. After retrieval: object is temporarily available in S3 Standard for the specified duration. Compare with Glacier Deep Archive: only Standard (12 hours) and Bulk (48 hours) — no Expedited option.", domain:3},
  {q:"A company uses AWS AppConfig (part of Systems Manager) to manage feature flags and application configuration. How does AppConfig differ from SSM Parameter Store?", opts:["They are the same service","AppConfig provides: deployment strategies (gradual rollout with automatic rollback), validation (JSON schema or Lambda validator before deployment), environment/application organization, and monitors integration (rollback if CloudWatch alarm triggers). Parameter Store is simple key-value with no deployment safety.","Parameter Store is more feature-rich than AppConfig","AppConfig is only for Lambda functions"], correct:1, exp:"AppConfig vs Parameter Store: Parameter Store: simple key-value storage, immediate availability, versioning. AppConfig: DEPLOYMENT management for configuration — deploy gradually (10% → 50% → 100%), validate before deployment (schema or Lambda), monitor during rollout (CloudWatch alarms trigger automatic rollback if metrics degrade), and organize by application/environment. Use Parameter Store for: static config, secrets reference. Use AppConfig for: feature flags, dynamic config that needs safe rollout.", domain:2},
  {q:"A company uses S3 for storage. They want to be alerted if their S3 costs increase by more than 20% compared to the previous month. Which AWS service provides this alerting?", opts:["S3 Storage Lens (analytics, not alerting)","AWS Budgets — create a budget with forecasted cost threshold and anomaly detection. Or use AWS Cost Anomaly Detection which uses ML to detect unexpected cost spikes and sends alerts automatically.","CloudWatch billing alarm (only for absolute threshold, not percentage change)","AWS Cost Explorer (visualization, not real-time alerting)"], correct:1, exp:"Two approaches: (1) AWS Budgets: set a budget for S3 spending with alert when actuals or forecast exceeds budget. Can alert on absolute amounts. (2) AWS Cost Anomaly Detection: ML-based — learns normal spending patterns and alerts on deviations (including percentage increases). Detects: sudden spikes, gradual creep, and unexpected patterns. More sophisticated than fixed-threshold alarms. CloudWatch billing alarms work for 'alert when total bill exceeds $X' but don't compare month-over-month or detect anomalies.", domain:4},
  {q:"A company uses Amazon Redshift and wants to automate data loading from S3 whenever new files arrive. Currently they run a COPY command on a schedule (every hour). They want near-real-time loading. What feature enables this?", opts:["Redshift can't detect S3 changes automatically","Redshift Auto-Copy (preview/GA) — automatically runs COPY commands when new files arrive in the configured S3 path. Or use: S3 Event → Lambda → Redshift Data API to trigger COPY on each file arrival.","Amazon Kinesis only (requires changing the data pipeline)","Redshift Streaming Ingestion from Kinesis (different pattern)"], correct:1, exp:"Options for real-time S3→Redshift: (1) Redshift Auto-Copy: configure S3 prefix monitoring, Redshift automatically COPYs new files. (2) S3 Event Notification → Lambda → Redshift Data API COPY: Lambda triggers on new S3 object, executes COPY via Data API. (3) Redshift Streaming Ingestion: for Kinesis/MSK streams directly into materialized views (different from file-based). (4) Glue Streaming ETL → Redshift. For S3 file landing: Auto-Copy or Lambda-triggered COPY are the most direct solutions.", domain:3},
  {q:"A company uses IAM roles for cross-account access. Account A (trusted) assumes a role in Account B (trusting). They want to restrict WHICH principals in Account A can assume the role — not everyone in Account A, only a specific Lambda function. How?", opts:["Condition in Account A's IAM policies","The role's trust policy in Account B specifies the EXACT principal ARN (the Lambda role ARN in Account A) — not the entire Account A. Only that specific principal can assume the role.","Both accounts must specify the restriction","Cross-account access always allows the entire account"], correct:1, exp:"Role trust policy granularity: instead of Principal: {AWS: 'arn:aws:iam::ACCOUNT_A:root'} (allows anyone in Account A), specify: Principal: {AWS: 'arn:aws:iam::ACCOUNT_A:role/lambda-role-name'} (allows ONLY that specific role). The trust policy in the trusting account (B) controls WHO can assume. Further restriction: add Conditions (sts:ExternalId, source IP, MFA, etc.). This is how you implement least-privilege cross-account access — never trust entire accounts when you can specify exact principals.", domain:1},
  {q:"A company uses AWS Glue DataBrew for data preparation. What does DataBrew provide that regular Glue ETL doesn't?", opts:["DataBrew is just a UI for Glue jobs","DataBrew is a VISUAL data preparation tool — no-code interface for data cleaning, normalization, and transformation. 250+ built-in transforms (filter, pivot, join, split, format, deduplicate). Data quality profiling with statistics and visualizations. Designed for analysts/data scientists who don't write code, not engineers.","DataBrew only works with CSV files","DataBrew replaces Glue ETL entirely"], correct:1, exp:"AWS Glue DataBrew vs Glue ETL: DataBrew: visual, no-code, for data analysts. Click-through transforms, data profiling (distribution, null counts, pattern detection), recipe-based workflows (reusable transform sequences). 250+ built-in transforms. Glue ETL: code-based (Python/Scala), for data engineers. Custom logic, complex transformations, DPU-based scaling. Use DataBrew for: data preparation, quality analysis, simple transforms. Use Glue ETL for: complex ETL logic, custom code, streaming.", domain:3},
  {q:"A company has a microservices application where Service A depends on Service B. When Service B is down, Service A also fails (cascading failure). How should Service A handle Service B's failures?", opts:["Increase retries to Service B (makes cascading failure worse)","Implement the Circuit Breaker pattern — after detecting Service B failures (threshold reached), 'open' the circuit: stop calling Service B, return a fallback response (cached data, default value, or graceful degradation). Periodically test if B has recovered ('half-open' state). AWS: use Step Functions retry/catch or implement in application with libraries.","Add more instances of Service B","Put a load balancer between A and B"], correct:1, exp:"Circuit Breaker pattern: (1) CLOSED (normal): requests flow to Service B. (2) Failure threshold reached → OPEN: all requests immediately fail-fast (return fallback, don't call B). (3) After timeout → HALF-OPEN: allow one request through to test. If succeeds → CLOSED. If fails → OPEN again. Benefits: prevent cascading failure, reduce latency (fail fast vs timeout wait), allow B time to recover without being overwhelmed by retries. Implementation: Application-level (Resilience4j, Polly) or infrastructure (App Mesh/Envoy).", domain:2},
  {q:"A company uses Amazon Comprehend. What types of NLP analysis does it provide?", opts:["Only translation (that's Amazon Translate)","Comprehend provides: Sentiment Analysis (positive/negative/neutral/mixed), Entity Recognition (people, places, dates, organizations), Key Phrase Extraction, Language Detection, Topic Modeling, and Custom Classification/Entity training. Fully managed NLP — no ML expertise needed.","Only text-to-speech (that's Amazon Polly)","Only speech-to-text (that's Amazon Transcribe)"], correct:1, exp:"Amazon Comprehend NLP capabilities: (1) Sentiment: positive/negative/neutral/mixed with confidence scores. (2) Entities: detect and categorize (PERSON, LOCATION, ORGANIZATION, DATE, QUANTITY, etc.). (3) Key phrases: extract important phrases. (4) Language: detect language from 100+ supported. (5) Topic modeling: discover topics across document collections. (6) Custom: train custom classifiers and entity recognizers on your data. Use cases: customer feedback analysis, document categorization, content moderation, medical text analysis (Comprehend Medical).", domain:3},
  {q:"A company uses AWS Cost Explorer. They want to understand the cost impact of a specific project that spans multiple services (EC2, S3, RDS, Lambda). Instances are shared with other projects. How can they track per-project costs?", opts:["Cost Explorer can't break down by project","Use AWS Cost Allocation Tags — tag all resources with a 'Project' tag (e.g., Project=Alpha). Activate the tag as a cost allocation tag in Billing. Cost Explorer then shows costs filterable and groupable by the Project tag. For shared resources: use split-cost allocation (newer feature) or proportional estimates.","Create separate AWS accounts per project (overkill)","CloudWatch metrics for cost per project (CloudWatch doesn't track costs)"], correct:1, exp:"Cost allocation tags: (1) Tag resources with Project=ProjectName. (2) Activate tag as cost allocation tag in Billing Console. (3) Cost Explorer groups/filters costs by tag. Reports show exactly how much each project costs. Best practices: enforce tagging via SCP/IAM conditions (deny resource creation without required tags), use AWS Tag Editor for bulk tagging, and AWS Organizations tag policies for standardization. For shared resources (shared RDS): consider AWS split cost allocation data or proportional billing.", domain:4},
  {q:"A company uses AWS Lake Formation to manage their data lake. They want to grant a data analyst access to only specific columns of a table (column-level security). Does Lake Formation support this?", opts:["Lake Formation only supports table-level permissions","Yes — Lake Formation supports column-level permissions (include or exclude specific columns), row-level security (filter rows based on conditions), and cell-level security (combine column + row filters). More granular than IAM S3 policies.","Column-level security requires custom application code","Only available with Redshift, not Athena"], correct:1, exp:"Lake Formation fine-grained access control: (1) Table-level: grant/deny access to entire tables. (2) Column-level: include-list (only these columns visible) or exclude-list (hide specific columns). (3) Row-level: filter expressions (e.g., 'department = HR'). (4) Cell-level: combination of column and row filters. Enforced across: Athena, Redshift Spectrum, EMR, and Glue. Much more manageable than S3 bucket policies for data lake access control. Central permission management for all data consumers.", domain:1},
  {q:"A company runs EC2 Spot Instances for batch processing. They get a 2-minute warning before Spot termination. What should their application do in those 2 minutes?", opts:["Nothing — instance terminates immediately with no warning","Save progress/checkpoint to durable storage (S3/DynamoDB), stop accepting new work, finish current unit of work if possible, and gracefully shut down. The 2-minute warning is available via instance metadata (169.254.169.254/latest/meta-data/spot/termination-time).","2-minute warning only available in specific regions","Request Spot extension (not a feature)"], correct:1, exp:"Spot termination handling: (1) Poll instance metadata endpoint every 5 seconds for termination notice. (2) When notice received: checkpoint current state to durable storage (S3 for files, DynamoDB for progress markers), drain connections (deregister from load balancer), stop accepting new tasks from the queue (change message visibility), and finish current small unit of work. (3) If using ASG: a new Spot instance launches automatically. The combination of checkpointing + queue-based architecture makes Spot interruptions seamless.", domain:2},
  {q:"A company uses AWS Systems Manager Patch Manager to patch their EC2 fleet. They want to patch DEVELOPMENT instances immediately when patches are available, but PRODUCTION instances should only be patched during a weekly maintenance window after dev testing. How is this configured?", opts:["Separate patch baselines (determines WHICH patches, not WHEN)","Patch Groups + Maintenance Windows — assign instances to patch groups (Dev, Prod). Create maintenance windows: Dev (daily/immediate), Prod (weekly Sunday 2AM). Associate each patch group with its maintenance window. Patches apply according to the schedule.","Systems Manager can't differentiate patch timing by environment","Manual patching for production (not automated)"], correct:1, exp:"Patch Manager architecture: (1) Patch Baselines: define WHICH patches (critical, security, all — with approval delays). Prod might auto-approve patches 7 days after release (tested in Dev). (2) Patch Groups: logical groups of instances (tag-based: PatchGroup=Dev, PatchGroup=Prod). (3) Maintenance Windows: define WHEN patching occurs per group. Result: Dev gets patches immediately for testing, Prod gets the same patches one week later during its maintenance window. Fully automated, risk-stratified patching.", domain:2},
  {q:"A company processes credit card payments. PCI DSS requires that cardholder data is encrypted and decryption keys are controlled. They use KMS. What KMS key type provides MAXIMUM control over the key material?", opts:["AWS managed keys (aws/rds, aws/s3 — AWS controls rotation and policies)","Customer managed keys with IMPORTED key material — you generate the key outside AWS, import it to KMS. You control: creation, backup, deletion, and rotation of the key material. You can delete it from KMS while retaining a copy (enables crypto-shredding).","AWS owned keys (least visibility/control)","CloudHSM (dedicated HSM — maximum control but different from KMS)"], correct:1, exp:"KMS key types by control level: (1) AWS owned: zero visibility, zero control (used by AWS services internally). (2) AWS managed: visible in your account, AWS controls policies/rotation (aws/ebs, aws/s3). (3) Customer managed CMK: YOU control key policy, rotation, enable/disable, deletion. (4) Customer managed with IMPORTED material: maximum KMS control — you generate key material externally, import to KMS, and maintain the only backup. (5) CloudHSM: dedicated hardware, FIPS 140-2 Level 3, full HSM control. For PCI: Customer managed (or CloudHSM for highest assurance).", domain:1},
  {q:"A company uses Amazon RDS and enables Enhanced Monitoring. What does Enhanced Monitoring provide that standard CloudWatch metrics don't?", opts:["Same metrics as CloudWatch (both show CPU, memory, I/O)","Enhanced Monitoring provides OS-LEVEL metrics at 1-second granularity: per-process CPU, memory breakdown (free, cached, buffers), disk I/O per device, network per interface, swap usage, and file system utilization. CloudWatch RDS metrics are 1-minute minimum and lack process-level detail.","Enhanced Monitoring only available for Aurora","It monitors the application queries (that's Performance Insights)"], correct:1, exp:"Enhanced Monitoring vs CloudWatch: CloudWatch: DB-level metrics at 60-second intervals (CPUUtilization, FreeableMemory, ReadIOPS — aggregate). Enhanced Monitoring: OS-level metrics at 1/5/10/15/30/60-second intervals directly from the OS agent. Shows: per-process CPU/memory (identify if it's the DB engine, agent, or other process consuming resources), detailed memory breakdown, per-disk I/O, file system space. Published to CloudWatch Logs (not CloudWatch Metrics). Essential for deep performance troubleshooting.", domain:3},
  {q:"A company uses AWS Direct Connect for hybrid connectivity. Their single Direct Connect connection to us-east-1 goes down. Their application fails because it can't reach on-premises databases. What resilience options exist?", opts:["Direct Connect has built-in redundancy (it doesn't — single connection = single point of failure)","Redundancy options: (1) Second Direct Connect connection to a different DX location (highest resilience). (2) Site-to-Site VPN as backup (failover from DX to VPN automatically via BGP). (3) Direct Connect LAG (link aggregation — HA within same location only).","Switch to VPN only (higher latency, less bandwidth)","Direct Connect redundancy isn't possible"], correct:1, exp:"Direct Connect resilience tiers: (1) Maximum: two DX connections from different DX locations (different physical paths, survives entire facility failure). (2) High: DX + Site-to-Site VPN backup. BGP routes failover from DX to VPN when DX goes down. VPN provides backup connectivity (lower bandwidth, higher latency but functional). (3) Standard: two connections in same DX location (LAG or separate connections). AWS recommends: at minimum, DX + VPN backup. For critical workloads: dual DX in different locations + VPN as third tier.", domain:2},
  {q:"A company uses Amazon SageMaker for ML model training and deployment. They want to detect when a deployed model's predictions become less accurate over time (model drift). What SageMaker feature monitors this?", opts:["CloudWatch metrics for endpoint latency (measures speed, not accuracy)","SageMaker Model Monitor — continuously monitors deployed models for: data drift (input data distribution changes), model quality drift (prediction accuracy degradation), bias drift, and feature attribution drift. Alerts when metrics deviate from baseline.","Manual periodic retraining","SageMaker Debugger (for training, not production)"], correct:1, exp:"SageMaker Model Monitor: (1) Data Quality: detects when input data statistics drift from training data (new patterns the model hasn't seen). (2) Model Quality: monitors prediction accuracy against ground truth labels (when available). (3) Bias: detects if model becomes biased over time. (4) Feature Attribution: monitors feature importance changes. Generates CloudWatch metrics + alerts. When drift is detected: trigger retraining pipeline. This is MLOps best practice — models degrade over time as data patterns change.", domain:2},
  {q:"A company uses S3 Object Lambda. What does Object Lambda allow?", opts:["Lambda triggers on S3 events (that's S3 Event Notifications)","S3 Object Lambda transforms objects ON-THE-FLY as they are retrieved — a Lambda function processes the object between S3 and the requester. Use cases: redacting PII before delivery, converting formats (XML→JSON), decompressing, watermarking images, customizing data per requester — all without storing multiple copies.","Lambda stores objects in S3","Lambda manages S3 lifecycle policies"], correct:1, exp:"S3 Object Lambda Access Points: when an application calls GetObject, the request is intercepted by a Lambda function that can transform the object before returning it. The original object in S3 is unchanged. Use cases: (1) Redact PII for non-privileged users. (2) Convert data format per consumer (CSV for one, JSON for another). (3) Resize images dynamically. (4) Filter rows based on requester identity. Single source of truth, multiple views — without maintaining multiple copies. Pay for Lambda invocation + data processed.", domain:3},
]
