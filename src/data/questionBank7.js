export const questionBank7 = [
  // Tricky scenarios - Commonly failed exam questions
  {q:"A company uses AWS Lambda to process files uploaded to S3. The Lambda function is configured with 128MB memory and a 3-second timeout. Processing works fine for files under 1MB, but files over 5MB cause timeouts. Increasing the timeout to 30 seconds helps but the function still occasionally times out for 10MB files. What is the MOST effective fix?", opts:["Increase timeout to maximum (15 minutes)","Increase memory allocation to 1024MB or higher (Lambda allocates CPU proportionally to memory)","Move processing to ECS Fargate for unlimited resources","Split files into smaller chunks before processing"], correct:1, exp:"Lambda allocates CPU power proportionally to memory. At 128MB, the function gets minimal CPU. Increasing to 1024MB gives 8x more CPU, dramatically speeding up processing. This is one of the most common Lambda performance misconceptions — memory doesn't just affect RAM, it directly controls CPU allocation. Often increasing memory REDUCES cost because duration drops proportionally.", domain:3},
  {q:"A company creates a VPC with the CIDR block 172.31.0.0/16. They create two subnets: 172.31.0.0/24 in us-east-1a and 172.31.1.0/24 in us-east-1b. They launch an EC2 instance in each subnet with a Security Group allowing all inbound traffic from 172.31.0.0/16. Instance A cannot ping Instance B. What is the MOST likely cause?", opts:["Security Groups don't allow ICMP by default — ICMP must be explicitly allowed in the Security Group","The instances are in different AZs which blocks direct communication","NACLs are blocking ICMP traffic between subnets","The VPC doesn't have DNS resolution enabled"], correct:0, exp:"Security Groups are stateful and allow ALL traffic means TCP/UDP but NOT necessarily ICMP. 'All traffic' type in SG rules does include ICMP, but if they specified 'All TCP' or specific ports, ICMP (used by ping) would be blocked. The most common gotcha: SG rules must explicitly include ICMP (type/code) for ping to work. NACLs default allow all traffic. Different AZs don't block communication.", domain:1},
  {q:"A company creates a public subnet by attaching an Internet Gateway and adding a route 0.0.0.0/0 -> IGW. They launch an EC2 instance in this subnet without a public IP or Elastic IP. The instance has a Security Group allowing all outbound traffic. Can this instance access the internet?", opts:["Yes — the subnet has a route to the IGW so all instances can access the internet","No — an instance in a public subnet still needs a public IP or Elastic IP to communicate through the Internet Gateway","Yes — the Internet Gateway performs NAT for all instances in public subnets","No — the Security Group must also allow inbound traffic for the return path"], correct:1, exp:"An Internet Gateway does NOT perform NAT. It provides a one-to-one mapping between a public IP and a private IP. Without a public IP (or Elastic IP), the IGW has no public address to map to, and the instance cannot send or receive internet traffic. This is a very common exam trap. A NAT Gateway would allow private-IP-only instances to access the internet.", domain:1},
  {q:"A solutions architect creates an S3 bucket policy that DENIES all access except from a specific VPC endpoint (aws:SourceVpce condition). An administrator tries to access the bucket from the AWS Console and is denied. Why, and how should this be fixed while maintaining the restriction?", opts:["The Console uses a different API endpoint — add Console access to the allow list","Console access doesn't come through the VPC endpoint, so it's denied by the explicit Deny. Add a condition that also allows access when aws:SourceVpce is null (for Console/CLI outside VPC) combined with restricting those to specific IAM roles","Console access inherits the IAM user's permissions which override bucket policies","This is expected — add the administrator's IP to an Allow statement in the bucket policy"], correct:1, exp:"When you Deny all access except from a VPC endpoint, Console/CLI access (which goes over the internet, not through the VPC endpoint) is denied. Fix: modify the policy to allow specific IAM principals (admin roles) when not coming through a VPC endpoint (aws:SourceVpce condition is null), while still requiring VPC endpoint for application access. Or add a VPN/Direct Connect path that routes through the VPC endpoint.", domain:1},
  {q:"An Auto Scaling Group is configured with min=2, max=10, desired=2. A scale-out alarm triggers when CPU > 70%. After scaling out to 4 instances, CPU drops to 50% and a scale-in policy triggers. The ASG scales in to 2, then CPU spikes again, causing repeated scaling out and in (thrashing). What configuration change prevents this?", opts:["Set a cooldown period of 300 seconds after scaling activities","Use target tracking scaling policy (set target CPU to 60%) instead of simple/step scaling policies","Increase the scale-in threshold to CPU < 30% to create a wider gap between scale-out and scale-in thresholds","All of the above help, but target tracking (B) is the MOST effective single change"], correct:3, exp:"Target tracking is the most effective because it automatically manages both scale-out and scale-in to maintain the target metric, with built-in scale-in protection (it waits for metrics to stabilize). It avoids the manual threshold gap problem. Cooldowns prevent rapid changes but delay legitimate scaling. Wider thresholds help but require manual tuning. Target tracking solves the fundamental problem of threshold oscillation.", domain:2},
  {q:"A company configures an ALB health check with path '/health' that returns HTTP 200. Their application returns 200 on /health even when the database is unreachable (the health endpoint only checks the web server). During a database outage, the ALB continues routing traffic to instances that return 500 errors on actual requests. How should this be fixed?", opts:["Change health check to a database-dependent endpoint (e.g., '/health/deep' that queries the database)","Reduce health check interval to detect failures faster","Use TCP health check instead of HTTP","Add CloudWatch alarms to detect the database failure separately"], correct:0, exp:"The health check endpoint must reflect the actual application health including dependencies. A 'deep health check' that verifies database connectivity (e.g., a simple SELECT 1) ensures the ALB only routes to instances that can actually serve requests. Shallow health checks (just web server) give false positives. Faster intervals don't help if the check itself is misleading. TCP checks only verify the port is open.", domain:2},
  {q:"A company uses Route 53 alias records pointing to an ALB. They notice that even after removing instances from the ALB target group, some users still see responses from removed instances for up to 60 seconds. What causes this?", opts:["Route 53 DNS TTL caching — reduce the alias record TTL","ALB connection draining (deregistration delay) keeps existing connections open to deregistered targets for the configured duration","DNS resolver caching on the client side","The instances are still running and responding to direct requests"], correct:1, exp:"When a target is deregistered, the ALB's deregistration delay (default 300 seconds) keeps existing connections open until they complete or the timeout expires. New connections go to healthy targets, but in-flight requests on existing connections continue to the old target. This explains the 60-second observation. Route 53 alias records have TTL 0 effectively. To reduce this, lower the deregistration delay value.", domain:2},
  {q:"A company has a Lambda function that connects to an RDS database. Under load, they see 'Too many connections' errors from the database. The Lambda function creates a new database connection on each invocation. With 100 concurrent Lambda invocations, there are 100 simultaneous database connections. How should this be resolved?", opts:["Increase RDS max_connections parameter","Use connection pooling inside the Lambda function handler","Use Amazon RDS Proxy between Lambda and the database (manages connection pooling externally)","Reduce Lambda reserved concurrency to limit simultaneous connections"], correct:2, exp:"Lambda functions can't effectively pool connections because each concurrent invocation runs in a separate execution environment. Connection 'pooling' inside the handler only reuses connections within the same warm container, not across concurrent invocations. RDS Proxy sits between Lambda and the database, multiplexing many Lambda connections into a small pool of database connections. This is THE solution for Lambda + RDS connection exhaustion.", domain:3},
  {q:"A company uses CloudFront with S3 as the origin. They enable Origin Access Control (OAC) and update the S3 bucket policy to allow only CloudFront's service principal. Users can access content via CloudFront, but they can also still access S3 directly using the S3 URL. Why?", opts:["OAC doesn't prevent direct S3 access — it only provides identity for CloudFront requests","The bucket policy allows both CloudFront AND public access — they need to explicitly DENY access that doesn't come from CloudFront","S3 Block Public Access is not enabled — enabling it would block direct access","The bucket has legacy ACLs granting public read access that override the bucket policy"], correct:1, exp:"OAC gives CloudFront a service principal to use when requesting from S3. But if the bucket policy has a separate Allow statement for public access (or Block Public Access is disabled with public ACLs), direct S3 access still works. Fix: the bucket policy should ONLY have a statement allowing s3:GetObject with Condition: StringEquals: AWS:SourceArn: CloudFront distribution ARN. Remove any other public Allow statements and enable Block Public Access.", domain:1},
  {q:"A company has an Aurora MySQL cluster with 1 writer and 3 reader instances. An application reads from the reader endpoint but occasionally gets stale data (reading a record immediately after writing it returns the old value). This is causing business logic errors. How should this be resolved?", opts:["Increase Aurora replica lag threshold and add application retry logic","For read-after-write consistency requirements, read from the writer endpoint (cluster endpoint) instead of the reader endpoint for those specific queries","Switch to Aurora Global Database for better consistency","Enable Aurora Strong Consistency mode"], correct:1, exp:"Aurora replicas have eventual consistency with typically <100ms lag, but this can cause stale reads immediately after writes. For read-after-write scenarios, the application should read from the writer endpoint. Best practice: use reader endpoint for general reads but route read-after-write queries to the writer endpoint. There's no 'Strong Consistency mode' in Aurora. Global Database adds MORE lag, not less.", domain:3},
  {q:"A company uses an Auto Scaling Group with a launch template specifying an AMI. They update the AMI in the launch template (new version) but existing instances continue running the old AMI. How do they update running instances to the new AMI?", opts:["Update the launch template version and existing instances automatically relaunch","Use Instance Refresh to gradually replace instances with new ones using the updated launch template","Terminate all instances manually — ASG will relaunch them with the new template","Change the ASG minimum to 0, wait, then back to original — forcing all replacements"], correct:1, exp:"Instance Refresh gracefully replaces instances in batches (configurable percentage). It respects the minimum healthy percentage, uses health checks during rollout, and can be rolled back. Simply updating the launch template version doesn't affect running instances (they keep their current AMI). Manual termination is disruptive. Setting min to 0 causes downtime. Instance Refresh is the proper operational procedure.", domain:2},
  {q:"A company uses DynamoDB with provisioned capacity (100 RCU, 100 WCU). A batch job runs nightly that consumes 1000 RCU for 30 minutes. They enable auto-scaling (target 70%, min 100, max 1500). However, the batch job still gets throttled for the first 5 minutes. Why?", opts:["Auto-scaling reacts to consumed capacity metrics which have a delay — the scale-up happens after throttling is already occurring","DynamoDB has a hard partition limit that auto-scaling can't overcome","The batch job exceeds the burst capacity before auto-scaling can react","Auto-scaling doesn't work with provisioned capacity mode"], correct:0, exp:"DynamoDB auto-scaling uses CloudWatch metrics (ConsumedReadCapacityUnits) with a 1-2 minute delay for alarm evaluation, plus time to update the table capacity. Total reaction time: 5-10 minutes. During this window, the sudden spike exceeds provisioned + burst capacity. Solutions: (1) Schedule a capacity increase via Application Auto Scaling scheduled actions, (2) Switch to on-demand mode for the table, or (3) Use Burst capacity (DynamoDB saves unused capacity for 5 minutes).", domain:3},
  {q:"A company configures VPC Flow Logs to capture all traffic in their VPC. They notice that some legitimate traffic between instances in the same subnet shows 'REJECT' in the flow logs even though Security Groups allow the traffic. What is the MOST likely explanation?", opts:["Network ACLs are blocking the return traffic (NACLs are stateless)","Security Groups are evaluated after NACLs, and NACLs are rejecting the traffic","VPC Flow Logs show rejected packets from the NACL evaluation even if Security Groups would allow them","The instances have host-based firewalls blocking traffic"], correct:0, exp:"NACLs are stateless — they evaluate inbound AND outbound traffic independently. If inbound traffic is allowed but the outbound rule for the return traffic (ephemeral ports) is missing, the return traffic is rejected. Security Groups are stateful (return traffic is automatically allowed), but NACLs aren't. Flow logs show the NACL rejection. This is the most common NACL/SG confusion on the exam.", domain:1},
  {q:"A company uses S3 versioning and accidentally deletes an important object. They then try to recover it but find the 'deleted' object still appears as deleted. They see a 'delete marker' but don't know how to recover the original. What is the correct recovery procedure?", opts:["Restore from the S3 recycle bin","Delete the delete marker (which is itself a version) — this restores the object's most recent version","Use S3 Object Recovery API to undelete the object","Contact AWS support to restore the deleted object"], correct:1, exp:"When versioning is enabled, S3 'delete' doesn't actually remove data — it places a delete marker (a zero-byte version) on top. The object appears deleted because the current version is the delete marker. To recover: delete the delete marker version specifically (using its version ID). This makes the previous actual version the current version, effectively undeleting the object. There is no recycle bin or undelete API.", domain:1},
  {q:"A company uses CloudFormation to manage their infrastructure. They need to update an RDS instance from db.r5.large to db.r5.xlarge. They update the template and run the update. The update fails midway through, and CloudFormation shows the stack in UPDATE_ROLLBACK_FAILED state. What should they do?", opts:["Delete the stack and recreate it from scratch","Use the ContinueUpdateRollback action, potentially skipping the resource that caused the failure","Manually fix the RDS instance and then update the stack again","Contact AWS support — UPDATE_ROLLBACK_FAILED cannot be resolved by the customer"], correct:1, exp:"UPDATE_ROLLBACK_FAILED means CloudFormation tried to rollback but that also failed. ContinueUpdateRollback retries the rollback, optionally skipping resources that can't be rolled back (you fix those manually). The stack can then be updated again once in a stable state. Deleting the stack is a last resort (may lose resources). Manual fixes don't update CloudFormation's state. Customers can resolve this themselves.", domain:2},
  {q:"A company uses API Gateway REST API with Lambda proxy integration. Users report intermittent 504 Gateway Timeout errors. CloudWatch shows Lambda duration is consistently under 5 seconds. API Gateway timeout is set to 29 seconds (maximum). What is causing the 504s?", opts:["Lambda cold starts occasionally exceed the timeout","The Lambda function is hitting a concurrency limit, causing invocations to queue and timeout at the API Gateway level","API Gateway has a 30-second hard limit on integration timeout that cannot be exceeded","Network connectivity issues between API Gateway and Lambda"], correct:1, exp:"When Lambda hits its concurrency limit, new invocations are throttled and queued. API Gateway waits for the Lambda response, but if the queue delay + execution time exceeds 29 seconds, API Gateway returns 504. The Lambda itself runs in 5 seconds, but the wait time in the queue pushes total time over 29 seconds. Solution: increase Lambda reserved/provisioned concurrency. Cold starts rarely exceed 10 seconds. API GW to Lambda is over AWS backbone.", domain:3},
  {q:"A company stores data in S3 with SSE-S3 encryption. Their auditor asks: 'Where are the encryption keys stored and who has access to them?' What is the correct answer?", opts:["Keys are stored in KMS and only the bucket owner can access them","Keys are managed entirely by S3 — AWS manages, rotates, and controls access to the keys; the customer cannot view, access, or manage these keys directly","Keys are stored in CloudHSM and are FIPS 140-2 Level 3 validated","Keys are stored on the S3 storage servers alongside the data"], correct:1, exp:"SSE-S3 uses AES-256 encryption with keys entirely managed by AWS S3 service. Customers cannot access, rotate, or audit individual key usage. AWS handles all key management internally. This is why SSE-KMS (customer-managed CMK) is preferred for compliance — it provides key management control, rotation control, and CloudTrail logging of key usage. SSE-S3 keys are NOT in KMS or CloudHSM.", domain:1},
  {q:"A company has a Lambda function triggered by DynamoDB Streams. The function occasionally throws errors. They notice that the same failed record is reprocessed hundreds of times, blocking newer records in the stream. How should this be resolved?", opts:["Increase Lambda timeout and memory to prevent errors","Configure a Maximum Retry Attempts value and an On-Failure Destination (SQS DLQ) for the event source mapping","Remove DynamoDB Streams trigger and use polling with SQS instead","Implement try-catch in the Lambda function to never throw errors"], correct:1, exp:"DynamoDB Streams (like Kinesis) processes records in order. A failed record blocks all subsequent records in that shard (to maintain ordering). Configuring Maximum Retry Attempts (e.g., 3) + On-Failure Destination moves the problematic batch to a DLQ after retries are exhausted, unblocking the stream. You can also configure BisectBatchOnFunctionError to narrow down the failing record. Swallowing all errors loses failure visibility.", domain:2},
]
