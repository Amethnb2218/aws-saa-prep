export const questionBank22 = [
  {q:"A company has a VPC peering connection between VPC-A (10.0.0.0/16) and VPC-B (172.16.0.0/16). VPC-B also peers with VPC-C (192.168.0.0/16). Can instances in VPC-A communicate with VPC-C through VPC-B?", opts:["Yes — traffic routes through VPC-B automatically","No — VPC peering is NOT transitive. VPC-A can reach VPC-B, VPC-B can reach VPC-C, but VPC-A CANNOT reach VPC-C through VPC-B. For A↔C communication: create a direct peering connection between A and C, or use Transit Gateway.","Only if route tables are properly configured in VPC-B","Yes — with a NAT in VPC-B"], correct:1, exp:"VPC Peering fundamental limitation: NON-TRANSITIVE. Each pair of VPCs needs its own peering connection. With 3 VPCs: need 3 peering connections (A↔B, B↔C, A↔C). This doesn't scale — N VPCs need N×(N-1)/2 connections. Transit Gateway solves this: all VPCs connect to TGW (hub-spoke), any-to-any routing with one attachment per VPC. This is THE most commonly tested VPC peering concept.", domain:1},
  {q:"A company uses Amazon SQS. They need to ensure that a message is processed by only ONE consumer even when multiple consumers are reading from the queue simultaneously. How does SQS handle this?", opts:["SQS delivers each message to all consumers (that's SNS fan-out)","SQS Visibility Timeout — when a consumer receives a message, it becomes INVISIBLE to other consumers for the visibility timeout duration. If processed successfully and deleted: no other consumer sees it. If not deleted before timeout expires: message reappears for another consumer.","Messages are locked to the first consumer permanently","SQS requires explicit message locking API calls"], correct:1, exp:"SQS message lifecycle: (1) Producer sends message → visible in queue. (2) Consumer A receives it → message becomes INVISIBLE (visibility timeout starts, default 30 seconds). (3) Consumer B calls ReceiveMessage → doesn't see it (invisible). (4) Consumer A processes and deletes it → done. OR if Consumer A fails/crashes → timeout expires → message becomes visible again → another consumer can process it. This ensures at-least-once processing without explicit locking. Increase visibility timeout if processing takes longer than default.", domain:2},
  {q:"A company uses AWS Organizations with consolidated billing. They purchase a Reserved Instance in Account A. Can Account B (same Organization) benefit from the RI discount?", opts:["No — RIs are account-specific","Yes — RI discount sharing is enabled by default across all accounts in an Organization. If Account A's RI is unused (instance not running), the discount automatically applies to matching instances in Account B. Can be disabled per account if needed.","Only if both accounts are in the same OU","Only Savings Plans share, not RIs"], correct:1, exp:"RI sharing in Organizations: by default, unused RI discounts 'float' across member accounts. If Account A has an RI for m5.xlarge in us-east-1 and no matching instance running, Account B's m5.xlarge in us-east-1 automatically gets the discount. This maximizes RI utilization across the Organization. Can be disabled: Billing → Preferences → disable RI sharing (per account opt-out). Also applies to Savings Plans. This is a significant cost optimization for multi-account setups.", domain:4},
  {q:"A company uses Amazon Aurora. They notice that during failover (primary instance fails), their application takes 60+ seconds to reconnect. Aurora failover itself takes ~30 seconds, but the application adds delay. What causes the extra delay?", opts:["Aurora failover is always 60+ seconds","Application-level DNS caching — the application caches the old IP address (from the previous primary) and doesn't re-resolve the cluster endpoint DNS. Fix: reduce DNS TTL on the application side, use the Aurora cluster endpoint (CNAME with 5-second TTL), and configure connection pools to validate connections before use.","The security group takes time to update","Read replicas slow down failover"], correct:1, exp:"Aurora failover delay sources: (1) Aurora promotes replica (~30 seconds). (2) DNS update: Aurora updates the cluster endpoint CNAME to the new primary IP (~5 seconds). (3) APPLICATION DNS CACHE: many connection pools cache resolved IPs for 30-60 seconds (ignoring the 5-second TTL). (4) Stale connections in pool: pool tries dead connections before refreshing. Fixes: set JVM DNS TTL to 5 seconds (networkaddress.cache.ttl=5), configure pool to validate connections (testOnBorrow), use Smart Driver (AWS JDBC driver with topology awareness).", domain:2},
  {q:"A company uses Amazon S3 for a multi-tenant application. Each tenant's data is stored under their own prefix (s3://bucket/tenant-123/). They want to ensure Tenant A's applications can NEVER access Tenant B's prefix, even by mistake. What enforces this?", opts:["Application-level path checking only (bug-prone)","IAM policies with Condition: s3:prefix matching the tenant's path. Each tenant's IAM role has a policy allowing s3:* only on s3://bucket/tenant-id/* — enforced by IAM, not the application.","S3 Access Points — create one per tenant with a bucket policy that restricts the access point to only the tenant's prefix. Access point policy + bucket policy together enforce isolation.","Both B and C — IAM conditions for role-based, Access Points for simplified per-tenant policies"], correct:3, exp:"Both approaches work: (1) IAM condition: Resource: arn:aws:s3:::bucket/tenant-123/* on the tenant's role. Simple, IAM-enforced. (2) S3 Access Points: each tenant gets their own access point with a scoped policy (only allows access to /tenant-123/). Simpler policy management at scale (each access point has a small focused policy vs one giant bucket policy). Access Points also support VPC restrictions. For multi-tenant at scale: Access Points are recommended (separate policy per tenant, delegation possible).", domain:1},
  {q:"A company uses AWS CodeDeploy for Lambda deployments. They want to gradually shift traffic from the old version to the new version over 30 minutes (not all at once). Which deployment configuration?", opts:["AllAtOnce (immediate, no gradual shift)","Linear10PercentEvery3Minutes — shifts 10% of traffic every 3 minutes until 100% reaches the new version. If CloudWatch alarms trigger during any step: automatic rollback to old version.","Canary10Percent5Minutes (shifts 10% initially, then 100% after 5 minutes — not gradual)","CodeDeploy doesn't support Lambda traffic shifting"], correct:1, exp:"CodeDeploy Lambda deployment configurations: (1) Canary: shift X% immediately, wait N minutes, then shift remaining 100%. Two-step. (2) Linear: shift X% every N minutes incrementally (e.g., 10% every 3 min = fully deployed in 30 min). Gradual. (3) AllAtOnce: immediate 100% shift. For 30-minute gradual rollout: Linear10PercentEvery3Minutes. At each step: CodeDeploy checks CloudWatch alarms. If any alarm triggers (error rate spike, latency increase): automatic rollback. Safest deployment for production Lambda functions.", domain:2},
  {q:"A company uses Amazon Redshift. They want to share specific tables with another Redshift cluster in a different AWS account without copying data. What Redshift feature enables this?", opts:["Cross-account COPY command (copies data, doesn't share live)","Redshift Data Sharing — share live, up-to-date data (tables, schemas, UDFs) across Redshift clusters/accounts without copying. Consumer cluster queries producer's data directly with no data movement, ETL, or duplication.","Export to S3 and share bucket access (stale data)","Federated query from the consumer to the producer (not a native feature between Redshift clusters)"], correct:1, exp:"Redshift Data Sharing: producer cluster creates a 'datashare' containing specific schemas/tables. Grants access to consumer cluster (same or different account/region). Consumer creates a database from the datashare — tables appear as read-only external tables. Data stays on the producer — no copying, always up-to-date. Benefits: real-time data access, no ETL, pay for compute on consumer side only. Use cases: cross-team analytics, data marketplace, isolating BI workloads from ETL workloads.", domain:3},
  {q:"A company uses AWS Lambda with Java runtime. They experience cold starts of 5-10 seconds (JVM initialization + framework loading). This is unacceptable for their API. What solutions reduce Java Lambda cold starts?", opts:["Increase memory (helps but won't eliminate 5-10s cold start for large Java apps)","Lambda SnapStart (for Java) — takes a snapshot of the initialized execution environment. On cold start, restores from snapshot instead of full initialization. Reduces cold start from seconds to <200ms. Alternatively: Provisioned Concurrency keeps environments warm (zero cold starts).","Switch to Python (not always feasible for existing Java codebases)","Use GraalVM native compilation"], correct:1, exp:"Lambda SnapStart for Java: (1) Lambda initializes the function (JVM + framework + your code), (2) Takes a snapshot (Firecracker microVM snapshot) of the initialized state, (3) On subsequent cold starts: restores from snapshot in ~200ms instead of full JVM bootstrap (5-10 seconds). Works with: Corretto 11+, Spring Boot, Quarkus, Micronaut. Alternative: Provisioned Concurrency (keeps N environments permanently warm — zero cold start but ongoing cost). SnapStart is cheaper for most workloads. Can combine both.", domain:3},
  {q:"A company has an application that processes messages from SQS. Processing each message takes 2 minutes. The default visibility timeout is 30 seconds. Messages are being processed TWICE (duplicates). Why?", opts:["SQS always delivers messages twice","The visibility timeout (30s) expires BEFORE processing completes (2 minutes). After 30 seconds, the message becomes visible again, and another consumer picks it up. Fix: increase visibility timeout to at least 2.5 minutes (processing time + buffer).","The consumer is polling too frequently","The queue is configured incorrectly"], correct:1, exp:"Visibility timeout must exceed processing time. Flow: Consumer receives message → 30s visibility timeout starts → consumer still processing at 30s → message reappears → second consumer receives same message → DUPLICATE processing. Fix: set VisibilityTimeout ≥ 150 seconds (2 min processing + 30s buffer). Also: use ChangeMessageVisibility API to extend timeout if processing takes longer than expected. This is a fundamental SQS concept — visibility timeout must always exceed maximum expected processing time.", domain:2},
  {q:"A company uses Amazon API Gateway. They notice their API responses include CORS errors in the browser. The backend Lambda returns correct data, but the browser blocks it. What is missing?", opts:["Lambda is returning wrong data","API Gateway (or the Lambda response) must include CORS headers: Access-Control-Allow-Origin, Access-Control-Allow-Methods, Access-Control-Allow-Headers. For REST API: enable CORS in the console (adds OPTIONS method + headers). For HTTP API: configure CORS in the API settings. The browser's preflight OPTIONS request must also be handled.","The browser has a bug","CloudFront is stripping headers"], correct:1, exp:"CORS requirements for API Gateway: (1) Lambda responses must include Access-Control-Allow-Origin header (e.g., 'https://myapp.com' or '*'). (2) API Gateway must handle OPTIONS preflight requests (browser sends OPTIONS before actual request). For REST API: enable CORS via console (auto-creates OPTIONS method with mock integration returning CORS headers). For HTTP API: configure CORS in the API settings. For Lambda proxy integration: Lambda function itself must return CORS headers in the response. Common gotcha: enabling CORS on the method but forgetting to add headers in the Lambda response.", domain:3},
  {q:"A company uses S3 for data storage. They notice their S3 bill shows significant 'Data Transfer Out' charges. Most of their data consumers are EC2 instances in the SAME region as the S3 bucket. Why are there transfer charges?", opts:["S3 always charges for data transfer out","Data transfer from S3 to EC2 in the SAME region is FREE. Check: (1) Are some consumers in DIFFERENT regions? (2) Is data being downloaded to the internet (non-AWS)? (3) Is CloudFront serving the data (CF to internet has transfer charges)? S3 → same-region EC2 = $0. S3 → different region or internet = charged.","EC2 instances are in a different AZ (cross-AZ transfer)","VPC endpoint charges"], correct:1, exp:"S3 data transfer pricing: S3 → EC2 same region = FREE. S3 → EC2 different region = $0.01-0.02/GB. S3 → Internet = $0.09/GB (first 10TB). Common sources of unexpected transfer charges: (1) Cross-region access (consumers in different region than bucket). (2) Internet egress (data served to end users/non-AWS destinations). (3) S3 Transfer Acceleration charges ($0.04-0.08/GB). Note: cross-AZ transfer within the same region for S3 is FREE (unlike EC2-to-EC2 cross-AZ which costs $0.01/GB each way).", domain:4},
  {q:"A company uses AWS Fargate for ECS tasks. They want to mount an EBS volume to a Fargate task for persistent block storage. Is this possible?", opts:["Yes — Fargate supports EBS volumes natively (as of 2024, EBS volumes ARE supported for Fargate tasks)","No — Fargate only supports ephemeral storage and EFS. For persistent block storage: use ECS on EC2 launch type with EBS, or use EFS (file-level, not block-level).","Fargate tasks can only use S3","EBS volumes attach automatically to all Fargate tasks"], correct:0, exp:"As of 2024, Amazon ECS supports EBS volume attachments for Fargate tasks! Configure in the task definition — EBS volume is created, attached to the task, and detached when the task stops. Supports gp3, io2, and other EBS types. Use cases: databases on Fargate, stateful workloads. Previously: Fargate only supported ephemeral storage (20-200GB, deleted on task stop) and EFS (shared file storage). EBS on Fargate enables persistent, high-performance block storage without EC2 management.", domain:3},
  {q:"A company uses AWS Glue for ETL jobs. They want Glue jobs to access resources in their VPC (e.g., an RDS database in a private subnet). How is VPC connectivity configured for Glue?", opts:["Glue automatically accesses VPC resources","Configure a Glue Connection specifying the VPC, subnet, and security group. The Glue job uses this connection to create ENIs in the specified subnet, enabling access to VPC resources. The subnet needs a NAT Gateway if the Glue job also needs internet/S3 access.","Glue can't access VPC resources","Put RDS in a public subnet for Glue access"], correct:1, exp:"Glue VPC access: create a Glue Connection (Type: Network) specifying: VPC, subnet (private), security group (must allow self-referencing for Glue ENI communication + outbound to targets). Attach the connection to the Glue job. Glue creates ENIs in the subnet → can reach RDS/Redshift/other VPC resources. IMPORTANT: the subnet needs a NAT Gateway for Glue to reach S3 (where it reads/writes data) and other AWS services. Without NAT: Glue ENIs have no internet access. Use VPC endpoints for S3 (free) to avoid NAT for S3 traffic.", domain:2},
  {q:"A company wants to run a batch of 1000 similar but independent data processing tasks. Each takes 5-10 minutes. They want maximum parallelism and cost efficiency. Which service?", opts:["Lambda with 1000 concurrent invocations (works if each task fits within 15 min and 10GB memory)","AWS Step Functions Distributed Map — processes 1000 items in parallel, each running a child workflow or Lambda. Combined with Spot for compute-heavy tasks.","Run 1000 EC2 instances simultaneously","AWS Batch with a managed compute environment (auto-provisions optimal instances, supports Spot, handles job scheduling for 1000 independent jobs)"], correct:3, exp:"For 1000 independent 5-10 minute tasks: AWS Batch is ideal: (1) Define job with resource requirements. (2) Submit 1000 jobs to a job queue. (3) Managed compute environment auto-provisions EC2 instances (Spot for cost savings), distributes jobs, and scales back to zero when done. No instance management, Spot-aware (handles interruptions), and optimizes instance types. Lambda also works if tasks fit <15min and <10GB memory. Batch provides more compute flexibility (any vCPU/memory combination).", domain:4},
  {q:"A company uses RDS PostgreSQL with a 1TB database. They want to encrypt it (currently unencrypted). RDS doesn't support enabling encryption on an existing unencrypted instance. What is the migration path?", opts:["Enable encryption on the existing instance (not possible)","Create an encrypted snapshot: take a snapshot → copy the snapshot with encryption enabled (specify KMS key) → restore from the encrypted snapshot copy → switch application to the new encrypted instance. Then delete the old unencrypted instance.","Export all data and reimport (slow and complex)","Use DMS to replicate to an encrypted target (works but heavier than snapshot approach)"], correct:1, exp:"RDS encryption migration (unencrypted → encrypted): (1) Create snapshot of unencrypted DB. (2) Copy snapshot WITH encryption (specify CMK): CopyDBSnapshot --source-db-snapshot-identifier original --target-db-snapshot-identifier encrypted-copy --kms-key-id arn:aws:kms:... (3) Restore the encrypted snapshot to a new DB instance. (4) Update application connection string to new instance. (5) Delete old unencrypted instance. Downtime: only the time to switch the connection string. Data volume doesn't affect the switch time.", domain:2},
  {q:"A company uses S3 Lifecycle rules. They have a rule transitioning objects from Standard to Standard-IA after 30 days, then to Glacier after 90 days. An object is uploaded and accessed daily for 60 days. What happens?", opts:["Object stays in Standard because it's being accessed","Object transitions to Standard-IA at 30 days REGARDLESS of access frequency. S3 lifecycle rules are based on object AGE (creation/modification time), NOT access patterns. The object moves to IA at day 30 even though it's actively accessed. For access-based tiering: use S3 Intelligent-Tiering instead.","Object transitions to Glacier at 60 days","Lifecycle rule is skipped for frequently accessed objects"], correct:1, exp:"Critical distinction: Lifecycle rules are TIME-BASED (object age since creation). They IGNORE access patterns. Object transitions to IA at day 30 even if accessed every second. For access-pattern-based tiering: use S3 Intelligent-Tiering (automatically moves objects based on actual access). Combining lifecycle rules with Standard-IA can lead to higher costs (per-access retrieval fees on frequently-accessed IA objects). This is a commonly tested misconception — lifecycle rules ≠ access-based tiering.", domain:4},
  {q:"A company uses CloudFront with an S3 origin using Origin Access Control (OAC). They need to allow both CloudFront AND a specific IAM role (for backend processing) to access the same S3 bucket. How is the bucket policy structured?", opts:["Can't have multiple principals — either CloudFront or IAM, not both","Bucket policy with multiple statements: Statement 1 allows CloudFront service principal (cloudfront.amazonaws.com) with condition on distribution ARN. Statement 2 allows the specific IAM role ARN. Both statements coexist in the same policy.","Use two separate buckets","CloudFront OAC blocks all other access"], correct:1, exp:"S3 bucket policies support multiple statements with different principals. OAC adds a statement for CloudFront; you add another for your IAM role. Example: Statement 1: Principal: cloudfront.amazonaws.com, Action: s3:GetObject, Condition: SourceArn matches your distribution. Statement 2: Principal: arn:aws:iam::account:role/backend-role, Action: s3:*, Resource: bucket/*. Block Public Access stays enabled (neither statement grants public access). Multiple access patterns coexist through well-structured policies.", domain:1},
  {q:"A company uses AWS Secrets Manager with automatic rotation for their RDS database password. The rotation interval is 30 days. On day 31, the rotation succeeds but the application can't connect (wrong password). What likely happened?", opts:["Secrets Manager didn't update the database password","The rotation Lambda successfully updated the secret in Secrets Manager AND changed the password in RDS. The application is caching the OLD password from before rotation. Fix: application should handle authentication failure by re-fetching the secret from Secrets Manager (retry logic on auth failure).","RDS rejected the new password","Secrets Manager rotation is broken"], correct:1, exp:"Secrets Manager rotation flow: Lambda (1) creates new password, (2) updates RDS password, (3) updates the secret value. All succeeded. Problem: application cached the OLD password value (in environment variable, in connection pool config, or in a global variable). After rotation: cached value is stale → auth failure. Best practice: implement retry-on-auth-failure (catch auth error → re-fetch secret → retry connection). Or use Secrets Manager caching library with short TTL. NEVER cache secrets indefinitely.", domain:2},
  {q:"A company uses DynamoDB. They have a table with partition key 'user_id' and frequently query by 'email' attribute. The query 'get user by email' requires a Scan (checking every item). How do they make this efficient?", opts:["Add a filter expression to the Scan (still scans everything, just returns less)","Create a Global Secondary Index (GSI) with partition key = email. Now Query on the GSI by email returns the item directly without scanning. GSIs provide alternative access patterns on non-key attributes.","Add email as a sort key (can't change table keys after creation)","Use DynamoDB Streams to maintain a lookup table"], correct:1, exp:"GSIs solve the 'query by non-key attribute' problem. Original table: PK=user_id (can only Query by user_id). GSI: PK=email → Query by email returns results in milliseconds (not scanning). GSIs are eventually consistent with the base table (slight replication lag). They have their own capacity (RCU/WCU). You can project specific attributes to the GSI (minimize GSI size). Maximum 20 GSIs per table. This is THE solution for secondary access patterns in DynamoDB.", domain:3},
  {q:"A company uses ECS on EC2. They want to update their container image to a new version with zero downtime. The service has 4 tasks running. What deployment configuration ensures zero downtime?", opts:["Stop all tasks then start new ones (causes downtime)","Rolling update with minimumHealthyPercent=100 and maximumPercent=200 — ECS launches 4 NEW tasks (with new image) alongside the 4 old tasks (200% max = 8 tasks simultaneously). After new tasks pass health checks: old tasks are drained and stopped. Zero downtime.","Replace tasks one at a time with minimumHealthyPercent=75","Blue/Green with CodeDeploy (also zero downtime but requires more setup)"], correct:1, exp:"ECS rolling update parameters: minimumHealthyPercent: minimum percentage of tasks that must stay running during update. maximumPercent: maximum total tasks allowed during update. For zero downtime with 4 tasks: min=100% (always 4 healthy tasks), max=200% (up to 8 tasks during transition). ECS: launches 4 new → waits for health → drains old → removes old. Alternative: min=50%, max=100% (update 2 at a time, some capacity reduction but fewer extra resources). 100/200 is the zero-downtime configuration.", domain:2},
  {q:"A company uses S3 and notices 503 SlowDown errors on PUT requests. Their application uploads thousands of objects per second to the same prefix. What causes this and how do they fix it?", opts:["S3 has a global limit of 100 requests/second","S3 supports 3,500 PUT requests per second PER PREFIX. If all uploads go to the same prefix (e.g., /uploads/), they may hit this limit. Fix: distribute objects across multiple prefixes (add date-based or hash-based prefix distribution: /uploads/2024/01/15/abc123/ or /a1b2c3/file.jpg). S3 automatically partitions by prefix.","S3 has no rate limits","Increase S3 storage class for higher throughput"], correct:1, exp:"S3 performance per prefix: 3,500 PUT/COPY/POST/DELETE and 5,500 GET/HEAD requests per second per prefix. '503 SlowDown' = prefix request rate exceeded. Solutions: (1) Distribute uploads across prefixes (add hash, date, or random prefix). S3 partitions based on key prefix — more unique prefixes = more partitions = higher aggregate throughput. (2) Use hex hash prefix: /ab/cd/file.jpg (256 possible first-level prefixes × 3,500 = 896,000 PUT/s potential). Modern S3 auto-partitions better, but extreme workloads still benefit from prefix distribution.", domain:3},
  {q:"A company uses Route 53 with weighted routing: 70% to us-east-1 ALB, 30% to eu-west-1 ALB. They want the weights to automatically adjust based on each region's health — if us-east-1 becomes unhealthy, ALL traffic should go to eu-west-1. How?", opts:["Weighted routing handles failover automatically","Associate health checks with each weighted record. When Route 53 detects the us-east-1 endpoint is unhealthy (health check fails): it removes that record from consideration. All DNS responses then point to eu-west-1 (100% of resolvable traffic goes to the healthy endpoint). Traffic automatically returns to 70/30 when us-east-1 recovers.","Use failover routing instead of weighted","Change weights manually during incidents"], correct:1, exp:"Route 53 weighted routing + health checks: when a record's associated health check fails, Route 53 REMOVES that record from the response pool. Remaining healthy records share traffic proportionally. In this case: if only eu-west-1 is healthy, it gets 100% of traffic. When us-east-1 recovers: returns to 70/30 split. This combines the benefits of traffic distribution (weighted) with automatic failover (health checks). Same principle works with latency-based, geolocation, and multivalue routing.", domain:2},
  {q:"A company uses Amazon OpenSearch Service. They want to keep their cluster secure. What are the access control options?", opts:["OpenSearch has no access control","Multiple layers: (1) Network: VPC access (cluster in private subnet, no public access). (2) IAM-based: fine-grained access control with IAM principal policies. (3) SAML: federate with corporate IdP for Dashboards access. (4) Internal database: username/password with role-based access control. (5) Index-level and document-level security (fine-grained: who can see what).","Only IP-based access (IP whitelist)","Only VPC access with no further controls"], correct:1, exp:"OpenSearch access control layers: (1) Network: deploy in VPC (private, no internet access) or public (with IP-based access policy). (2) Resource-based policy: IAM conditions (account, IP, VPC endpoint). (3) Fine-Grained Access Control (FGAC): index/document/field level permissions — define roles with access to specific indexes, apply row/column level security. (4) SAML for Dashboards SSO. (5) Encryption: at-rest (KMS) + in-transit (TLS). Multi-layered security model supporting compliance requirements.", domain:1},
  {q:"A company uses AWS DataSync to replicate data from on-premises NFS to S3 daily. They only want to transfer files that have CHANGED since the last sync (incremental). Does DataSync support this?", opts:["No — DataSync always transfers all files","Yes — DataSync automatically performs INCREMENTAL transfers. It compares source and destination (metadata: timestamps, file sizes) and only transfers new/modified files. First run: full transfer. Subsequent runs: only changes. No manual file tracking needed.","Must use custom scripts to track changed files","Only supports full transfers on schedule"], correct:1, exp:"DataSync incremental transfer: each task execution compares source and destination metadata. Only files that are new, modified, or deleted since the last run are transferred. This is the DEFAULT behavior — no configuration needed. For large datasets with millions of files: DataSync efficiently identifies changes (parallel listing + comparison). Options: transfer data + metadata, data only, or metadata only. Handles: new files, modified files, deleted files (optional sync deletion). Dramatically reduces transfer time for subsequent runs.", domain:2},
  {q:"A company uses EC2 Spot Instances with a Spot Fleet. They want to maintain 100 instances at all times but minimize interruptions. What Spot Fleet allocation strategy should they use?", opts:["lowestPrice (cheapest but highest interruption rate)","capacityOptimized — selects instance pools with the HIGHEST availability (lowest chance of interruption) based on real-time Spot capacity data. Prioritizes continuity over lowest price.","diversified (spreads across all pools equally, regardless of capacity)","price-capacity-optimized (balances between lowest price AND highest availability — recommended default strategy)"], correct:3, exp:"Spot Fleet allocation strategies: (1) lowestPrice: cheapest pools (highest interruption risk). (2) capacityOptimized: pools with most available capacity (lowest interruption rate but potentially more expensive). (3) diversified: equal across pools (good for long-running workloads). (4) priceCapacityOptimized (RECOMMENDED): considers BOTH price AND capacity — finds pools that are cheap AND have good availability. Best overall strategy for most workloads. Minimizes interruptions while maintaining cost savings. Always specify multiple instance types for all strategies.", domain:4},
  {q:"A company uses CloudFormation DeletionPolicy attribute on their RDS resource. They set DeletionPolicy: Snapshot. What happens when the stack is deleted?", opts:["The RDS instance is deleted immediately with all data","CloudFormation takes a final snapshot of the RDS instance BEFORE deleting it. The snapshot is retained (not deleted with the stack) and can be used to restore the database later. Data is preserved even after stack deletion.","The RDS instance is retained (not deleted)","The stack deletion fails"], correct:1, exp:"DeletionPolicy options: (1) Delete (default): resource is deleted with the stack (data lost). (2) Retain: resource is kept but no longer managed by CloudFormation. (3) Snapshot (RDS, ElastiCache, EBS, Redshift, Neptune): takes a final snapshot before deletion — data preserved in the snapshot. Best practice for stateful resources: always use DeletionPolicy: Snapshot (or Retain) for databases. This prevents accidental data loss during stack operations.", domain:2},
  {q:"A company uses Amazon Kinesis Data Streams. They want to retain stream data for 30 days (for reprocessing if needed). Default retention is 24 hours. What is the maximum retention period?", opts:["7 days maximum","365 days (1 year) — Kinesis supports retention from 24 hours to 365 days. Longer retention costs more ($0.020 per shard-hour for extended retention beyond 24h + $0.012 per GB-month for long-term retention beyond 7 days).","24 hours is the maximum (it's the default, not max)","30 days maximum"], correct:1, exp:"Kinesis Data Streams retention: minimum 24 hours (default), maximum 365 days. Pricing tiers: (1) First 24 hours: included in shard-hour price. (2) Extended retention (24h-7 days): additional $0.020/shard-hour. (3) Long-term retention (7-365 days): $0.012/GB-month for stored data. For 30-day retention: enable extended + long-term retention. Use cases: reprocessing after consumer bug fixes, compliance requirements, replay for new analytics. Data is available for Consumers using GetRecords/Enhanced Fan-Out throughout the retention period.", domain:3},
  {q:"A company has a REST API that must respond within 50ms. They use Lambda behind API Gateway. Cold starts occasionally push latency to 500ms+. They don't want to over-provision. What gives consistent low-latency WITHOUT wasting money?", opts:["Accept occasional cold starts (violates SLA)","Provisioned Concurrency with auto-scaling — pre-initialize a BASE number of environments (e.g., 5) for guaranteed warm response + auto-scale provisioned concurrency based on utilization. Scale up during known peak hours (scheduled), scale down at night.","Always keep 1000 provisioned environments","Use EC2 instead of Lambda"], correct:1, exp:"Provisioned Concurrency + Auto Scaling: (1) Set baseline provisioned concurrency (always warm, zero cold starts for that capacity). (2) Auto-scale provisioned concurrency using Application Auto Scaling (target: utilization 70%). (3) Scheduled scaling: increase before known peak hours, decrease at night. Cost: pay for provisioned environments ($0.015/GB-hour) only when needed. Traffic exceeding provisioned capacity uses on-demand Lambda (with cold starts). This balances cost and latency SLA. You pay for warm capacity but only as much as needed.", domain:4},
  {q:"A company uses Amazon S3 Replication (CRR or SRR). After enabling replication, they notice existing objects are NOT replicated — only new objects uploaded after enabling replication. How do they replicate existing objects?", opts:["Replication automatically includes existing objects (it doesn't by default)","Use S3 Batch Replication — a feature that replicates existing objects that were uploaded before replication was enabled. Submit a Batch Replication job to copy all pre-existing objects to the destination.","Must manually copy existing objects with aws s3 cp","Existing objects can never be replicated"], correct:1, exp:"S3 Replication behavior: by default, only objects uploaded AFTER enabling replication are replicated. Existing objects: not automatically replicated. S3 Batch Replication (released 2022): replicates existing objects, previously-failed objects, and objects replicated to a prior destination. Submit a job → S3 Batch Operations handles the replication at scale. No custom scripts needed. This addresses the common gap: 'I enabled replication but my 10TB of existing data didn't copy.'", domain:2},
  {q:"A company uses EC2 Mac instances for iOS app building. What is unique about EC2 Mac instances compared to standard EC2?", opts:["Mac instances run Linux (they don't — they run actual macOS)","EC2 Mac instances have a MINIMUM allocation of 24 hours (dedicated host minimum). They run macOS on Apple hardware (Mac Mini) in AWS data centers. Required for: Xcode, iOS/macOS builds, Apple platform development. Can't be shared or stopped like regular EC2.","Mac instances are serverless","Mac instances support auto-scaling (they don't — dedicated hosts with 24h minimum)"], correct:1, exp:"EC2 Mac instances: actual Mac Mini hardware (M1, M2, Intel) in AWS racks. Key constraints: (1) Dedicated Host ONLY (can't launch on shared hardware — Apple licensing). (2) 24-hour minimum allocation period (can't release host before 24h). (3) No hibernation/stop like regular EC2 — must release the entire dedicated host. (4) Limited instance types (mac1.metal for Intel, mac2.metal for M1, mac2-m2.metal for M2). Use cases: iOS/macOS CI/CD, Xcode builds, macOS testing. Cost: ~$1/hour for mac2.metal (24h minimum = ~$25 per allocation period).", domain:3},
  {q:"A company uses Amazon SQS Dead Letter Queue. Messages have been in the DLQ for investigation. After fixing the consumer bug, they want to reprocess all DLQ messages through the original queue. What is the EASIEST approach?", opts:["Manually read and re-send each message (tedious at scale)","SQS DLQ Redrive — a native feature that moves messages from the DLQ back to the source queue (or any other queue) for reprocessing. Configure via console or API: select DLQ, choose destination queue, start redrive.","Delete DLQ and recreate (loses messages)","There's no way to move messages between queues natively"], correct:1, exp:"SQS Dead Letter Queue Redrive (released 2021): native feature to move messages from DLQ back to their source queue (or custom destination). Steps: (1) Select the DLQ in console. (2) Click 'Start DLQ redrive'. (3) Choose: redrive to source queue or custom queue. (4) Set velocity (messages per second). Messages are moved back for reprocessing. No custom Lambda or scripts needed. After redrive: verify messages process successfully. If they fail again: back to DLQ for further investigation.", domain:2},
  {q:"A company uses ECS with Service Connect. What does Service Connect provide over traditional service discovery?", opts:["Same as Cloud Map service discovery","Service Connect provides managed service mesh capabilities: client-side load balancing, automatic retries, connection timeouts, circuit breaking, and observability metrics per service-to-service connection. It abstracts Envoy proxy management — easier than App Mesh with similar benefits for ECS-native workloads.","Service Connect is only for EKS","Only provides DNS-based discovery like Cloud Map"], correct:1, exp:"ECS Service Connect (released 2022): simplified service mesh for ECS. Adds an Envoy proxy sidecar automatically (managed by ECS). Provides: (1) Client-side load balancing (round-robin, least connections). (2) Health-checked connections (routes away from unhealthy tasks). (3) Automatic retries + timeouts. (4) CloudWatch metrics per connection (requests, latency, errors). (5) No DNS propagation delay (instant mesh updates). Simpler than full App Mesh — purpose-built for ECS services. Cloud Map provides only DNS discovery without proxy features.", domain:2},
  {q:"A company uses AWS Proton. What problem does Proton solve?", opts:["Network monitoring","AWS Proton standardizes infrastructure provisioning for PLATFORM TEAMS: define templates (environment + service templates) that developers deploy self-service. Platform team manages IaC templates, developers deploy services without understanding CloudFormation/Terraform. Provides: template versioning, environment management, and deployment pipelines — enabling platform engineering at scale.","Container image scanning","Database migration"], correct:1, exp:"AWS Proton: platform engineering service. Platform team: defines environment templates (VPC, ECS cluster, RDS) and service templates (Fargate service, Lambda API). Developers: choose a template, provide inputs (app name, port, image), and deploy — without writing or understanding IaC. Proton manages: template versions (upgrade services when template updates), deployments, and integrated CI/CD pipelines. Bridges the gap between platform expertise and developer productivity. Alternative to custom internal developer platforms.", domain:2},
  {q:"A company uses Amazon Personalize. What does this service do?", opts:["Personalizes AWS console experience","Amazon Personalize provides ML-powered RECOMMENDATIONS — the same technology used by Amazon.com. Input: user interaction data (clicks, purchases, views). Output: personalized recommendations (products, content, search ranking) via real-time API. No ML expertise needed — provides recipes for common recommendation patterns (user personalization, similar items, trending).","Customizes IAM policies per user","Personalizes EC2 instance configurations"], correct:1, exp:"Amazon Personalize: managed recommendation system. Use cases: e-commerce product recommendations ('customers who bought X also bought Y'), content recommendations (Netflix-style 'watch next'), personalized search ranking, personalized email campaigns. Workflow: (1) Upload interaction dataset (user, item, timestamp, event_type). (2) Select recipe (USER_PERSONALIZATION, SIMILAR_ITEMS, POPULARITY_COUNT). (3) Train solution. (4) Deploy campaign (real-time API). Updates in real-time as new interactions are recorded. No ML expertise required.", domain:3},
  {q:"A company runs their application in a single AZ. A power outage takes down that AZ for 2 hours. Their application is completely down. What is the MINIMUM change for AZ-level resilience?", opts:["Move to a different AZ (same problem, different AZ)","Deploy across at least 2 AZs: ALB spanning 2 AZs, Auto Scaling Group in 2 AZs (min=2), RDS Multi-AZ. If one AZ fails: ALB routes to healthy AZ, ASG launches replacement instances, RDS fails over to standby. Application remains available.","Use S3 instead of EC2 (not applicable for all workloads)","Enable CloudWatch alarms (alerts but doesn't fix availability)"], correct:1, exp:"Multi-AZ architecture (minimum HA): (1) ALB: distributes across 2+ AZs. (2) EC2 Auto Scaling: instances in 2+ AZs, min capacity across AZs. (3) RDS Multi-AZ: synchronous standby in different AZ, auto failover. (4) ElastiCache Multi-AZ: replica in different AZ. One AZ fails → other AZ handles all traffic. Recovery time: near instant for ALB + ASG (health check-based), 30-60 seconds for RDS failover. This is the foundational AWS HA pattern. Cost: ~2x (running in 2 AZs) but can use Spot/RI to offset.", domain:2},
  {q:"A company uses AWS Certificate Manager (ACM) to provision SSL/TLS certificates. They need a certificate for *.example.com AND example.com (apex domain). Can one certificate cover both?", opts:["Need two separate certificates","Yes — one ACM certificate can include multiple Subject Alternative Names (SANs). Add both '*.example.com' (wildcard for subdomains) and 'example.com' (apex) as domain names when requesting the certificate. One certificate covers: example.com, www.example.com, api.example.com, etc.","Wildcard certificates automatically include the apex","ACM doesn't support wildcard certificates"], correct:1, exp:"ACM certificate with multiple SANs: request with: Domain name: *.example.com, Additional name: example.com. The wildcard covers all single-level subdomains (www, api, app, etc.) but NOT the apex domain itself. By adding example.com as an additional SAN, one certificate covers both. Validation (DNS or email) must be completed for both domain names. This is a common requirement — most users want both the naked domain and all subdomains covered by one cert.", domain:1},
  {q:"A company uses DynamoDB. They have a table that receives 100,000 writes per second. They use on-demand capacity mode. What is their maximum write throughput?", opts:["40,000 WCU (on-demand default limit)","On-demand scales to double the previous peak instantly, then adapts over time. For sustained 100K writes/sec: on-demand handles it if the table has been at similar levels before (or gradually ramped up). New tables start at 40K WCU equivalent, but adapt quickly. No hard limit for on-demand — scales based on traffic patterns.","10,000 WCU is the maximum","On-demand has the same limits as provisioned"], correct:1, exp:"DynamoDB on-demand capacity: (1) New table: starts with capacity for 40,000 RCU and 40,000 WCU (equivalent). (2) Adapts: as traffic increases, DynamoDB allocates more capacity. Previous peak × 2 is available instantly; beyond that takes minutes. (3) For sustained 100K WCU: if the table was previously at 50K+, on-demand instantly provides 100K. If jumping from 5K to 100K: may throttle briefly then adapt. (4) Per-partition limit: 1000 WCU per partition (hot key issue remains). On-demand removes capacity planning but doesn't eliminate partition-level limits.", domain:3},
]
