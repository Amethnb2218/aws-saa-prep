export const questionBank10 = [
  // Storage Gateway, EFS, FSx (commonly tested)
  {q:"A company needs to give on-premises applications access to virtually unlimited cloud storage through standard file protocols (NFS/SMB) while caching frequently accessed data locally. Which service provides this?", opts:["AWS DataSync","AWS Storage Gateway - File Gateway","Amazon EFS with Direct Connect","Amazon S3 mounted via s3fs"], correct:1, exp:"Storage Gateway File Gateway presents S3 as NFS or SMB file shares. Frequently accessed data is cached locally on the gateway appliance for low-latency access. The full dataset is in S3 (unlimited). DataSync is for one-time or scheduled transfers, not ongoing file system access. s3fs is not recommended for production.", domain:2},
  {q:"A company needs a high-performance shared file system for their Linux-based HPC cluster that can deliver hundreds of GB/s throughput. The data is also stored in S3. Which service is appropriate?", opts:["Amazon EFS with Max I/O performance mode","Amazon FSx for Lustre linked to an S3 bucket","Amazon FSx for Windows File Server","EBS io2 with Multi-Attach"], correct:1, exp:"FSx for Lustre is designed for HPC workloads with hundreds of GB/s throughput and sub-millisecond latency. It natively integrates with S3 (can read from and write back to S3). EFS Max I/O provides higher throughput but not at HPC scale. FSx for Windows uses SMB, not suitable for Linux HPC. EBS Multi-Attach only supports 16 instances.", domain:3},
  {q:"A company needs a Windows-compatible shared file system for their Windows EC2 instances. The file system must support Active Directory integration, Windows ACLs, and DFS namespaces. Which service should they use?", opts:["Amazon EFS","Amazon FSx for Windows File Server","Storage Gateway File Gateway with SMB","Windows File Server on EC2"], correct:1, exp:"FSx for Windows File Server is a fully managed Windows native file system supporting SMB protocol, Active Directory integration, Windows NTFS permissions/ACLs, DFS namespaces, and shadow copies. EFS uses NFS (Linux). Storage Gateway provides S3 access via SMB but lacks full Windows file server features. Self-managed on EC2 requires operational overhead.", domain:3},
  {q:"A company has an on-premises backup application that writes backups to a local disk using iSCSI protocol. They want to store these backups in AWS (S3/Glacier) without changing the backup application. What should they use?", opts:["Storage Gateway - Volume Gateway (Stored mode)","Storage Gateway - Volume Gateway (Cached mode)","Storage Gateway - Tape Gateway","AWS Backup with on-premises agent"], correct:2, exp:"Tape Gateway presents a virtual tape library (VTL) using iSCSI, which backup applications already support. Backups are written to virtual tapes, which are then stored in S3 (for active tapes) or Glacier/Deep Archive (for archived tapes). No application changes needed — it looks like a standard tape library. Volume Gateway uses iSCSI block storage, not tape backup protocol.", domain:2},
  {q:"A company has 100TB of data on-premises that they want to migrate to EFS. The migration must complete within 1 week with data integrity validation. Which service is designed for this?", opts:["AWS DataSync with source as NFS and destination as EFS","Storage Gateway File Gateway","AWS Snowball Edge with EFS as destination","rsync over Direct Connect"], correct:0, exp:"AWS DataSync is purpose-built for data migration/transfer between on-premises NFS/SMB and AWS storage (S3, EFS, FSx). It handles scheduling, integrity validation, bandwidth throttling, and encryption in transit. It can saturate a 10 Gbps link. Snowball doesn't support EFS directly. rsync requires custom management. Storage Gateway is for ongoing hybrid access, not bulk migration.", domain:2},

  // CloudWatch, CloudTrail, Config, Monitoring (frequently tested)
  {q:"A company needs to monitor the memory utilization of their EC2 instances. CloudWatch default metrics don't include memory. What should they do?", opts:["Enable detailed monitoring in EC2 settings","Install the CloudWatch Agent on instances to push custom memory metrics","Use AWS Config to track memory utilization","Enable Enhanced Monitoring for the instances"], correct:1, exp:"CloudWatch default metrics include CPU, network, and disk but NOT memory or disk space utilization. The CloudWatch Agent must be installed on instances to collect and publish OS-level metrics (memory, disk, processes) as custom metrics. Detailed monitoring increases metric frequency (1-minute vs 5-minute) but doesn't add new metrics. Enhanced Monitoring is for RDS, not EC2.", domain:2},
  {q:"A company needs to be alerted when their monthly AWS bill exceeds $10,000. What is the simplest way to set this up?", opts:["AWS Budgets with an alert threshold at $10,000","CloudWatch billing alarm in us-east-1 with threshold $10,000","AWS Cost Explorer with email reports","AWS Trusted Advisor cost checks"], correct:0, exp:"AWS Budgets is the simplest tool for budget alerts. Set a monthly cost budget of $10,000 with email/SNS notifications at desired thresholds (e.g., 80%, 100%). CloudWatch billing alarms also work but require enabling billing alerts first and are less feature-rich. Cost Explorer provides analysis but not real-time alerts. Trusted Advisor provides recommendations, not budget tracking.", domain:4},
  {q:"A company wants to ensure that all resources in their account are tagged with 'Environment' and 'CostCenter' tags. Non-compliant resources should be flagged and reported daily. Which service provides this?", opts:["AWS Config with required-tags managed rule","AWS Organizations tag policies","AWS Tag Editor","IAM policies requiring tags on resource creation"], correct:0, exp:"AWS Config required-tags rule continuously evaluates resources and flags those missing required tags. It provides compliance dashboard and can trigger remediation. Tag Policies in Organizations enforce tag formats but don't detect existing non-compliant resources. Tag Editor helps find/manage tags but doesn't enforce. IAM policies prevent creation without tags (preventive) but don't find existing non-compliant resources.", domain:1},
  {q:"A company needs to investigate a security incident. They need to determine WHO made a specific API call, WHEN it was made, FROM which IP address, and WHAT resources were affected. Which service provides this information?", opts:["Amazon GuardDuty","AWS CloudTrail","AWS Config","Amazon Detective"], correct:1, exp:"CloudTrail records API calls to AWS services including: who (IAM identity), when (timestamp), from where (source IP), what action (API call), and which resources (request parameters). This is the primary audit trail for all AWS API activity. GuardDuty detects threats using ML. Config records resource state changes. Detective analyzes and visualizes security findings.", domain:1},
  {q:"A company uses multiple AWS services and wants to correlate logs from CloudTrail, VPC Flow Logs, GuardDuty findings, and DNS logs to investigate security incidents visually. Which service provides automated investigation and visualization?", opts:["Amazon CloudWatch Logs Insights","Amazon Athena querying all log sources","Amazon Detective","AWS Security Hub"], correct:2, exp:"Amazon Detective automatically collects and analyzes data from CloudTrail, VPC Flow Logs, GuardDuty, and EKS audit logs. It builds behavior graphs and provides visual investigations to determine the root cause and scope of security issues. CloudWatch Logs Insights requires manual queries. Athena requires setting up tables. Security Hub aggregates findings but doesn't investigate.", domain:1},

  // Kinesis & Streaming (tested frequently)
  {q:"A company needs to process streaming data from IoT devices. They need: real-time processing with sub-second latency, the ability to replay data, and 7-day data retention. Which service combination is BEST?", opts:["SQS Standard queue with Lambda","Kinesis Data Streams (7-day retention) with Lambda or KCL consumers","Amazon MSK (Kafka) with 7-day retention","EventBridge with archive and replay"], correct:1, exp:"Kinesis Data Streams supports configurable retention (1-365 days), real-time processing with sub-second latency, data replay (re-read from any position), and multiple consumers. Lambda or KCL consumers process the stream. SQS doesn't support replay (messages deleted after processing). MSK works but adds operational complexity. EventBridge is for events, not high-throughput IoT streaming.", domain:3},
  {q:"A company needs to load streaming data into S3 in near-real-time (within 60 seconds), automatically converted to Parquet format and partitioned by date. The data comes from a Kinesis Data Stream. Which service delivers this with LEAST effort?", opts:["Lambda consuming from Kinesis, converting to Parquet, and writing to S3","Kinesis Data Firehose with format conversion (Parquet) and dynamic partitioning","AWS Glue Streaming ETL reading from Kinesis and writing to S3","EMR Spark Streaming reading from Kinesis and writing to S3 in Parquet"], correct:1, exp:"Kinesis Data Firehose natively supports: reading from Kinesis Data Streams, format conversion to Parquet/ORC (using a Glue table schema), dynamic partitioning (date/custom keys in S3 prefix), and configurable buffering (60-900 seconds). Fully managed, no code needed. Lambda requires writing Parquet conversion logic. Glue/EMR Streaming require managing compute infrastructure.", domain:3},
  {q:"A company's Kinesis Data Stream has 10 shards. They have 3 consumer applications that each need to read all data. They observe ReadProvisionedThroughputExceeded errors. What is the BEST solution?", opts:["Add more shards to increase read capacity","Enable Enhanced Fan-Out for each consumer (dedicated 2MB/s per shard per consumer)","Reduce the number of consumers","Increase the data retention period"], correct:1, exp:"Standard consumers share the 2MB/s read capacity per shard. With 3 consumers, each effectively gets ~667KB/s per shard. Enhanced Fan-Out provides DEDICATED 2MB/s per shard PER consumer using HTTP/2 push (no polling). Each consumer gets its own pipe, eliminating contention. Adding shards increases total capacity but doesn't solve per-consumer contention. Reducing consumers defeats the purpose.", domain:3},
  {q:"A data pipeline uses Kinesis Data Streams. A consumer application crashes and is restarted 2 hours later. It needs to resume processing from exactly where it stopped. How is this achieved?", opts:["Kinesis automatically remembers the consumer's position","Use the Kinesis Client Library (KCL) which checkpoints the last processed sequence number in DynamoDB","Read from TRIM_HORIZON to reprocess all available data","Use AT_TIMESTAMP iterator to start from 2 hours ago"], correct:1, exp:"KCL maintains checkpoints (last successfully processed sequence number) in a DynamoDB table. When the consumer restarts, it reads the checkpoint and resumes from that exact position. Without KCL, you'd need to manage checkpointing yourself. TRIM_HORIZON reprocesses everything (wasteful). AT_TIMESTAMP is approximate and may miss or duplicate records near the boundary.", domain:2},

  // SQS patterns (very common)
  {q:"A company uses SQS to decouple a web tier from a processing tier. They notice that some messages are processed twice, causing duplicate orders. The application is not idempotent. What is the QUICKEST fix?", opts:["Switch from SQS Standard to SQS FIFO queue (provides exactly-once processing)","Add message deduplication IDs to prevent duplicate sends","Implement idempotency in the consumer using a DynamoDB deduplication table","Increase the visibility timeout to prevent premature reprocessing"], correct:0, exp:"SQS FIFO queues provide exactly-once processing (no duplicates delivered). Switching from Standard to FIFO eliminates duplicates at the queue level. This is the quickest fix if throughput requirements allow (FIFO supports 3,000 msg/s with batching, 300/s without). Visibility timeout helps if duplicates are from premature returns (consumer not finishing in time). Idempotency is the gold standard but requires code changes.", domain:2},
  {q:"A company uses an SQS queue with Lambda. Messages that fail processing go to a Dead Letter Queue (DLQ) after 3 retries. The operations team wants to easily reprocess DLQ messages after fixing the underlying bug. What is the simplest approach?", opts:["Write a Lambda function that moves messages from DLQ back to the source queue","Use the SQS DLQ Redrive feature (Start DLQ redrive) in the console or API to move messages back to the source queue","Delete the DLQ and let messages be reprocessed from scratch","Configure the DLQ as a source for another Lambda function that reprocesses them"], correct:1, exp:"SQS DLQ Redrive (dead-letter queue redrive) is a native feature that moves messages back from the DLQ to the source queue (or a custom destination). One click in the console or API call. No Lambda function needed. This was added specifically for the use case of reprocessing after bug fixes. Custom Lambda redrive works but is unnecessary since the native feature exists.", domain:2},
  {q:"A company needs message ordering AND exactly-once processing. They also need to process different customers' messages in parallel. Their SQS FIFO queue is a bottleneck at 300 msg/sec. How can they increase throughput while maintaining per-customer ordering?", opts:["Switch to SQS Standard queue with application-level ordering","Enable SQS FIFO High Throughput mode (up to 70,000 msg/sec per queue with message group parallelism)","Create multiple FIFO queues and distribute customers across them","Switch to Kinesis Data Streams with customer_id as partition key"], correct:1, exp:"SQS FIFO High Throughput mode increases throughput to 70,000 messages per second per queue. It maintains ordering within each MessageGroupId (customer_id). Different MessageGroupIds are processed in parallel automatically. This is the native scaling solution. Multiple queues add routing complexity. Kinesis provides ordering but not exactly-once processing natively.", domain:3},

  // ECS/EKS/Containers (increasingly tested)
  {q:"A company runs microservices on ECS. They want to minimize operational overhead of managing the underlying infrastructure while supporting both x86 and ARM containers. Which launch type should they use?", opts:["ECS with EC2 launch type and mixed instance ASG","ECS with Fargate launch type","ECS Anywhere with on-premises servers","EKS with managed node groups"], correct:1, exp:"Fargate is serverless — no EC2 instances to manage, patch, or scale. AWS handles the infrastructure. It supports both x86 (default) and ARM64 (Graviton) containers via the runtimePlatform task definition parameter. EC2 launch type requires managing instances. ECS Anywhere is for on-premises. EKS managed node groups still require node management decisions.", domain:2},
  {q:"A company runs containers on ECS Fargate. Their application needs to persist data that survives container restarts and is shared between multiple containers in the same task. What storage should they use?", opts:["EBS volume mounted to the task","EFS file system mounted via task definition volume configuration","S3 bucket accessed via AWS SDK","Fargate ephemeral storage (20-200GB)"], correct:1, exp:"EFS can be mounted as a volume in ECS Fargate task definitions. It persists across container/task restarts and can be shared between containers within a task AND across tasks. EBS is not supported with Fargate (only EC2 launch type). Ephemeral storage is lost on task stop. S3 requires application-level I/O (not a file system mount).", domain:2},
  {q:"A company wants to run Kubernetes workloads on AWS with minimal operational overhead. They don't want to manage control plane or worker nodes. Which option provides this?", opts:["Self-managed Kubernetes on EC2","EKS with managed node groups","EKS with Fargate profiles","ECS with Fargate (not Kubernetes but minimal overhead)"], correct:2, exp:"EKS with Fargate profiles runs Kubernetes pods on Fargate — no worker nodes to manage. AWS manages both the control plane AND the compute. Each pod runs in its own micro-VM. Managed node groups still require some node decisions (instance types, scaling). Self-managed requires managing everything. ECS Fargate has minimal overhead but isn't Kubernetes.", domain:2},
  {q:"A company needs to run a batch processing container that takes 20 minutes and requires 16 vCPUs and 32GB RAM. It runs 100 times per day at unpredictable intervals. What is the MOST cost-effective compute option?", opts:["ECS on EC2 with a large instance running 24/7","ECS on Fargate (pay per task: vCPU-seconds + memory-seconds)","AWS Batch with Fargate compute environment","Lambda with container image (up to 10GB)"], correct:2, exp:"AWS Batch with Fargate automatically provisions right-sized compute only when jobs are submitted, and scales to zero between jobs. For 100 x 20-minute tasks/day = ~33 hours of compute, you only pay for those hours. ECS Fargate tasks need a trigger mechanism (you'd build it yourself). Batch provides native job queue, scheduling, retry logic. Lambda has a 15-minute timeout limit (20 min exceeds it). EC2 24/7 wastes idle time.", domain:4},

  // Identity & Federation (commonly tested)
  {q:"A company has 10,000 employees who need access to multiple AWS accounts. They use Okta as their identity provider. Users should authenticate once with Okta and access any permitted AWS account without additional credentials. Which service enables this?", opts:["IAM users in each account synced from Okta","AWS IAM Identity Center with SCIM provisioning from Okta and SAML 2.0 SSO","Cognito User Pool federated with Okta","IAM roles in each account with Okta SAML federation configured per-account"], correct:1, exp:"IAM Identity Center (formerly AWS SSO) provides centralized SSO to multiple AWS accounts with SAML 2.0 federation to Okta. SCIM automatically syncs users/groups from Okta. Users authenticate once with Okta and see all their permitted accounts in the Identity Center portal. Per-account SAML federation works but doesn't scale for 10+ accounts. Cognito is for application users, not AWS account access.", domain:1},
  {q:"A mobile application needs to allow users to sign in with Google and then access S3 to upload profile photos to their own prefix (users/<user-id>/*). Which service and configuration enables this?", opts:["Cognito User Pool with Google as a social identity provider, then Cognito Identity Pool to provide scoped temporary AWS credentials","IAM user per mobile user with S3 permissions","API Gateway with Lambda authorizer that validates Google tokens","Cognito User Pool only with S3 permissions in the user pool"], correct:0, exp:"Cognito User Pool handles authentication (Google sign-in). Cognito Identity Pool exchanges the Cognito token for temporary AWS credentials scoped via IAM role with a policy using ${cognito-identity.amazonaws.com:sub} as the S3 prefix. This gives each user access only to their own folder. IAM users for mobile users is unmanageable. API Gateway doesn't provide direct S3 access. User Pools authenticate but don't grant AWS credentials.", domain:1},
  {q:"A company's application needs to assume an IAM role in another AWS account. The role's trust policy is configured correctly. However, the application receives 'AccessDenied' when calling sts:AssumeRole. What is the MOST likely cause?", opts:["The role doesn't exist in the target account","The calling identity's IAM policy doesn't include permission to call sts:AssumeRole on the target role ARN","STS is not enabled in the target region","The role has exceeded its maximum session duration"], correct:1, exp:"Cross-account role assumption requires BOTH: (1) The role's trust policy allows the caller (configured correctly per the question), AND (2) The caller's own IAM policy permits sts:AssumeRole on the target role ARN. Many people forget the caller needs explicit permission to assume the role, even if the trust policy allows them. Missing either side results in AccessDenied.", domain:1},
  {q:"A company wants to provide temporary, limited-privilege credentials to a third-party contractor who doesn't have an AWS account. The contractor needs to upload files to a specific S3 bucket for 4 hours. What is the SIMPLEST approach?", opts:["Create a temporary IAM user with time-limited permissions","Generate S3 pre-signed URLs for PUT operations with 4-hour expiry","Create a role with S3 permissions and share temporary credentials via STS GetFederationToken","Use Cognito Identity Pool with unauthenticated access to S3"], correct:1, exp:"Pre-signed URLs are the simplest: generate PUT pre-signed URLs with 4-hour expiry. The contractor uses standard HTTP PUT to upload — no AWS credentials, SDK, or account needed. Just a URL. Federation tokens require more setup. Temporary IAM users require AWS Console/CLI access. Cognito is overkill for a single contractor. Pre-signed URLs are purpose-built for this exact use case.", domain:1},

  // Well-Architected & Best Practices (conceptual questions)
  {q:"A solutions architect is reviewing an application that stores session state on individual EC2 instances. When an instance terminates, users lose their session. Which Well-Architected Framework design principle does this violate?", opts:["Design for failure (everything fails, plan for it)","Implement elasticity (scale in and out)","Decouple components","Stop guessing capacity"], correct:0, exp:"Storing state on individual instances violates 'Design for failure' — when the instance fails, the session is lost. The fix: externalize session state to ElastiCache, DynamoDB, or another shared store. This is also related to making the application stateless (enabling horizontal scaling), but the core violation is not designing for the inevitable failure of an individual instance.", domain:2},
  {q:"A company runs all their workloads on a single large EC2 instance. The CTO asks the solutions architect what the main risk is. What should they highlight?", opts:["Cost — large instances are more expensive per compute unit","Single point of failure — if the instance fails, everything is down","Security — large instances have a bigger attack surface","Compliance — single instances don't meet audit requirements"], correct:1, exp:"Single point of failure (SPOF) is the primary architectural risk. If that one instance fails (hardware, AZ outage, software crash), ALL workloads are unavailable simultaneously. The solution: distribute workloads across multiple smaller instances, multiple AZs, with load balancing. This provides redundancy and fault isolation. Cost, security, and compliance are secondary concerns compared to total availability loss.", domain:2},
  {q:"A company is designing a new application. The architect proposes using managed services (RDS, ElastiCache, SQS) rather than self-managed alternatives (MySQL on EC2, Redis on EC2, RabbitMQ on EC2). What is the PRIMARY benefit?", opts:["Cost savings — managed services are always cheaper","Reduced operational overhead — AWS handles patching, backups, failover, and scaling","Better performance — managed services are optimized","Vendor lock-in prevention — managed services are more portable"], correct:1, exp:"The primary benefit of managed services is reduced undifferentiated heavy lifting. AWS handles: OS/software patching, automated backups, Multi-AZ failover, monitoring, scaling, and security patches. This frees the team to focus on business logic. Managed services aren't always cheaper (sometimes more expensive) and are less portable, but the operational efficiency gain is the primary architect justification.", domain:2},
  {q:"A solutions architect wants to implement the principle of least privilege for an application. The application currently uses an IAM role with AdministratorAccess. How should they scope down permissions?", opts:["Remove AdministratorAccess and add permissions one by one as the application fails","Use IAM Access Analyzer to generate a policy based on the role's actual CloudTrail activity over the last 90 days","Replace with PowerUserAccess (less than Admin but still broad)","Add a permission boundary that limits to specific services"], correct:1, exp:"IAM Access Analyzer policy generation reviews the role's actual API calls in CloudTrail and generates a least-privilege policy that covers exactly what the application has done. This is data-driven rather than guessing. Removing and adding on failures causes outages. PowerUserAccess is still overly broad. Permission boundaries add a cap but don't generate the minimal policy. Access Analyzer is the AWS-recommended approach.", domain:1},
  {q:"A company wants to implement a landing zone for their multi-account AWS environment with governance guardrails, centralized logging, and automated account provisioning. What is the FASTEST way to set this up?", opts:["Build a custom solution with CloudFormation, Organizations, and Config","Deploy AWS Control Tower","Use a third-party tool like Terraform with custom modules","Set up Organizations manually with SCPs and Config rules"], correct:1, exp:"AWS Control Tower sets up a complete landing zone in hours: multi-account structure with OUs, guardrails (preventive SCPs + detective Config rules), centralized CloudTrail/Config logging, account factory for provisioned new accounts. Building custom takes weeks/months. Control Tower is the fastest path to a well-governed multi-account environment. It uses Organizations, SCPs, Config under the hood.", domain:1},
]
