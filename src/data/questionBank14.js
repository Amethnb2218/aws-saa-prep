export const questionBank14 = [
  // Final massive bank to reach 1000+ questions
  {q:"A company wants to use AWS for disaster recovery of their on-premises VMware workloads. They need RTO under 15 minutes and want to pay only for storage during normal operations (no running EC2 instances). Which service provides this?", opts:["AWS Backup with VMware support","AWS Elastic Disaster Recovery (continuously replicates servers, launches recovery instances on demand)","VMware Cloud on AWS (running full VMware environment)","EC2 instances from AMIs created by VM Import/Export"], correct:1, exp:"AWS Elastic Disaster Recovery (DRS) continuously replicates on-premises servers to AWS using lightweight staging instances (low cost). During disaster, full-sized recovery instances launch in minutes (RTO <15 min). You only pay for: staging instances (small/cheap) + EBS storage for replicated data during normal operations. No production-sized instances running until failover.", domain:2},
  {q:"A company has an S3 bucket with millions of objects. They need to run a one-time operation to add a specific tag to all objects that match certain criteria (objects larger than 1MB created before 2024). Which S3 feature handles this at scale?", opts:["S3 Batch Operations with a manifest from S3 Inventory, using the PutObjectTagging operation","Lambda triggered by S3 Inventory report to tag each object individually","AWS CLI script iterating through all objects","S3 lifecycle rules to add tags"], correct:0, exp:"S3 Batch Operations performs large-scale operations on millions/billions of objects. Create a manifest from S3 Inventory (filtered to your criteria), then run a Batch Operation with PutObjectTagging. It handles retries, reporting, and parallel execution across millions of objects. Lambda per-object doesn't scale well for millions. CLI scripts are slow and unreliable at scale. Lifecycle rules can't add arbitrary tags.", domain:3},
  {q:"A company uses Amazon EKS. Their pods need to access S3 and DynamoDB. Instead of using the EC2 instance profile (which gives ALL pods on the node the same permissions), they want per-pod IAM permissions. What feature enables this?", opts:["IAM Roles for Service Accounts (IRSA) — associates an IAM role with a Kubernetes service account","Kubernetes Network Policies restricting AWS API access","Pod Security Policies with IAM conditions","EKS Pod Identity (newer feature that simplifies IAM role association with pods)"], correct:3, exp:"EKS Pod Identity (launched 2023) is the newer, simpler way to associate IAM roles with specific Kubernetes pods. It's simpler than IRSA (no OIDC provider setup needed). Each pod gets only the permissions of its associated role. IRSA also works (the original solution) but Pod Identity is the recommended approach going forward. Both provide per-pod IAM credentials rather than sharing the node's instance profile.", domain:1},
  {q:"A company has a REST API that serves both mobile apps and web browsers. Mobile requests should be routed to optimized Lambda functions (returning minimal JSON), while browser requests should be routed to different Lambda functions (returning full HTML). Both share the same URL paths. How can API Gateway route based on the User-Agent or Accept header?", opts:["Two separate API Gateway stages","API Gateway with Lambda authorizer that inspects headers and routes accordingly","API Gateway doesn't support header-based routing — use CloudFront with Lambda@Edge to route to different origins based on headers","ALB with listener rules based on HTTP headers routing to different target groups"], correct:3, exp:"ALB supports routing rules based on HTTP headers (host, path, query string, and custom headers like User-Agent or Accept). Different rules route to different target groups (each containing different Lambda functions or compute). API Gateway REST API doesn't support content-based routing within the same resource/method. CloudFront can route by header but adds complexity. ALB header-based routing is the simplest solution.", domain:3},
  {q:"A company stores customer data in DynamoDB. GDPR requires them to delete all data for a specific customer when they request deletion ('right to be forgotten'). Customer data is spread across 5 DynamoDB tables. What is the MOST efficient approach?", opts:["Scan each table with filter on customer_id and delete matching items","Use DynamoDB partition key = customer_id in all tables (or GSI on customer_id), Query to find all items, then BatchWriteItem to delete","Store customer_id in all items and use DynamoDB Streams to propagate deletion across tables","Tag items with customer_id and use S3-style lifecycle deletion"], correct:1, exp:"With customer_id as partition key (or GSI), Query efficiently finds all items for a customer without scanning. BatchWriteItem deletes up to 25 items per request (parallelize for speed). This is efficient and reliable. Scan reads the entire table (expensive for millions of items). Streams propagate changes but don't initiate deletion. The key design decision: ensure customer_id is queryable in every table (PK or GSI).", domain:1},
  {q:"A company has an Auto Scaling Group with min=2, desired=2, max=10. They configure a scheduled scaling action to set desired=8 every Monday at 9 AM (weekly traffic spike). But the ASG also has a target tracking policy (target CPU 60%). At 9 AM Monday, desired goes to 8, but by 9:05 AM, target tracking scales it back to 3 (CPU is low with 8 instances). How do they prevent this?", opts:["Remove the target tracking policy during scheduled scaling periods","Set the ASG minimum to 8 during the scheduled period (min=8 from 9 AM to 6 PM Monday)","Use Predictive Scaling which learns the weekly pattern and coordinates with target tracking","Both B and C work — setting min prevents scale-in, predictive scaling learns patterns automatically"], correct:3, exp:"Setting min=8 during the scheduled period prevents target tracking from scaling below 8 (target tracking respects min). Alternatively, Predictive Scaling learns weekly patterns and proactively scales (coordinating with target tracking to avoid conflicts). Both solve the problem: min constrains scale-in, predictive scaling anticipates the need. Just setting desired without adjusting min allows target tracking to override immediately.", domain:2},
  {q:"A company processes IoT telemetry data. Each device sends 1KB messages every second. They have 100,000 devices. They need to store ALL data for 90 days and provide real-time dashboards. What is the MOST cost-effective architecture for ingestion and storage?", opts:["API Gateway → Lambda → DynamoDB (too expensive at 100K writes/sec)","IoT Core → IoT Rules → Kinesis Data Firehose → S3 (Parquet, partitioned) for storage, with Kinesis Data Analytics for real-time dashboards","Direct to Kinesis Data Streams → Lambda → S3","IoT Core → DynamoDB with TTL for 90-day retention"], correct:1, exp:"100K messages/sec × 1KB = 100MB/sec ingestion. IoT Core handles MQTT connections from 100K devices natively. IoT Rules route to Kinesis Firehose which batches, converts to Parquet, and delivers to S3 (cheapest storage for 90 days: ~$300/TB/month × ~780TB = manageable with compression). Kinesis Data Analytics provides real-time SQL dashboards. DynamoDB at this write rate would cost thousands/day. API Gateway has throughput limits.", domain:4},
  {q:"A company uses RDS MySQL Multi-AZ. During a failover test, the application experienced 60 seconds of downtime. The connection string in the application points to the database endpoint. What caused the 60 seconds and how can it be reduced?", opts:["DNS TTL propagation — reduce the application's DNS cache timeout","The application caches DNS lookups. Use RDS Proxy which maintains persistent connections and redirects to the new primary within seconds","Failover took 60 seconds to promote the standby — switch to Aurora for faster failover","The application doesn't implement connection retry logic — add retry with exponential backoff"], correct:1, exp:"RDS Multi-AZ failover updates the DNS endpoint (~30 seconds), but applications that cache DNS or maintain connection pools continue connecting to the old IP. RDS Proxy solves this: it maintains the connection from the application side and transparently redirects to the new primary database within seconds. The application sees a brief pause, not a full disconnect. Aurora failover is faster (~30s) but RDS Proxy makes even standard RDS failover near-instant for apps.", domain:2},
  {q:"A company has an S3 bucket with 10 billion objects. They need to find objects matching specific metadata criteria (uploaded by user X, between date Y and Z, larger than 5MB). S3 listing APIs are too slow for 10 billion objects. What provides efficient metadata queries?", opts:["S3 Select to query object content","S3 Inventory (generates CSV/Parquet manifest of all objects with metadata) + Athena for SQL queries on the manifest","Use S3 tags and filter by tag (still requires listing)","DynamoDB metadata table populated by S3 event notifications at upload time"], correct:1, exp:"S3 Inventory generates daily/weekly reports of all objects with metadata (size, date, storage class, encryption status, etc.) in CSV/ORC/Parquet format in S3. Athena queries this manifest with SQL in seconds (vs listing 10B objects via API which would take days). For ongoing real-time queries, a DynamoDB metadata table (option D) works but requires setup. For existing data, Inventory + Athena is immediate.", domain:3},
  {q:"A company runs a web application on EC2. They need to deploy a new version with ZERO downtime and the ability to instantly roll back to the previous version if issues arise. The application takes 5 minutes to warm up. Which deployment pattern ensures this?", opts:["Rolling deployment replacing 25% at a time","Blue/Green deployment: deploy new version to a separate identical environment, warm it up, then switch traffic (Route 53 or ALB target group swap)","In-place deployment with a pre-deployment health check","Canary deployment with 10% traffic shift"], correct:1, exp:"Blue/Green keeps both old (blue) and new (green) environments running simultaneously. The green environment is fully deployed and warmed up before any traffic switches. Switch is instant (DNS change or ALB target group swap). Rollback is instant (switch back to blue). Zero downtime because blue handles all traffic until green is verified ready. Rolling deployment has mixed versions and can't roll back instantly. Canary is gradual, not instant switch.", domain:2},
  {q:"A company uses Lambda functions that need to share a 500MB machine learning model file. Loading it from S3 on each cold start takes 30 seconds. How can they reduce cold start time for model loading?", opts:["Increase Lambda memory for faster S3 download","Package the model in a Lambda Layer (up to 250MB unzipped total — won't fit 500MB)","Use Lambda with EFS mount — store the model on EFS and Lambda reads it locally (EFS is mounted at function initialization)","Use Lambda container image (up to 10GB) with the model baked into the image"], correct:2, exp:"EFS mount provides shared file system access. The model is stored once on EFS and mounted by all Lambda invocations. First access may be slow (EFS caching), but subsequent invocations in the same AZ benefit from cached reads. Lambda Layers have a 250MB unzipped limit (500MB won't fit). Container images bake the model in (10GB limit) but still need loading time. Provisioned concurrency + EFS is the best combo for warm starts.", domain:3},
  {q:"A company's CloudWatch alarm for CPU > 80% sends SNS notifications. The team receives hundreds of alarm notifications per day because CPU briefly spikes above 80% then drops below (alarm flapping). How should they reduce noise without missing real issues?", opts:["Increase the alarm threshold to 95%","Add datapoints to alarm: 'M out of N evaluation periods' — e.g., 3 out of 5 periods must be above 80% before alarming","Increase the evaluation period from 1 minute to 15 minutes","Disable the alarm and rely on CloudWatch dashboards instead"], correct:1, exp:"'M out of N' evaluation (also called 'datapoints to alarm') requires sustained high CPU before triggering. '3 out of 5' means 3 of the last 5 one-minute periods must exceed 80% — brief spikes won't trigger the alarm. This filters noise while catching real sustained issues. Increasing threshold might miss real problems at 80-95%. Longer periods add detection delay. Disabling alarms loses automation.", domain:2},
  {q:"A company uses AWS Organizations with consolidated billing. They want to ensure that development accounts cannot launch expensive instance types (anything larger than m5.xlarge). How should this be enforced?", opts:["IAM policies in each dev account restricting instance types","SCP on the Development OU denying ec2:RunInstances when ec2:InstanceType is NOT in the allowed list (t3.*, m5.medium, m5.large, m5.xlarge)","AWS Config rule detecting large instances with auto-termination","Budget alerts when dev accounts exceed cost thresholds"], correct:1, exp:"SCP on the Development OU applies to all dev accounts (current and future) and cannot be overridden by account admins. Using StringNotLike condition on ec2:InstanceType denies launches of any instance type not in the allowed list. IAM policies can be modified by account admins. Config is detective (instances launch then get terminated). SCPs are the proper preventive control for org-wide restrictions.", domain:1},
  {q:"A company stores secrets in Secrets Manager. Their application retrieves a database password 10,000 times per minute (each Lambda invocation fetches it). At $0.05 per 10,000 API calls, this costs $216/month just for secret retrieval. How should they optimize?", opts:["Cache the secret in a Lambda environment variable (doesn't handle rotation)","Use the AWS Parameters and Secrets Lambda Extension with caching (retrieves once per TTL, serves from local cache)","Store the secret in Parameter Store instead (same pricing)","Move secrets to DynamoDB for faster and cheaper reads"], correct:1, exp:"The Lambda Extension caches secrets locally in the execution environment. With a 5-minute TTL, 10,000 calls/minute are served from cache with only 1 actual Secrets Manager API call every 5 minutes. This reduces API calls from 10,000/min to ~0.2/min (99.998% reduction). Cost drops from $216/month to pennies. The extension also handles automatic cache invalidation when secrets rotate.", domain:4},
  {q:"A company needs to serve both their apex domain (example.com) and www.example.com from CloudFront. The apex domain cannot use a CNAME record (DNS limitation). How should Route 53 be configured?", opts:["CNAME records for both example.com and www.example.com pointing to CloudFront","Alias record for example.com pointing to CloudFront distribution + CNAME or Alias for www.example.com","A record with the CloudFront IP address (manual, doesn't work as IPs change)","Only www.example.com can use CloudFront — apex domains are not supported"], correct:1, exp:"Route 53 Alias records solve the apex domain CNAME limitation. An Alias record for example.com (zone apex) can point to CloudFront, ALB, S3, or other AWS resources — it works like a CNAME but is allowed at the apex. www.example.com can use either a CNAME or Alias. CloudFront fully supports apex domains via Route 53 Alias records. This is a commonly tested DNS concept.", domain:3},
  {q:"A company runs a batch job that reads 1TB from S3, processes it on EC2, and writes results back to S3. The job runs in us-east-1. They want to minimize data transfer costs. Which statement about S3 data transfer pricing is correct?", opts:["Data transfer INTO S3 is free, data transfer OUT of S3 to EC2 in the same region is free — total data transfer cost is $0","Data transfer INTO S3 is free, but OUT of S3 costs $0.09/GB — 1TB out = $90","Both in and out between S3 and EC2 in the same region are free","Data transfer is free only if using a VPC endpoint"], correct:2, exp:"Data transfer between S3 and EC2 (or any AWS service) in the SAME region is FREE in both directions. No charges for ingress (uploads to S3) and no charges for egress to same-region AWS services. Data transfer OUT of S3 to the internet costs money. Same-region EC2↔S3 = $0. This is why keeping compute and storage in the same region is a cost optimization best practice.", domain:4},
  {q:"A company uses Amazon Cognito User Pool for authentication. After a user signs in, the application needs to call AWS services (S3, DynamoDB) on behalf of the user. Cognito User Pool tokens alone don't grant AWS access. What additional component is needed?", opts:["IAM user credentials stored in the application","Cognito Identity Pool (exchanges User Pool token for temporary AWS credentials via STS)","API Gateway with IAM authorization","Lambda custom authorizer creating STS credentials"], correct:1, exp:"Cognito Identity Pool (Federated Identities) exchanges authentication tokens (from Cognito User Pool, Google, Facebook, SAML) for temporary AWS credentials (access key, secret key, session token). These credentials are scoped by an IAM role associated with the identity pool. The User Pool handles authentication; the Identity Pool handles authorization (AWS access). This two-pool architecture is the standard pattern.", domain:1},
  {q:"A company has an application that needs exactly-once, ordered processing of financial transactions at 500 transactions per second. Each transaction is for a specific account. Transactions for the SAME account must be ordered, but different accounts can be processed in parallel. Which is the BEST service?", opts:["SQS FIFO with MessageGroupId = account_id (ordering per account, parallel across accounts)","Kinesis Data Streams with account_id as partition key","Both work — SQS FIFO for lower throughput (<3000/sec) with exactly-once, Kinesis for higher throughput with ordering (but at-least-once)","DynamoDB Streams for ordered processing"], correct:0, exp:"At 500 TPS, SQS FIFO (which supports up to 70,000 msg/sec with high throughput mode) provides BOTH exactly-once processing AND per-MessageGroupId ordering. Each account_id is a message group — ordered within the group, parallel across groups. Kinesis provides ordering but only at-least-once (not exactly-once natively). For this specific combination of requirements (exactly-once + ordering + 500 TPS), SQS FIFO is ideal.", domain:2},
  {q:"A company needs to deploy a machine learning model for real-time inference. The model receives image data (2MB per request), processes it in 3 seconds, and returns predictions. Expected traffic: 100 concurrent requests. What compute platform is MOST cost-effective?", opts:["Lambda with 10GB memory (supports 2MB payload, 3s processing)","SageMaker Real-Time Inference endpoint with auto-scaling","ECS Fargate tasks behind an ALB","API Gateway + Lambda (API Gateway payload limit is 10MB, Lambda timeout allows 3s)"], correct:3, exp:"Lambda handles this well: 2MB payload is within the 6MB API Gateway synchronous limit, 3 seconds is well within Lambda timeout, and 100 concurrent requests are within default concurrency. Lambda scales automatically and costs only for execution time. SageMaker endpoints have hourly costs even when idle. Fargate requires capacity management. API Gateway + Lambda is serverless, scales automatically, and is most cost-effective for bursty inference workloads.", domain:4},
  {q:"A company has an existing CloudFormation stack. They want to add a new resource (an S3 bucket) to the stack without affecting existing resources. But they're worried that if the stack update fails (e.g., bucket name conflict), CloudFormation might rollback and DELETE existing resources. How do they protect against this?", opts:["Use change sets — preview the changes before executing, and ensure no existing resources are modified","Enable stack rollback protection (doesn't exist as a feature)","Add DeletionPolicy: Retain to all existing resources before the update","Both A and C: change sets to verify no unintended changes, and DeletionPolicy: Retain as a safety net on critical resources"], correct:3, exp:"Change sets show exactly what CloudFormation will do (add/modify/replace/delete) before execution. Verify no existing resources are marked for replacement. DeletionPolicy: Retain on critical resources (databases, buckets) ensures they survive even if the stack is deleted or rolls back. Together: change sets prevent surprises, DeletionPolicy provides a safety net. This is the production best practice for stack updates.", domain:2},
  {q:"A company needs to connect 5 VPCs to their on-premises data center. Each VPC needs access to on-premises and to each other. They have a 1 Gbps Direct Connect connection. What is the MOST scalable architecture?", opts:["Direct Connect with 5 private VIFs (one per VPC) + VPC peering between each VPC pair","Transit Gateway attached to all 5 VPCs + Direct Connect Gateway connecting Transit Gateway to Direct Connect","5 separate VPN connections from on-premises to each VPC","AWS Cloud WAN connecting all VPCs and Direct Connect"], correct:1, exp:"Transit Gateway is the hub connecting all 5 VPCs to each other. Direct Connect Gateway connects the Transit Gateway to the Direct Connect connection, providing on-premises access to all VPCs through a single transit VIF. This scales easily as new VPCs are added (just attach to TGW). Individual private VIFs per VPC consume VIF quota and don't provide VPC-to-VPC transit. This is the standard enterprise hybrid architecture.", domain:2},
  {q:"A company uses EventBridge to trigger Lambda functions based on EC2 instance state changes. They want to automatically tag new EC2 instances with the creator's IAM username. Which event detail provides this information?", opts:["EC2 instance state change event includes userIdentity","CloudTrail event for RunInstances (captured by EventBridge) contains userIdentity.arn with the IAM user/role who launched it","EC2 event metadata includes the launcher's information","Instance tags automatically include the creator"], correct:1, exp:"EventBridge captures CloudTrail events. The RunInstances CloudTrail event contains userIdentity.arn (who made the API call). An EventBridge rule matching 'detail-type: AWS API Call via CloudTrail, detail.eventName: RunInstances' triggers a Lambda that reads userIdentity from the event and tags the new instance. EC2 state change events don't include who launched the instance — only CloudTrail events do.", domain:1},
  {q:"A company runs a high-traffic website. Static assets (JS, CSS, images) change with each deployment (new hashed filenames). After deploying, some users report seeing broken pages because their browser loads new HTML but cached old CSS/JS. How should this be solved architecturally?", opts:["CloudFront invalidation after each deployment","Use versioned filenames (main.abc123.js) — new deployment = new filenames = new cache entries, old users keep loading old files until they refresh HTML","Reduce CloudFront TTL to 0 for all static assets","Add no-cache headers to all responses"], correct:1, exp:"Versioned/hashed filenames are the gold standard for cache busting. HTML references specific versioned files (main.abc123.js). New deployment creates main.def456.js — old cached HTML still loads old JS (consistent). New HTML loads new JS. No cache invalidation needed, long TTLs are fine (immutable content). Invalidation has propagation delay (can cause the exact problem described). Zero TTL defeats caching benefits entirely.", domain:3},
  {q:"A company uses AWS Config to track configuration changes to their resources. They want to be notified immediately when someone changes a Security Group rule to allow 0.0.0.0/0 on any port. What is the fastest detection mechanism?", opts:["Config rule with periodic evaluation (every 24 hours)","Config rule with change-triggered evaluation + SNS notification on non-compliant finding","GuardDuty finding for open security groups","CloudTrail alarm for AuthorizeSecurityGroupIngress API call with manual review"], correct:1, exp:"Change-triggered Config rules evaluate IMMEDIATELY when the relevant resource changes (not waiting for periodic schedule). When someone modifies a Security Group, Config evaluates within seconds. If non-compliant (0.0.0.0/0 detected), it sends an SNS notification immediately. This provides near-real-time detection. Periodic evaluation has up to 24-hour delay. CloudTrail alarms detect the API call but require manual determination of whether it's non-compliant.", domain:1},
  {q:"A company stores time-series data (metrics from 50,000 servers sampled every 10 seconds) in DynamoDB. The table is 5TB with hot data (last 24 hours) and cold data (last 90 days). Queries on hot data must return in <10ms but cold data queries can take 1-2 seconds. What is the most cost-effective design?", opts:["Single DynamoDB table with DAX for hot data caching","Two DynamoDB tables: hot table (last 24h) and cold table (older data) with a migration process between them","DynamoDB with S3 export for cold data, Athena for cold queries","Amazon Timestream (purpose-built time-series database with automatic hot/warm/cold tiering)"], correct:3, exp:"Amazon Timestream is purpose-built for time-series data. It automatically tiers data: in-memory store for recent data (<10ms queries), magnetic store for older data (seconds query time). Automatic data lifecycle management moves data between tiers based on age. 50,000 servers × 6 samples/min = 300,000 writes/sec — Timestream handles this natively. DynamoDB requires manual tiering management. Timestream also has built-in time-series functions (interpolation, smoothing).", domain:3},
  {q:"A company uses AWS Backup to protect their Aurora database, EBS volumes, and DynamoDB tables. They need to test their disaster recovery plan by restoring backups to a different AWS account (DR account). Is this possible and how?", opts:["AWS Backup doesn't support cross-account restore","Enable cross-account backup in AWS Backup with an Organization policy, then restore backups in the DR account from the shared backup vault","Copy backups manually to the DR account using snapshot sharing","AWS Backup cross-account copy feature — copy backup to a vault in the DR account, then restore locally in that account"], correct:3, exp:"AWS Backup supports cross-account copy: backups from the source account are copied to a backup vault in the destination account. The destination account can then restore from its local vault independently. This enables DR testing in a separate account. Cross-account management is configured through AWS Organizations. The DR account has full control to restore without depending on the source account (important during actual disaster).", domain:2},
  {q:"A company's application uses an SQS queue. They need to process messages that contain a 'priority' field: high-priority messages should be processed before low-priority ones. SQS doesn't natively support priority queues. What is the recommended pattern?", opts:["Sort messages in the consumer before processing (SQS doesn't guarantee order)","Use two separate SQS queues (high-priority and low-priority) with the consumer polling high-priority first, then low-priority when high is empty","Use SQS FIFO with MessageGroupId = priority level","Use a Lambda function to reorder messages in the queue"], correct:1, exp:"The standard pattern for priority processing with SQS: separate queues per priority level. The consumer ALWAYS checks the high-priority queue first. Only when it's empty does it check low-priority. This ensures high-priority messages are always processed first. You can't reorder messages within an SQS queue. FIFO MessageGroupId provides ordering within a group, not priority between groups. Multiple queues is the AWS-recommended approach.", domain:2},
  {q:"A company uses CloudFormation to deploy Lambda functions. Each time they update the function code and deploy, CloudFormation shows 'UPDATE_COMPLETE' but the Lambda function still runs old code. What is the issue?", opts:["CloudFormation caches Lambda code","The Lambda function code is in a ZIP on S3 but the S3 key hasn't changed — CloudFormation sees the same template parameters and doesn't update the resource","Lambda versions are immutable — they need to create a new version","CloudFormation has a bug with Lambda updates"], correct:1, exp:"If the S3 key (or S3ObjectVersion) in the CloudFormation template doesn't change, CloudFormation detects no change in the resource properties and skips the update. Fix: include a version number or hash in the S3 key (e.g., code-v2.zip or code-abc123.zip). Each deployment uses a new key, forcing CloudFormation to update the Lambda function. Alternatively, use the AWS::Lambda::Function's CodeUri with SAM which handles this automatically.", domain:2},
  {q:"A company uses Amazon Kinesis Data Firehose to deliver streaming data to S3. They want the data in Parquet format. The source data is JSON from IoT devices. What Firehose configuration enables the JSON-to-Parquet conversion?", opts:["Firehose Lambda transformation to convert JSON to Parquet","Firehose record format conversion using an AWS Glue table schema as the conversion target","Post-delivery Glue ETL job to convert S3 JSON files to Parquet","Kinesis Data Analytics SQL to transform before Firehose delivery"], correct:1, exp:"Firehose natively supports format conversion (JSON/CSV to Parquet/ORC) using a Glue Data Catalog table as the schema definition. No Lambda needed. Firehose reads the Glue table schema, deserializes the incoming JSON, and serializes to Parquet before writing to S3. This is a built-in feature — just specify the output format and Glue table in the Firehose configuration. Lambda transformation is for data modification, not format conversion.", domain:3},
  {q:"A company has 50 AWS accounts. They want to see the total AWS spend across all accounts in a single view, with the ability to break down costs by account, service, and tag. Which service provides this?", opts:["CloudWatch billing metrics per account","AWS Cost Explorer with consolidated billing (Organizations management account sees all accounts)","Individual billing dashboards in each account","AWS Budgets with organization-level budgets"], correct:1, exp:"With AWS Organizations consolidated billing, the management account's Cost Explorer shows total spend across ALL member accounts. It supports filtering and grouping by account, service, region, tag, and more. This is the native multi-account cost visibility solution. CloudWatch billing only shows per-account data. Budgets track against targets but don't provide the detailed exploration that Cost Explorer does.", domain:4},
  {q:"A company needs to ensure that their ALB only accepts HTTPS traffic (port 443) and redirects all HTTP (port 80) requests to HTTPS. How should the ALB listeners be configured?", opts:["Only create an HTTPS listener on port 443 (don't create port 80 listener)","Create an HTTP listener on port 80 with a REDIRECT action to HTTPS (port 443), and an HTTPS listener on port 443 with a FORWARD action to the target group","Block port 80 in the Security Group","Use AWS WAF to redirect HTTP to HTTPS"], correct:1, exp:"ALB listener rules support a REDIRECT action. The HTTP:80 listener has a single rule: redirect to HTTPS:443 (301 permanent redirect). The HTTPS:443 listener forwards to the target group with the ACM certificate. This ensures: (1) users typing http:// are automatically redirected to https://, (2) all actual traffic reaches targets over HTTPS. Blocking port 80 in SG means users typing http:// get a connection error (bad UX).", domain:1},
  {q:"A company wants to use Infrastructure as Code but their team is more familiar with Python than JSON/YAML. Which AWS tool lets them define cloud infrastructure using Python?", opts:["AWS CloudFormation (only JSON/YAML)","AWS CDK (Cloud Development Kit) — define infrastructure in Python, TypeScript, Java, or Go, which synthesizes to CloudFormation","Terraform HCL (not Python)","AWS SAM (YAML-based)"], correct:1, exp:"AWS CDK allows defining infrastructure using familiar programming languages (Python, TypeScript, Java, Go, C#). CDK code 'synthesizes' into CloudFormation templates. This enables loops, conditions, abstractions, and unit testing using the language's full power. CDK constructs represent AWS resources with sensible defaults. It's higher-level than raw CloudFormation while generating standard CF under the hood.", domain:2},
  {q:"A company has a public-facing application behind an ALB. They discover that 30% of requests are from known bot networks scraping their content. These bots use many different IPs (rotating). IP blocking won't work. How can they block sophisticated bots?", opts:["Rate-based WAF rules (bots rotate IPs so per-IP rate limiting won't work)","AWS WAF Bot Control (uses ML to detect bot patterns regardless of IP)","CAPTCHA challenges on all pages","Block requests without valid session cookies"], correct:1, exp:"AWS WAF Bot Control managed rule group uses ML and behavioral analysis to detect bots regardless of IP rotation. It identifies bots by: TLS fingerprint, browser fingerprint, request patterns, JavaScript challenge responses, and interaction patterns. It categorizes bots (verified bots like Googlebot, vs malicious scrapers) and blocks/rate-limits accordingly. IP-based blocking fails against rotating IPs. CAPTCHA hurts user experience.", domain:1},
  {q:"A company uses S3 for a data lake. They want to enforce that: (1) all objects must be encrypted with a specific KMS key, (2) objects uploaded without this encryption are rejected. What S3 configuration ensures this?", opts:["Set default bucket encryption to the specific KMS key","Bucket policy with a DENY on s3:PutObject when s3:x-amz-server-side-encryption-aws-kms-key-id is NOT the specific key ARN","Both A and B together — default encryption handles requests without headers, the deny policy rejects requests with wrong encryption","Just the bucket policy (B) — it handles all cases"], correct:2, exp:"Default bucket encryption applies the KMS key when no encryption header is specified (covers SDK calls without explicit encryption). The bucket policy DENY rejects requests that specify a DIFFERENT KMS key or a different encryption method (SSE-S3, SSE-C). Together: uploads without encryption headers → default applies correct key. Uploads with wrong encryption → denied. Only the bucket policy alone wouldn't handle missing-header cases (those would use default which might be wrong without A).", domain:1},
  {q:"A company runs a multi-tenant SaaS application. They need to limit each tenant to a maximum of 1000 API requests per day. Exceeding the limit should return HTTP 429. Which service provides this with minimal custom code?", opts:["API Gateway Usage Plans with API Keys (one key per tenant, configured with daily quota of 1000)","Custom Lambda authorizer tracking usage in DynamoDB","WAF rate-based rules per API key","Application-level rate limiting middleware"], correct:0, exp:"API Gateway Usage Plans provide built-in per-key throttling and quotas. Create a usage plan with daily quota of 1000 requests. Each tenant gets an API key associated with this plan. API Gateway automatically tracks usage and returns 429 when quota is exceeded. Zero custom code. The usage data is also available via API for billing/dashboard purposes. Custom DynamoDB tracking works but reinvents what API Gateway provides natively.", domain:4},
  {q:"A company uses EventBridge to route application events to various targets. They discover that occasionally events are delivered to targets more than once (EventBridge guarantees at-least-once delivery). For their payment processing Lambda, duplicate delivery could cause double-charges. How should they handle this?", opts:["EventBridge guarantees exactly-once, so this shouldn't happen","Implement idempotency in the Lambda function — use DynamoDB conditional writes with the event's 'id' field to detect and skip duplicates","Configure EventBridge retry policy to 0 retries (may lose events)","Use SQS FIFO between EventBridge and Lambda for deduplication"], correct:1, exp:"EventBridge provides at-least-once delivery (not exactly-once). The correct approach: make the Lambda function idempotent. Use the event's unique 'id' field as an idempotency key. Before processing, check DynamoDB (PutItem with ConditionExpression attribute_not_exists(id)). If the ID exists, skip processing (duplicate). If new, process and store. SQS FIFO deduplication only works within a 5-minute window. Idempotent processing is the universal solution.", domain:2},
  {q:"A company needs to analyze their AWS costs and identify unused resources across 200 accounts. They want recommendations for rightsizing EC2 instances based on actual CPU/memory utilization. Which AWS service provides specific rightsizing recommendations with projected savings?", opts:["AWS Cost Explorer rightsizing recommendations","AWS Compute Optimizer (analyzes CloudWatch metrics and recommends optimal instance types with projected savings)","AWS Trusted Advisor cost optimization checks","CloudWatch with custom dashboards per instance"], correct:1, exp:"AWS Compute Optimizer analyzes historical utilization metrics (CPU, memory, network, disk) and recommends optimal instance types. It shows: current instance, recommended instance, projected savings, and performance risk. It works across Organizations (200 accounts). Cost Explorer rightsizing is more limited. Trusted Advisor gives broad recommendations but not ML-driven per-instance analysis. Compute Optimizer is the purpose-built rightsizing tool.", domain:4},
  {q:"A company stores data in S3 with versioning enabled. They accidentally uploaded a new version of a critical file that is corrupted. They need to restore the previous version. What is the procedure?", opts:["Use S3 undo feature to revert the last upload","Copy the previous version over the current version (GET previous version by version-id, then PUT it as the current object)","Delete the current version (the latest version) and the previous version automatically becomes current","Contact AWS Support to restore previous versions"], correct:2, exp:"With versioning, deleting a specific version ID removes only that version. When you delete the CURRENT (latest) version, the PREVIOUS version becomes the current version. Alternatively, you can GET the specific previous version (by version-id) and PUT it back as a new current version (creating yet another version). Both approaches work. There's no 'undo' button, but the previous versions are always preserved.", domain:2},
  {q:"A company processes customer orders. Each order goes through states: RECEIVED → PROCESSING → SHIPPED → DELIVERED. They need to track order state, allow queries by state (all PROCESSING orders), and emit events when state changes. What AWS architecture supports this?", opts:["DynamoDB for state storage + DynamoDB Streams + Lambda for event emission on state changes + GSI on 'status' field for queries by state","Step Functions for state management + DynamoDB for querying","SQS with different queues per state","RDS with triggers for state change events"], correct:0, exp:"DynamoDB stores order state (partition key: order_id, attributes include status). GSI with status as partition key enables efficient queries for all orders in a specific state. DynamoDB Streams captures every item modification — a Lambda consumer emits events (to EventBridge/SNS) when status changes. This is the standard event-driven state management pattern. Step Functions track execution state but don't provide queryable state storage.", domain:2},
  {q:"A company uses CloudFront with an S3 origin. They restrict S3 access using Origin Access Control (OAC). A developer tries to access an object directly via the S3 URL and is denied. However, they CAN access it via the CloudFront URL. This is the desired behavior. But the developer asks: how does CloudFront authenticate to S3 if the bucket denies all direct access?", opts:["CloudFront uses its own IAM role to access S3","CloudFront signs requests using a service principal that the bucket policy allows (condition: aws:SourceArn matching the CloudFront distribution ARN)","CloudFront has a special VPC endpoint to S3","CloudFront uses a pre-configured access key stored in the distribution settings"], correct:1, exp:"Origin Access Control (OAC) configures CloudFront to sign requests to S3 using a CloudFront service principal. The S3 bucket policy allows s3:GetObject only when the request is signed by the specific CloudFront distribution (condition: StringEquals: AWS:SourceArn: arn:aws:cloudfront::account-id:distribution/distribution-id). Direct access lacks this signature and is denied. OAC replaced the older Origin Access Identity (OAI).", domain:1},
  {q:"A company runs Aurora PostgreSQL. They want to track all DDL changes (CREATE TABLE, ALTER TABLE, DROP TABLE) and be alerted when someone drops a table in production. Which combination provides this monitoring?", opts:["Enable Aurora Database Activity Streams → Kinesis → Lambda to detect DROP statements → SNS alert","Enable pgaudit extension with DDL logging → CloudWatch Logs → Metric Filter for DROP → CloudWatch Alarm → SNS","CloudTrail logging (only captures RDS API calls, not SQL statements)","RDS Event Notifications for database modifications"], correct:1, exp:"pgaudit extension logs DDL/DML statements to PostgreSQL logs → shipped to CloudWatch Logs. A metric filter pattern matching 'DROP TABLE' creates a CloudWatch metric that triggers an alarm → SNS notification. This is the standard Aurora PostgreSQL audit logging approach. Database Activity Streams also work but are more expensive and complex for this simple use case. CloudTrail captures API calls (CreateDBInstance), not SQL queries. RDS Events are for instance-level events.", domain:1},
  {q:"A company uses Terraform to manage their AWS infrastructure. During a deployment, Terraform applies changes but the process is interrupted (developer's laptop loses connection mid-apply). Some resources were created, others weren't. What is the state of the infrastructure and how should they recover?", opts:["Terraform rollbacks automatically like CloudFormation","The infrastructure is in a partial state. Run 'terraform apply' again — Terraform compares desired state with actual state (stored in state file + AWS reality) and completes the remaining changes","The state file is corrupted — must delete all resources and start over","AWS resources are orphaned — must import them back into Terraform state"], correct:1, exp:"Terraform is declarative and convergent. Running 'terraform apply' again: Terraform reads the state file, queries AWS for actual resource status, computes the diff between desired (config) and actual state, and applies only the missing/different changes. Already-created resources are detected as existing. Uncreated resources are created. Terraform handles interrupted applies gracefully — just re-run. No rollback concept (unlike CloudFormation).", domain:2},
  {q:"A company needs to give a third-party analytics vendor READ-ONLY access to specific S3 objects in their bucket. The vendor has their own AWS account (account ID: 111222333444). The company doesn't want to manage credentials. What is the MOST secure approach?", opts:["Create IAM user credentials and share with the vendor","Add a bucket policy granting s3:GetObject to the vendor's account principal for the specific prefix, and the vendor assumes a role in their own account that is allowed by the bucket policy","Create a cross-account IAM role in the company's account that the vendor's account can assume, with a policy allowing only s3:GetObject on specific objects","Share pre-signed URLs that expire after 24 hours"], correct:2, exp:"Cross-account IAM role in the company's account: the role has a trust policy allowing the vendor's account to assume it, and a permissions policy granting s3:GetObject on specific objects only. The vendor uses STS AssumeRole from their account. No long-term credentials exchanged. The company controls the role (can revoke anytime). Bucket policies granting to account principals are less secure (harder to audit/restrict). Pre-signed URLs are temporary but not manageable at scale.", domain:1},
  {q:"A company's development team frequently creates EC2 instances for testing but forgets to terminate them. Monthly wasted spend on forgotten instances is $5,000. What automated solution identifies and handles unused instances?", opts:["AWS Trusted Advisor checks for idle instances + manual termination","AWS Instance Scheduler to stop instances on a schedule + tag-based policies requiring an 'ExpirationDate' tag","Create a Lambda function triggered by a CloudWatch cron that identifies instances with low CPU (<5%) for >7 days and sends SNS notification for approval to terminate","Implement a mandatory 'auto-terminate' tag with a date, and a Lambda function running daily that terminates instances past their expiration date"], correct:3, exp:"Mandatory tag (auto-terminate date) enforced via SCP or IAM policy at launch ensures every instance has an expiration. Daily Lambda checks tags and terminates expired instances automatically. This is self-service (developers choose their instance lifetime at launch) and automated (no manual review needed). Instance Scheduler stops at fixed times but doesn't handle ad-hoc test instances. Low-CPU detection may flag legitimate idle instances.", domain:4},
  {q:"A company has strict data sovereignty requirements — customer data must NEVER leave the eu-west-1 region. They use S3, RDS, and Lambda in eu-west-1. What controls ensure data doesn't accidentally get replicated or accessed from other regions?", opts:["S3 bucket policy restricting access to eu-west-1 IP ranges","SCP denying all actions outside eu-west-1 (with exceptions for global services) + disable S3 Cross-Region Replication + restrict Lambda VPC to eu-west-1","VPC configuration preventing traffic to other regions","AWS Config rules checking resource locations"], correct:1, exp:"SCP at the Organization level prevents creating or using resources in other regions entirely. Disabling/preventing CRR ensures S3 data doesn't replicate out. Lambda in VPC with no internet access prevents data exfiltration. These controls together ensure data physically stays in eu-west-1. VPC alone doesn't prevent S3 CRR or Lambda environment variable exposure. Config is detective. The SCP is the primary preventive control for region lockdown.", domain:1},
  {q:"A company uses AWS CloudTrail and notices 'Access Denied' events in the trail for an IAM role. The role should have access to the resource. CloudTrail shows the userIdentity is the role, but the error is 'access denied by organization SCP'. What does this tell them?", opts:["The IAM role's policy is wrong","A Service Control Policy at the Organization level is blocking the action, overriding the role's IAM permissions","The resource-based policy is denying access","The role's permission boundary is too restrictive"], correct:1, exp:"When CloudTrail explicitly mentions 'access denied by organization SCP', it means an SCP is blocking the action. SCPs are evaluated BEFORE IAM policies. Even if the IAM role has full permissions, an SCP denial overrides everything. The team needs to check the SCPs attached to the account's OU and the Organization root. This is important for troubleshooting: always check SCPs first when getting unexpected access denials in Organizations.", domain:1},
  {q:"A company uses Amazon Aurora MySQL. Their database is 2TB. They need to create an exact copy of the production database for the QA team to run destructive tests. The copy should be available in under 5 minutes and shouldn't impact production performance. What feature provides this?", opts:["Aurora Restore from Snapshot (takes 20-30 minutes for 2TB)","Aurora Database Cloning (creates a copy-on-write clone in seconds, initially shares storage with source)","Aurora Read Replica promoted to standalone (takes time to replicate 2TB)","RDS Export to S3 then Import (very slow for 2TB)"], correct:1, exp:"Aurora Cloning uses copy-on-write protocol — the clone initially shares the same storage pages as the source. It's created in seconds regardless of database size. Only pages that are modified (by either source or clone) are copied. This means: 2TB database clones in <5 minutes, minimal additional storage initially, and ZERO impact on production performance. The clone is a fully independent database. This is the standard approach for QA/dev database provisioning.", domain:2},
]
