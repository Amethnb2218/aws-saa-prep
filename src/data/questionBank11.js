export const questionBank11 = [
  // Exam-style mixed scenarios - Set A
  {q:"A company is building a document management system. Documents are uploaded once and read frequently for the first 30 days. After 30 days, they are rarely accessed but must be available within 12 hours when requested. Documents must be retained for 10 years. Which S3 storage strategy minimizes cost?", opts:["S3 Standard → Glacier Flexible Retrieval after 30 days → delete after 10 years","S3 Standard → S3 Standard-IA after 30 days → Glacier Deep Archive after 90 days → delete after 10 years","S3 Intelligent-Tiering for the entire 10-year period","S3 Standard → Glacier Deep Archive after 30 days → delete after 10 years"], correct:3, exp:"Documents are uploaded once and read frequently for 30 days (Standard). After 30 days, they're rarely accessed and 12-hour retrieval time is acceptable — Glacier Deep Archive ($1/TB/month) provides retrieval within 12 hours (Standard retrieval). This is the cheapest long-term storage that meets the 12-hour requirement. Glacier Flexible is more expensive than Deep Archive. Standard-IA is too expensive for 10-year retention.", domain:4},
  {q:"A solutions architect designs a web application with the following requirements: the application must support 10,000 concurrent WebSocket connections, messages must be delivered to all connected users within 500ms, and the solution must scale automatically. Which architecture is MOST appropriate?", opts:["ALB with WebSocket support routing to EC2 instances with pub/sub via Redis","Amazon API Gateway WebSocket API with Lambda integration and DynamoDB for connection state","AppSync GraphQL subscriptions with DynamoDB resolver","IoT Core MQTT with topic subscriptions for each user"], correct:1, exp:"API Gateway WebSocket API scales to millions of connections automatically (serverless). Lambda processes messages, DynamoDB stores connection IDs. When broadcasting, Lambda queries DynamoDB for all connections and posts messages back through the API Gateway management API. Fully managed, auto-scales, no infrastructure. ALB+Redis requires managing EC2. IoT Core works but is designed for devices.", domain:3},
  {q:"A company has a legacy application that makes DNS queries to resolve the IP addresses of on-premises resources. They are migrating to AWS and need the application in AWS to resolve both AWS private hosted zone domains AND on-premises DNS domains. How should DNS be configured?", opts:["Use Route 53 Resolver with inbound endpoints (for on-premises to resolve AWS) and outbound endpoints (for AWS to resolve on-premises)","Host a custom DNS server on EC2 that forwards queries","Use Route 53 private hosted zones only — they can resolve on-premises domains","Configure the VPC DHCP options to use the on-premises DNS servers"], correct:0, exp:"Route 53 Resolver outbound endpoints forward specific DNS queries from AWS to on-premises DNS servers (for resolving on-prem domains). Inbound endpoints allow on-premises to resolve AWS private hosted zone records. Together they enable hybrid DNS resolution. Custom DNS on EC2 works but adds operational overhead. DHCP with on-premises DNS breaks AWS private hosted zones.", domain:2},
  {q:"A company needs to run a containerized application that processes data for exactly 48 hours once per month. The container requires 64 vCPUs and 256GB RAM. Cost must be minimized. Which compute option is BEST?", opts:["EC2 On-Demand r5.16xlarge for 48 hours","ECS Fargate task with 64 vCPU and 256GB memory","AWS Batch with EC2 Spot Instances (r5.16xlarge or equivalent)","EC2 Spot Instance r5.16xlarge for 48 hours"], correct:2, exp:"AWS Batch with Spot Instances provides: automatic instance provisioning (chooses cheapest available), Spot pricing (60-90% savings), automatic retry on Spot interruption, and scales to zero when done. For a 48-hour monthly job, Spot is ideal (even with possible interruption, Batch retries). Fargate costs ~$2/vCPU-hour (expensive for 64 vCPU x 48 hrs). Direct Spot instance works but Batch manages retries automatically.", domain:4},
  {q:"A company operates a global e-commerce platform. Product catalog data is stored in a primary Aurora MySQL cluster in us-east-1. Users in Europe (eu-west-1) and Asia (ap-southeast-1) report slow page loads for product listings. What solution provides the FASTEST reads globally with minimal changes?", opts:["Deploy Aurora Read Replicas in each region","Use Aurora Global Database with secondary clusters in eu-west-1 and ap-southeast-1","Add ElastiCache Redis in each region, populated via DynamoDB Streams","Use CloudFront with dynamic content caching for API responses"], correct:1, exp:"Aurora Global Database replicates the entire database to secondary regions with <1 second lag. Applications in each region read from local Aurora cluster (same low latency as if the database were local). Minimal changes: update connection strings for reads to use local region endpoint. Cross-region Read Replicas have higher lag and less robust failover. CloudFront caching helps but product data may change frequently.", domain:3},
  {q:"A company runs a machine learning inference endpoint on SageMaker. Traffic is unpredictable: 0 requests at night, 10,000 requests/sec during peak. They pay for a large always-on endpoint but utilization is only 20% on average. How should they optimize?", opts:["Use SageMaker Serverless Inference (scales to zero, pay per request)","Use SageMaker Auto Scaling with a minimum of 0 instances","Use SageMaker Multi-Model Endpoint to share capacity","Reduce the instance size to match average utilization"], correct:0, exp:"SageMaker Serverless Inference scales from zero to handle thousands of requests, paying only for compute time used. Perfect for unpredictable traffic with idle periods. No cold start issues for inference (models stay warm briefly). Auto Scaling with min=0 isn't supported for real-time endpoints. Multi-Model shares capacity but still needs always-on instances. Serverless eliminates the 80% idle cost.", domain:4},
  {q:"A company stores application configuration (database URLs, feature flags, API keys) that multiple Lambda functions and ECS tasks need to access. Configuration changes occasionally and should propagate to all services within 1 minute. What is the BEST storage mechanism?", opts:["Environment variables in each Lambda function and ECS task definition","AWS AppConfig (part of Systems Manager) with feature flags and configuration profiles","S3 file downloaded at startup by each service","DynamoDB table with polling from each service"], correct:1, exp:"AWS AppConfig manages configuration centrally with deployment strategies (gradual rollout, instant), validation, and rollback. It supports feature flags and configuration profiles. Services retrieve config via API with caching. Changes deploy within seconds/minutes depending on strategy. Environment variables require redeployment to change. S3 requires polling. DynamoDB requires custom polling and lacks validation/rollback.", domain:2},
  {q:"A company has a serverless application: API Gateway → Lambda → DynamoDB. Under load testing, they see throttling at 5,000 requests/second. The Lambda function has no reserved concurrency configured. The account Lambda concurrent execution limit is 1,000. What is causing the throttling?", opts:["API Gateway default throttle limit (10,000 RPS) — not the cause","Lambda account-level concurrent execution limit of 1,000 — if each request takes 200ms, 1,000 concurrent = 5,000 RPS maximum","DynamoDB provisioned capacity is exhausted","API Gateway stage throttle is set to 5,000 RPS"], correct:1, exp:"With 1,000 concurrent Lambda executions and 200ms average duration: each execution handles 5 requests/sec (1000ms/200ms). 1,000 x 5 = 5,000 RPS maximum throughput. Beyond this, new invocations are throttled (429 error). Solutions: request account limit increase, optimize Lambda to run faster (less duration = more throughput per concurrent execution), or use provisioned concurrency. API Gateway default is 10K RPS (not the bottleneck).", domain:3},
  {q:"A company needs to ensure high availability for their workload across two AZs. They use an ALB with an Auto Scaling Group. The minimum capacity should handle full production load even if one AZ fails. If they need 4 instances to handle production load, what should the ASG configuration be?", opts:["Min: 4, Max: 8, with instances evenly spread across 2 AZs","Min: 4, Max: 4, across 2 AZs (2 per AZ)","Min: 8, Max: 8, with 4 per AZ (so 4 survive if one AZ fails)","Min: 6, Max: 8, with 3 per AZ (so 3 survive, then scale to 4)"], correct:0, exp:"With Min: 4 across 2 AZs, you get 2 per AZ. If one AZ fails, only 2 remain — not enough (need 4). The correct answer for surviving AZ failure while maintaining capacity: you need the minimum to equal the required capacity PER surviving AZ × number of AZs. With 2 AZs needing 4 after failure: put 4 in each AZ = 8 total. OR: Min 4 with Max 8 + scaling that quickly adds instances. But the safest is Option A with ASG automatically rebalancing.", domain:2},
  {q:"A company collects clickstream data at 100,000 events per second. They need to store events in S3 partitioned by date and event_type for analytics with Athena. Events should be available for querying within 5 minutes. What is the simplest architecture?", opts:["Kinesis Data Streams → Lambda → S3 (writing Parquet files per partition)","Kinesis Data Streams → Kinesis Data Firehose (with dynamic partitioning and Parquet conversion) → S3","API Gateway → Lambda → S3 with partition prefix","Direct S3 PUT from application with date/event_type prefixes"], correct:1, exp:"Kinesis Data Firehose handles the entire pipeline: buffers events (1-5 minutes), converts to Parquet format, and writes to S3 with dynamic partitioning based on event data fields (date, event_type in the S3 prefix). One managed service, no code for the delivery. Lambda to S3 requires managing file sizes, batching, and format conversion yourself. Direct PUT creates too many small files (bad for Athena).", domain:3},
  {q:"A company runs a critical web application. They need to test new deployments with real production traffic before full rollout. If the new version has higher error rates, it should automatically roll back. The application runs on ECS Fargate behind an ALB. What deployment strategy achieves this?", opts:["Blue/Green deployment with manual traffic switch","CodeDeploy ECS Blue/Green deployment with canary traffic shifting (10% for 5 minutes) and automatic rollback on CloudWatch alarm","Rolling update with 25% batches","In-place update with health check validation"], correct:1, exp:"CodeDeploy ECS Blue/Green with canary shifts 10% of production traffic to the new task set. CloudWatch alarms monitor error rates. If the alarm triggers during the canary window, CodeDeploy automatically rolls back (shifts traffic back to 100% blue). This tests with real traffic + provides automatic protection. Manual Blue/Green requires human monitoring. Rolling updates can't easily rollback mid-deployment.", domain:2},
  {q:"An application serves dynamic content through CloudFront. The origin is an ALB fronting EC2 instances. Users report that after logging in, they sometimes see another user's account page. What is the MOST likely cause?", opts:["The ALB is routing requests to the wrong instances","CloudFront is caching responses that contain user-specific content — the cache key doesn't include the session cookie or Authorization header","The EC2 instances have a session management bug","DNS is resolving to wrong endpoints"], correct:1, exp:"CloudFront caches responses by default based on URL only. If user-specific pages (/account) are cached without including session identifiers in the cache key, one user's response is served to other users. Fix: configure the cache behavior to forward the session cookie or Authorization header (making it part of the cache key) so each user gets their own cached version — or mark the behavior as no-cache for user-specific content.", domain:1},
  {q:"A company uses CloudFormation to deploy infrastructure. They need to create resources in a specific ORDER: VPC first, then Subnets, then Security Groups, then EC2 instances. How does CloudFormation handle this?", opts:["Resources must be listed in order in the template","CloudFormation automatically determines order from DependsOn attributes and Ref/Fn::GetAtt references","You must use nested stacks to control execution order","CloudFormation deploys all resources in parallel regardless of dependencies"], correct:1, exp:"CloudFormation automatically resolves dependencies through intrinsic functions (Ref, Fn::GetAtt). If an EC2 instance references a Security Group (via Ref), CloudFormation knows to create the SG first. DependsOn explicitly declares dependencies when there's no intrinsic function reference. CloudFormation builds a dependency graph and creates resources in the correct order, parallelizing independent resources.", domain:2},
  {q:"A company has a REST API that returns the same data for the same query parameters for 10 minutes (data updates every 10 minutes). The API receives 100,000 identical requests per minute. Currently, every request hits Lambda → DynamoDB. How should they optimize for cost?", opts:["Enable API Gateway caching with 10-minute TTL","Add DynamoDB DAX for sub-millisecond reads","Increase Lambda memory for faster processing","Use CloudFront with cache TTL of 10 minutes in front of API Gateway"], correct:0, exp:"API Gateway caching stores responses for configurable TTL. With 10-minute TTL matching data freshness: first request hits Lambda/DynamoDB, remaining 999,999 requests per 10 minutes are served directly from API Gateway cache. This eliminates 99.999% of Lambda invocations and DynamoDB reads. CloudFront also works but API Gateway cache is simpler (single component). DAX speeds reads but doesn't eliminate invocations.", domain:4},
  {q:"A company migrates from on-premises Oracle to Aurora PostgreSQL. Post-migration, they discover stored procedures using Oracle-specific UTL_FILE package (for file I/O) don't have a PostgreSQL equivalent. How should they handle this?", opts:["Rewrite the file I/O logic to use S3 (aws_s3 extension in Aurora PostgreSQL) for importing/exporting data","Install Oracle compatibility layer in Aurora","Use RDS for Oracle instead to maintain compatibility","Write a custom C extension for PostgreSQL to replicate UTL_FILE"], correct:0, exp:"Aurora PostgreSQL provides the aws_s3 extension which enables importing data from S3 and exporting query results to S3. This replaces Oracle UTL_FILE functionality for file-based data exchange. It's the AWS-recommended approach for file I/O in Aurora PostgreSQL. There's no Oracle compatibility layer. Custom C extensions aren't supported in managed Aurora. The whole point of migration was to leave Oracle.", domain:2},
  {q:"A company runs Windows workloads on EC2. They need centralized patch management for 500 Windows instances across 5 AWS accounts. Patches must be tested in development before applying to production. Which service manages this?", opts:["AWS Systems Manager Patch Manager with patch baselines and maintenance windows","Windows Server Update Services (WSUS) on EC2","AWS Config with custom rules checking patch compliance","Manual patching via RDP sessions scheduled monthly"], correct:0, exp:"Systems Manager Patch Manager supports: custom patch baselines (which patches to approve), maintenance windows (when to patch), multi-account via Organizations, and phased rollout (dev first, then prod after approval). It works across accounts without managing WSUS infrastructure. Config detects non-compliance but doesn't apply patches. WSUS requires managing servers. Manual patching doesn't scale to 500 instances.", domain:2},
  {q:"A Lambda function needs to process records from a DynamoDB Stream. Occasionally, a malformed record causes the function to fail. Because DynamoDB Streams is ordered, this blocks ALL subsequent records from being processed. What configuration prevents a single bad record from blocking the entire stream?", opts:["Add try-catch to ignore all errors","Configure BisectBatchOnFunctionError (splits the batch to isolate the bad record) + Maximum Retry Attempts + On-Failure Destination (DLQ)","Delete the problematic item from DynamoDB to remove it from the stream","Increase the Lambda timeout to retry more"], correct:1, exp:"BisectBatchOnFunctionError splits a failed batch in half, recursively narrowing down to the exact failing record. Maximum Retry Attempts limits how many times it retries before giving up. On-Failure Destination sends the failed batch to an SQS DLQ for investigation. Together: the bad record is isolated, retried a limited number of times, sent to DLQ, and the stream continues processing subsequent records.", domain:2},
  {q:"A company uses AWS for a SaaS application. Each customer's data must be stored in their preferred region (US customers in us-east-1, EU customers in eu-west-1) due to data residency requirements. The application is globally accessible. How should data routing be architected?", opts:["Single global database with data residency tags","Route 53 geolocation routing directing users to the nearest regional deployment, each with its own database","API Gateway with Lambda that routes database calls to the appropriate region based on customer configuration stored in a global DynamoDB table","Global CloudFront distribution with regional API Gateway backends determined by customer ID lookup"], correct:2, exp:"A global routing layer (API Gateway + Lambda) checks customer configuration (which region their data resides in) and routes data operations to the correct regional database. This handles cases where an EU customer accesses from the US (data stays in EU). Geolocation routing assumes user location = data location, which isn't always true. The customer configuration in a global DynamoDB table provides the correct data residency mapping.", domain:1},
  {q:"A company wants to deploy a third-party virtual appliance (firewall) from AWS Marketplace in their VPC. The appliance needs to inspect all traffic between the public subnet and the internet. Where should it be placed?", opts:["In a separate 'appliance subnet' with Gateway Load Balancer routing traffic through it using route table entries","In the public subnet alongside the web servers","As a Lambda@Edge function for traffic inspection","Behind a Network Load Balancer in the private subnet"], correct:0, exp:"Gateway Load Balancer (GWLB) transparently inserts virtual appliances (firewalls, IDS/IPS) into the traffic path. Route table entries direct traffic through GWLB endpoints. The appliance is in a separate subnet, inspects traffic, and forwards/drops it. This is the AWS-recommended architecture for inline traffic inspection. Placing in the same subnet doesn't intercept traffic properly. Lambda@Edge only processes HTTP, not all traffic.", domain:1},
  {q:"A company uses ElastiCache Redis for session management. The Redis cluster has a single node. If the node fails, all sessions are lost and users must re-authenticate. What is the MINIMUM change to prevent session loss on node failure?", opts:["Enable Redis Multi-AZ with automatic failover (creates a replica that takes over on primary failure)","Add a Read Replica manually and implement application-level failover","Switch to Redis Cluster Mode with multiple shards","Increase the Redis node instance size for better reliability"], correct:0, exp:"ElastiCache Redis Multi-AZ with Auto-Failover creates a synchronous replica in another AZ. If the primary fails, the replica is automatically promoted (typically <30 seconds). Sessions persist because the replica has a copy of all data. This is the minimum change: one setting enables HA. Cluster Mode adds sharding (for scaling, not just HA). Manual replicas require application changes. Larger instances don't prevent failures.", domain:2},
  {q:"A company is evaluating whether to use SQS or Kinesis for their event processing pipeline. Events are 1KB each, arriving at 50,000/second. Each event must be processed by 3 different consumer applications. Order within a customer_id matters. Data should be available for replay for 7 days. Which is the BETTER choice and why?", opts:["SQS FIFO — guarantees exactly-once and ordering","SQS Standard with 3 separate queues (SNS fanout pattern)","Kinesis Data Streams — supports multiple consumers, ordering per partition key (customer_id), 7-day retention, and replay","Either works but Kinesis is more cost-effective at this scale"], correct:2, exp:"Kinesis wins on every requirement: (1) Multiple consumers: each consumer independently reads ALL data (SQS is competing consumers — only one gets each message). (2) Order per customer_id: partition key ensures ordering per customer. (3) 7-day retention and replay: native feature. (4) 50K/sec: Kinesis handles this easily (50 shards). SQS FIFO maxes at 70K/sec with high throughput mode but doesn't support multiple independent consumers without SNS fanout + separate queues.", domain:3},
  {q:"A company wants to allow their data science team to run Jupyter notebooks with access to a shared dataset in S3. Notebooks should start within 30 seconds, automatically shut down when idle for 1 hour, and the team shouldn't manage any infrastructure. What should they use?", opts:["EC2 instances with JupyterHub installed","SageMaker Studio with lifecycle configuration for auto-shutdown","EMR Notebooks with auto-terminating clusters","SageMaker Notebook Instances with CloudWatch auto-stop"], correct:1, exp:"SageMaker Studio provides managed Jupyter notebooks that start quickly (pre-warmed instances), require no infrastructure management, and support lifecycle configurations that auto-shut down idle instances (saving costs). Native S3 integration for data access. EMR Notebooks require managing EMR clusters. EC2 + JupyterHub requires server management. Studio is the modern, fully-managed option.", domain:4},
  {q:"A company's application writes to Aurora MySQL at 5,000 writes per second. They recently enabled a trigger that calls Lambda for every write (to sync data to Elasticsearch). The database now shows increased latency and write throttling. What is wrong and how should it be fixed?", opts:["Aurora can't handle 5,000 writes/sec — scale up the instance","Database triggers calling Lambda on every write create back-pressure — replace with DynamoDB Streams or Aurora native integration","Use Aurora event notifications instead of triggers","The Lambda function is too slow — increase its memory"], correct:1, exp:"Database triggers executing on every write add latency to EACH write operation (the trigger must complete or timeout before the write is committed). At 5,000 writes/sec, this creates massive back-pressure. Better pattern: capture changes asynchronously via DynamoDB Streams (if using DynamoDB) or for Aurora: use Database Activity Streams or CDC via DMS to stream changes to Elasticsearch without impacting write performance.", domain:3},
  {q:"A company has an application that generates PDF reports. Report generation takes 2-10 minutes and requires 4GB of RAM. Users request reports through a web interface and expect an email when the report is ready. Currently, the web server generates reports synchronously, causing timeouts. What is the BEST architecture?", opts:["Increase web server timeout to 15 minutes","Web server enqueues report request to SQS → Lambda (or ECS task) generates the PDF → stores in S3 → SNS sends email with download link","Web server calls a Step Functions workflow that generates the report and emails it","Use API Gateway with Lambda proxy for longer timeout"], correct:1, exp:"Asynchronous pattern: the web server immediately acknowledges the request and puts a message in SQS. A background worker (Lambda with 10GB memory supports 4GB RAM, or ECS task for longer processes) generates the PDF, stores it in S3, and sends an email via SNS/SES with a pre-signed download URL. The user gets an immediate response. Step Functions work but add complexity for a simple queue-process-notify pattern.", domain:2},
  {q:"A company has a data lake in S3 with 500TB of data. Multiple teams query this data using Athena with their own workgroups. The finance team complains that their queries run slowly when the data science team is running large queries simultaneously. How can they ensure fair resource allocation?", opts:["Create separate S3 buckets for each team","Configure Athena workgroups with query execution limits (bytes scanned limit, concurrent query limit) per team","Migrate to Redshift for better concurrency management","Use S3 Select to pre-filter data before Athena processes it"], correct:1, exp:"Athena workgroups provide per-team resource management: bytes scanned limits (cost control), concurrent query limits (prevent one team from monopolizing capacity), and separate query history/results. This ensures fair sharing of Athena's capacity. Separate buckets don't solve query concurrency. Redshift is a different architecture. S3 Select doesn't work with Athena directly.", domain:4},
  {q:"A company needs to share an AMI containing proprietary software with specific AWS accounts (their customers). The AMI should NOT be publicly accessible. How should this be configured?", opts:["Make the AMI public and trust that only authorized customers use it","Modify the AMI permissions to add specific AWS account IDs as 'Launch permissions'","Upload the AMI to S3 and share the pre-signed URL","Create the AMI in each customer's account using CloudFormation"], correct:1, exp:"AMI launch permissions can be granted to specific AWS account IDs. The AMI remains private (not public) but the specified accounts can see and launch it. This is the standard way to share AMIs with specific customers without making them public. No S3 export needed. CloudFormation can't create AMIs from nothing in another account.", domain:1},
  {q:"A company's EC2 instances in a private subnet need to access AWS Systems Manager for patching and management. The company's security policy prohibits any internet connectivity (no NAT Gateway, no Internet Gateway). How can SSM access be provided?", opts:["Impossible — SSM requires internet connectivity","Create VPC Interface Endpoints (PrivateLink) for ssm, ssmmessages, and ec2messages services","Allow outbound HTTPS in the Security Group (SSM will work without internet)","Use a bastion host in a public subnet to proxy SSM connections"], correct:1, exp:"VPC Interface Endpoints (PrivateLink) provide private connectivity to AWS services without internet. For SSM, three endpoints are required: com.amazonaws.region.ssm, com.amazonaws.region.ssmmessages, com.amazonaws.region.ec2messages. Traffic stays on the AWS private network. The instance also needs an IAM instance profile with SSM permissions. No internet connectivity required.", domain:1},
  {q:"A company deploys their application using an Auto Scaling Group with a launch template. They update the launch template to a new version (v2) with an updated AMI. However, existing instances continue running the OLD AMI. Only new instances launched by scale-out events use v2. How do they update ALL instances?", opts:["Delete the old instances — ASG will replace them with v2","Use Instance Refresh (start a rolling replacement of all instances)","Change the ASG to use the new launch template version (this triggers automatic replacement)","Run a manual update command on each instance via Systems Manager"], correct:1, exp:"Instance Refresh performs a controlled rolling replacement of instances in the ASG. You specify the minimum healthy percentage (e.g., 90%), and ASG replaces instances in batches, waiting for new instances to pass health checks before continuing. This is the proper operational procedure. Simply changing the launch template version doesn't affect running instances. Deleting instances is disruptive and uncontrolled.", domain:2},
  {q:"A company uses Amazon Cognito for user authentication. They want to trigger a custom email verification workflow using their own email service instead of Cognito's built-in emails. How can this be customized?", opts:["Configure Cognito to use SES instead of the built-in email","Use a Custom Message Lambda trigger to modify the email content and sending logic","Use a Pre-Sign-Up Lambda trigger to send the verification email through a custom service and suppress Cognito's email","Configure the email provider in the Cognito User Pool message customization settings"], correct:1, exp:"Custom Message Lambda trigger is invoked when Cognito sends messages (verification, password reset, MFA). The trigger can customize the message content and override the delivery mechanism. You can integrate your own email service (SendGrid, custom SMTP) through this Lambda. Pre-Sign-Up runs before user creation and can't replace the email workflow. SES integration changes the sender but not the entire workflow.", domain:1},
  {q:"A company runs a batch ETL job that reads from Aurora, transforms data, and loads into Redshift. The job currently runs on a large EC2 instance and takes 4 hours. They want to make it serverless with no infrastructure to manage. Which service should they use?", opts:["Lambda with 15-minute timeout (won't complete in time)","AWS Glue ETL job (serverless Spark environment)","Step Functions with multiple Lambda functions chained together","ECS Fargate task triggered by EventBridge schedule"], correct:1, exp:"AWS Glue ETL provides serverless Apache Spark environment for ETL. It connects to Aurora (JDBC) and Redshift natively, handles the transformation, and scales automatically. No infrastructure to manage. Lambda can't run 4 hours. Step Functions + Lambda adds complexity and timeout issues. Fargate requires container management. Glue is purpose-built for ETL workloads with managed Spark runtime.", domain:3},
  {q:"A company wants ALL S3 API calls (GetObject, PutObject, DeleteObject) across all buckets in their account to be logged. Where should they configure this?", opts:["Enable S3 server access logging on each bucket","Create a CloudTrail trail with S3 data events enabled for all S3 buckets in the account","Enable S3 metrics in CloudWatch","Use AWS Config to record S3 resource changes"], correct:1, exp:"CloudTrail with S3 data events logs ALL S3 API calls (Get/Put/Delete) for all buckets in the account. A single trail configuration covers everything. S3 server access logging provides similar data but must be configured per-bucket and has a different format. CloudWatch metrics show counts, not individual API calls. AWS Config records resource configuration changes, not data plane operations.", domain:1},
  {q:"A company uses CodePipeline for CI/CD. They deploy to ECS Fargate. The deployment must: (1) run automated integration tests against the new version, (2) route 10% of production traffic to it, (3) if tests pass and no errors after 30 minutes, shift to 100%. Which CodeDeploy configuration achieves this?", opts:["Linear10PercentEvery10Minutes deployment configuration","Custom canary deployment: 10% for 30 minutes with pre-traffic and post-traffic validation hooks (Lambda functions running integration tests)","Rolling update with health checks","Blue/Green with all-at-once traffic shift after manual approval"], correct:1, exp:"CodeDeploy ECS Blue/Green supports lifecycle hooks: BeforeAllowTraffic (run integration tests against new task set before any traffic), AfterAllowTraffic (monitor after canary traffic). Custom configuration sets 10% initial traffic. Lambda hooks run tests. After 30 minutes with no CloudWatch alarms, traffic shifts to 100%. This provides automated testing + gradual rollout + automatic rollback.", domain:2},
  {q:"A company uses an NLB to expose a TCP service running on EC2 instances. They notice that the NLB health checks mark instances as unhealthy even though the application is running correctly. The health check is configured on the application port (8443). What is the MOST likely cause?", opts:["The Security Group on the instances doesn't allow health check traffic from the NLB","NLB uses TCP health checks that only verify the port is open — the application might be rejecting the connection","The NLB health check doesn't use Security Groups — it uses the instance's NACL. The NACL might be blocking NLB subnet IPs","The instances' Security Group must allow traffic from the NLB's PRIVATE IP addresses (NLB doesn't use a Security Group itself)"], correct:3, exp:"NLB doesn't have its own Security Group. It forwards traffic directly to instances using the NLB's private IP addresses (within the VPC). The instances' Security Group must allow inbound traffic from the NLB's subnet CIDR range (or the specific NLB node IPs). Many people configure SG to allow the NLB's public IP, which doesn't work. This is a very common NLB troubleshooting issue.", domain:2},
  {q:"A company uses AWS Backup to centrally manage backups for EC2, RDS, EFS, and DynamoDB across 50 accounts. They need to ensure backups cannot be deleted by anyone (including administrators) for 1 year. What should they enable?", opts:["Backup vault lock in Compliance mode with a 1-year minimum retention period","IAM policies denying backup:DeleteRecoveryPoint in all accounts","MFA Delete on the backup vault","S3 Object Lock for backup storage"], correct:0, exp:"AWS Backup Vault Lock in Compliance mode makes backups immutable (WORM). Once locked, NO ONE (including root user, AWS Support) can delete backups before the minimum retention period expires. This meets regulatory requirements for backup immutability. IAM policies can be modified by admins. MFA Delete doesn't exist for Backup vaults. S3 Object Lock is for S3, not AWS Backup vaults.", domain:1},
  {q:"A company serves a GraphQL API using AWS AppSync. They need real-time updates pushed to mobile clients when data changes (e.g., new message in a chat). Which AppSync feature provides this?", opts:["AppSync polling queries every 5 seconds","AppSync Subscriptions (GraphQL subscriptions over WebSocket)","AppSync push notifications via SNS","AppSync with EventBridge integration for real-time events"], correct:1, exp:"AppSync Subscriptions use GraphQL subscription operations over WebSocket connections. When a mutation occurs (new message), all subscribed clients automatically receive the update in real-time. No polling needed. This is built into AppSync — clients subscribe to specific mutations and get pushed updates. SNS is for push notifications to mobile (different use case). EventBridge doesn't push to mobile clients directly.", domain:3},
  {q:"A company uses Route 53 for DNS. Their application runs in us-east-1 and eu-west-1 (active-active). They want Route 53 to route users to the closest healthy region. If a region becomes unhealthy, ALL traffic should go to the healthy region. Which routing policy achieves this?", opts:["Latency-based routing with health checks associated with each record","Failover routing with primary (us-east-1) and secondary (eu-west-1)","Weighted routing with 50/50 split and health checks","Geolocation routing with health checks"], correct:0, exp:"Latency-based routing directs users to the lowest-latency healthy endpoint. If one region's health check fails, Route 53 removes it from responses and all traffic routes to the remaining healthy region automatically. Failover routing has one primary/one secondary (not active-active, doesn't optimize latency). Weighted 50/50 doesn't optimize for proximity. Geolocation maps by geography, not latency.", domain:2},
  {q:"A company needs their AWS Lambda function to process messages from an SQS queue. They want to process up to 10 messages at a time (batch) but only when at least 5 messages are available (to avoid processing tiny batches inefficiently). Which event source mapping configuration achieves this?", opts:["Batch size: 10, no additional configuration needed","Batch size: 10 with Maximum Batching Window of 60 seconds","Batch size: 10 with Minimum Batch Size of 5","Batch size: 10 with Batch Window — Lambda waits up to the window duration to accumulate messages, then invokes with whatever it has (up to batch size)"], correct:3, exp:"Maximum Batching Window tells Lambda to wait up to X seconds to accumulate messages before invoking. If 10 messages arrive in 3 seconds, it invokes immediately with 10. If messages trickle in slowly, it waits up to the window duration then invokes with whatever accumulated. There is no 'Minimum Batch Size' parameter — Lambda invokes as soon as messages are available or window expires. The window helps batch efficiency.", domain:3},
  {q:"A company notices that their monthly AWS bill shows $3,000 for NAT Gateway data processing. Investigation reveals most traffic is between Lambda functions in private subnets and DynamoDB. What change eliminates this cost?", opts:["Move Lambda functions to public subnets","Remove VPC configuration from Lambda functions (Lambda can access DynamoDB without VPC)","Add a VPC Gateway Endpoint for DynamoDB (free, no data processing charges)","Replace NAT Gateway with a NAT instance (cheaper)"], correct:2, exp:"If Lambda doesn't need VPC for other reasons (like RDS access), removing VPC configuration (option B) is simplest — Lambda natively accesses DynamoDB over the internet without VPC. If Lambda MUST be in VPC (for RDS access), add a VPC Gateway Endpoint for DynamoDB (option C) — it's free and routes DynamoDB traffic privately without NAT. Both B and C work but C keeps VPC while eliminating the cost.", domain:4},
  {q:"A company uses Amazon Redshift for data warehousing. Analysts run queries during business hours (9am-6pm). The cluster sits idle for 15 hours every day and all weekend. The monthly cost is $15,000. What is the MOST effective cost reduction?", opts:["Pause the Redshift cluster during off-hours (storage only cost when paused)","Migrate to Redshift Serverless (pay per query, no idle cost)","Use smaller node types during off-hours","Purchase Reserved Nodes for the cluster"], correct:1, exp:"Redshift Serverless charges only for actual compute used (measured in RPU-seconds). With queries only during business hours (~45 hours/week out of 168), serverless eliminates ~73% idle cost. Pausing also works (free compute when paused, ~$0.024/GB/month storage) but has 30-60 second resume time. Serverless is instantly available. RI saves ~40% but still pays for idle. Serverless is best for predictable low-utilization patterns.", domain:4},
  {q:"A company has a microservices architecture with 20 services. They need distributed tracing to identify latency bottlenecks across service calls. Which AWS service provides end-to-end distributed tracing with service maps?", opts:["CloudWatch Logs with correlation IDs","AWS X-Ray with SDK instrumentation in each service","CloudWatch ServiceLens","AWS CloudTrail"], correct:1, exp:"AWS X-Ray provides distributed tracing: tracks requests across microservices, generates service maps showing call flow and latency, identifies bottlenecks. The X-Ray SDK/agent instruments each service to capture trace segments. Service maps visualize dependencies and pinpoint slow services. CloudWatch Logs requires manual correlation. CloudTrail traces API calls, not application requests. ServiceLens integrates X-Ray data into CloudWatch but X-Ray is the tracing engine.", domain:2},
  {q:"A company stores secrets (database passwords, API keys) in AWS Secrets Manager. They have 100 Lambda functions across 5 accounts that need these secrets. They want to minimize secret retrieval costs and latency. What is the BEST practice?", opts:["Cache secrets in Lambda environment variables (updated at deploy time)","Use the AWS Parameters and Secrets Lambda Extension (caches secrets in Lambda execution environment with configurable TTL)","Store secrets in DynamoDB for faster access","Retrieve the secret once in the Lambda init phase (outside the handler) and reuse across invocations in the same container"], correct:1, exp:"The AWS Parameters and Secrets Lambda Extension is a Lambda layer that provides an in-memory cache for Secrets Manager (and Parameter Store) values. It caches secrets with configurable TTL, reducing API calls and latency. The extension handles cache invalidation. Option D (init-phase retrieval) works for a single container but doesn't handle rotation. Environment variables are static and don't rotate. The extension is the AWS-recommended approach.", domain:4},
]
