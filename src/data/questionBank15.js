export const questionBank15 = [
  {q:"A company runs a web application that allows users to upload videos up to 5GB. The application runs on EC2 behind an ALB. Users report timeout errors when uploading large files. The ALB idle timeout is 60 seconds. What is the BEST solution?", opts:["Increase ALB idle timeout to 3600 seconds","Implement multipart upload directly to S3 using pre-signed URLs (bypass ALB for large uploads)","Increase EC2 instance size for faster processing","Use a Network Load Balancer instead of ALB"], correct:1, exp:"Pre-signed URLs allow clients to upload directly to S3 without going through the ALB/EC2. S3 multipart upload handles files up to 5TB with automatic retry per part. This bypasses the ALB timeout entirely and offloads upload bandwidth from EC2 instances. Increasing ALB timeout wastes connections. NLB doesn't solve application-level timeouts.", domain:3},
  {q:"A company needs to run a scheduled ETL job at 2 AM daily. The job takes 45 minutes and requires 8 vCPU, 32GB RAM. It must complete successfully even if one execution fails (retry). What is the MOST operationally simple serverless solution?", opts:["EventBridge scheduled rule → Lambda (15min timeout won't work for 45min job)","EventBridge scheduled rule → Step Functions → ECS Fargate task with retry logic","EventBridge scheduled rule → AWS Batch job with retry count of 3","CloudWatch Events → EC2 instance with cron job"], correct:2, exp:"AWS Batch handles: scheduling (via EventBridge trigger), compute provisioning (scales from zero), retries (configurable retry count), and resource requirements (8 vCPU, 32GB). It's serverless for the operator — no instances to manage. Step Functions + Fargate also works but Batch is purpose-built for batch jobs with retry semantics. Lambda can't run 45 minutes.", domain:2},
  {q:"A company uses Aurora MySQL. Developers need read access to production data for debugging but must NOT be able to see PII columns (email, phone, SSN). How can this be implemented without creating separate databases?", opts:["Create MySQL views excluding PII columns and grant developers access only to views","Use AWS Lake Formation column-level security on the Aurora table","Create Aurora Read Replicas with data masking (not a native feature)","Implement application-level access control"], correct:0, exp:"MySQL views that exclude PII columns provide a simple, database-native solution. Grant developers SELECT permission only on views, not on base tables. Views act as a security boundary — developers query the view and never see restricted columns. Lake Formation is for data lakes (S3/Athena), not Aurora directly. This is the simplest approach using native MySQL features.", domain:1},
  {q:"A company operates a SaaS platform serving 500 tenants. Each tenant generates approximately 1GB of logs per day. All logs must be searchable with sub-second latency for the last 30 days. What is the MOST cost-effective log analytics solution?", opts:["CloudWatch Logs with Logs Insights (expensive at 500GB/day ingestion)","Amazon OpenSearch Service with UltraWarm nodes for older data","S3 + Athena with partitioning by tenant and date","Amazon OpenSearch Serverless (pay per OCU, scales automatically)"], correct:1, exp:"OpenSearch provides sub-second search latency. UltraWarm nodes are 80% cheaper than hot nodes for data >7 days old. Architecture: hot nodes for 0-7 days (fast ingest + search), UltraWarm for 7-30 days (searchable at slightly higher latency). 500GB/day × 30 days = 15TB total. CloudWatch Logs at this scale is very expensive ($0.50/GB ingestion = $250/day). Athena can't provide sub-second latency for log search.", domain:4},
  {q:"A company uses S3 for storing application data. They want to ensure that if someone accidentally deletes an object, it can be recovered. They also want to prevent even privileged users from permanently deleting objects for compliance. Which combination provides both recovery AND tamper-proof retention?", opts:["S3 Versioning (recovery) + S3 Object Lock in Compliance mode (tamper-proof retention)","S3 Versioning only","S3 Cross-Region Replication for backup","MFA Delete enabled on the bucket"], correct:0, exp:"Versioning provides recovery (delete markers can be removed to restore objects). Object Lock in Compliance mode makes objects immutable — nobody (including root user) can delete them before the retention period expires. Together: accidental deletes are recoverable via versioning, and malicious/intentional permanent deletion is prevented by Object Lock. MFA Delete adds a layer but can be bypassed by root.", domain:1},
  {q:"A company builds a real-time chat application. Messages must be delivered to recipients within 200ms. Users can be in multiple chat rooms simultaneously. When offline, users should receive messages upon reconnecting. Which architecture supports all requirements?", opts:["API Gateway REST API with polling every 1 second","API Gateway WebSocket API + DynamoDB for message persistence + Lambda for message routing","AppSync Subscriptions + DynamoDB","IoT Core MQTT with persistent sessions (QoS 1 delivers missed messages on reconnect)"], correct:3, exp:"IoT Core MQTT with persistent sessions satisfies ALL requirements: (1) MQTT delivers messages in <100ms (well under 200ms), (2) Topics support multiple subscriptions (chat rooms), (3) Persistent sessions with QoS 1 automatically store messages for offline clients and deliver them upon reconnection. WebSocket API would work for real-time but requires custom offline message queuing. IoT Core handles this natively.", domain:3},
  {q:"A company has a VPC in us-east-1. They need EC2 instances in this VPC to access an Amazon S3 bucket in eu-west-1 (different region). Data must not traverse the public internet. Is this possible with a VPC endpoint?", opts:["Yes — VPC Gateway Endpoints for S3 support cross-region access","No — VPC Gateway Endpoints only work for S3 buckets in the same region. Use a VPC Interface Endpoint for S3 which supports cross-region access","No — VPC endpoints only work within the same region. Traffic to cross-region S3 goes via the internet (or Inter-Region VPC Peering + endpoint in the remote region)","Yes — use a Transit Gateway with inter-region peering"], correct:2, exp:"VPC endpoints (both Gateway and Interface) are REGIONAL — they only route traffic to services in the same region. To access an S3 bucket in eu-west-1 from us-east-1, traffic must go over the internet (AWS backbone). Alternatively, create a proxy in eu-west-1 (with a VPC endpoint there) accessible via VPC peering or Transit Gateway inter-region peering. This is a commonly misunderstood limitation.", domain:1},
  {q:"A company runs an Auto Scaling Group with a step scaling policy. They notice that during rapid scale-out events, many instances launch simultaneously and then scale back in minutes later (over-scaling). What configuration change prevents this over-provisioning?", opts:["Add a cooldown period after scale-out activities","Switch to target tracking scaling (automatically adjusts without over-provisioning)","Use step scaling with warmup period for new instances (don't count new instance metrics until they're warm)","Reduce the step scaling adjustment percentage"], correct:1, exp:"Target tracking automatically manages scaling to maintain a target metric (e.g., 60% CPU). It has built-in intelligence to avoid over-provisioning: it considers in-flight scaling activities, uses proportional adjustment, and includes scale-in protection. Step scaling reacts to alarm thresholds without considering current scaling activities — leading to over-shoot. Target tracking is the recommended policy for most use cases.", domain:2},
  {q:"A company uses DynamoDB for an e-commerce product catalog. Products have a 'category' attribute. They need to display all products in a category sorted by price. The table's partition key is product_id. What should they create?", opts:["Scan the table with FilterExpression on category, sort results in application code","Global Secondary Index with partition key = category and sort key = price","Local Secondary Index with sort key = price","Query with KeyConditionExpression on category (won't work — category isn't the table's partition key)"], correct:1, exp:"A GSI with PK=category and SK=price enables efficient Query: get all products in 'Electronics' sorted by price (ascending or descending). The query reads only relevant items (no scan). LSI requires the same partition key as the base table (product_id), which doesn't help. Scan + filter reads the entire table and is expensive. GSI is the correct solution for a new access pattern with different keys.", domain:3},
  {q:"A company uses AWS Lambda with Python. They notice that the same function deployed in two regions (us-east-1 and eu-west-1) behaves differently — one succeeds and the other fails with import errors. The deployment package is identical. What could cause this?", opts:["Lambda runtimes differ between regions","The function uses Lambda Layers that are region-specific (Layer ARNs are regional and must exist in each deployment region)","Different Python versions in each region","Memory allocation differs between regions"], correct:1, exp:"Lambda Layer ARNs are region-specific. A layer created in us-east-1 (arn:aws:lambda:us-east-1:...) doesn't exist in eu-west-1. If the function references a layer by ARN, it must reference the layer in the SAME region. When deploying cross-region, ensure layers are published in each target region and the function references the correct regional ARN. This is a common multi-region deployment gotcha.", domain:2},
  {q:"A company uses Amazon RDS PostgreSQL. They need to run a complex analytical query that joins 5 tables and processes 100GB of data. This query takes 30 minutes and significantly impacts online transaction processing during execution. What is the BEST isolation approach?", opts:["Run the query during off-peak hours only","Create a Read Replica specifically for analytical queries (isolates workload from the primary)","Export data to S3 and query with Athena","Increase RDS instance size to handle both workloads"], correct:1, exp:"A dedicated Read Replica for analytics completely isolates the analytical workload from production OLTP. The replica has its own compute resources (can be a larger instance type optimized for analytics). The 30-minute query won't impact the primary at all. Replication lag is typically seconds (acceptable for analytics). Athena works but requires data export. Larger primary still shares resources between workloads.", domain:3},
  {q:"A company runs EC2 instances in a private subnet. They need to install packages from the internet during UserData bootstrap. After bootstrap, instances should have NO internet access (security requirement). How can this be achieved?", opts:["Use a NAT Gateway with a Security Group that's removed after bootstrap (NAT GW doesn't have SGs)","Use a NAT Gateway and modify the private subnet's route table AFTER bootstrap to remove the NAT route (via Lambda triggered by instance state change)","Install packages from an S3-hosted package repository (no internet needed at any time)","Temporarily attach a public IP, then remove it after bootstrap"], correct:2, exp:"Hosting packages in S3 (or using CodeArtifact, or VPC endpoints for package repositories like Amazon Linux Extras) eliminates the need for internet access entirely. VPC endpoints for S3 provide free private access to the S3-hosted repo. This is more secure than temporarilyallowing internet access. Many companies mirror yum/apt repositories in S3 for exactly this reason. No complex post-bootstrap route modifications needed.", domain:1},
  {q:"A company has an API that receives webhook callbacks from a third-party payment processor. The webhooks must be acknowledged with HTTP 200 within 5 seconds or the processor retries (causing duplicate processing). Current Lambda processing takes 10-30 seconds. How should this be redesigned?", opts:["Increase Lambda memory for faster processing","Split into two Lambda functions: first Lambda receives webhook, immediately returns 200, and asynchronously invokes second Lambda (or sends to SQS) for actual processing","Use API Gateway mock integration to return 200 immediately","Configure the payment processor to allow longer timeout"], correct:1, exp:"The receiver Lambda immediately stores the webhook payload (SQS or async Lambda invoke) and returns HTTP 200 to the processor within milliseconds. A separate consumer Lambda processes the payment asynchronously without time pressure. This decouples acknowledgment from processing. API Gateway mock would return 200 but not capture the webhook data. You can't always change third-party timeout settings.", domain:2},
  {q:"A company has 3 AWS accounts: Development, Staging, Production. They want a CI/CD pipeline that deploys to Dev automatically, requires manual approval for Staging, and requires two approvals for Production. Which service supports this workflow?", opts:["AWS CodePipeline with manual approval actions between stages","GitHub Actions with environment protection rules","Both work — CodePipeline is AWS-native, GitHub Actions is more flexible for multi-approval","Jenkins pipeline with AWS plugins"], correct:0, exp:"AWS CodePipeline supports manual approval actions between stages. The pipeline: Source → Build → Deploy-Dev (automatic) → Approval-Staging (one manual approval) → Deploy-Staging → Approval-Prod (can use SNS notification requiring two approvals via custom Lambda approval logic) → Deploy-Prod. Cross-account deployment uses IAM roles in each account. CodePipeline is the AWS-native CI/CD orchestration service.", domain:2},
  {q:"A company uses CloudFront to serve a single-page application (React/Vue/Angular) from S3. Users report 403 errors when they directly access deep URLs like /dashboard/settings (not the root /). What is the cause and fix?", opts:["S3 doesn't have a /dashboard/settings object — configure CloudFront to return index.html for all 403/404 errors (custom error response)","The S3 bucket policy doesn't allow access to subdirectories","CloudFront needs a behavior for each URL path","The SPA needs server-side routing configuration"], correct:0, exp:"SPAs use client-side routing — all routes (/dashboard/settings) are handled by the same index.html file. When CloudFront requests /dashboard/settings from S3, the object doesn't exist → S3 returns 403/404. Fix: configure CloudFront Custom Error Responses to return index.html (with 200 status) for 403 and 404 errors. The SPA's JavaScript router then handles the URL client-side. This is the #1 SPA deployment issue.", domain:3},
  {q:"A company uses Amazon ElastiCache Redis (Cluster Mode Enabled) with 3 shards. They notice high ReplicationLag on one shard. What is the MOST likely cause?", opts:["Network congestion between the primary and replica","The shard has a hot key causing excessive write load (replication can't keep up with the write rate on that specific shard)","Redis version is outdated","ElastiCache has a bug"], correct:1, exp:"High ReplicationLag on a SPECIFIC shard (not all shards) indicates that shard is receiving disproportionate writes (hot key problem). The replica can't keep up with the primary's write rate. Solutions: identify the hot key (CloudWatch KeyspaceMisses, Redis SLOWLOG), redesign the key to spread across shards, or increase the shard's node type. If ALL shards had high lag, it would suggest a systemic issue.", domain:3},
  {q:"A company uses DynamoDB Global Tables (multi-region). They write to the table in us-east-1 and read in eu-west-1. After a write in us-east-1, a read in eu-west-1 immediately after sometimes returns the OLD value. Is this expected?", opts:["No — Global Tables provides strong consistency across regions","Yes — Global Tables provides eventual consistency across regions (replication lag is typically <1 second but not zero)","No — this indicates a configuration error","Yes — but only if on-demand mode is disabled"], correct:1, exp:"DynamoDB Global Tables replicates asynchronously across regions. Writes in one region are eventually consistent in other regions (typically <1 second lag, but not guaranteed instant). A read immediately after a write in a different region may see stale data. For read-after-write consistency, read from the SAME region where the write occurred. This is the fundamental trade-off of active-active multi-region replication.", domain:3},
  {q:"A company wants to migrate their on-premises Oracle Data Warehouse (20TB) to AWS. They want to modernize to a cloud-native analytics service, not just lift-and-shift Oracle. The warehouse has complex ETL, stored procedures, and 500 reports. What is the recommended migration approach?", opts:["Migrate directly to Redshift using DMS","Use AWS SCT to assess migration complexity, convert Oracle warehouse schema to Redshift, use DMS for data migration, and incrementally migrate ETL/reports","Rewrite everything from scratch on Redshift","Use RDS Oracle to lift-and-shift, then modernize later"], correct:1, exp:"AWS Schema Conversion Tool (SCT) analyzes Oracle-to-Redshift conversion complexity (automatic vs manual conversion percentage). It converts schema, views, and some stored procedures automatically. DMS handles data migration with CDC for minimal downtime. ETL and reports are migrated incrementally (most complex part). This phased approach manages risk. Direct migration without assessment risks failures. Complete rewrite is too expensive/slow.", domain:2},
  {q:"A company has a Lambda function that processes S3 events. They enable S3 event notifications for PUT events. During testing, they upload a file but the Lambda is NOT triggered. The Lambda has correct permissions. What should they check?", opts:["S3 event notifications have a propagation delay of up to 1 hour","The S3 bucket and Lambda function must be in the same region","Check if there's a resource-based policy on the Lambda allowing S3 to invoke it (s3.amazonaws.com must have lambda:InvokeFunction permission)","Lambda cold start is preventing invocation"], correct:2, exp:"For S3 to invoke Lambda, the Lambda function needs a resource-based policy allowing the S3 service principal (s3.amazonaws.com) to invoke it. This is set via `aws lambda add-permission`. Without this permission, S3 can't trigger the function even if the event notification is configured. The Lambda execution role (what Lambda can DO) is separate from the resource policy (WHO can invoke Lambda). This is a common setup issue.", domain:2},
  {q:"A company runs a data pipeline: IoT devices → API Gateway → Lambda → DynamoDB. During peak (100K requests/sec), they hit API Gateway's default throttle limit (10,000 requests/sec at account level) and get 429 errors. How should they handle this?", opts:["Request API Gateway throttle limit increase from AWS Support","Replace API Gateway with Kinesis Data Streams (handles 100K+ writes/sec per shard) → Lambda → DynamoDB","Use API Gateway with usage plans to prioritize critical devices","Add CloudFront in front of API Gateway for caching"], correct:1, exp:"For 100K IoT messages per second, Kinesis Data Streams is the appropriate ingestion service (not API Gateway which has a 10K TPS default regional limit). Kinesis handles millions of records/sec with proper shard count (100+ shards for 100K/sec). Lambda processes batches from the stream. API Gateway is designed for request-response APIs, not high-throughput IoT ingestion. IoT Core → Kinesis is even better for IoT specifically.", domain:3},
  {q:"A company's security team discovers an EC2 instance in their VPC communicating with a known command-and-control (C2) server. Which AWS service DETECTED this threat?", opts:["AWS Config (resource configuration monitoring)","Amazon GuardDuty (analyzes VPC Flow Logs, DNS logs, and CloudTrail for threats including C2 communication)","AWS Shield (DDoS protection)","Amazon Inspector (vulnerability assessment)"], correct:1, exp:"Amazon GuardDuty analyzes VPC Flow Logs, DNS query logs, and CloudTrail events using threat intelligence feeds and ML. It detects: C2 communication (Backdoor:EC2/C&CActivity), cryptocurrency mining, DNS exfiltration, unauthorized access attempts, and more. It specifically correlates outbound traffic with known malicious IP databases. Config tracks configuration, not network threats. Shield is DDoS-focused. Inspector scans for vulnerabilities, not active threats.", domain:1},
  {q:"A company uses Amazon MQ (ActiveMQ) for message queuing. They notice the broker occasionally becomes unavailable for 30 seconds during maintenance windows. Their application can't tolerate any message loss during these periods. What architecture change ensures zero message loss?", opts:["Configure Amazon MQ with active/standby deployment (Multi-AZ) for automatic failover","Enable message persistence (already default in ActiveMQ)","Add a secondary broker in another region with network bridge","Implement client-side message buffering during outages"], correct:0, exp:"Amazon MQ active/standby deployment provides Multi-AZ redundancy. If the active broker fails or undergoes maintenance, the standby broker in another AZ takes over automatically (typically 30-60 seconds). With persistent messages (stored to EFS shared between AZs), zero messages are lost during failover. Client libraries automatically reconnect to the new active broker. This is the standard HA configuration for Amazon MQ.", domain:2},
  {q:"A company uses AWS Glue for their ETL pipeline. Glue crawlers discover the schema of data in S3 and populate the Data Catalog. After a schema change in the source data (new column added), the Glue Crawler updates the catalog but downstream Athena queries fail because they reference the OLD schema. How should schema evolution be handled?", opts:["Run the crawler before every Athena query","Configure the Glue Crawler with schema change policy: add new columns, don't delete old ones, and use Athena's schema-on-read (Athena adapts to catalog changes automatically)","Delete and recreate the Glue table on each schema change","Use a fixed schema and ignore new columns"], correct:1, exp:"Glue Crawler schema change policy controls how schema evolution is handled: 'Add new columns' adds discovered columns without removing existing ones. Athena uses schema-on-read — it references the current Glue catalog schema at query time. After the crawler updates the catalog with new columns, Athena queries automatically see them. Existing queries referencing old columns continue working. No table recreation needed.", domain:3},
  {q:"A company uses Secrets Manager to store a database password. Their Lambda function retrieves the secret at startup. After enabling automatic rotation (every 30 days), the Lambda function intermittently fails with authentication errors. What is the MOST likely cause?", opts:["The rotation Lambda failed and the secret is corrupted","The application Lambda caches the secret and doesn't pick up the rotated value — implement the Secrets Manager Lambda Extension with TTL-based caching or re-fetch on authentication failure","Secrets Manager doesn't notify applications of rotation","The database doesn't accept the new password"], correct:1, exp:"After rotation, the secret value changes but the Lambda function has the OLD value cached (either in a global variable outside the handler, or in a container environment variable). The fix: implement re-fetch logic on authentication failure, or use the Secrets Manager Lambda Extension with appropriate cache TTL (shorter than rotation period). The function should handle secret refresh gracefully, not just fail.", domain:2},
  {q:"A company wants to monitor their application's user experience. They need to measure: page load time, JavaScript errors, API call latency from the browser, and user click patterns. Which AWS service provides Real User Monitoring (RUM)?", opts:["CloudWatch Logs from the application server","Amazon CloudWatch RUM (Real-User Monitoring) — JavaScript snippet captures real user metrics from browsers","AWS X-Ray","CloudFront access logs"], correct:1, exp:"CloudWatch RUM provides a JavaScript snippet that captures real user experience data directly from the browser: page load performance, JavaScript errors, HTTP errors, client-side latency, and user journeys. Data flows to CloudWatch for dashboards and alerts. X-Ray traces server-side requests but not client-side browser experience. CloudFront logs show edge-level metrics, not browser-level. RUM provides the TRUE end-user perspective.", domain:2},
  {q:"A company deploys their application using ECS with Fargate. They want to run database migration scripts BEFORE the new application version starts receiving traffic. The migration takes 2 minutes. How should this be orchestrated?", opts:["Run migration in the application startup code (delays container readiness)","Use a CodeDeploy BeforeInstall lifecycle hook that runs an ECS task for migration before shifting traffic to the new version","Create a separate ECS task definition for migration, triggered by CodePipeline BEFORE the deploy stage","Use a Step Functions workflow: run migration task → wait for success → deploy new ECS service version"], correct:2, exp:"CodePipeline with a pre-deployment stage runs the migration ECS task. Only after successful completion does the pipeline proceed to the ECS deploy stage. This ensures: migrations complete before new code runs, failures stop the pipeline (preventing broken deployments), and the pattern is auditable. Application startup migration delays health checks. CodeDeploy hooks also work but the CodePipeline approach is cleaner for ECS deployments.", domain:2},
  {q:"A company uses S3 for storing data. Their bucket has millions of objects with various prefixes. They want to find all objects that haven't been accessed in the last 90 days to move them to a cheaper storage class. S3 lifecycle rules can't distinguish by LAST ACCESS time (only creation time). What can they use?", opts:["S3 Analytics (provides access pattern analysis per prefix but not per-object last access)","S3 Intelligent-Tiering (automatically moves objects based on access patterns — exactly what they need)","Enable S3 access logging and parse logs to find unaccessed objects","CloudWatch metrics for S3 per-object access (doesn't exist)"], correct:1, exp:"S3 Intelligent-Tiering monitors access patterns PER OBJECT and automatically moves objects between tiers (Frequent Access, Infrequent Access, Archive Instant, Archive, Deep Archive) based on actual access. Objects not accessed for 30 days move to IA, 90 days to Archive Instant, etc. This is exactly what they need — automatic, per-object, access-pattern-based tiering. No manual log parsing needed. Small monthly monitoring fee per object.", domain:4},
  {q:"A company uses Amazon Connect for their contact center. They want to implement a chatbot that handles common questions (order status, password reset) before routing to a human agent if the bot can't help. Which service powers the chatbot?", opts:["Amazon Comprehend (NLP but not conversational)","Amazon Lex (conversational AI service for building chatbots, integrates natively with Amazon Connect)","Amazon Polly (text-to-speech only)","Custom Lambda chatbot logic"], correct:1, exp:"Amazon Lex provides the conversational AI engine for chatbots. It understands natural language intents and slot filling (e.g., 'What's my order status?' → intent: OrderStatus, slot: order_number). Connect integrates natively with Lex — the contact flow routes to a Lex bot first. If Lex can't resolve the issue (fallback intent), it transfers to a human agent queue. Comprehend analyzes text but doesn't have conversation flow. Polly converts text to speech.", domain:3},
  {q:"A company has a REST API served by Lambda behind API Gateway. They want to support both JSON and XML response formats. The client specifies their preference via the Accept header (application/json or application/xml). How should this be implemented?", opts:["Two separate API endpoints (/api/json and /api/xml)","Lambda function checks the Accept header from the event object and returns the appropriate format (JSON or XML marshaling in code)","API Gateway response mapping templates that transform based on the Accept header","Content negotiation is handled automatically by API Gateway"], correct:1, exp:"With Lambda proxy integration (most common), the Lambda function receives the full request including headers. It reads the Accept header from event.headers, formats the response accordingly (JSON or XML), and returns with the appropriate Content-Type header. This keeps content negotiation logic in application code. API Gateway mapping templates work with non-proxy integration but add complexity. There's no automatic content negotiation.", domain:3},
  {q:"A company runs a production database on RDS MySQL. A junior DBA accidentally runs a DELETE without a WHERE clause, deleting all rows from a critical table. The last automated backup was 4 hours ago. How can they recover the data with minimal loss?", opts:["Restore from the last automated backup (lose 4 hours of data)","Use Point-in-Time Recovery (PITR) to restore to 1 second BEFORE the DELETE statement was executed (lose only seconds of data)","Enable binary logging after the fact to replay transactions","Contact AWS support for emergency data recovery"], correct:1, exp:"RDS PITR restores to ANY point within the backup retention window (down to the second) by replaying transaction logs from the last backup. Restore to the timestamp just before the DELETE executed — lose only seconds of data after that point. PITR creates a NEW instance (don't forget to redirect the application). This is why you should always note the exact time of incidents. Automated backups alone lose 4 hours.", domain:2},
  {q:"A company uses AWS Certificate Manager (ACM) for SSL/TLS certificates on their ALB. The certificate is about to expire in 30 days. What action is required?", opts:["Manually renew the certificate through ACM console","No action needed — ACM automatically renews certificates it has issued (if the domain ownership can be validated)","Contact the certificate authority for renewal","Re-create the certificate and update the ALB listener"], correct:1, exp:"ACM automatically renews certificates it issues (ACM-issued, not imported) as long as the certificate is in use (attached to ALB, CloudFront, etc.) and domain validation can succeed. For DNS-validated certificates, if the CNAME records are still in place, renewal is fully automatic. For email-validated, the domain owner must respond to the renewal email. Imported certificates must be manually renewed. ACM-issued + DNS validation = zero-maintenance.", domain:1},
  {q:"A company deploys EC2 instances using launch templates. They want to ensure all instances have the SSM Agent installed and configured automatically at launch. What is the BEST approach?", opts:["Install SSM Agent via UserData script in the launch template","Use a custom AMI with SSM Agent pre-installed (Amazon Linux 2/2023 and recent Windows AMIs have it pre-installed)","Install SSM Agent after launch using a Systems Manager Run Command","Include SSM Agent in a Lambda function that triggers on instance launch"], correct:1, exp:"Amazon Linux 2, Amazon Linux 2023, and recent Windows Server AMIs come with SSM Agent PRE-INSTALLED. If using these AMIs, no additional action is needed — just attach an IAM instance profile with the AmazonSSMManagedInstanceCore policy. Using a custom AMI with SSM Agent baked in is the most reliable approach (no dependency on UserData completion). UserData can fail silently.", domain:2},
  {q:"A company uses API Gateway with a custom domain name (api.example.com). They want to route /v1/* to one Lambda function and /v2/* to a different Lambda function (API versioning). How is this configured?", opts:["Two separate API Gateway APIs with the same custom domain using base path mappings (/v1 → API1, /v2 → API2)","Single API with two resources and different Lambda integrations","Two custom domain names (v1.api.example.com, v2.api.example.com)","CloudFront with path-based routing to different API Gateway stages"], correct:0, exp:"API Gateway custom domain base path mappings route different base paths to different APIs or stages. Map /v1 to API-v1 and /v2 to API-v2 under the same custom domain (api.example.com). Each API is independent with its own Lambda integrations. This is the standard API versioning pattern with API Gateway. A single API with resources also works but base path mappings provide complete independence between versions.", domain:3},
  {q:"A company uses CloudFormation and needs to reference the latest Amazon Linux 2023 AMI ID without hardcoding it. AMI IDs change between regions and over time. What is the BEST approach?", opts:["Use a CloudFormation Mapping with AMI IDs per region (requires manual updates)","Use the AWS::SSM::Parameter::Value dynamic reference to resolve the public SSM parameter /aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-x86_64","Create a Lambda-backed custom resource that looks up the AMI","Use EC2 DescribeImages API in UserData to find the AMI"], correct:1, exp:"AWS maintains public SSM parameters with the latest AMI IDs for Amazon Linux, Windows, ECS-optimized, etc. CloudFormation resolves {{resolve:ssm:/aws/service/ami-amazon-linux-latest/...}} at deploy time to the current AMI ID in that region. Always up-to-date, no manual maintenance, works cross-region. This is the AWS-recommended approach for referencing AMIs in CloudFormation templates.", domain:2},
  {q:"A company processes events from an SQS queue using Lambda. Each event triggers an API call to a third-party service that charges $0.01 per call. During testing, the Lambda function fails and the same message is retried 5 times before going to DLQ — resulting in 5 API calls for one message ($0.05 instead of $0.01). How do they prevent duplicate API charges on retries?", opts:["Set maxReceiveCount to 1 (message goes to DLQ after first failure, no retries)","Implement idempotency: before calling the third-party API, check DynamoDB if this message ID was already processed. If yes, skip. If no, call API and record.","Set Lambda reserved concurrency to 1","Increase message visibility timeout"], correct:1, exp:"Idempotency prevents duplicate side effects. Before making the API call: check DynamoDB for the SQS message ID. If found → skip (already processed, this is a retry). If not found → call the API, then write the message ID to DynamoDB. On retry (message reappears because the function crashed after the API call but before deleting the message), the DynamoDB check catches it. The external API is called exactly once per message.", domain:4},
  {q:"A company runs a global application with Route 53 latency-based routing pointing to ALBs in us-east-1 and eu-west-1. They notice some users in South America are routed to eu-west-1 (higher latency) instead of us-east-1 (lower latency). Why might this happen?", opts:["Route 53 latency routing is based on historical network data which may not perfectly reflect current conditions for all users","The user's DNS resolver location determines routing — if their ISP's DNS resolver is in Europe, Route 53 thinks the user is in Europe","Both A and B are possible causes","Route 53 has a bug for South American users"], correct:2, exp:"Route 53 latency-based routing determines the 'closest' region based on the SOURCE IP of the DNS query — which is typically the user's DNS resolver, not the user themselves. If a South American user's ISP uses a DNS resolver in Europe, Route 53 sees a European source IP and routes to eu-west-1. Also, AWS's latency database may not perfectly reflect all network paths. Fix: use EDNS Client Subnet-aware resolvers (like 8.8.8.8 or 1.1.1.1) that forward the user's subnet to Route 53.", domain:2},
  {q:"A company has a DynamoDB table with a composite primary key (PK: user_id, SK: timestamp). They want to retrieve the 10 most recent items for a user. Which query configuration achieves this efficiently?", opts:["Query with PK=user_id, ScanIndexForward=false (descending order), Limit=10","Query with PK=user_id, then sort results in application code and take top 10","Scan with FilterExpression on user_id and sort by timestamp","Query with PK=user_id and use a between() condition on timestamp for the last 10 timestamps"], correct:0, exp:"Query with PK=user_id retrieves all items for that user. ScanIndexForward=false returns items in DESCENDING sort key order (newest timestamp first). Limit=10 returns only the first 10 results. This reads exactly 10 items from the table — maximally efficient. Application-side sorting reads ALL items then discards most. Scan reads the entire table. Between() requires knowing the exact timestamps in advance.", domain:3},
  {q:"A company runs Lambda functions that need to be in a VPC (to access RDS). They experience occasional cold starts of 10+ seconds due to VPC ENI attachment. What feature eliminates VPC-related cold starts?", opts:["Lambda Provisioned Concurrency (keeps environments warm with ENI already attached)","Lambda uses Hyperplane ENI (shared ENI infrastructure since 2019) which reduced VPC cold starts to <1 second — this might be a configuration issue, not inherent VPC delay","Remove VPC configuration and use RDS Proxy public endpoint","Use smaller Lambda memory allocation"], correct:1, exp:"Since 2019, Lambda uses Hyperplane (shared NAT/ENI infrastructure) which reduced VPC cold starts from 10-30 seconds to typically <1 second. If they're still seeing 10+ second cold starts, it could be: an old function configuration, large deployment package, or heavy initialization code (not VPC-related). Provisioned Concurrency eliminates ALL cold starts. But first verify the actual cause — modern VPC Lambda shouldn't have 10s ENI delays.", domain:3},
  {q:"A company wants to implement blue/green deployments for their RDS database changes (schema migrations). AWS RDS now supports Blue/Green Deployments natively. What does this feature provide?", opts:["Creates a separate read replica and promotes it (losing replica lag data)","Creates a complete copy (green) of the production database (blue), applies changes to green, syncs via logical replication, then switches over with minimal downtime (typically seconds)","Maintains two separate databases permanently (active-active)","Only works with Aurora, not standard RDS"], correct:1, exp:"RDS Blue/Green Deployments create a staging (green) environment that stays synchronized with production (blue) via logical replication. You make changes on green (schema updates, engine upgrades, parameter changes), test, then switch over. During switchover, writes are briefly paused (seconds) while green becomes the new production. Blue is kept for rollback. Supports both RDS and Aurora (MySQL and PostgreSQL). Game-changer for safe database changes.", domain:2},
  {q:"A company has 1000 EC2 instances across multiple accounts and regions. They need to execute a shell command on all Linux instances simultaneously (e.g., security patch). How can this be done without SSH keys?", opts:["SSH into each instance individually","Use AWS Systems Manager Run Command with a target of all managed instances (tag-based or instance-group targeting)","Create a Lambda function that SSHs into each instance","Use EC2 Serial Console for remote access"], correct:1, exp:"SSM Run Command executes commands on managed instances WITHOUT SSH access. Target instances by tags, instance IDs, or resource groups across accounts and regions. It handles: parallel execution (thousands simultaneously), output collection, error handling, and rate control. Prerequisites: SSM Agent installed + IAM instance profile with SSM permissions. No SSH keys, no bastion hosts, no security groups for port 22 needed. Full audit trail in CloudTrail.", domain:2},
  {q:"A company has strict compliance requirements: ALL data must be encrypted at rest using customer-managed keys (CMK). They use S3, EBS, RDS, and DynamoDB. Which services support CMK encryption with customer-managed keys in KMS?", opts:["Only S3 and EBS support customer-managed CMKs","Only S3, EBS, and RDS support CMKs — DynamoDB only uses AWS-managed keys","ALL four services (S3, EBS, RDS, DynamoDB) support encryption with customer-managed CMKs in KMS","S3 and DynamoDB support CMKs, but EBS and RDS use only AWS-managed keys"], correct:2, exp:"All four services support customer-managed CMKs: S3 (SSE-KMS with CMK), EBS (encrypted volumes with CMK), RDS (encryption at rest with CMK), and DynamoDB (encryption at rest with CMK or AWS-managed key). Customer-managed CMKs provide: key policy control, rotation control, CloudTrail key usage audit, and the ability to revoke access by disabling the key. This is a factual question testing service encryption knowledge.", domain:1},
  {q:"A company has a Lambda function that is invoked asynchronously (via S3 event, SNS, or EventBridge). If the function fails after 2 retries, they want the failed event to be sent to an SQS queue for investigation. How is this configured?", opts:["Dead Letter Queue (DLQ) configured on the Lambda function","Lambda Destinations — configure an 'on failure' destination pointing to the SQS queue","Both DLQ and Destinations work for this — Destinations provide more event context (request/response payload)","SQS subscription to Lambda error logs"], correct:2, exp:"Both Lambda DLQ and Lambda Destinations handle failed async invocations. DLQ (older feature) sends just the original event payload to SQS/SNS. Destinations (newer) send enriched JSON including: original event, response payload, error details, and metadata. Destinations provide MORE context for debugging. Both send after exhausting retry attempts (2 for async). For new implementations, Destinations are preferred for richer failure information.", domain:2},
  {q:"A company has a multi-region application using DynamoDB Global Tables in us-east-1 and eu-west-1. A network partition between regions causes the tables to diverge temporarily. When connectivity is restored, what happens to conflicting writes (same item modified in both regions during the partition)?", opts:["Both writes are rejected and require manual resolution","Last writer wins — DynamoDB uses the item timestamp to determine which write takes precedence (the most recent write wins globally)","First writer wins — the earlier write is preserved","DynamoDB creates a conflict record for manual resolution"], correct:1, exp:"DynamoDB Global Tables uses last-writer-wins (LWW) conflict resolution based on the item's last-modified timestamp. When connectivity is restored, the write with the most recent timestamp becomes the definitive version globally. There's no manual conflict resolution — it's fully automatic. Applications should be designed to handle this: use conditional writes for critical operations, or design operations to be commutative/idempotent.", domain:2},
  {q:"A company uses API Gateway HTTP API (not REST API). They need WebSocket support. Can HTTP APIs do WebSockets?", opts:["Yes — HTTP APIs support WebSocket connections","No — WebSocket APIs are a separate API type in API Gateway (distinct from REST API and HTTP API)","Yes — all API Gateway types support WebSockets","No — use ALB for WebSocket support instead"], correct:1, exp:"API Gateway has THREE separate API types: (1) REST API (full featured, $3.50/million requests), (2) HTTP API (cheaper, fewer features, $1.00/million requests), (3) WebSocket API (persistent connections, $1.00/million messages). They are distinct — you create a WebSocket API specifically for WebSocket needs. HTTP API doesn't support WebSocket. This is an important API Gateway distinction for the exam.", domain:3},
  {q:"A company uses S3 Glacier Deep Archive for long-term backup storage. An auditor requests a specific file urgently (needed within 1 hour). Standard retrieval from Deep Archive takes 12 hours. Is there a faster option?", opts:["No — Deep Archive minimum retrieval time is 12 hours","Yes — use Expedited retrieval (not available for Deep Archive)","Yes — use Bulk retrieval (even slower, 48 hours)","No — Deep Archive only has Standard (12 hours) and Bulk (48 hours) retrieval tiers. For urgent access within 1 hour, the data should have been stored in Glacier Flexible Retrieval or Glacier Instant Retrieval instead"], correct:3, exp:"Glacier Deep Archive only offers Standard (12 hours) and Bulk (48 hours) retrieval — there is NO expedited option. If urgent access is a possibility, store in: Glacier Instant Retrieval (milliseconds), Glacier Flexible Retrieval with Expedited (1-5 minutes), or Standard-IA/One Zone-IA (instant). Deep Archive is ONLY for data that truly never needs urgent access. This is a critical storage class selection consideration.", domain:4},
  {q:"A company operates an EKS cluster with 50 microservices. They want to enforce that all inter-service communication uses mutual TLS (mTLS) and that service A can only communicate with services B and C (not others). Which EKS feature provides this?", opts:["Kubernetes Network Policies (Layer 4 only, no mTLS)","AWS App Mesh (Envoy sidecars providing mTLS and traffic policies)","Security Groups for Pods (Layer 4, no mTLS)","Both B and Network Policies together for defense in depth"], correct:1, exp:"AWS App Mesh with Envoy sidecar proxies provides: mTLS between services (automatic certificate management via ACM PCA), traffic routing policies (which services can communicate with which), and observability (metrics, traces). Network Policies are Layer 4 only (allow/deny by IP/port, no encryption). Security Groups for Pods also L4. App Mesh is the correct service mesh solution for mTLS + service-level authorization.", domain:1},
  {q:"A company has an Aurora PostgreSQL database that they need to share (read-only) with a partner company's AWS account. The partner should be able to query the data using their own tools but NOT be able to modify anything or access other tables. What is the MOST secure approach?", opts:["Create a read-only IAM role for the partner to assume, granting RDS data API access","Use Aurora cross-account data sharing (not a native Aurora feature for relational data)","Create a database user with SELECT permission on specific tables only, share credentials via Secrets Manager cross-account","Share Aurora Serverless Data API access with cross-account IAM role limited to executeStatement for SELECT queries only"], correct:2, exp:"Create a PostgreSQL user with GRANT SELECT on specific tables only. Share the credentials securely via Secrets Manager cross-account (resource policy allowing the partner's account to retrieve the secret). The partner connects using standard PostgreSQL tools with their read-only credentials. This uses native database access control (most granular) + Secrets Manager for secure credential sharing. The partner can only SELECT on granted tables.", domain:1},
  {q:"A company uses CloudWatch Logs for application logging. Their monthly CloudWatch Logs bill is $10,000 (mostly log ingestion at $0.50/GB and storage at $0.03/GB/month). They retain logs for 90 days. What is the MOST impactful cost reduction?", opts:["Reduce log retention to 30 days (saves storage but ingestion is the main cost)","Filter logs at the source — only send ERROR and WARN levels to CloudWatch (reduce ingestion volume by 80-90%)","Export logs to S3 after 1 day (cheaper storage)","Use CloudWatch Logs Insights instead of storing raw logs"], correct:1, exp:"At $0.50/GB ingestion, reducing the VOLUME of logs sent is the highest-impact change. Most applications log verbose DEBUG/INFO messages that aren't needed in CloudWatch. Filtering at source (only ERROR/WARN/critical INFO) can reduce volume by 80-90%, directly reducing the $10K ingestion cost to $1-2K. Storage savings from shorter retention are smaller ($0.03/GB vs $0.50/GB ingestion). Focus on the biggest cost driver: ingestion volume.", domain:4},
]
