export const questionBank2 = [
  // DOMAIN 1: Design Secure Architectures
  {q:"A company wants to enforce MFA for all IAM users when accessing the AWS Management Console but not for programmatic access. Which approach achieves this?", opts:["Create an IAM policy with a Condition key aws:MultiFactorAuthPresent for console actions only","Use an SCP that denies all actions without MFA and exclude programmatic calls using aws:ViaAWSService","Create an IAM policy denying all actions with a condition that MFA is not present, combined with a StringLike condition on aws:UserAgent for console","Require MFA at the account level using the IAM credential report"], correct:0, exp:"The aws:MultiFactorAuthPresent condition key in IAM policies can enforce MFA selectively. When attached with a Deny effect for actions when MFA is false, it blocks console access without MFA. Programmatic access with access keys doesn't involve the console login flow.", domain:1},
  {q:"A solutions architect needs to give an AWS Lambda function in Account A access to a DynamoDB table in Account B. What is the correct configuration?", opts:["Create an IAM role in Account B with DynamoDB permissions and a trust policy allowing Account A's Lambda execution role to assume it","Share the DynamoDB table using AWS RAM between the two accounts","Create an IAM user in Account B and store the credentials in Secrets Manager in Account A","Enable VPC peering between the accounts and use the private DynamoDB endpoint"], correct:0, exp:"Cross-account access is achieved by creating a role in the target account (B) with a trust policy allowing the source account (A) to assume it. Lambda in Account A assumes the role in Account B to access DynamoDB. DynamoDB cannot be shared via RAM. VPC peering doesn't affect IAM permissions for DynamoDB.", domain:1},
  {q:"An application needs to store database credentials and rotate them every 30 days automatically. The credentials must be encrypted and access must be auditable. Which service meets all requirements?", opts:["AWS Systems Manager Parameter Store SecureString with a CloudWatch Events rule for rotation","AWS Secrets Manager with automatic rotation enabled","AWS KMS with encrypted environment variables in Lambda","Store credentials in S3 with SSE-KMS and a lifecycle policy"], correct:1, exp:"AWS Secrets Manager natively supports automatic credential rotation with built-in Lambda functions for RDS, Redshift, and DocumentDB. It encrypts secrets using KMS, and all access is logged in CloudTrail. Parameter Store requires custom Lambda for rotation. KMS stores keys, not credentials.", domain:1},
  {q:"A company needs to ensure all EC2 instances launched in their VPC can only communicate with approved AWS services and specific IP ranges. Internet access must be completely blocked. Which combination of controls provides defense in depth?", opts:["Remove all internet gateways and NAT gateways, use VPC endpoints for AWS services, and configure NACLs to deny all other outbound traffic","Use Security Groups to block all outbound traffic except to specific CIDR ranges","Configure AWS Network Firewall with deny-all rules and allow specific endpoints","Deploy a proxy server in the VPC and route all traffic through it"], correct:0, exp:"Removing Internet Gateway and NAT Gateway physically prevents internet access. VPC endpoints provide private connectivity to AWS services. NACLs as an additional layer deny any remaining unauthorized outbound traffic. This is defense in depth: network topology + endpoint + NACL. Security Groups alone can be modified by admins.", domain:1},
  {q:"A company uses AWS Config to monitor compliance. They need to automatically remediate non-compliant Security Groups that allow SSH (port 22) from 0.0.0.0/0. What is the MOST automated approach?", opts:["Use AWS Config managed rule with manual remediation by the security team","Create an AWS Config custom rule with an automatic remediation action using SSM Automation to revoke the ingress rule","Use CloudWatch Events to detect Security Group changes and trigger a Lambda function to check and fix the rule","Enable AWS GuardDuty to detect open SSH ports and trigger SNS notifications"], correct:1, exp:"AWS Config rules with automatic remediation using SSM Automation documents can detect and fix non-compliant resources without human intervention. The SSM Automation document revokes the offending ingress rule automatically. CloudWatch Events works but requires more custom code. GuardDuty detects threats, not configuration drift.", domain:1},
  {q:"A web application behind CloudFront needs to restrict access so that users can ONLY access the application through CloudFront, not directly via the ALB URL. How should this be implemented?", opts:["Use a Security Group on the ALB that only allows CloudFront IP ranges","Configure CloudFront to add a custom header and create an ALB rule that only forwards requests with that header","Use AWS WAF on the ALB to block all requests not from CloudFront","Make the ALB internal and use a VPN connection from CloudFront"], correct:1, exp:"Adding a custom origin header in CloudFront and configuring the ALB listener rule to validate it ensures only CloudFront requests reach the origin. CloudFront IP ranges change frequently making SG-based solutions fragile. WAF on ALB can work but custom headers are simpler and AWS-recommended. ALBs can't be private with CloudFront.", domain:1},
  {q:"An organization needs to centrally manage encryption keys across 50 AWS accounts. Each account should be able to use shared keys but not delete or modify them. How should this be architected?", opts:["Create KMS keys in each account with identical key policies","Create KMS keys in a central security account and share them via cross-account key policies granting only Encrypt/Decrypt permissions","Use AWS CloudHSM in a shared VPC accessible by all accounts","Store encryption keys in Secrets Manager in a central account and share via Resource Access Manager"], correct:1, exp:"KMS supports cross-account key sharing via key policies. The central account creates and manages keys, granting only kms:Encrypt and kms:Decrypt to other accounts. Other accounts cannot delete, disable, or modify key policies. CloudHSM is expensive for 50 accounts. Secrets Manager stores secrets, not KMS keys.", domain:1},
  {q:"A company has sensitive workloads that must run on dedicated physical servers not shared with other AWS customers. They also need visibility into the number of sockets and physical cores for software licensing. Which EC2 option meets these requirements?", opts:["Dedicated Instances","Dedicated Hosts","Bare Metal instances (*.metal)","Placement Groups with spread strategy"], correct:1, exp:"Dedicated Hosts provide dedicated physical servers with visibility into sockets, cores, and host ID — essential for bring-your-own-license (BYOL) models. Dedicated Instances ensure hardware isolation but don't provide physical server visibility. Bare Metal gives full hardware access but doesn't guarantee dedicated hardware. Placement Groups are about proximity, not isolation.", domain:1},

  // DOMAIN 2: Design Resilient Architectures
  {q:"A company needs a message queuing service that supports exactly-once processing and message ordering. Messages must be processed in the exact order they were sent. Which service and configuration should be used?", opts:["Amazon SQS Standard queue with deduplication logic in the consumer","Amazon SQS FIFO queue with message group IDs","Amazon Kinesis Data Streams with a single shard","Amazon MQ with ActiveMQ in a single-broker configuration"], correct:1, exp:"SQS FIFO queues guarantee exactly-once processing and strict ordering within a message group. Message group IDs allow multiple ordered streams within one queue. Standard queues don't guarantee order. Kinesis preserves order per shard but doesn't provide exactly-once. Amazon MQ can order but lacks native exactly-once.", domain:2},
  {q:"An application runs on EC2 instances in an Auto Scaling Group. The application takes 10 minutes to bootstrap (download configs, warm caches). During scaling events, new instances receive traffic before they're ready, causing errors. What is the BEST solution?", opts:["Increase the instance size so fewer instances are needed","Configure an ALB health check with a custom health endpoint that returns healthy only after bootstrap completes","Use lifecycle hooks to hold instances in Pending:Wait state until bootstrap completes, then signal to proceed","Both B and C together for maximum reliability"], correct:3, exp:"Lifecycle hooks pause the instance in Pending:Wait during bootstrap, preventing premature traffic. The ALB health check custom endpoint provides an additional layer ensuring the ALB only routes traffic to fully healthy instances. Together they provide maximum reliability: hooks prevent premature ASG actions, and ALB health check prevents premature traffic.", domain:2},
  {q:"A company's application uses Amazon Aurora MySQL. They need to perform heavy reporting queries without impacting production write performance. The reports can tolerate up to 1 second of data staleness. What is the MOST cost-effective solution?", opts:["Create an Aurora Read Replica and direct reporting queries to the reader endpoint","Use Aurora parallel query feature on the primary instance","Create a custom endpoint pointing to a specific read replica dedicated to reporting","Replicate data to Redshift using DMS for reporting"], correct:2, exp:"A custom endpoint pointing to a dedicated read replica isolates reporting workload completely. You can use a larger instance class for this replica to handle heavy queries. The standard reader endpoint round-robins across all replicas which may affect application reads. Aurora replicas have <1s lag, meeting the staleness requirement. Redshift is overkill and more expensive.", domain:2},
  {q:"A serverless application uses API Gateway, Lambda, and DynamoDB. During a sudden spike, Lambda functions fail with ProvisionedThroughputExceededException from DynamoDB. What is the BEST immediate action to prevent data loss while the team investigates?", opts:["Switch DynamoDB to on-demand capacity mode","Enable DynamoDB auto-scaling with higher maximum capacity","Put an SQS queue between Lambda and DynamoDB to buffer writes","Increase Lambda timeout to allow for DynamoDB retries"], correct:2, exp:"SQS immediately buffers all writes, preventing data loss even if DynamoDB is throttled. Messages persist in the queue until DynamoDB can process them. Switching to on-demand takes a few minutes and has a transition period. Auto-scaling takes time to react. Increasing timeout helps retries but doesn't prevent data loss during sustained throttling.", domain:2},
  {q:"A microservices architecture uses multiple Lambda functions communicating via API calls. When one downstream service fails, all upstream services cascade into failure. How should the architect prevent cascading failures?", opts:["Increase timeout values for all Lambda functions","Implement a circuit breaker pattern using Step Functions or a library like AWS Lambda Powertools","Use synchronous invocations with retry logic built into each Lambda","Deploy all services in the same VPC for faster communication"], correct:1, exp:"Circuit breaker pattern prevents cascading failures by detecting failures in downstream services and short-circuiting calls to them (returning a fallback response). Step Functions can implement this with error handling states. Lambda Powertools includes circuit breaker utilities. Retries without circuit breaking actually worsen cascading failures.", domain:2},
  {q:"A company runs a critical PostgreSQL database. They need RPO of 0 (zero data loss) and RTO under 1 minute. The budget allows for same-region HA only. Which solution meets these requirements at lowest cost?", opts:["RDS PostgreSQL Multi-AZ with synchronous replication","Aurora PostgreSQL with Multi-AZ","Aurora PostgreSQL Serverless v2 with Multi-AZ","Two EC2 instances with PostgreSQL and synchronous streaming replication"], correct:1, exp:"Aurora PostgreSQL Multi-AZ provides RPO=0 (synchronous replication to 6 storage copies across 3 AZs) and RTO <30 seconds (automatic failover). It's cheaper than maintaining self-managed replication on EC2. RDS Multi-AZ also provides RPO=0 but has slightly longer RTO (60-120s). Aurora has the best RTO/RPO combination at managed-service cost.", domain:2},
  {q:"A company operates a global application with users in 30 countries. They need to route users to the closest healthy endpoint with instant failover. The application runs in 3 AWS regions. Which Route 53 routing configuration is BEST?", opts:["Latency-based routing with health checks and failover to the next closest region","Geolocation routing with failover routing policy as secondary","Geoproximity routing with Route 53 health checks","Multivalue answer routing with health checks across all regions"], correct:0, exp:"Latency-based routing directs users to the lowest-latency region. With health checks, if the closest region is unhealthy, Route 53 automatically routes to the next-lowest-latency healthy region. This provides instant failover to the best alternative. Geolocation maps to fixed regions (less optimal). Geoproximity requires Traffic Flow. Multivalue doesn't optimize for latency.", domain:2},
  {q:"An application processes events that must be handled exactly once and in order. Events come in at 100,000 per second. If the processing application crashes, it must resume from exactly where it left off. Which architecture ensures this?", opts:["SQS FIFO queue with a single consumer","Kinesis Data Streams with KCL (Kinesis Client Library) consumers using checkpoint sequencing","Amazon MSK (Kafka) with consumer group offset management","DynamoDB Streams with Lambda triggers"], correct:1, exp:"Kinesis Data Streams handles 100K+ events/sec with ordering per shard. KCL manages checkpointing to DynamoDB — if a consumer crashes, it resumes from the last checkpoint (exactly where it left off). SQS FIFO is limited to 3,000 msg/sec per queue (300 with batching without high throughput mode). MSK works but adds operational complexity. DynamoDB Streams is for DB change events, not generic event ingestion.", domain:2},
  {q:"A data pipeline uses S3 -> Lambda -> RDS. The Lambda function occasionally times out during large batch inserts (taking over 15 minutes). The data must be fully inserted or not at all (transactional). What architecture change solves this?", opts:["Increase Lambda timeout to the maximum (15 minutes)","Replace Lambda with an ECS Fargate task triggered by S3 events (via EventBridge) that performs batch inserts within a database transaction","Split the file into smaller chunks using a first Lambda and process each chunk with a second Lambda","Use RDS Proxy to pool connections and speed up inserts"], correct:1, exp:"ECS Fargate tasks have no time limit, can run for hours, and support long-running database transactions. S3 events via EventBridge trigger the task. Database transactions ensure atomicity (all-or-nothing). Lambda's max timeout is 15 minutes — not enough. Splitting files breaks transactional guarantees across chunks. RDS Proxy helps connections but not time limits.", domain:2},
  {q:"A company deploys a critical application across 3 Availability Zones with 2 instances in each AZ (6 total). The application needs at least 4 instances to handle production load. What is the minimum number of instances they should run to survive an AZ failure?", opts:["6 instances (2 per AZ) — they already have enough","6 instances (3 per AZ across 2 AZs) to survive one AZ loss","9 instances (3 per AZ across 3 AZs) to survive one AZ loss","6 instances (2 per AZ across 3 AZs) is correct because 4 remain if one AZ fails"], correct:3, exp:"With 2 instances in each of 3 AZs (6 total), if one AZ fails, 4 instances remain (2+2 from the other two AZs). Since 4 instances is the minimum needed, this configuration already survives a single AZ failure. No additional instances needed. The key insight: distribute across more AZs rather than adding more instances.", domain:2},

  // DOMAIN 3: Design High-Performing Architectures
  {q:"A company needs to perform real-time sentiment analysis on social media posts (10,000 posts per minute). Results must be available within 5 seconds for a dashboard. Which architecture is MOST appropriate?", opts:["S3 -> Lambda -> Comprehend -> DynamoDB -> API Gateway","Kinesis Data Streams -> Lambda -> Comprehend -> ElastiCache -> API Gateway","SQS -> EC2 -> Comprehend -> RDS -> CloudFront","API Gateway -> Step Functions -> Comprehend -> S3 -> Athena"], correct:1, exp:"Kinesis Data Streams ingests high-throughput real-time data. Lambda processes records and calls Comprehend for sentiment analysis. ElastiCache (Redis) stores results for sub-millisecond dashboard reads via API Gateway. This achieves <5 second end-to-end. SQS adds latency. S3->Athena is batch, not real-time. Step Functions add orchestration overhead for simple processing.", domain:3},
  {q:"A company's DynamoDB table experiences throttling on writes. The table uses provisioned capacity with partition key user_id. Analysis shows 70% of writes go to 5% of users (hot keys). DAX won't help since it's a write issue. What is the BEST solution?", opts:["Switch to on-demand capacity mode to handle spikes","Implement write sharding by appending a random number (0-9) to the partition key","Use SQS to buffer writes and smooth out the traffic","Increase the provisioned write capacity units to handle the peak"], correct:1, exp:"Write sharding distributes hot key writes across multiple partitions. Appending a random suffix (user_id#0 to user_id#9) spreads one hot key across 10 partitions. Reads aggregate across shards. On-demand still throttles on hot partitions (it's per-partition, not table-level). SQS buffers but doesn't fix the partition hotspot. More WCU doesn't help if one partition is hot.", domain:3},
  {q:"An application needs a database for the following requirements: flexible schema, horizontal scaling, single-digit millisecond reads at any scale, and automatic backup. The data model is a product catalog with varying attributes per product. Which database is BEST?", opts:["Amazon RDS MySQL with JSON columns","Amazon DynamoDB","Amazon DocumentDB (MongoDB-compatible)","Amazon Neptune"], correct:1, exp:"DynamoDB provides flexible schema (each item can have different attributes), horizontal scaling (automatic partitioning), single-digit millisecond reads at any scale, and automatic continuous backups (PITR). DocumentDB also handles flexible schemas but doesn't scale as seamlessly. RDS requires schema definition. Neptune is for graph data.", domain:3},
  {q:"A high-traffic website needs to handle 1 million WebSocket connections concurrently for real-time chat. Each user receives messages within 100ms. Which AWS architecture supports this?", opts:["ALB with WebSocket support routing to EC2 instances in an Auto Scaling Group","API Gateway WebSocket API with Lambda integration and DynamoDB for connection management","Amazon AppSync with GraphQL subscriptions","CloudFront with WebSocket origin to EC2 instances"], correct:1, exp:"API Gateway WebSocket API scales to millions of concurrent connections without managing infrastructure. Lambda processes messages, DynamoDB stores connection IDs for routing. API Gateway handles the WebSocket lifecycle (connect, message, disconnect). ALB WebSockets would require managing many EC2 instances. AppSync subscriptions work but are optimized for GraphQL data sync, not generic chat.", domain:3},
  {q:"A company needs to accelerate the transfer of 10GB files from offices worldwide to a central S3 bucket in us-east-1. Current uploads take 2+ hours from Asia. What provides the GREATEST speed improvement?", opts:["S3 multipart upload with increased part size","S3 Transfer Acceleration","AWS Global Accelerator with an NLB fronting an upload service","Direct Connect from each office to the nearest AWS region"], correct:1, exp:"S3 Transfer Acceleration uses CloudFront's global edge network to route uploads to S3 over optimized AWS backbone networks instead of the public internet. This dramatically speeds up long-distance transfers (Asia to us-east-1). Multipart upload parallelizes but doesn't fix network path. Global Accelerator improves TCP performance but Transfer Acceleration is designed specifically for S3 uploads. Direct Connect is expensive per-office.", domain:3},
  {q:"A data warehouse query on Redshift takes 20 minutes because it performs a nested loop join on two large tables (500M rows each). What optimization would provide the LARGEST performance improvement?", opts:["Increase the Redshift cluster size by adding more nodes","Change the distribution style of the joined tables to KEY distribution on the join column","Enable concurrency scaling to handle the query faster","Convert the nested loop to a hash join by adding appropriate sort keys"], correct:1, exp:"KEY distribution co-locates rows with the same join key on the same node slice, eliminating expensive network shuffling during joins. For large table joins, this converts an O(n*m) operation to parallel local joins. Adding nodes helps but doesn't fix the data distribution problem. Sort keys help with range scans but distribution style is the primary join optimization.", domain:3},
  {q:"A gaming application needs to store and retrieve player profiles with sub-millisecond latency. Each profile is 50KB and there are 10 million active players. Profiles are read 100x more than written. Which solution provides the best cost-to-performance ratio?", opts:["DynamoDB with DAX cluster (cache nodes)","ElastiCache Redis cluster with RDS as backing store","DynamoDB with on-demand capacity","Aurora with ElastiCache read-through caching"], correct:0, exp:"DynamoDB with DAX provides sub-millisecond reads with the caching layer absorbing the 100:1 read ratio. DynamoDB handles the writes, DAX serves cached reads. This is simpler and cheaper than maintaining a separate ElastiCache cluster with a backing RDS database. On-demand DynamoDB alone provides single-digit ms (not sub-ms). DAX is purpose-built for DynamoDB acceleration.", domain:3},
  {q:"A machine learning pipeline needs to process 1TB of images daily. Each image requires 30 seconds of GPU processing. The pipeline must complete within 4 hours. What is the most cost-effective compute architecture?", opts:["A single p3.16xlarge instance running all day","An Auto Scaling Group of p3.2xlarge Spot Instances triggered by an SQS queue","AWS Batch with a Spot Instance compute environment using GPU instances","Amazon SageMaker Processing jobs with ml.p3 instances"], correct:2, exp:"AWS Batch with Spot GPU instances handles batch workloads optimally: it auto-provisions the right number of GPU instances, uses Spot for 60-90% savings, scales to process within the time window, and scales to zero when done. SQS + ASG works but requires more management. SageMaker Processing is more expensive. A single instance would take too long (1TB / 30s per image depends on image size, but parallelism is needed).", domain:3},
  {q:"An IoT platform receives telemetry data from 500,000 devices, each sending data every 5 seconds. The data must be available for real-time monitoring AND stored for 90 days of historical analysis. Which architecture handles both requirements?", opts:["API Gateway -> Lambda -> DynamoDB (with TTL for 90 days) -> DynamoDB Streams -> Lambda -> CloudWatch Dashboard","IoT Core -> IoT Rules -> Kinesis Data Streams -> Kinesis Data Analytics (real-time) + Kinesis Firehose -> S3 (historical) -> Athena","IoT Core -> Lambda -> ElastiCache (real-time) + S3 (historical)","API Gateway -> SQS -> Lambda -> TimeStream -> Grafana"], correct:1, exp:"IoT Core natively handles 500K devices with MQTT. IoT Rules route to Kinesis Data Streams (handles 100K msg/sec per shard). Kinesis Data Analytics provides real-time monitoring. Kinesis Firehose delivers to S3 for cheap 90-day storage queryable via Athena. This splits real-time and historical cleanly. API Gateway can't handle 100K msg/sec from IoT devices. Lambda at this scale has concurrency issues.", domain:3},

  // DOMAIN 4: Design Cost-Optimized Architectures
  {q:"A company has an application with predictable traffic patterns: 1000 requests/sec during weekdays and 100 requests/sec on weekends. They currently use 10 On-Demand EC2 instances 24/7. What change provides the MOST cost savings?", opts:["Purchase 10 Reserved Instances for the full fleet","Purchase 2 Reserved Instances for weekend baseline, use On-Demand for weekday scaling","Use Compute Savings Plans for baseline (2 instances worth) and Auto Scaling with On-Demand for peak","Use Compute Savings Plans covering weekday compute (10 instances) since it applies to any EC2 usage"], correct:2, exp:"Savings Plans for the baseline (2 instances worth that run 24/7) provides the deepest discount on committed compute. Auto Scaling adds On-Demand instances only during weekday peaks and removes them on weekends. This avoids paying for idle capacity on weekends. 10 RIs waste money on 8 instances unused on weekends. Savings Plans for 10 instances also wastes on weekends.", domain:4},
  {q:"A company has 500TB in S3 Standard. Analysis shows 80% of the data hasn't been accessed in 6 months but they can't predict which objects will be needed with less than 1-hour retrieval time. What provides the MOST savings?", opts:["Move all data to S3 Glacier Instant Retrieval","Move all data to S3 Intelligent-Tiering","Move the 80% cold data to S3 Glacier Flexible Retrieval with Expedited retrievals","Keep in Standard but enable S3 Analytics to identify cold data, then move identified cold data to Glacier Instant Retrieval"], correct:1, exp:"S3 Intelligent-Tiering automatically identifies and moves infrequently accessed objects to cheaper tiers (Frequent, Infrequent, Archive Instant, Archive, Deep Archive) without retrieval fees. Since they can't predict access patterns, IT is ideal. Glacier Flexible doesn't meet <1 hour retrieval reliably (Expedited is 1-5min but not guaranteed without provisioned capacity). Moving everything to Glacier Instant works but IT optimizes better over time.", domain:4},
  {q:"A startup processes customer data using Lambda functions. As they grew, their Lambda bill tripled because functions are invoked 10 million times per month with an average duration of 3 seconds using 1GB memory. What should they evaluate to reduce costs?", opts:["Use ARM/Graviton-based Lambda (arm64) for 20% cost reduction","Right-size Lambda memory using AWS Lambda Power Tuning to find optimal memory/cost balance","Switch from Lambda to Fargate for long-running constant workloads if cheaper","Evaluate all of the above: arm64 architecture, memory optimization, and compute alternative comparison"], correct:3, exp:"All three should be evaluated: (1) arm64 provides automatic 20% price reduction with often better performance, (2) Power Tuning finds the optimal memory setting where cost (duration * memory) is minimized (sometimes more memory = less duration = lower cost), (3) At 10M invocations x 3s, the total compute-seconds may be cheaper on Fargate with constant tasks. A comprehensive analysis considers all options.", domain:4},
  {q:"A company serves static content from S3 through CloudFront. Their data transfer bill is $10,000/month. Most traffic goes to users in North America and Europe. How can they reduce this cost?", opts:["Use CloudFront Price Class 100 (NA and Europe only) instead of Price Class All","Enable CloudFront compression for all text-based content","Increase CloudFront cache TTL to reduce origin fetches","All of the above provide incremental savings and should all be implemented"], correct:3, exp:"Price Class 100 removes expensive edge locations (Australia, Asia, South America) — if users are mostly NA/EU, this saves without impacting them. Compression (gzip/brotli) reduces transferred bytes by 60-80% for text content. Longer TTL reduces origin fetches (S3 to CloudFront is free, but cache HIT ratio affects edge efficiency). Together, these can reduce the bill by 40-60%.", domain:4},
  {q:"A company runs AWS Fargate tasks that process jobs from an SQS queue. Each task runs for 5-30 minutes. During off-peak hours, tasks sit idle polling the empty queue. The company pays $4,000/month for Fargate. How should costs be reduced?", opts:["Switch to Lambda with SQS trigger (pay only when messages exist)","Implement a scale-to-zero pattern: set Fargate desired count to 0 and use a CloudWatch alarm on SQS ApproximateNumberOfMessagesVisible to scale up","Use EC2 Spot Instances instead of Fargate for the processing","Run Fargate on a schedule matching peak hours only"], correct:1, exp:"Scale-to-zero eliminates idle costs entirely. When no messages exist, desired count is 0 (no charges). CloudWatch alarm detects new messages and scales Fargate back up. Lambda can't run 5-30 minute tasks (15min limit). Spot EC2 requires more management than Fargate. Schedule-based only works if traffic is predictable.", domain:4},
  {q:"A company's monthly AWS bill shows: EC2 $20K, RDS $10K, NAT Gateway $8K, Data Transfer $5K. The NAT Gateway cost seems high. Investigation shows most traffic is to S3 and DynamoDB. What reduces NAT Gateway costs the MOST?", opts:["Replace NAT Gateway with a NAT instance (t3.nano)","Create VPC Gateway Endpoints for S3 and DynamoDB (free, no data processing charges)","Move workloads to public subnets to avoid NAT entirely","Compress all data before sending through NAT Gateway"], correct:1, exp:"VPC Gateway Endpoints for S3 and DynamoDB are completely free (no hourly charge, no data processing charge). Traffic to these services goes through the endpoint instead of NAT Gateway, eliminating the $0.045/GB data processing charge. If most of the $8K NAT cost is S3/DynamoDB traffic, this could save 60-80%. NAT instances have lower throughput. Public subnets are a security risk.", domain:4},
  {q:"A SaaS company charges customers per API call. They need to track API usage per customer accurately. Currently, they use CloudWatch custom metrics ($0.30 per metric per month) and have 10,000 customers. The tracking costs $3,000/month. How can they reduce this?", opts:["Use CloudWatch Logs with metric filters instead of custom metrics","Log API calls to Kinesis Firehose -> S3 and query with Athena for billing calculations","Use API Gateway usage plans with API keys per customer for built-in tracking","Switch to CloudWatch Embedded Metric Format (EMF) to reduce metric costs"], correct:2, exp:"API Gateway usage plans with API keys provide built-in per-customer usage tracking at no additional cost per customer. It counts requests per API key and enforces throttling/quotas. This eliminates the need for 10,000 custom metrics. EMF still creates individual metrics. Kinesis->S3->Athena works but adds complexity. Usage plans are purpose-built for this use case.", domain:4},
  {q:"A company runs a legacy Windows application on a single m5.4xlarge instance ($0.768/hr = ~$553/month). The application uses only 15% CPU on average but spikes to 80% for 2 hours daily. What provides the MOST cost savings while maintaining performance during spikes?", opts:["Switch to a t3.xlarge with unlimited mode (burstable instances handle the spike pattern perfectly)","Purchase a Reserved Instance for the m5.4xlarge","Right-size to m5.xlarge (1/4 the size) and accept slower spike processing","Use Spot Instances for the workload"], correct:0, exp:"T3 instances are designed for this exact pattern: low average utilization with periodic spikes. A t3.xlarge at $0.1664/hr costs ~$120/month (vs $553). Unlimited mode allows sustained high CPU during the 2-hour spike (paying a small surplus charge for credits used). The baseline 15% stays within credit accumulation. Total savings ~75%. Windows application can't easily use Spot (single instance, not fault-tolerant).", domain:4},
]
