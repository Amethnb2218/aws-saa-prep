export const questionBank5 = [
  // DOMAIN 1: Security scenarios - Real exam style
  {q:"A company has a public-facing web application. They need to protect it against common web exploits (SQL injection, XSS) and also automatically block IP addresses that make more than 2000 requests in 5 minutes. Which combination of services provides this protection with LEAST operational overhead?", opts:["AWS Shield Standard + Security Groups with IP blocking","AWS WAF with managed rule groups for OWASP + a rate-based rule set to 2000 requests per 5-minute period","Amazon GuardDuty + AWS Lambda for automatic IP blocking","AWS Network Firewall with Suricata IPS rules"], correct:1, exp:"AWS WAF managed rule groups (like AWS Managed Rules for Common Threats) protect against SQL injection, XSS, and other OWASP vulnerabilities out-of-the-box. Rate-based rules automatically block IPs exceeding the threshold. This is fully managed with least operational overhead. GuardDuty detects threats but doesn't block them directly. Network Firewall requires managing Suricata rules.", domain:1},
  {q:"A company wants to ensure that ALL API calls made to their AWS account are logged, including management events and data events for S3 and Lambda. The logs must be tamper-proof and retained for 7 years. What is the correct configuration?", opts:["CloudTrail trail with management events only, logs stored in S3 with Object Lock","CloudTrail trail logging management events + S3 data events + Lambda data events, stored in S3 with Object Lock (Compliance mode) and 7-year retention","CloudTrail trail logging all events to CloudWatch Logs with a 7-year retention policy","AWS Config recording all resource types with S3 delivery and lifecycle policy"], correct:1, exp:"CloudTrail logs management events by default, but data events (S3 object-level access, Lambda invocations) must be explicitly enabled. S3 Object Lock in Compliance mode makes logs tamper-proof (nobody, including root, can delete within retention period). 7-year retention satisfies the requirement. CloudWatch Logs retention max is limited and more expensive. Config records resource state changes, not API calls.", domain:1},
  {q:"A solutions architect is designing a multi-account strategy. Sensitive workloads must NOT be deployed in regions outside of eu-west-1 and eu-central-1 for GDPR compliance. How should this be enforced organization-wide?", opts:["Create IAM policies in each account denying actions outside EU regions","Attach an SCP to the Organization root that denies all actions with a condition on aws:RequestedRegion NOT in [eu-west-1, eu-central-1], with exceptions for global services","Use AWS Config organizational rules to detect resources in non-EU regions and auto-delete them","Configure VPC settings to block outbound traffic to non-EU region endpoints"], correct:1, exp:"SCPs at the Organization root enforce region restrictions across ALL accounts (current and future). The aws:RequestedRegion condition restricts where resources can be created. Global services (IAM, CloudFront, Route 53) must be excluded since they only operate from us-east-1. IAM policies per account don't scale. Config is detective. VPC settings can't restrict API calls to other regions.", domain:1},
  {q:"A Lambda function needs to call a third-party API that requires a static IP address for whitelisting. The Lambda function is currently deployed without VPC configuration. How should the architect provide a static IP?", opts:["Assign an Elastic IP directly to the Lambda function","Deploy the Lambda function in a VPC with a private subnet, route outbound traffic through a NAT Gateway that has an Elastic IP","Use AWS Global Accelerator to provide a static IP for the Lambda function","Configure the Lambda function's security group with a fixed IP"], correct:1, exp:"Lambda functions in a VPC private subnet use the NAT Gateway's Elastic IP for outbound internet traffic, providing a consistent static IP. Without VPC, Lambda uses random AWS-owned IPs. EIPs can't be directly assigned to Lambda. Global Accelerator provides static IPs for INBOUND traffic, not outbound. Security Groups control access, not source IP.", domain:1},
  {q:"A company discovers that sensitive data (SSN, credit card numbers) exists unencrypted in their S3 data lake. They need to: (1) discover all sensitive data across 500+ buckets, (2) classify it by type, and (3) get automated alerts for new sensitive data uploads. Which service provides this capability?", opts:["AWS Inspector scanning S3 objects for vulnerabilities","Amazon Macie with automated sensitive data discovery jobs and EventBridge integration for alerts","AWS Config rules checking for S3 encryption settings","Amazon GuardDuty S3 protection"], correct:1, exp:"Amazon Macie uses machine learning and pattern matching to discover, classify, and protect sensitive data (PII, financial data, credentials) in S3. It can scan across all buckets in an account/organization, categorize findings by type, and send automated alerts via EventBridge. GuardDuty monitors for threats/anomalies but doesn't classify data content. Inspector scans EC2/ECR, not S3 content.", domain:1},
  {q:"A company wants to implement single sign-on (SSO) for their 5,000 employees to access multiple AWS accounts. They use Microsoft Active Directory on-premises. What is the MOST streamlined approach?", opts:["Create IAM users in each account and sync passwords from AD","Use AWS IAM Identity Center (successor to AWS SSO) with AD Connector connecting to on-premises AD","Set up SAML 2.0 federation from AD FS to each AWS account individually","Create a Managed Microsoft AD in AWS and establish a trust with on-premises AD"], correct:1, exp:"IAM Identity Center provides centralized SSO across all AWS accounts in an Organization. AD Connector provides a proxy to the on-premises AD without syncing data to AWS. Users authenticate against their existing AD credentials. Per-account SAML federation doesn't scale. Creating IAM users defeats the purpose of SSO. Managed AD works but AD Connector is simpler when you want to keep AD on-premises.", domain:1},
  {q:"A company uses Amazon Cognito User Pool for their web application. They want to enforce that passwords must be at least 12 characters, contain uppercase, lowercase, numbers, and special characters. They also want to block common passwords and compromised credentials. What combination of Cognito features achieves this?", opts:["Configure password policy in Cognito (length, character requirements) and enable 'Compromised credentials' advanced security feature","Use a Pre-Sign-Up Lambda trigger to validate password complexity against a custom dictionary","Configure password policy only — Cognito automatically blocks common passwords","Use Amazon Inspector to scan the Cognito user pool for weak passwords"], correct:0, exp:"Cognito User Pool password policies enforce length and character requirements natively. The Advanced Security feature 'Compromised credentials' checks passwords against databases of known leaked credentials and blocks them. Together they provide strong password enforcement + breach detection. Lambda triggers can't access the plaintext password in all flows. Inspector doesn't scan Cognito.", domain:1},
  {q:"A solutions architect needs to design a system where an EC2 instance in a private subnet can access the AWS Systems Manager (SSM) service for patching without any internet connectivity. What is required?", opts:["A NAT Gateway for internet access to SSM public endpoints","VPC Interface Endpoints (PrivateLink) for ssm, ssmmessages, and ec2messages services","A VPN connection to AWS for SSM access","An internet gateway with restrictive Security Groups"], correct:1, exp:"SSM requires three PrivateLink endpoints: com.amazonaws.region.ssm, com.amazonaws.region.ssmmessages, and com.amazonaws.region.ec2messages. These provide private connectivity to SSM without internet access. The instance also needs an IAM role with SSM permissions. NAT Gateway or IGW provides internet path which the requirement explicitly excludes.", domain:1},
  {q:"A company needs to rotate the master credentials for an Amazon RDS database every 30 days. The rotation must happen automatically without any application downtime. Which approach ensures zero downtime during rotation?", opts:["Use Secrets Manager automatic rotation with a multi-user rotation strategy (alternating users)","Use Secrets Manager single-user rotation and restart the application after rotation","Store credentials in Parameter Store and use a Lambda to rotate and update the application","Manually change the password and update the application configuration"], correct:0, exp:"Multi-user rotation strategy maintains two sets of credentials. While one is being rotated, the other remains active. The application always has valid credentials. Single-user rotation has a brief period where the old password is invalid but the new one isn't yet propagated (potential downtime). Multi-user strategy eliminates this gap entirely.", domain:1},

  // DOMAIN 2: Resilient - Real exam patterns
  {q:"A company runs a web application on EC2 with an Auto Scaling Group. They want to implement a deployment strategy that allows them to test new code with a small percentage of production traffic before rolling out fully. If issues are detected, they want instant rollback. Which deployment strategy fits?", opts:["Blue/Green deployment with Route 53 weighted routing (10% to new, 90% to old)","Rolling deployment with 25% batch size","Canary deployment using CodeDeploy with traffic shifting (10% for 10 minutes, then 100%)","In-place deployment with a pre-deployment test phase"], correct:2, exp:"CodeDeploy Canary deployment shifts a small percentage (e.g., 10%) of traffic to the new version. After a specified time (10 minutes), if CloudWatch alarms don't trigger, the remaining 90% shifts. If alarms trigger during the canary phase, automatic rollback occurs instantly. Blue/Green with Route 53 works but requires manual monitoring. Rolling can't easily rollback mid-deployment.", domain:2},
  {q:"A company stores customer orders in DynamoDB. During a partition key design review, they realize order_id is a good partition key for single-item access, but they frequently need to query all orders for a customer sorted by date. What is the BEST table design?", opts:["Partition key: order_id, with a GSI having partition key: customer_id and sort key: order_date","Partition key: customer_id, sort key: order_date, with a GSI on order_id for single-item lookups","Partition key: order_id, perform a Scan with FilterExpression on customer_id","Composite partition key: customer_id#order_date with order_id as sort key"], correct:1, exp:"Partition key customer_id with sort key order_date allows efficient Query operations for all orders by a customer in date order (primary access pattern). A GSI on order_id enables efficient single-item lookup by order_id. This optimizes both access patterns. Having order_id as PK requires a GSI for customer queries (workable but less efficient for the dominant query pattern).", domain:2},
  {q:"A company uses CloudFormation to deploy infrastructure across multiple regions for disaster recovery. After a regional failure, the DR stack fails to deploy because resources reference AMI IDs that don't exist in the DR region. How should this be fixed?", opts:["Hardcode AMI IDs for each region in the template parameters","Use CloudFormation Mappings with region-specific AMI IDs and Fn::FindInMap to select the correct AMI per region","Use a Lambda-backed custom resource that looks up the latest AMI ID in the current region at deploy time","Store AMI IDs in SSM Parameter Store in each region and reference them with dynamic references"], correct:3, exp:"SSM Parameter Store with public parameters (e.g., /aws/service/ami-amazon-linux-latest/amzn2-ami-hvm-x86_64-gp2) provides always-up-to-date AMI IDs per region. CloudFormation dynamic references ({{resolve:ssm:parameter-name}}) pull the value at deploy time. Mappings require manual updates when new AMIs are released. Lambda custom resource adds complexity. SSM public parameters are automatically maintained by AWS.", domain:2},
  {q:"A serverless application uses Lambda with a 15-second timeout. Occasionally, calls to a downstream API take longer than expected, causing Lambda to timeout and the request to be lost. The downstream call is critical and must eventually succeed. How should this be redesigned?", opts:["Increase Lambda timeout to 15 minutes","Implement an asynchronous pattern: Lambda enqueues the request to SQS, a separate consumer Lambda processes it with retries and exponential backoff","Use Step Functions with a Wait state and retry logic","Lambda destinations with an on-failure SQS queue for retried processing"], correct:1, exp:"Decoupling with SQS allows the request-handling Lambda to return immediately while a consumer Lambda handles the downstream call independently with configurable visibility timeout, retries, and DLQ for failures. This ensures the request is never lost (SQS retention up to 14 days). Step Functions work but add cost and complexity for a simple retry pattern. Increasing timeout wastes money on waiting.", domain:2},
  {q:"A company runs a stateful application on EC2. The application uses in-memory session data that is lost when instances are replaced during Auto Scaling events. Which solution makes the application stateless with MINIMAL code changes?", opts:["Enable ALB sticky sessions to keep users on the same instance","Use ElastiCache Redis for session storage — applications simply change the session handler to use Redis instead of local memory","Store sessions in DynamoDB with TTL for automatic expiration","Move to ECS Fargate with shared volume mounts"], correct:1, exp:"ElastiCache Redis as a session store requires minimal code change (typically just changing the session handler/driver configuration). Sessions persist across instance replacements and are accessible from any instance. Most frameworks (Express, Spring, Django) support Redis sessions via a configuration change. Sticky sessions don't survive instance termination. DynamoDB works but has higher latency than Redis for session reads.", domain:2},
  {q:"A company wants to implement a multi-region active-active architecture for their web application. The application writes to a backend database. What is the PRIMARY challenge of active-active and how should it be addressed?", opts:["Network latency between regions — use CloudFront to cache API responses","Write conflicts when the same data is modified in both regions simultaneously — use DynamoDB Global Tables with conflict resolution or implement application-level conflict detection","Data sovereignty — keep data in the region where it was created","Cost — active-active doubles the infrastructure cost, use smaller instances in the secondary region"], correct:1, exp:"The primary challenge of active-active with writes is concurrent modification conflicts (same record updated in both regions before replication completes). DynamoDB Global Tables handles this with last-writer-wins. Alternatively, applications can partition writes by region (user A always writes to region 1) or implement application-level conflict resolution (CRDTs, versioning). Latency, sovereignty, and cost are secondary concerns.", domain:2},
  {q:"A company notices that their Auto Scaling Group launches instances faster than the application can configure itself via UserData (which takes 8 minutes). Users hitting new instances get errors. What prevents traffic from reaching unconfigured instances?", opts:["Use a launch template with a longer boot timeout","Implement an ASG lifecycle hook (EC2_INSTANCE_LAUNCHING) that holds the instance in Pending:Wait state, complete the hook only after UserData finishes successfully","Configure a longer ALB target group health check interval","Use EC2 Instance Connect to verify readiness before allowing traffic"], correct:1, exp:"Lifecycle hooks pause the instance in Pending:Wait. The ASG doesn't consider the instance InService (and the ALB doesn't add it as a target) until the hook is completed. UserData or a CloudWatch agent signals completion via CompleteLifecycleAction. This guarantees no traffic before the instance is ready. Health checks help but only after the instance is registered as a target.", domain:2},
  {q:"An application uses RDS MySQL with a Multi-AZ deployment. After a failover event, the application experiences connection errors for 60 seconds because DNS propagation of the new primary endpoint takes time. How can failover be made faster for the application?", opts:["Reduce the DNS TTL of the RDS endpoint to 1 second","Use RDS Proxy — it maintains a connection pool and redirects to the new primary within seconds, abstracting failover from the application","Implement application-level retry logic with the current RDS endpoint","Switch to Aurora which has faster failover"], correct:1, exp:"RDS Proxy maintains persistent connections to the database and handles failover transparently. When the primary fails over, RDS Proxy redirects existing connections to the new primary within seconds — the application doesn't need to re-establish connections or wait for DNS. Aurora has ~30 second failover but RDS Proxy makes even standard RDS failover near-instant for the application.", domain:2},
  {q:"A company processes files uploaded to S3. Processing takes between 1 second and 30 minutes depending on file size. They use Lambda triggered by S3 events but large files exceed Lambda's 15-minute timeout. What architecture handles ALL file sizes?", opts:["Increase Lambda memory to speed up processing of large files","Use Step Functions: first Lambda determines file size, small files are processed by Lambda, large files are processed by ECS Fargate tasks","Split large files into smaller chunks using a Lambda function and process each chunk separately","Use EventBridge to route S3 events to either Lambda (small files) or EC2 (large files) based on object size"], correct:1, exp:"Step Functions can route based on file size: a classification Lambda checks the size and a Choice state routes to Lambda (small) or Fargate (large, no time limit). This handles the full range elegantly with proper orchestration, error handling, and retry logic. EventBridge can't inspect S3 object size without a preceding Lambda. Splitting files may not be possible for all file types.", domain:2},

  // DOMAIN 3: High-Performance patterns
  {q:"A company needs to migrate their 30TB Oracle data warehouse to AWS. They want to maintain complex stored procedures and require ACID transactions. Analytics queries involve 50+ table joins and window functions. Which service is BEST?", opts:["Amazon Redshift (rewrite stored procedures as Redshift stored procedures)","Amazon Aurora PostgreSQL (supports stored procedures and complex SQL)","Amazon RDS for Oracle (lift and shift, maintain compatibility)","Amazon Athena federated query against data in S3"], correct:0, exp:"Redshift supports stored procedures (PL/pgSQL), ACID transactions, and is optimized for complex analytical queries with 50+ table joins using MPP and columnar storage. It's purpose-built for data warehousing. Aurora/RDS are OLTP, not optimized for analytical joins on 30TB. RDS Oracle maintains compatibility but doesn't optimize for analytics. Athena can't handle complex stored procedures.", domain:3},
  {q:"A real-time bidding platform must respond to ad requests within 100ms (p99). The system must evaluate 1000 bid rules against each request. Currently running on c5.4xlarge instances with 80ms p99. They want to add more rules but can't exceed 100ms. What scaling approach maintains latency?", opts:["Vertical scaling — move to c5.9xlarge with more CPU","Horizontal scaling — add more instances behind NLB and partition bid rules across instances","Use Lambda@Edge to evaluate rules at CloudFront edge locations closer to users","Cache bid rule results in ElastiCache Redis with request pattern matching"], correct:1, exp:"Horizontal scaling with rule partitioning reduces per-instance computation (each instance evaluates a subset of rules). NLB provides <1ms added latency. Results from partitioned instances are aggregated. This maintains sub-100ms as rules grow. Vertical scaling has diminishing returns and single-point-of-failure. Lambda@Edge has cold starts and limited execution time. Caching doesn't work for real-time bidding (each request is unique).", domain:3},
  {q:"A company needs to serve a machine learning model for real-time inference with the following requirements: auto-scaling from 0 to thousands of requests per second, model updates without downtime, and A/B testing between model versions. Which AWS service provides ALL of these capabilities?", opts:["Lambda with the ML model packaged in a container image","ECS Fargate with Application Load Balancer weighted target groups","Amazon SageMaker Endpoints with auto-scaling and production variants","API Gateway with Lambda for model serving"], correct:2, exp:"SageMaker Endpoints provide: auto-scaling (including scale-to-zero with serverless inference), seamless model updates via endpoint configurations (blue/green), and production variants for A/B testing (traffic splitting between model versions). All managed. Lambda requires packaging models in containers (250MB limit, cold starts). ECS requires managing scaling and deployment logic. SageMaker is purpose-built for this.", domain:3},
  {q:"A company stores 1 billion records in DynamoDB. Each record is 4KB. They need to export the entire table daily for analytics in Redshift. The export must not impact production read/write performance. What is the MOST efficient method?", opts:["Use DynamoDB Scan operation with parallel segments during off-peak hours","Use DynamoDB Export to S3 feature (uses PITR backup, zero impact on table capacity)","Set up DynamoDB Streams to capture all changes and reconstruct the full dataset","Use AWS Glue ETL job to read from DynamoDB with a read throughput limit"], correct:1, exp:"DynamoDB Export to S3 reads directly from the continuous backup (PITR), not from the live table. It has ZERO impact on provisioned capacity and doesn't consume any read units. Outputs in DynamoDB JSON or Ion format, directly loadable by Redshift or Athena. Scan operations consume read capacity. Streams capture changes but not full exports. Glue uses read capacity even with throttling.", domain:3},
  {q:"A media company needs to transcode video files into multiple formats (HLS, DASH, MP4) with different quality levels. They process 1000 videos per day, each 2-5GB. What is the MOST cost-effective and operationally simple service?", opts:["EC2 instances with FFmpeg in an Auto Scaling Group triggered by SQS","AWS Elastic Transcoder","Amazon MediaConvert (serverless, pay per minute of output)","ECS Fargate tasks with custom transcoding containers"], correct:2, exp:"Amazon MediaConvert is serverless, supports all major output formats (HLS, DASH, MP4), handles multiple output quality levels in a single job, and charges per minute of output. No infrastructure to manage. It replaces Elastic Transcoder (older, fewer features). EC2/ECS require managing the transcoding software and infrastructure. MediaConvert also supports HDR, audio normalization, and watermarking.", domain:3},
  {q:"A financial application needs to perform sub-millisecond key-value lookups on a dataset of 500GB with millions of reads per second. The data changes infrequently (a few writes per hour). Cost optimization is important. Which solution provides the BEST performance-to-cost ratio?", opts:["DynamoDB with DAX (cache nodes absorb all reads)","ElastiCache Redis cluster with data loaded from S3","DynamoDB with on-demand capacity (pay per request)","Aurora with in-memory caching enabled"], correct:0, exp:"DynamoDB with DAX provides sub-millisecond reads for the hot data. Since writes are rare, the DAX cache hit ratio approaches 100%, meaning almost all reads are served from cache (much cheaper than per-request DynamoDB reads). DAX handles millions of reads per second. The underlying DynamoDB table stores the 500GB durably. Redis would require managing data loading and failover manually. On-demand DynamoDB is single-digit ms, not sub-ms.", domain:3},
  {q:"A company needs to analyze clickstream data in real-time to detect fraud patterns. The data arrives at 500,000 events per second. They need to run sliding window aggregations (5-minute windows) and join the stream with a reference table of known fraudulent patterns. Which service handles this with LEAST operational overhead?", opts:["Kinesis Data Streams with Lambda consumers performing windowed aggregations","Kinesis Data Streams with Amazon Managed Service for Apache Flink (Kinesis Data Analytics) using SQL or Apache Flink application","Amazon MSK (Kafka) with Kafka Streams for processing","EMR with Spark Structured Streaming"], correct:1, exp:"Amazon Managed Service for Apache Flink (formerly Kinesis Data Analytics) provides serverless stream processing with built-in support for sliding windows, tumbling windows, and stream-table joins. It handles 500K events/sec with auto-scaling. SQL-based queries make it accessible. Lambda can't do sliding window aggregations natively. MSK + Kafka Streams requires managing the Kafka cluster. EMR Spark requires cluster management.", domain:3},
  {q:"A company migrates a legacy application that connects to an on-premises Redis cluster (120GB, 50,000 operations per second). The application uses Redis Cluster mode with 6 shards. They need the MOST compatible migration target on AWS. What should they use?", opts:["ElastiCache Redis (Cluster Mode Enabled) with 6 shards","ElastiCache Redis (Cluster Mode Disabled) with a large node type","Amazon MemoryDB for Redis with 6 shards","ElastiCache Memcached with 6 nodes"], correct:0, exp:"ElastiCache Redis (Cluster Mode Enabled) provides the most compatible migration path from an on-premises Redis Cluster. Same cluster mode architecture with sharding, same Redis commands, same data distribution. 6 shards match the existing topology. MemoryDB adds durability but isn't needed unless data persistence to disk is required (it's also more expensive). Cluster Mode Disabled doesn't support sharding. Memcached isn't Redis-compatible.", domain:3},

  // DOMAIN 4: Cost Optimization patterns
  {q:"A company has 50TB in EBS gp3 volumes attached to stopped EC2 instances that haven't been used in 6 months. What should they do?", opts:["Convert gp3 to sc1 (cold HDD) volumes for lower storage cost","Create EBS snapshots, delete the original volumes, and restore from snapshot only if needed","Delete the volumes entirely since they're on stopped instances","Leave them as-is since stopped instances don't incur compute charges"], correct:1, exp:"EBS volumes incur charges regardless of whether the attached instance is running. Snapshots store only used blocks incrementally and cost $0.05/GB/month vs $0.08/GB/month for gp3. For unused data, snapshot + delete volume saves costs AND retains the ability to restore. 50TB: gp3 = $4,000/month vs snapshot ≈ $2,500/month (depending on data). sc1 is $0.015/GB but volumes still incur full charges for allocated size.", domain:4},
  {q:"A company runs a website that receives 95% of traffic from 3 countries. They use CloudFront with all edge locations (Price Class All). Their CloudFront bill is $15,000/month. How can they reduce this while maintaining performance for their primary audience?", opts:["Switch to Price Class 200 (US, Canada, Europe, Asia) or Price Class 100 (US, Canada, Europe) depending on which 3 countries","Enable CloudFront Origin Shield to reduce origin fetches","Increase Cache-Control max-age headers to improve cache hit ratio","Implement all three: appropriate Price Class + Origin Shield + better caching headers"], correct:3, exp:"Price Class reduction eliminates expensive edge locations not serving the primary audience. Origin Shield reduces origin requests (one cache layer). Better caching headers improve hit ratio (fewer origin fetches = lower origin costs + faster responses). Combined, these can reduce the bill by 30-50%. Each individually helps, but together they compound savings.", domain:4},
  {q:"A data processing pipeline runs for exactly 2 hours every day and requires exactly 16 vCPUs and 64GB RAM. The workload is NOT fault-tolerant (cannot be interrupted). It must run daily without fail. What is the MOST cost-effective EC2 purchasing option?", opts:["On-Demand m5.4xlarge for the 2 hours each day","Spot Instance m5.4xlarge with no interruption tolerance","Compute Savings Plan committed to the equivalent of 2 hours/day compute usage","On-Demand with Scheduled Scaling: launch at start, terminate after 2 hours"], correct:2, exp:"Compute Savings Plan commitment is calculated on an hourly basis averaged over the commitment period. If you commit to the $/hour equivalent of running the instance 2/24 hours per day, you get the Savings Plan discount (up to 66%) for those hours. On-Demand for 2 hours costs more. Spot can't be used (not fault-tolerant). Scheduled Scaling still uses On-Demand pricing. The SP covers exactly the predictable daily usage.", domain:4},
  {q:"A company operates a dev/test environment that mirrors production (15 EC2 instances, 3 RDS databases, 2 ElastiCache clusters, load balancers). Monthly cost: $8,000. The environment is only used Monday-Friday 9am-6pm (45 hours/week). What is the estimated savings from implementing automated stop/start?", opts:["~25% savings ($2,000/month)","~50% savings ($4,000/month)","~73% savings ($5,840/month)","~90% savings ($7,200/month)"], correct:2, exp:"45 hours/week out of 168 total hours = 27% utilization. Stopping during unused time saves 73% (127 hours/168 hours). $8,000 x 73% = $5,840/month savings. Note: EBS storage and Elastic IPs still incur charges when stopped, so actual savings slightly less. RDS stop works for up to 7 days, then auto-restarts. EC2 stop eliminates compute charges entirely.", domain:4},
  {q:"A company runs a Lambda function that processes SQS messages. The function is triggered every time a message arrives (batch size 1). Processing 10,000 messages per hour, each invocation takes 500ms with 256MB memory. How can they reduce Lambda costs?", opts:["Increase batch size to 10 (process 10 messages per invocation)","Decrease memory to 128MB","Increase timeout to batch more naturally","Switch to ARM architecture (Graviton2)"], correct:0, exp:"Increasing batch size from 1 to 10 reduces invocations from 10,000/hour to 1,000/hour. Each invocation processes 10 messages (might take 2-3 seconds instead of 500ms), but total compute-seconds drops because: invocation overhead eliminated for 9,000 calls, and Lambda charges per 1ms now. 10x fewer invocations ≈ 50-60% cost reduction. ARM saves 20%. Lower memory might slow execution.", domain:4},
  {q:"A machine learning team uses SageMaker notebook instances (ml.m5.xlarge) that run 24/7 for 5 data scientists. Each scientist works 8 hours/day. Monthly notebook cost: $5,400. What architecture reduces costs while improving collaboration?", opts:["Switch to SageMaker Studio with auto-shutdown lifecycle configuration (stops instances after 1 hour of inactivity)","Use smaller instance types (ml.t3.medium) running 24/7","Schedule notebooks to start/stop with CloudWatch Events","Use EC2 instances with JupyterHub for shared notebooks"], correct:0, exp:"SageMaker Studio with auto-shutdown lifecycle configuration automatically stops the compute instance after idle time. 5 scientists x 8 hours = 40 compute-hours/day vs 120 compute-hours/day (5 x 24h). Savings: ~67% ($3,600/month saved). Studio also provides collaboration features, shared spaces, and persistent storage independent of compute. Smaller instances may lack needed compute power for ML.", domain:4},
  {q:"A company transfers 100TB of data per month between EC2 instances in us-east-1 and S3 buckets in eu-west-1. Cross-region data transfer costs $0.02/GB = $2,000/month. How can this be reduced?", opts:["Use S3 Transfer Acceleration to reduce transfer costs","Replicate the S3 bucket to us-east-1 using CRR and access the local copy (pay $0 for same-region transfer)","Compress data before transfer to reduce the volume","Use AWS Direct Connect for cheaper data transfer"], correct:1, exp:"Same-region data transfer between EC2 and S3 is FREE. By replicating the S3 data to a bucket in us-east-1 (CRR), EC2 accesses the local copy at $0 transfer cost. CRR costs $0.02/GB for the one-time replication, but if data is read multiple times, the savings compound. Transfer Acceleration doesn't reduce cost (speeds up transfer). Compression helps but adds CPU overhead. Direct Connect has its own fees.", domain:4},
  {q:"A company uses AWS Backup to protect 200TB of EBS volumes with daily snapshots retained for 30 days. Monthly snapshot storage costs $8,000. EBS snapshots are incremental but the first full snapshot is large. How can they reduce snapshot costs?", opts:["Reduce retention to 7 days","Use EBS Snapshot Archive for snapshots older than 7 days (75% cheaper storage at the cost of longer restore time)","Delete unused/orphaned snapshots that no longer have associated volumes","Implement lifecycle policies: keep daily for 7 days, weekly for 30 days, monthly for 1 year in Archive tier"], correct:3, exp:"Tiered retention reduces the number of snapshots stored: 7 daily + 4 weekly + 12 monthly = 23 snapshots vs 30 daily = 30 snapshots, but with better long-term coverage. Archive tier for older snapshots costs 75% less ($0.0125/GB/month vs $0.05/GB/month). This combination optimizes both retention and cost. Simply reducing retention loses recovery points. Orphan cleanup helps but doesn't address the fundamental cost of active snapshots.", domain:4},
]
